# EPIC E - core packages (C1-C5)

packages/amlich-core, packages/content, packages/ui. Report section 5.

---

## C1. Build a real golden-fixture table and wire the P0 gate

Priority P1 | Effort L | Executor: agent+human | Launch exit criterion

Context: `test/fixtures/gold-1900-2199.json` holds 9 rows, its own README calls it a placeholder, and no test loads it. The current 1900-2199 assurance is a round-trip self-consistency sweep - necessary but not ground truth. Founder decision 3 requires an exact match 1900-2100.

Agent steps:
1. Write `test/fixtures/gen-golden.md` documenting the sourcing method, then produce `gold-1900-2199.json` with at least: the solar date of every lunar month start (thang 1 mung 1 through thang 12/nhuan mung 1) for 1900-2100; the leap-month index for every leap year in range; Tet dates 1900-2100; the documented VN/China divergence years (1968, 1984-85, 2007, 2030, 2053 and any others the reference lists); a sparser spot-check set for 2101-2199 flagged `suspect` per the founder decision.
2. Source options in preference order: (a) run Ho Ngoc Duc's published reference implementation (amlich JS/Java) offline and export; (b) cross-compute with a second independent port and keep only agreeing rows, flagging disagreements for human adjudication. Never generate fixtures from `@cyberskill/amlich-core` itself.
3. New `test/golden-table.test.ts` asserting every row through `convertLunar2Solar`/`convertSolar2Lunar`; hard-fail 1900-2100; for 2101-2199 allow the documented 1-day tolerance and emit the suspect list (compare `docs/lunar-suspect-dates-1900-2199.md`).
4. Wire into `gate:p0` and CI; delete the placeholder README caveat; update the root README's gate description (with D1).

Human steps: adjudicate any disagreement rows; sign off the sourcing method in the handoff.

Acceptance criteria:
- [ ] Golden table has full month-start coverage 1900-2100 (about 2,486 months) sourced independently of this codebase.
- [ ] `pnpm gate:p0` fails if any 1900-2100 row mismatches; suspect flow covers 2101-2199.
- [ ] Sourcing method documented and reproducible.

Verify:
```bash
pnpm gate:p0
node -e "const g=require('./packages/amlich-core/test/fixtures/gold-1900-2199.json');console.log(g.length)"
```

---

## C2. Verify day-quality against ground truth; fix the UTC trap

Priority P1 | Effort M | Depends: C1 | Executor: agent

Context: day-quality fixtures are generated by the function under test, one assertion is a tautology (`expect(q.truc.name).toBe(q.truc.name)`, dayquality.test.ts:15), and `dayquality.ts:74-76` reads the input Date with getUTC*, giving the wrong VN day for callers passing `new Date()` during a 7-hour window.

Steps:
1. Change the public API to accept `(dd, mm, yy)` or a JDN instead of a `Date` (update CONTRACT.md and all call sites in web/zalo); keep a deprecated Date overload that converts via `todayInHCM` semantics if removal is too breaking.
2. Hand-build a small independent fixture set (20-40 dates) from a published Vietnamese almanac (lich van nien) covering Hoang Dao/Hac Dao, Truc, and 28 sao; cite the source per date in the fixture file.
3. Delete the tautological assertion and the self-generated-fixture pattern (`gen-fixtures.ts` may stay for regression snapshots but must be labeled as such, separate from correctness tests).
4. Add a boundary test: instant 2025-01-28T18:00:00Z (01:00 VN Jan 29) resolves to VN day 29, not 28.

Acceptance criteria:
- [ ] No tautological assertions; correctness tests reference external almanac data with citations.
- [ ] The 7-hour-window bug is unrepresentable through the new API; boundary test green.

Verify: `pnpm --filter @cyberskill/amlich-core test -- dayquality`

---

## C3. Move commerce code out of amlich-core

Priority P2 | Effort M | Executor: agent

Context: `src/commerce/affiliate-resolver.ts` (mock offers, example.com images) is exported from the lunar-math barrel and consumed by `apps/web/components/commerce/AffiliateWidget.tsx` and `services/genie-api/lib/proactive-zns.ts`.

Steps: create `packages/commerce` (`@cyberskill/genie-commerce`); move resolver + its test; update the two consumers; remove the export from amlich-core's barrel; replace `example.com` placeholders with env-configurable asset hosts or a documented mock flag.

Acceptance criteria:
- [ ] amlich-core exports contain nothing commerce-related; all workspaces typecheck and test green.

Verify: `git grep -n "commerce" packages/amlich-core/src` (empty) then `pnpm -r typecheck && pnpm -r test`

---

## C4. Core package hygiene

Priority P2 | Effort M | Depends: C3 | Executor: agent

Steps, one commit each or one grouped commit with a clear message:
1. Delete stale `dist/` outputs (built from the stub era) and gitignore `dist/`; CI builds fresh (I1 covers running builds).
2. Remove the unused `astronomy-engine` devDependency, or add the promised cross-check test that imports it (pick one; removing is fine, then fix the doc-comment in `src/index.ts` that claims the cross-check).
3. packages/ui: delete one of the two conflicting `apca-w3` ambient declaration files (`src/env.d.ts` vs `src/types/apca-w3.d.ts` - keep the 3-arg one matching the real library), then remove the `as any` casts in `theme/apca.ts`.
4. Dedupe the leap-month span logic: extract the shared helper used by both `convert.ts` and `recurrence.ts` (`leapMonthForSpan`), with a test pinning byte-identical behavior.
5. `packages/content/src/index.ts`: use `isInvalidSolar()` instead of the inline `dd !== 0`; drop the unused `_InternalRecord` import.
6. Add `license`, `files`, `sideEffects: false` to the three package.json files; fix the extensionless import in `dayquality-tables.ts` to the `.js` convention.
7. Add tests for `canChiMonth` and `canChiYear` against known values (e.g. 2025 is At Ty; check month can-chi against the almanac used in C2).

Acceptance criteria:
- [ ] All seven items done; `pnpm -r typecheck && pnpm -r test` green; no `as any` remains in packages/ui.

---

## C5. Locale-keyed content structure

Priority P2 | Effort M | Executor: agent

Context: all 13 festival records hardcode Vietnamese strings. Fine for v1; restructuring now (13 records) is cheap, later is not.

Steps: reshape `FestivalContent` text fields as `LocalizedText = {vi: string; en?: string}` (or keep flat and add a parallel `locales/vi.ts` map - pick the shape that keeps `content` consumable without an i18n runtime); update `getFestivalById` consumers to select a locale with vi default; keep tests asserting vi completeness (13 records, disclaimers, offerings minimums).

Acceptance criteria:
- [ ] Adding an English translation later touches content data only, no consumer code.
- [ ] Existing vi behavior byte-identical (snapshot test across all 13 records).

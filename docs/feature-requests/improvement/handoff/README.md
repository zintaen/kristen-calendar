# Handoff notes

One file per task, named `<ID>.md` (for example `S2.md`), written by the implementing agent before setting the task to `in_review`. Template lives in `../PROMPT.md`. The human reviewer reads the handoff, runs the verification, and flips the backlog status to `done` or back to `todo` with a rejection note.

window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LUNAR-020"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>TASK-020 builds the freemium tier split and the server-side entitlement gate for \"Genie Am Lich\". The three-tier model is designed to maximize the value for Free users (encouraging retention) while clearly creating additional value for Premium and Family.</p>\n<ol><li>MUST define three clear tiers (DEC-LUNAR-200):</li></ol>\n<ul><li><strong>Free</strong>: Ram/Mung Mot/gio/custom reminders, the lunar-solar month calendar, static content (festival content), local notifications, basic can-chi day view; NO AI Genie, NO advanced good-day picker, NO family sharing (sharedWith = []).</li><li><strong>Premium</strong>: everything in Free plus AI Genie (50 calls/month), the full good-day picker (TASK-012), ZNS reminders (TASK-017), shareable cards (TASK-014).</li><li><strong>Family</strong>: everything in Premium plus family sharing/cloud sync (TASK-018), 100 Genie calls/month/user, sharing up to 10 members.</li></ul>\n<ol><li>MUST enforce the entitlement gate <strong>server-side</strong> in every serverless handler; the client MUST NOT decide its own tier - every decision is based on the result from the server (DEC-LUNAR-201).</li><li>MUST store entitlement in the <code>user_entitlements</code> table in Supabase with RLS; the client caches locally with a 24h TTL; on TTL expiry it MUST re-validate with the server (DEC-LUNAR-204).</li><li>MUST enforce the tier rate-limit on the server for AI Genie: Free = 0 calls; Premium = 50 calls/month; Family = 100 calls/month/user; over the limit returns HTTP 429 with the <code>Retry-After</code> and <code>X-RateLimit-Remaining</code> headers (DEC-LUNAR-202).</li><li>MUST enforce the family-sharing gate in <code>api/sync.ts</code>: Free and Premium users cannot add members to <code>sharedWith</code>; only the Family tier can (DEC-LUNAR-200).</li><li>MUST handle the payment webhook from the App Store and Zalo Pay in <code>api/webhook-payment.ts</code>; it MUST verify the provider's HMAC/JWT signature before updating entitlement; updating incorrectly without verifying is a security hole.</li><li>MUST NOT handle payment directly in TASK-020; this function only enforces the webhook from App Store IAP (iOS) and Zalo Pay (Zalo Mini App) - TASK-020 does not build the payment UI (DEC-LUNAR-203).</li><li>MUST have <code>GET /api/entitlement</code> returning <code>{ tier, features, quota, validUntil }</code> to the client; this response MUST include enough information to display the correct <code>UpgradePrompt</code>.</li><li>MUST display an <code>UpgradePrompt</code> when a Free user taps a Premium feature; the prompt MUST state the specific benefit (\"Unlock AI Genie - ask about offering trays, holiday meanings\") not just \"Upgrade to Premium\".</li><li>MUST NOT hide or disable a Premium feature in the client in a way the user cannot see - it should show the feature with a lock icon and an <code>UpgradePrompt</code>, not remove the working UI (SHOULD encourage discovery).</li><li>SHOULD track the conversion rate (Free -&gt; Premium, Premium -&gt; Family) via analytics (with <code>consentFlags.analyticsUsage = true</code> per TASK-019) to serve the DEC-LUNAR-205 success metric.</li><li>MUST have a \"graceful downgrade\" mechanism: if entitlement expires (validUntil &lt; now()), immediately move back to the Free tier; the Premium data (Genie history, sharedWith) IS KEPT for 30 days to allow renewal; only after 30 days is it deleted.</li><li>SHOULD provide a trial mode: a 7-day free Premium trial for new users; the trial is tracked by a <code>trial_used: boolean</code> field in <code>user_entitlements</code>; each user gets only 1 trial.</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why three tiers (Free/Premium/Family) instead of two (Free/Premium) (DEC-LUNAR-200)?</strong> The persona \"Co Hoa\" (the homemaker keeping the ancestral incense) is a distinct segment with the need to \"share gio reminders with the whole family\" - this is a different value from \"using AI Genie\". A separate Family tier allows higher pricing for the family use case without forcing a single user to pay more. The tier split is also cleaner technically: TASK-018 can be gated for the Family tier only.</p>\n<p><strong>Why MUST entitlement be server-side (DEC-LUNAR-201)?</strong> Client-trusted entitlement is a fundamental security hole: anyone who opens DevTools and edits <code>localStorage.tier = \"premium\"</code> gets Premium for free. For AI Genie, each call is a real cost ($1/$5 per 1M tokens) - this hole directly affects operating cost. A server-side gate ensures every request is checked against the real data in the database.</p>\n<p><strong>Why cache entitlement locally with a 24h TTL (DEC-LUNAR-204)?</strong> Calling the entitlement-check API before every user action is too slow - it increases latency and hurts UX. A 24h TTL is a balance: if the user's tier expires, the system knows within 24h (acceptable for an app like this). Important features like AI Genie still call the server - the cache is only for displaying the UI (lock icon, UpgradePrompt).</p>\n<p><strong>Why not build a payment UI in TASK-020 (DEC-LUNAR-203)?</strong> iOS payment via App Store IAP requires native Swift/StoreKit; the Zalo Mini App requires the separate Zalo Pay SDK. Integrating these two payment systems is a large, separate scope. TASK-020 only handles the \"after payment confirmation\" part via a webhook - this is much simpler and does not block the rollout of the entitlement logic.</p>\n<p><strong>Why is verifying the webhook HMAC/JWT mandatory?</strong> Without verification, anyone can POST to <code>/api/webhook/payment</code> and update their entitlement. This is the simplest possible attack. HMAC (App Store uses JWS, Zalo Pay uses HMAC-SHA256) is the standard mechanism of both providers - read the official documentation before implementing.</p>\n<p><strong>Why show Premium features with a lock icon instead of hiding them (§1 #10)?</strong> Showing a feature with a lock icon creates \"aspirational pull\" - the user sees \"AI Genie\" and wants to use it. Hiding it means the user does not know it exists and never converts. This pattern is used successfully by Duolingo, Spotify, and many other freemium products.</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// services/genie-api/lib/entitlement.ts\n\nexport type Tier = \"free\" | \"premium\" | \"family\";\n\nexport interface EntitlementRecord {\n  userId: string;\n  tier: Tier;\n  validUntil: string | null;    // null = vinh vien (khong su dung cho trial)\n  source: \"app_store\" | \"zalo_pay\" | \"manual\" | \"trial\";\n  trialUsed: boolean;\n  updatedAt: string;\n}\n\nexport interface FeatureGate {\n  genieAI: boolean;\n  genieMonthlyQuota: number;    // 0 = Free, 50 = Premium, 100 = Family\n  goodDayPicker: boolean;\n  familySharing: boolean;\n  maxFamilyMembers: number;     // 0 = Free/Premium, 10 = Family\n  znsReminder: boolean;\n  shareableCards: boolean;\n}\n\nexport const TIER_FEATURES: Record&lt;Tier, FeatureGate&gt; = {\n  free: {\n    genieAI: false, genieMonthlyQuota: 0,\n    goodDayPicker: false, familySharing: false, maxFamilyMembers: 0,\n    znsReminder: false, shareableCards: false,\n  },\n  premium: {\n    genieAI: true, genieMonthlyQuota: 50,\n    goodDayPicker: true, familySharing: false, maxFamilyMembers: 0,\n    znsReminder: true, shareableCards: true,\n  },\n  family: {\n    genieAI: true, genieMonthlyQuota: 100,\n    goodDayPicker: true, familySharing: true, maxFamilyMembers: 10,\n    znsReminder: true, shareableCards: true,\n  },\n};\n\n// Lay tier hien tai cua user (SERVER-SIDE ONLY)\nexport async function getEntitlement(userId: string): Promise&lt;EntitlementRecord&gt;;\n\n// Kiem tra mot tinh nang co duoc phep voi tier nay khong\nexport function isFeatureAllowed(tier: Tier, feature: keyof FeatureGate): boolean;\n\n// Lay quota con lai cua thang nay\nexport async function getRemainingQuota(\n  userId: string,\n  feature: \"genieAI\"\n): Promise&lt;number&gt;;</code></pre>\n<pre><code class=\"language-typescript\">// services/genie-api/lib/rate-limiter.ts\n\nexport interface RateLimitResult {\n  allowed: boolean;\n  remaining: number;\n  resetAt: string;   // ISO 8601 dau thang sau\n}\n\n// Kiem tra va tang dem su dung AI Genie\n// Dung Supabase (hoac Upstash Redis neu can toc do cao hon)\nexport async function checkAndIncrementGenieUsage(\n  userId: string,\n  quota: number\n): Promise&lt;RateLimitResult&gt;;</code></pre>\n<pre><code class=\"language-typescript\">// services/genie-api/api/entitlement.ts\n\n// GET /api/entitlement\n// Response: { tier, features, quota, validUntil, trialAvailable }\nexport interface EntitlementResponse {\n  tier: Tier;\n  features: FeatureGate;\n  genieUsedThisMonth: number;\n  validUntil: string | null;\n  trialAvailable: boolean;    // true neu chua dung trial\n  gracePeriodEndsAt: string | null; // 30 ngay sau khi het han (downgrade)\n}\n\nexport async function handleGetEntitlement(\n  req: VercelRequest,\n  res: VercelResponse\n): Promise&lt;void&gt;;\n\n// POST /api/entitlement/trial\n// Kich hoat trial 7 ngay; tra ve HTTP 409 neu trialUsed = true\nexport async function handleStartTrial(\n  req: VercelRequest,\n  res: VercelResponse\n): Promise&lt;void&gt;;</code></pre>\n<pre><code class=\"language-typescript\">// services/genie-api/api/webhook-payment.ts\n\nexport interface AppStoreWebhookPayload {\n  signedTransactionInfo: string;  // JWS, xac minh voi Apple public key\n  signedRenewalInfo: string;\n}\n\nexport interface ZaloPayWebhookPayload {\n  data: string;      // JSON string\n  mac: string;       // HMAC-SHA256(data, ZALO_PAY_KEY2)\n}\n\n// POST /api/webhook/payment/appstore\nexport async function handleAppStoreWebhook(\n  req: VercelRequest,\n  res: VercelResponse\n): Promise&lt;void&gt;;\n\n// POST /api/webhook/payment/zalopay\nexport async function handleZaloPayWebhook(\n  req: VercelRequest,\n  res: VercelResponse\n): Promise&lt;void&gt;;\n\n// Ham chung: xac minh webhook va cap nhat tier\nexport async function processPaymentConfirmation(\n  userId: string,\n  tier: Tier,\n  validUntil: string,\n  source: EntitlementRecord[\"source\"]\n): Promise&lt;void&gt;;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/lib/entitlement-client.ts\n\nexport class EntitlementClient {\n  private cache: { data: EntitlementResponse; fetchedAt: number } | null = null;\n  private readonly TTL_MS = 24 * 60 * 60 * 1000; // 24h\n\n  // Doc entitlement: dung cache neu con han, goi API neu het TTL\n  async get(): Promise&lt;EntitlementResponse&gt;;\n\n  // Buoc invalidate cache (vi du sau khi thanh toan)\n  invalidateCache(): void;\n\n  // Tat ca quyet dinh UI dung ham nay - KHONG dung localStorage.tier truc tiep\n  async canUseFeature(feature: keyof FeatureGate): Promise&lt;boolean&gt;;\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A Free user calling <code>api/genie.ts</code> directly (bypassing the UI) receives HTTP 403 with body <code>{ error: \"feature_not_allowed\", feature: \"genieAI\", tier: \"free\" }</code>.</li><li>A Premium user calling Genie 50 times in a month: the 51st call receives HTTP 429 with the header <code>X-RateLimit-Remaining: 0</code> and <code>Retry-After</code> equal to the number of seconds until the start of next month.</li><li>A Family user calling Genie 100 times in a month: the 101st call receives HTTP 429.</li><li>A Free user trying to add a member to <code>sharedWith</code> on the <code>/api/sync/share</code> endpoint receives HTTP 403.</li><li>A Premium user trying to add a member to <code>sharedWith</code> receives HTTP 403 (only Family is allowed).</li><li>A Family user adding up to 10 members: adding the 11th member receives HTTP 422 with a clear message.</li><li>Editing <code>localStorage.tier = \"premium\"</code> on the client and calling <code>/api/genie</code>: the server returns HTTP 403 if the real entitlement is Free (client-trusted entitlement is defeated).</li><li>An App Store webhook POST with a valid JWS -&gt; entitlement is updated to Premium in <code>user_entitlements</code>; <code>GET /api/entitlement</code> right after returns <code>tier: \"premium\"</code>.</li><li>An App Store webhook POST with a forged JWS (wrong signature) -&gt; HTTP 401, entitlement does NOT change.</li><li>A Zalo Pay webhook POST with a valid MAC -&gt; entitlement is updated; a wrong MAC -&gt; HTTP 401.</li><li><code>EntitlementClient.get()</code> calls the API the first time and caches; a second call within 24h uses the cache (0 additional API requests).</li><li><code>EntitlementClient.get()</code> after the 24h TTL expires calls the API again.</li><li>7-day trial: calling <code>POST /api/entitlement/trial</code> the first time -&gt; the tier moves to premium, <code>trialUsed = true</code>, <code>validUntil = now() + 7 days</code>. Calling a second time -&gt; HTTP 409.</li><li>Trial expiry: <code>validUntil &lt; now()</code> -&gt; the tier automatically moves back to Free; the Premium data is kept for 30 days (<code>gracePeriodEndsAt = validUntil + 30 days</code>).</li><li><code>UpgradePrompt</code> appears when a Free user taps AI Genie; the prompt has text describing the specific benefit (\"Ask about offering trays, holiday meanings\") and a \"Try 7 days free\" button if <code>trialAvailable = true</code>.</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// services/genie-api/test/entitlement.test.ts\nimport { describe, it, expect, vi, beforeEach } from \"vitest\";\nimport {\n  TIER_FEATURES, isFeatureAllowed, getEntitlement\n} from \"../lib/entitlement\";\nimport { checkAndIncrementGenieUsage } from \"../lib/rate-limiter\";\n\ndescribe(\"TIER_FEATURES - dinh nghia tier bat bien\", () =&gt; {\n  it(\"Free tier khong co genieAI\", () =&gt; {\n    expect(TIER_FEATURES.free.genieAI).toBe(false);\n    expect(TIER_FEATURES.free.genieMonthlyQuota).toBe(0);\n    expect(TIER_FEATURES.free.familySharing).toBe(false);\n  });\n\n  it(\"Premium co genieAI nhung khong co familySharing\", () =&gt; {\n    expect(TIER_FEATURES.premium.genieAI).toBe(true);\n    expect(TIER_FEATURES.premium.genieMonthlyQuota).toBe(50);\n    expect(TIER_FEATURES.premium.familySharing).toBe(false);\n  });\n\n  it(\"Family co tat ca tinh nang Premium cong familySharing\", () =&gt; {\n    expect(TIER_FEATURES.family.familySharing).toBe(true);\n    expect(TIER_FEATURES.family.maxFamilyMembers).toBe(10);\n    expect(TIER_FEATURES.family.genieMonthlyQuota).toBe(100);\n  });\n});\n\ndescribe(\"isFeatureAllowed\", () =&gt; {\n  it(\"Free khong duoc phep genieAI\", () =&gt; {\n    expect(isFeatureAllowed(\"free\", \"genieAI\")).toBe(false);\n  });\n  it(\"Premium duoc phep genieAI\", () =&gt; {\n    expect(isFeatureAllowed(\"premium\", \"genieAI\")).toBe(true);\n  });\n  it(\"Premium khong duoc phep familySharing\", () =&gt; {\n    expect(isFeatureAllowed(\"premium\", \"familySharing\")).toBe(false);\n  });\n  it(\"Family duoc phep familySharing\", () =&gt; {\n    expect(isFeatureAllowed(\"family\", \"familySharing\")).toBe(true);\n  });\n});\n\ndescribe(\"rate-limiter\", () =&gt; {\n  it(\"cho phep trong gioi han quota\", async () =&gt; {\n    const mockDb = { getCount: vi.fn().mockResolvedValue(49), increment: vi.fn() };\n    const result = await checkAndIncrementGenieUsage(\"user1\", 50, mockDb as any);\n    expect(result.allowed).toBe(true);\n    expect(result.remaining).toBe(0); // lan cuoi\n  });\n\n  it(\"chan khi vuot quota\", async () =&gt; {\n    const mockDb = { getCount: vi.fn().mockResolvedValue(50), increment: vi.fn() };\n    const result = await checkAndIncrementGenieUsage(\"user1\", 50, mockDb as any);\n    expect(result.allowed).toBe(false);\n    expect(result.remaining).toBe(0);\n  });\n});\n\ndescribe(\"webhook - xac minh HMAC\", () =&gt; {\n  it(\"MAC sai bi tu choi\", async () =&gt; {\n    const fakePayload = {\n      data: JSON.stringify({ orderId: \"123\", amount: 50000 }),\n      mac: \"invalid_mac\"\n    };\n    const response = await simulateWebhookPost(\"/api/webhook/payment/zalopay\", fakePayload);\n    expect(response.status).toBe(401);\n  });\n});\n\ndescribe(\"EntitlementClient - cache TTL\", () =&gt; {\n  it(\"goi API chi mot lan trong 24h\", async () =&gt; {\n    const fetchSpy = vi.spyOn(global, \"fetch\").mockResolvedValue(\n      makeFakeEntitlementResponse(\"free\")\n    );\n    const client = new EntitlementClient();\n    await client.get();\n    await client.get(); // lan 2: dung cache\n    expect(fetchSpy).toHaveBeenCalledTimes(1);\n  });\n\n  it(\"goi API moi sau khi invalidate\", async () =&gt; {\n    const fetchSpy = vi.spyOn(global, \"fetch\").mockResolvedValue(\n      makeFakeEntitlementResponse(\"free\")\n    );\n    const client = new EntitlementClient();\n    await client.get();\n    client.invalidateCache();\n    await client.get(); // phai goi lai\n    expect(fetchSpy).toHaveBeenCalledTimes(2);\n  });\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p><code>TIER_FEATURES</code> in §3 is the single source of truth for every gate decision. Two key points:</p>\n<pre><code class=\"language-typescript\">// Mau gate trong api/genie.ts (ap dung tuong tu cho moi tinh nang Premium)\nexport async function handleGenie(req, res) {\n  const userId = authenticate(req);                    // throw 401 neu khong co\n  const entitlement = await getEntitlement(userId);    // server DB lookup\n  const tier = entitlement.tier;\n\n  if (!isFeatureAllowed(tier, \"genieAI\")) {\n    return res.status(403).json({\n      error: \"feature_not_allowed\",\n      feature: \"genieAI\",\n      tier,\n    });\n  }\n\n  const rateCheck = await checkAndIncrementGenieUsage(\n    userId,\n    TIER_FEATURES[tier].genieMonthlyQuota\n  );\n  if (!rateCheck.allowed) {\n    return res.status(429).json({ error: \"quota_exceeded\" })\n      .setHeader(\"X-RateLimit-Remaining\", \"0\")\n      .setHeader(\"Retry-After\", String(secondsUntilMonthEnd()));\n  }\n\n  // Tiep tuc goi Claude...\n}</code></pre>\n<pre><code class=\"language-typescript\">// Mau graceful downgrade trong getEntitlement()\nexport async function getEntitlement(userId: string): Promise&lt;EntitlementRecord&gt; {\n  const row = await db.from(\"user_entitlements\").select(\"*\").eq(\"user_id\", userId);\n  if (row &amp;&amp; row.valid_until &amp;&amp; new Date(row.valid_until) &lt; new Date()) {\n    // Het han -&gt; ha ve Free\n    return { ...row, tier: \"free\" };\n  }\n  return row ?? { userId, tier: \"free\", trialUsed: false, ... };\n}</code></pre>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream: TASK-LUNAR-015 (the AI Genie Claude proxy) - TASK-020 adds a server-side gate into TASK-015's <code>api/genie.ts</code>; TASK-015 must already exist before the gate can be added. TASK-LUNAR-018 (family sharing cloud sync) - TASK-020 adds the <code>familySharing</code> gate into TASK-018's <code>api/sync.ts</code>.</p>\n<p>Downstream: TASK-020 blocks no task. But the entire Phase 3 depends on TASK-020 working to earn revenue: without the entitlement gate there is no tier distinction, no conversion incentive, and the operating cost of AI Genie and ZNS rises uncontrolled.</p>\n<p>Cross-cutting: TASK-LUNAR-019 (PDPL consent) - if <code>analyticsUsage = true</code>, TASK-020 can track the conversion rate. TASK-LUNAR-016 (Zalo Mini App) - the entitlement check must work similarly on the Zalo Mini App client (calling the same <code>/api/entitlement</code> API).</p>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-sql\">-- services/genie-api/supabase/migrations/0020_entitlements.sql\n\nCREATE TABLE IF NOT EXISTS user_entitlements (\n  id            UUID PRIMARY KEY DEFAULT uuid_generate_v4(),\n  user_id       UUID NOT NULL UNIQUE REFERENCES users(id) ON DELETE CASCADE,\n  tier          TEXT NOT NULL DEFAULT 'free'\n                CHECK (tier IN ('free','premium','family')),\n  valid_until   TIMESTAMPTZ,       -- null = vinh vien; dung cho subscription\n  source        TEXT NOT NULL DEFAULT 'manual'\n                CHECK (source IN ('app_store','zalo_pay','manual','trial')),\n  trial_used    BOOLEAN NOT NULL DEFAULT FALSE,\n  updated_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE IF NOT EXISTS genie_usage_monthly (\n  id         BIGSERIAL PRIMARY KEY,\n  user_id    UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n  year_month TEXT NOT NULL,         -- \"2026-06\" (YYYY-MM)\n  call_count INTEGER NOT NULL DEFAULT 0,\n  UNIQUE(user_id, year_month)\n);\n\nALTER TABLE user_entitlements ENABLE ROW LEVEL SECURITY;\nALTER TABLE genie_usage_monthly ENABLE ROW LEVEL SECURITY;\n\n-- User chi doc entitlement cua chinh ho\nCREATE POLICY \"user_own_entitlement_select\" ON user_entitlements\n  FOR SELECT USING (auth.uid() = user_id);\n\n-- Chi server (service role) cap nhat entitlement\nCREATE POLICY \"server_update_entitlement\" ON user_entitlements\n  FOR ALL WITH CHECK (FALSE);\n\nCREATE TRIGGER trg_entitlements_updated_at\n  BEFORE UPDATE ON user_entitlements\n  FOR EACH ROW EXECUTE FUNCTION update_updated_at();</code></pre>\n<pre><code class=\"language-json\">// GET /api/entitlement response mau - Free user co trial kha dung\n{\n  \"tier\": \"free\",\n  \"features\": {\n    \"genieAI\": false,\n    \"genieMonthlyQuota\": 0,\n    \"goodDayPicker\": false,\n    \"familySharing\": false,\n    \"maxFamilyMembers\": 0,\n    \"znsReminder\": false,\n    \"shareableCards\": false\n  },\n  \"genieUsedThisMonth\": 0,\n  \"validUntil\": null,\n  \"trialAvailable\": true,\n  \"gracePeriodEndsAt\": null\n}</code></pre>\n<pre><code class=\"language-json\">// GET /api/entitlement response mau - Family tier dang hoat dong\n{\n  \"tier\": \"family\",\n  \"features\": {\n    \"genieAI\": true,\n    \"genieMonthlyQuota\": 100,\n    \"goodDayPicker\": true,\n    \"familySharing\": true,\n    \"maxFamilyMembers\": 10,\n    \"znsReminder\": true,\n    \"shareableCards\": true\n  },\n  \"genieUsedThisMonth\": 23,\n  \"validUntil\": \"2027-06-27T00:00:00.000Z\",\n  \"trialAvailable\": false,\n  \"gracePeriodEndsAt\": null\n}</code></pre>\n<pre><code class=\"language-json\">// HTTP 403 khi Free user truy cap genieAI\n{\n  \"error\": \"feature_not_allowed\",\n  \"feature\": \"genieAI\",\n  \"tier\": \"free\",\n  \"upgradeUrl\": \"/upgrade\"\n}</code></pre>\n<pre><code class=\"language-json\">// HTTP 429 khi vuot quota\n{\n  \"error\": \"quota_exceeded\",\n  \"feature\": \"genieAI\",\n  \"quota\": 50,\n  \"used\": 50,\n  \"resetAt\": \"2026-07-01T00:00:00.000Z\"\n}</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li>Three tiers (Free/Premium/Family): defined immutably in <code>TIER_FEATURES</code> (DEC-LUNAR-200); changing a tier needs a new DEC.</li><li>Server-side gate: the pattern in §6 applies to all Premium features (DEC-LUNAR-201).</li><li>Rate-limit storage: Supabase <code>genie_usage_monthly</code> is enough for the initial phase; can move to Upstash Redis if higher speed is needed in Phase 4.</li><li>Cache TTL 24h: a balance between UX and freshness (DEC-LUNAR-204); used for Phase 3.</li></ul>\n<p>Still deferred:</p>\n<ul><li>Payment UI (StoreKit for iOS, the Zalo Pay SDK for the Mini App): outside the scope of TASK-020 (DEC-LUNAR-203). Will be split into a separate task once the payment provider is chosen.</li><li>Specific pricing (VND/month for Premium and Family): not yet set. The success metric (&gt;= 3% conversion - DEC-LUNAR-205) is the threshold for adjusting price later.</li><li>Subscription management (cancel, pause, resume via the App Store): depends on App Store subscription management; deferred until there is a payment integration.</li><li>Promo code / referral: can be added once there is real conversion data; not suitable for the initial Phase 3.</li><li>Detailed analytics on the conversion rate: depends on <code>consentFlags.analyticsUsage = true</code> from TASK-019; deferred until there is enough data.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Client-trusted entitlement (edited localStorage)</td><td>The server-side gate does not read client data</td><td>HTTP 403 correctly</td><td>Not needed - the server gate works</td></tr><tr><td>Forged webhook (wrong HMAC)</td><td>Verify the HMAC before processing</td><td>HTTP 401, entitlement unchanged</td><td>Log the request IP; do not grant the tier</td></tr><tr><td>Entitlement expired but not yet checked</td><td><code>getEntitlement()</code> compares <code>valid_until</code> with <code>now()</code></td><td>Auto-downgrade to Free</td><td>Graceful; data kept for 30 days</td></tr><tr><td>Genie quota exceeded</td><td><code>checkAndIncrementGenieUsage()</code> counter</td><td>HTTP 429 with <code>Retry-After</code></td><td>User waits for the start of the month or upgrades</td></tr><tr><td>Supabase down during the entitlement check</td><td>HTTP 5xx from the DB</td><td>Fallback to Free (fail-safe across the board)</td><td>Log the error; the user sees \"service temporarily interrupted\"</td></tr><tr><td>Race condition: 2 concurrent Genie calls at the quota threshold</td><td>Atomic upsert in <code>genie_usage_monthly</code></td><td>One call goes through, one is blocked</td><td>Use <code>ON CONFLICT DO UPDATE</code> with an atomic increment</td></tr><tr><td>Premium user hits the Family gate (sharedWith)</td><td><code>isFeatureAllowed(tier, \"familySharing\")</code></td><td>Clear HTTP 403</td><td><code>UpgradePrompt</code> shows the Family tier with its benefits</td></tr><tr><td>Trial activated twice</td><td>Check <code>trial_used = true</code> before granting</td><td>HTTP 409</td><td>Message \"You have already used the trial\"</td></tr><tr><td>Cache TTL expired but the server is down</td><td><code>EntitlementClient.get()</code> falls back to the old cache</td><td>Use the old cache for another 1h (fallback window)</td><td>Log a warning; do not block UX</td></tr><tr><td>The 11th family member</td><td>Gate <code>maxFamilyMembers = 10</code></td><td>HTTP 422 with a clear message</td><td>The family owner needs to upgrade or remove a member</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li><code>TIER_FEATURES</code> is the single source of truth: every \"is feature X allowed\" decision goes through <code>isFeatureAllowed(tier, feature)</code> reading from this table. MUST NOT hardcode tier checks in different places in the code - to avoid drift.</li><li><code>genie_usage_monthly</code> uses <code>year_month = \"YYYY-MM\"</code> (text) instead of TIMESTAMPTZ to simplify the start-of-month reset: <code>WHERE year_month = to_char(NOW(), 'YYYY-MM')</code>. Atomic increment uses <code>INSERT ... ON CONFLICT DO UPDATE SET call_count = call_count + 1</code>.</li><li>Webhook security: App Store uses JWS (JWT with RS256 + the Apple root certificate); Zalo Pay uses HMAC-SHA256 with <code>ZALO_PAY_KEY2</code> (an environment variable). Both must be verified before processing. Read each provider's official documentation - use the correct variable names and procedures.</li><li><code>EntitlementClient</code> on the client: cache in memory (not localStorage) to prevent the user from reading/editing the cache; the 24h TTL resets on app reload. <code>invalidateCache()</code> is called after the user finishes payment.</li><li>Graceful downgrade of 30 days: needs a cron job (or a check at access time) to mark <code>grace_period_ends_at</code> and send a message \"your Premium account will expire in X days\". This cron is part of the TASK-017 infrastructure (ZNS), not built in TASK-020.</li><li><code>UpgradePrompt</code> should have A/B testing once there is enough data: test \"Try 7 days\" vs \"Upgrade now - 30% off the first month\". Depends on the analytics consent (TASK-019) and having enough users.</li><li>Success metric DEC-LUNAR-205 (&gt;= 3% conversion): track it by counting <code>user_entitlements</code> with <code>tier != 'free'</code> divided by total users; set an alert when it is not met after 90 days of Phase 3.</li><li>The P3 migration numbers are distributed to avoid collisions: 0016-0017 for TASK-018, 0018 for TASK-017, 0019 for TASK-019, 0020 for TASK-020.</li></ul>\n<p><em>End of TASK-LUNAR-020.</em></p>";

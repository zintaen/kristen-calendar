window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LUNAR-014"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>The system MUST render a purple-toned card image as a 1080x1080px square on an <code>HTMLCanvasElement</code> and provide a share or download button. Contract:</p>\n<ol><li>MUST render the card on an <code>HTMLCanvasElement</code> 1080x1080px with the Canvas 2D API; MUST NOT use <code>html2canvas</code>, <code>puppeteer</code>, or any DOM-to-image library (DEC-LUNAR-140).</li><li>MUST export PNG via <code>canvas.toBlob(\"image/png\")</code> with uncompressed quality; the target file size is &lt; 500 KB.</li><li>MUST display at least three content areas: (a) the lunar date title line (for example \"Full Moon of the first lunar month\"), (b) the corresponding solar date, (c) a small logo/watermark \"Genie Am Lich - CyberSkill\".</li><li>MUST load the font \"Be Vietnam Pro\" weight 700 via the <code>FontFace</code> API before calling <code>drawCard</code>; if loading fails within 3s, fall back to sans-serif and log a warning (DEC-LUNAR-143).</li><li>MUST meet the APCA threshold by font size for every text/background pair on the card, checked with <code>apca-w3</code> in the test suite; the build MUST fail if it does not (DEC-LUNAR-141): primary text (&gt;= 60px, <code>textPrimary</code>) MUST be Lc &gt;= 75; secondary text (<code>textSecondary</code>) MUST be Lc &gt;= 60; the small watermark (~18-22px) MUST be Lc &gt;= 90 (per APCA for small text, §11). Each threshold has its own assertion corresponding to the actual font size.</li><li>MUST support at least two color templates: (a) \"purple-cream\" - deep purple text (<code>#3B1F6E</code>) on a cream background (<code>#FDF6EC</code>), (b) \"purple-dark\" - cream text on a deep purple background (<code>#3B1F6E</code>); both MUST pass APCA &gt;= 75.</li><li>MUST provide a \"Share\" button that calls <code>navigator.share({ files: [File] })</code> (Web Share API Level 2) to share the image directly to social media / Zalo on iOS/Android (DEC-LUNAR-142).</li><li>MUST have a \"Download\" fallback that creates an <code>&lt;a download&gt;</code> element and triggers a click when <code>navigator.canShare</code> returns false or does not exist (DEC-LUNAR-142).</li><li>MUST display a scaled-down 360x360px card preview in the UI (scaled from the 1080px canvas) before the user shares.</li><li>MUST provide <code>ShareCardSheet</code> - a bottom-sheet that lets the user choose a template and a date (defaulting to the day being viewed in the TASK-LUNAR-007 month calendar).</li><li>MUST NOT recompute <code>DayInfo</code> inside the card renderer; it MUST receive the already-computed <code>CardData</code> from the layer above (DEC-LUNAR-143).</li><li>SHOULD let the user additionally choose to display the day's can-chi on the card (for example \"Giap Ty - auspicious day\").</li><li>SHOULD add light decorative motifs (a gradient, abstract patterns) that do not obscure the text; the motifs MUST be drawn with Canvas 2D, not using external images, to avoid CORS.</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why use Canvas 2D instead of html2canvas?</strong> <code>html2canvas</code> captures the DOM into an image, but font rendering, CORS images, and z-index often produce results that differ across browsers. The Canvas 2D API is a consistent surface, controllable pixel by pixel, independent of DOM layout, and easy to test with node-canvas in a CI environment (DEC-LUNAR-140).</p>\n<p><strong>Why the 1080x1080px size?</strong> The Instagram feed requires at least 1080px to avoid blurring on upload; a 1:1 ratio is the most common format for a square story/post. Zalo Stories also recommends 1080px. Displaying a 360px preview in the app loads quickly without affecting the exported image quality (DEC-LUNAR-140).</p>\n<p><strong>Why an APCA assertion in the test?</strong> Purple tones easily fall into the mid-contrast range if the wrong shade is chosen. Catching the error at compile time with <code>apca-w3</code> ensures that any color-token change in <code>packages/ui/</code> does not silently break the card contrast without anyone noticing (DEC-LUNAR-141, NFR-Accessibility).</p>\n<p><strong>Why use the Web Share API instead of opening an upload link?</strong> The Web Share API Level 2 lets the user share a file directly into any app (Instagram, Zalo, Messenger, Save to Photos) via a native OS action sheet. This is the smoothest experience on iOS Safari and Android Chrome without needing OAuth or a server upload (DEC-LUNAR-142).</p>\n<p><strong>Why does CardData come from the layer above?</strong> The card renderer is a pure drawing function with no state. Separating it from the calendar computation makes it independently testable and reusable when more templates are added. Recomputing <code>DayInfo</code> in the renderer would create an unnecessary circular dependency with <code>amlich-core</code> (DEC-LUNAR-143).</p>\n<p><strong>Why use two templates at minimum?</strong> \"Purple-cream\" suits a light mood (First Day, Full Moon); \"purple-dark\" gives a more solemn feel (Vu Lan, death anniversaries). Two templates are also a way to test that both color directions pass APCA, not just one (DEC-LUNAR-141).</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// apps/web/lib/card-renderer.ts\n\nexport interface CardData {\n  lunarLabel: string;       // \"Rằm tháng Giêng\"\n  solarLabel: string;       // \"13/02/2025\"\n  canChiLabel?: string;     // \"Giáp Tý - Hoàng đạo\" (tùy chọn)\n  eventType: \"ram\" | \"mung_mot\" | \"gio\" | \"custom\";\n  watermark: string;        // \"Genie Am Lich · CyberSkill\"\n}\n\nexport type CardThemeId = \"purple-cream\" | \"purple-dark\";\n\nexport interface CardTheme {\n  id: CardThemeId;\n  background: string;       // CSS color\n  textPrimary: string;\n  textSecondary: string;\n  accentColor: string;\n}\n\nexport const CARD_THEMES: Record&lt;CardThemeId, CardTheme&gt; = {\n  \"purple-cream\": {\n    id: \"purple-cream\",\n    background: \"#FDF6EC\",\n    textPrimary: \"#3B1F6E\",\n    textSecondary: \"#6B4E9B\",\n    accentColor: \"#8A5CD8\",\n  },\n  \"purple-dark\": {\n    id: \"purple-dark\",\n    background: \"#3B1F6E\",\n    textPrimary: \"#FDF6EC\",\n    textSecondary: \"#E8D5FF\",\n    accentColor: \"#C9A4FF\",\n  },\n};\n\nexport interface CardRenderResult {\n  canvas: HTMLCanvasElement;\n  // computed Lc values theo cap text/nen thuc te (DEC-LUNAR-141, §1 #5):\n  //   primary &gt;= 75, secondary &gt;= 60, watermark &gt;= 90 (chu nho)\n  apcaLc: { primary: number; secondary: number; watermark: number };\n}\n\n/**\n * Vẽ card lên ctx đã chuẩn bị. Hàm thuần túy: không fetch, không đọc DOM.\n * Font PHẢI được load trước khi gọi hàm này.\n */\nexport async function drawCard(\n  canvas: HTMLCanvasElement,\n  data: CardData,\n  theme: CardTheme\n): Promise&lt;CardRenderResult&gt;;\n\n/**\n * Load Be Vietnam Pro weight 700 truoc khi draw.\n * Trả về true nếu thành công, false nếu timeout (3000ms).\n */\nexport async function loadCardFont(): Promise&lt;boolean&gt;;\n\n/**\n * Export canvas thành PNG Blob.\n */\nexport async function exportCardBlob(canvas: HTMLCanvasElement): Promise&lt;Blob&gt;;\n\n/**\n * Chia sẻ card qua Web Share API Level 2.\n * Nếu canShare = false hoặc share thất bại, tạo download link thay thế.\n */\nexport async function shareCard(blob: Blob, filename: string): Promise&lt;\"shared\" | \"downloaded\" | \"failed\"&gt;;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/components/ShareCard.tsx\ninterface ShareCardProps {\n  data: CardData;\n  themeId?: CardThemeId;\n  onClose: () =&gt; void;\n}\nexport function ShareCard(props: ShareCardProps): React.ReactElement;\n\n// apps/web/components/ShareCardSheet.tsx\ninterface ShareCardSheetProps {\n  defaultData: CardData;\n  open: boolean;\n  onClose: () =&gt; void;\n}\nexport function ShareCardSheet(props: ShareCardSheetProps): React.ReactElement;</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>The canvas is created at exactly 1080x1080px; <code>canvas.width === 1080 &amp;&amp; canvas.height === 1080</code>.</li><li><code>lunarLabel</code> appears in the center area of the card with a font size &gt;= 60px; <code>solarLabel</code> appears below it.</li><li>The <code>textPrimary</code> / <code>background</code> pair of \"purple-cream\" reaches APCA Lc &gt;= 75 when checked with <code>apca-w3</code>; the assertion fails the build if &lt; 75.</li><li>The <code>textPrimary</code> / <code>background</code> pair of \"purple-dark\" reaches APCA Lc &gt;= 75; the assertion fails the build if &lt; 75.</li><li><code>exportCardBlob</code> returns a <code>Blob</code> of type <code>\"image/png\"</code> with size &gt; 0 and &lt; 500 KB for standard content.</li><li>When <code>navigator.canShare</code> is true, <code>shareCard</code> calls <code>navigator.share</code> with an object containing <code>files: [File]</code>.</li><li>When <code>navigator.canShare</code> is false or undefined, <code>shareCard</code> creates an <code>&lt;a download&gt;</code> element and triggers a click; it does not throw.</li><li>A 360px preview displays in the <code>ShareCard</code> component before the user presses the share button.</li><li><code>ShareCardSheet</code> displays a bottom-sheet with at least 2 template options; choosing a template updates the preview immediately.</li><li><code>loadCardFont</code> returns <code>false</code> on a 3s timeout; <code>drawCard</code> still runs with the fallback font and does not throw.</li><li><code>drawCard</code> does not call <code>fetch</code>, <code>XMLHttpRequest</code>, or any network API.</li><li>The watermark \"Genie Am Lich · CyberSkill\" appears in the bottom corner of the card with a smaller font size than the primary text, and <code>drawCard(...).apcaLc.watermark &gt;= 90</code> for both themes (small text needs a higher Lc per APCA, §1 #5/§11); the assertion fails the build if &lt; 90.</li><li>The entire <code>card-renderer.test.ts</code> test suite passes in the CI environment (jsdom or node-canvas).</li><li><code>drawCard</code> receives <code>CardData</code> as a parameter; there is no import of <code>canChiDay</code>, <code>getDayQuality</code>, <code>convertSolar2Lunar</code>, or any symbol from <code>@cyberskill/amlich-core</code> in the file <code>card-renderer.ts</code> (DEC-LUNAR-143) - checked with a grep assertion in CI: <code>grep -r \"amlich-core\" apps/web/lib/card-renderer.ts</code> returns 0 results.</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// apps/web/lib/card-renderer.test.ts\nimport { calcAPCA } from \"apca-w3\";\nimport { CARD_THEMES, drawCard, exportCardBlob, shareCard, loadCardFont } from \"./card-renderer\";\nimport { createCanvas } from \"canvas\"; // node-canvas cho CI\n\n// --- Kích thuoc ---\ntest(\"canvas 1080x1080\", async () =&gt; {\n  const canvas = createCanvas(1080, 1080) as unknown as HTMLCanvasElement;\n  const result = await drawCard(canvas, mockData(), CARD_THEMES[\"purple-cream\"]);\n  expect(result.canvas.width).toBe(1080);\n  expect(result.canvas.height).toBe(1080);\n});\n\n// --- APCA assertions (build gate) ---\ndescribe(\"APCA Lc &gt;= 75 for all themes\", () =&gt; {\n  for (const [id, theme] of Object.entries(CARD_THEMES)) {\n    test(`theme ${id}: textPrimary vs background`, () =&gt; {\n      const lc = Math.abs(\n        calcAPCA(theme.textPrimary, theme.background) as number\n      );\n      expect(lc).toBeGreaterThanOrEqual(75);\n    });\n    test(`theme ${id}: textSecondary vs background`, () =&gt; {\n      const lc = Math.abs(\n        calcAPCA(theme.textSecondary, theme.background) as number\n      );\n      expect(lc).toBeGreaterThanOrEqual(60); // secondary text nhỏ hơn, ngưỡng thấp hơn\n    });\n    // Watermark (~18-22px) PHAI dat Lc &gt;= 90 theo APCA cho chu nho (§1 #5, §11).\n    // drawCard tra ve apcaLc.watermark = Lc cua mau watermark thuc te tren nen.\n    test(`theme ${id}: watermark dat Lc &gt;= 90`, async () =&gt; {\n      const canvas = createCanvas(1080, 1080) as unknown as HTMLCanvasElement;\n      const result = await drawCard(canvas, mockData(), theme);\n      expect(result.apcaLc.watermark).toBeGreaterThanOrEqual(90);\n    });\n  }\n});\n\n// --- Export PNG ---\ntest(\"exportCardBlob produces png &lt; 500KB\", async () =&gt; {\n  const canvas = createCanvas(1080, 1080) as unknown as HTMLCanvasElement;\n  await drawCard(canvas, mockData(), CARD_THEMES[\"purple-cream\"]);\n  const blob = await exportCardBlob(canvas);\n  expect(blob.type).toBe(\"image/png\");\n  expect(blob.size).toBeGreaterThan(0);\n  expect(blob.size).toBeLessThan(500 * 1024);\n});\n\n// --- Web Share fallback ---\ntest(\"shareCard downloads when canShare unavailable\", async () =&gt; {\n  const origShare = (navigator as Navigator).share;\n  const origCanShare = (navigator as Navigator).canShare;\n  Object.defineProperty(navigator, \"canShare\", { value: () =&gt; false, configurable: true });\n  const appendSpy = jest.spyOn(document.body, \"appendChild\");\n  const blob = new Blob([\"test\"], { type: \"image/png\" });\n  const result = await shareCard(blob, \"card.png\");\n  expect(result).toBe(\"downloaded\");\n  expect(appendSpy).toHaveBeenCalled();\n  Object.defineProperty(navigator, \"canShare\", { value: origCanShare, configurable: true });\n  Object.defineProperty(navigator, \"share\", { value: origShare, configurable: true });\n});\n\n// --- No network calls ---\ntest(\"drawCard makes no network calls\", async () =&gt; {\n  const fetchSpy = jest.spyOn(global, \"fetch\");\n  const canvas = createCanvas(1080, 1080) as unknown as HTMLCanvasElement;\n  await drawCard(canvas, mockData(), CARD_THEMES[\"purple-dark\"]);\n  expect(fetchSpy).not.toHaveBeenCalled();\n});\n\n// --- Font fallback ---\ntest(\"drawCard runs with fallback font when load fails\", async () =&gt; {\n  jest.useFakeTimers();\n  const fontPromise = loadCardFont();\n  jest.advanceTimersByTime(3001);\n  const loaded = await fontPromise;\n  expect(loaded).toBe(false);\n  // drawCard vẫn không throw\n  const canvas = createCanvas(1080, 1080) as unknown as HTMLCanvasElement;\n  await expect(drawCard(canvas, mockData(), CARD_THEMES[\"purple-cream\"])).resolves.toBeDefined();\n  jest.useRealTimers();\n});\n\n// AC #14 - card-renderer.ts khong import amlich-core (DEC-LUNAR-143)\n// Kiem tra bang static analysis: file card-renderer.ts khong chua \"@cyberskill/amlich-core\"\nimport * as cardRendererModule from \"./card-renderer\";\ntest(\"card-renderer khong re-import amlich-core (DayInfo la tham so, khong tinh lai)\", () =&gt; {\n  // Tat ca symbol export tu card-renderer phai la drawing functions, khong phai calc functions\n  const exports = Object.keys(cardRendererModule);\n  expect(exports).toContain(\"drawCard\");\n  expect(exports).toContain(\"exportCardBlob\");\n  expect(exports).toContain(\"shareCard\");\n  // Khong co getDayQuality hay canChiDay trong exports cua card-renderer\n  expect(exports).not.toContain(\"getDayQuality\");\n  expect(exports).not.toContain(\"canChiDay\");\n  expect(exports).not.toContain(\"convertSolar2Lunar\");\n});\n\nfunction mockData() {\n  return {\n    lunarLabel: \"Rằm tháng Giêng\",\n    solarLabel: \"13/02/2025\",\n    canChiLabel: \"Giáp Tý\",\n    eventType: \"ram\" as const,\n    watermark: \"Genie Am Lich · CyberSkill\",\n  };\n}</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The full contract is in §3. The single point to pin down: drawing text MUST call <code>ctx.font = \\</code>700 ${size}px \"Be Vietnam Pro\", sans-serif\\`<code> after the font has loaded. If the font is not in </code>document.fonts<code>, call </code>document.fonts.load(...)<code> and await it before </code>drawCard<code>. On node-canvas (CI), use the library's </code>registerFont` instead.</p>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream: TASK-LUNAR-007 provides <code>DayInfo</code> (lunarDate, canChiDay, ...) as the source of <code>CardData</code>; TASK-LUNAR-009 provides the purple color tokens - the hex values in <code>CARD_THEMES</code> MUST come from <code>packages/ui/src/theme/tokens.ts</code> and must not be separately hardcoded, to ensure consistency.</p>\n<p>Downstream: no task is directly blocked by TASK-014.</p>\n<p>Cross-cutting: <code>apca-w3</code> is a dev dependency of <code>apps/web/</code>; check that it is already in <code>packages/ui/</code> (TASK-LUNAR-009 uses it for the theme gate) to avoid duplication.</p>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"cardData\": {\n    \"lunarLabel\": \"Rằm tháng Giêng\",\n    \"solarLabel\": \"13/02/2025\",\n    \"canChiLabel\": \"Giáp Tý - Hoàng đạo\",\n    \"eventType\": \"ram\",\n    \"watermark\": \"Genie Am Lich · CyberSkill\"\n  },\n  \"themeId\": \"purple-cream\",\n  \"apcaResult\": {\n    \"primary\": 87.4,\n    \"secondary\": 63.1,\n    \"watermark\": 92.6\n  },\n  \"exportResult\": {\n    \"blobType\": \"image/png\",\n    \"sizeKB\": 142\n  },\n  \"shareResult\": \"shared\"\n}</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved: the 1080px size, the Canvas 2D renderer, the Web Share API Level 2.</p>\n<p>Still deferred: (a) support for an additional 9:16 format (1080x1920) for Instagram Story - not needed for slice 5, can be added after real feedback; (b) adding animation (a GIF/video card) - noted in the PRD Caveats but no task yet; (c) user-designed templates - out of scope for TASK-014.</p>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Font \"Be Vietnam Pro\" timeout</td><td><code>loadCardFont</code> returns false after 3s</td><td>Fallback sans-serif, text still renders</td><td>Log a warning; do not block UX</td></tr><tr><td>APCA &lt; 75 when a color token changes</td><td>CI test <code>calcAPCA</code> fails</td><td>Build fails</td><td>Developer fixes the token before merge</td></tr><tr><td><code>canvas.toBlob</code> returns null</td><td>Check <code>blob == null</code></td><td>Throw <code>CardExportError</code></td><td>Show a toast \"Cannot export the image, try again\"</td></tr><tr><td>PNG file &gt; 500 KB</td><td>Check <code>blob.size</code> in the test</td><td>Test fails</td><td>Reduce the motifs or use JPEG compression</td></tr><tr><td><code>navigator.share</code> throws <code>AbortError</code></td><td>catch AbortError</td><td>Ignore (user cancelled)</td><td>No error reported</td></tr><tr><td><code>navigator.share</code> throws otherwise</td><td>catch generic Error</td><td>Log + fallback download</td><td>Toast \"Share failed, downloaded instead\"</td></tr><tr><td><code>canShare</code> = false (desktop browser)</td><td><code>!navigator.canShare</code></td><td>Activate the download path</td><td>Button relabeled to \"Download\"</td></tr><tr><td>CORS when loading an external image into the canvas</td><td>canvas is \"tainted\"</td><td><code>toBlob</code> throws SecurityError</td><td>DEC-LUNAR-140: do not use external images; draw the motifs with Canvas 2D</td></tr><tr><td>node-canvas missing in CI</td><td>import fails</td><td>Test crash</td><td>Add <code>canvas</code> to devDependencies</td></tr><tr><td>Web Share API does not support <code>files</code></td><td><code>canShare({ files })</code> = false</td><td>Fallback download</td><td>Documented behavior</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li><code>drawCard</code> is async because it needs to await <code>document.fonts.ready</code> after loading; in a CI environment using node-canvas this step is skipped.</li><li>When computing <code>apcaLc</code>, remember that <code>calcAPCA</code> from <code>apca-w3</code> returns a negative value when dark text is on a light background; use <code>Math.abs</code> before comparing against the threshold of 75.</li><li>The Web Share API <code>navigator.share({ files })</code> requires a gesture (a user click); it MUST NOT be called inside <code>useEffect</code> or an automatic timeout.</li><li>The 360px preview is a CSS <code>transform: scale(0.333)</code> from the 1080px canvas - this preserves pixel quality instead of redrawing a smaller canvas.</li><li><code>ShareCardSheet</code> should lazy-load <code>card-renderer.ts</code> via a dynamic import so it does not affect the main-screen bundle size.</li><li>The color tokens in <code>CARD_THEMES</code> MUST be imported from <code>packages/ui/src/theme/tokens.ts</code> so that when TASK-LUNAR-009 updates the palette, the card updates automatically without editing two places.</li><li>The watermark text is small (about 18-22px, equivalent to 18px on the 1080px canvas) - it needs a separate APCA check because the small size requires Lc &gt;= 90 per APCA guidelines; if it does not meet this, increase the contrast or add a semi-transparent background.</li></ul>\n<p><em>End of TASK-LUNAR-014.</em></p>";

window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LUNAR-017"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>The ZNS sender MUST send lunar-calendar reminder messages via the Zalo Official Account (OA) to users who granted a phone number and consented, in full compliance with Zalo platform rules. This is the contract:</p>\n<ol><li>MUST send ZNS only to phone numbers the user voluntarily provided via the Zalo Mini App (TASK-LUNAR-016) and that have a transactional relationship with the OA; MUST NOT send to phone numbers collected from any other source or without consent (DEC-LUNAR-170, NFR-Privacy/PDPL).</li><li>MUST use a Zalo-approved template before sending; the template MUST have at most 400 characters and at least 1 personalization parameter (DEC-LUNAR-171). Default template: \"Chao {ten}, ngay mai ({ngay duong}) la {dip} ({ngay am}). Dung quen chuan bi nhe!\".</li><li>MUST NOT use the ZNS template to send purely advertising content (pure advertising); every message must be tied to a specific lunar event of the user (DEC-LUNAR-171, Zalo rules).</li><li>MUST send messages only within the 06:00-22:00 window in <code>Asia/Ho_Chi_Minh</code> (UTC+7); if the send time falls outside this window, it MUST wait until 06:00 the next day or be cancelled depending on the configured policy (DEC-LUNAR-172).</li><li>MUST send messages only when the event is within [0 days, 7 days] of the send date; specifically: <code>0 &lt;= daysUntilEvent &lt;= 7</code>; an overdue event (&lt; 0) or a too-distant event (&gt; 7 days) MUST NOT be sent (DEC-LUNAR-172).</li><li>MUST manage the OA access token automatically: read <code>OA_ACCESS_TOKEN</code> and <code>OA_REFRESH_TOKEN</code> from environment variables or a secret store; check the expiry before each cron batch; if the token has under 10 minutes to expiry, it MUST refresh before sending (DEC-LUNAR-173, NFR-Security).</li><li>MUST NOT hardcode <code>OA_ACCESS_TOKEN</code>, <code>OA_REFRESH_TOKEN</code>, or any ZNS credential into the code or the client (NFR-Security); all communication with the Zalo OA API MUST go through the server side (services/genie-api/).</li><li>MUST run a serverless cron (integrated with Vercel Cron or equivalent) on a suitable schedule (not more often than every 15 minutes) to scan the list of Reminders whose <code>channels</code> contain <code>\"ZNS\"</code> and <code>enabled = true</code>, compute occurrences with <code>amlich-core</code>, filter by the rules in §1 #4 and #5, then send the messages (DEC-LUNAR-174).</li><li>MUST compute the occurrence's lunar and solar dates with <code>convertLunar2Solar</code> from <code>@cyberskill/amlich-core</code> with <code>tz = 7.0</code>; MUST NOT use another calendar-computation source, to ensure consistency with the clients (DEC-LUNAR-174).</li><li>MUST log each send into the <code>zns_send_log</code> table (fields: <code>reminderId</code>, <code>phone</code>, <code>sentAt</code>, <code>status</code>, <code>zaloMessageId</code>); the fee of ~200 VND is incurred only when Zalo returns a successful send - record this clearly for cost reporting (DEC-LUNAR-174).</li><li>MUST handle ZNS API error codes clearly: a failed send (OA token error, rate-limit, phone number not on Zalo) MUST be logged with the specific error code; then it MUST NOT retry immediately but MUST defer to the next cron run (to avoid piling up messages).</li><li>SHOULD support the option to use a distributor such as VietGuys, Infobip, or 8x8 to send ZNS instead of integrating directly via the Zalo OA Open API; the specific decision is deferred until official OA registration (DEC-LUNAR-175).</li><li>MUST fill the template parameters using the exact parameter names Zalo approved; for example, if the template has <code>{ten}</code>, <code>{ngay duong}</code>, <code>{dip}</code>, <code>{ngay am}</code>, the sent payload must contain all 4 parameters.</li><li>MUST NOT send more than 1 ZNS message for the same Reminder and the same user on the same day; check <code>zns_send_log</code> before sending to avoid duplicate sends.</li><li>MUST support <code>recurrence = \"MONTHLY\"</code> in <code>SchedulerReminder</code>: the cron MUST use <code>candidateLunarYears()</code> to generate an occurrence for each lunar month in the scan window (at least the current month and the next month); each month generates exactly 1 occurrence within the 06:00-22:00 Asia/Ho_Chi_Minh window and within 7 days before/after the event - ensuring the Ram and Mung Mot reminders repeat all 12 times a year via ZNS (not just once a year like ANNUAL).</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why ZNS instead of a regular push notification?</strong> The Zalo Mini App has no native push API (DEC-LUNAR-162 in TASK-016). ZNS via the OA is the only way for \"Genie Am Lich\" to send proactive messages to Zalo users. Beyond that, ZNS has a higher delivery rate than web push because it goes through Zalo's official messaging system of roughly 2.1 billion messages/day (PRD Key Findings 3).</p>\n<p><strong>Why the 06:00-22:00 window and &lt;= 7 days?</strong> These are hard Zalo platform rules for ZNS (Key Findings 4, PRD Caveats). Sending outside the window will be rejected by Zalo and may get the OA blocked. The &lt;= 7 days before/after constraint keeps ZNS aligned with the intent of \"reminding about an upcoming event\", not for periodic advertising.</p>\n<p><strong>Why auto-refresh the OA token?</strong> The OA access token has an expiry (typically 1 hour to 30 days depending on OA configuration); letting it expire mid-cron-run fails the entire batch. Checking the expiry before each cron batch and refreshing proactively when under 10 minutes remain ensures no batch is cancelled due to a token error (DEC-LUNAR-173, NFR-Security).</p>\n<p><strong>Why use a serverless cron instead of real-time events?</strong> A stateless serverless function is cheaper than keeping a server always alive. For lunar-calendar reminders (the needed moment is only the early morning before a holiday), a 15-minute cron frequency is precise enough; no real-time sub-second is needed. This also scales easily and is inexpensive (DEC-LUNAR-174).</p>\n<p><strong>Why log to zns_send_log?</strong> Zalo charges ~200 VND per successful send (PRD Caveats). Without a log, you cannot report cost, debug send errors, or guard against duplicate sends (ISS-004). The log is the protective table against over-billing and a data source for commercial analytics.</p>\n<p><strong>Why support a distributor (VietGuys/Infobip/8x8)?</strong> Onboarding an OA directly with Zalo requires official OA registration, verification, and possibly a Zalo Channel Agent (ZCA) for billing. A distributor shortens this process and may provide a simpler SDK. The final decision depends on the official registration process; the API contract in §3 is designed to be neutral between direct and distributor (DEC-LUNAR-175).</p>\n<p><strong>Why amlich-core to compute the date in the cron?</strong> The cron runs server-side, imports <code>@cyberskill/amlich-core</code> (TypeScript, zero-dependency), and calls <code>convertLunar2Solar</code> to compute the solar date from the Reminder's lunar date. This guarantees 100% consistency with the computation used by the Zalo Mini App and Web clients, avoiding the case where the server and client return different dates (DEC-LUNAR-174).</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// services/genie-api/lib/oa-token.ts\n\nexport interface OATokenPair {\n  accessToken: string;\n  refreshToken: string;\n  expiresAt: number; // Unix ms\n}\n\n// Đọc từ env hoặc Supabase secret store\nexport async function getOATokenPair(): Promise&lt;OATokenPair&gt; { /* ... */ }\n\n// Gọi Zalo OA refresh endpoint; cập nhật lưu trữ\nexport async function refreshOAToken(pair: OATokenPair): Promise&lt;OATokenPair&gt; { /* ... */ }\n\n// Đảm bảo token còn hiệu lực; refresh nếu còn &lt; 10 phút hết hạn\nexport async function ensureFreshToken(): Promise&lt;string&gt; { /* ... */ }</code></pre>\n<pre><code class=\"language-typescript\">// services/genie-api/lib/zns-window.ts\n\nexport interface SendWindowResult {\n  allowed: boolean;\n  reason?: \"outside_hour_range\" | \"event_too_far\" | \"event_past\";\n}\n\n// Kiểm tra khung 06:00-22:00 Asia/Ho_Chi_Minh\nexport function isWithinHourWindow(now: Date): boolean {\n  const vnHour = new Date(now.toLocaleString(\"en-US\", { timeZone: \"Asia/Ho_Chi_Minh\" })).getHours();\n  return vnHour &gt;= 6 &amp;&amp; vnHour &lt; 22;\n}\n\n// Kiểm tra 0 &lt;= daysUntilEvent &lt;= 7\nexport function isWithinDayRange(eventDate: Date, now: Date): SendWindowResult {\n  const msPerDay = 86_400_000;\n  const diff = Math.floor((eventDate.getTime() - now.getTime()) / msPerDay);\n  if (diff &lt; 0) return { allowed: false, reason: \"event_past\" };\n  if (diff &gt; 7) return { allowed: false, reason: \"event_too_far\" };\n  return { allowed: true };\n}\n\nexport function canSendNow(eventDate: Date, now: Date): SendWindowResult {\n  if (!isWithinHourWindow(now)) return { allowed: false, reason: \"outside_hour_range\" };\n  return isWithinDayRange(eventDate, now);\n}</code></pre>\n<pre><code class=\"language-typescript\">// services/genie-api/lib/zns-client.ts\n\nexport interface ZNSPayload {\n  phone: string;             // số điện thoại người nhận (đã lấy qua OA API)\n  templateId: string;        // ID template đã duyệt của Zalo OA\n  templateData: {\n    ten: string;             // tham số {tên}\n    ngay_duong: string;      // tham số {ngày dương}, ví dụ \"08/08/2025\"\n    dip: string;             // tham số {dịp}, ví dụ \"Giỗ Bà Nội\"\n    ngay_am: string;         // tham số {ngày âm}, ví dụ \"15/7 Ất Tỵ\"\n    [key: string]: string;   // các tham số phụ thêm nếu template mở rộng\n  };\n  trackingId?: string;       // để đối chiếu với zns_send_log.reminderId\n}\n\nexport interface ZNSSendResult {\n  success: boolean;\n  zaloMessageId?: string;\n  errorCode?: string;\n  errorMessage?: string;\n}\n\n// Gửi tin ZNS qua OA trực tiếp hoặc qua nhà phân phối\n// accessToken: kết quả từ ensureFreshToken()\nexport async function sendZNS(payload: ZNSPayload, accessToken: string): Promise&lt;ZNSSendResult&gt; {\n  // POST https://business.openapi.zalo.me/message/template\n  // Headers: access_token: accessToken\n  // Body: { phone, template_id, template_data }\n  // ...\n}</code></pre>\n<pre><code class=\"language-typescript\">// services/genie-api/lib/zns-scheduler.ts\nimport { convertLunar2Solar } from \"@cyberskill/amlich-core\";\nimport { canSendNow } from \"./zns-window\";\nimport { sendZNS } from \"./zns-client\";\nimport { ensureFreshToken } from \"./oa-token\";\n\nexport interface SchedulerReminder {\n  id: string;\n  title: string;\n  lunarDay: number;\n  lunarMonth: number;\n  lunarYear?: number;\n  isLeapMonth: boolean;\n  recurrence: \"MONTHLY\" | \"ANNUAL\" | \"ONCE\"; // PHẢI có: MONTHLY = Rằm/Mùng Một lặp mỗi tháng; ANNUAL = đám giỗ lặp mỗi năm; ONCE = chỉ 1 lần\n  leadTimes: number[];\n  notifyTime: string;       // \"HH:MM\"\n  userPhone: string;        // số điện thoại người dùng (đã đổi từ token)\n  userName: string;\n  channels: string[];\n  enabled: boolean;\n}\n\nexport interface CronRunResult {\n  scanned: number;\n  sent: number;\n  skipped: number;\n  errors: number;\n}\n\n// Tính tất cả năm-tháng cần quét để sinh occurrence cho một reminder.\n// - MONTHLY: quét tháng dương hiện tại + tháng tới (đủ để cron 15 phút bắt được sự kiện cách &lt;= 7 ngày)\n// - ANNUAL / ONCE: quét năm âm hiện tại và năm âm tới (reminder.lunarYear nếu có, không thì ước tính)\nfunction candidateLunarYears(\n  reminder: SchedulerReminder,\n  now: Date\n): Array&lt;{ lunarYear: number; lunarMonth: number }&gt; {\n  const currentYear = now.getFullYear();\n  if (reminder.recurrence === \"MONTHLY\") {\n    // Rằm/Mùng Một: lunarMonth thay đổi mỗi tháng âm; sinh occurrence cho 2 tháng âm tiếp theo.\n    // Dùng mốc âm lịch của ngày hiện tại làm neo - tăng lunarMonth thủ công, wrap tại 12.\n    // Để đảm bảo bao trùm \"7 ngày tới\" không bị bỏ sót tháng giáp ranh, quét cả tháng hiện tại\n    // và tháng tiếp theo với năm âm ước tính currentYear và currentYear+1.\n    const months: Array&lt;{ lunarYear: number; lunarMonth: number }&gt; = [];\n    for (const yearOff of [0, 1]) {\n      for (let mOffset = 0; mOffset &lt;= 1; mOffset++) {\n        // MONTHLY reminder có lunarMonth = 0 (mọi tháng) hoặc cụ thể;\n        // nếu = 0 thì sinh occurrence theo từng tháng dương tương ứng (tính bên ngoài).\n        // Ở đây truyền lunarMonth của reminder nếu cụ thể, hoặc tháng dương hiện tại nếu = 0.\n        const lm = reminder.lunarMonth !== 0\n          ? reminder.lunarMonth\n          : (now.getMonth() + 1 + mOffset - 1) % 12 + 1;\n        months.push({ lunarYear: currentYear + yearOff, lunarMonth: lm });\n      }\n    }\n    return months;\n  }\n  // ANNUAL hoặc ONCE: 2 năm âm ước tính (currentYear, currentYear+1) với lunarMonth cố định\n  return [\n    { lunarYear: reminder.lunarYear ?? currentYear, lunarMonth: reminder.lunarMonth },\n    { lunarYear: reminder.lunarYear ?? currentYear + 1, lunarMonth: reminder.lunarMonth },\n  ];\n}\n\n// Hàm chính được gọi bởi cron\nexport async function runZNSCron(\n  reminders: SchedulerReminder[],\n  now: Date = new Date()\n): Promise&lt;CronRunResult&gt; {\n  const TZ = 7.0; // Asia/Ho_Chi_Minh, kinh tuyến 105E\n  const accessToken = await ensureFreshToken();\n  const result: CronRunResult = { scanned: 0, sent: 0, skipped: 0, errors: 0 };\n\n  for (const reminder of reminders) {\n    if (!reminder.enabled || !reminder.channels.includes(\"ZNS\")) { result.skipped++; continue; }\n\n    // Sinh danh sách (lunarYear, lunarMonth) cần kiểm tra tùy theo recurrence.\n    // MONTHLY: nhiều tháng; ANNUAL/ONCE: 1-2 năm với tháng cố định.\n    const candidates = candidateLunarYears(reminder, now);\n\n    for (const { lunarYear, lunarMonth } of candidates) {\n      for (const leadDays of reminder.leadTimes) {\n        // convertLunar2Solar trả về tuple SolarDate = [dd, mm, yy] (TASK-LUNAR-001);\n        // tháng nhuận không khớp trả sentinel [0, 0, 0] (KHÔNG phải null) -&gt; phải kiểm tra rõ.\n        const [gd, gm, gy] = convertLunar2Solar(\n          reminder.lunarDay, lunarMonth, lunarYear, reminder.isLeapMonth ? 1 : 0, TZ\n        );\n        if (gd === 0 &amp;&amp; gm === 0 &amp;&amp; gy === 0) { result.skipped++; continue; }\n\n        const mmStr = String(gm).padStart(2, \"0\");\n        const ddStr = String(gd).padStart(2, \"0\");\n        const eventDate = new Date(`${gy}-${mmStr}-${ddStr}T${reminder.notifyTime}:00+07:00`);\n\n        result.scanned++;\n        const check = canSendNow(eventDate, now);\n        if (!check.allowed) { result.skipped++; continue; }\n\n        // Kiểm tra đã gửi chưa (tránh gửi trùng - query zns_send_log)\n        // ...\n\n        const sendResult = await sendZNS({\n          phone: reminder.userPhone,\n          templateId: process.env.ZNS_TEMPLATE_ID!,\n          templateData: {\n            ten: reminder.userName,\n            // Dùng tuple ICT (gd/gm/gy) từ engine, KHÔNG đọc eventDate.getDate()/getMonth()\n            // vì chúng đọc theo TZ runtime của server (lệch 1 ngày trên server UTC).\n            ngay_duong: `${ddStr}/${mmStr}/${gy}`,\n            dip: reminder.title,\n            ngay_am: `${reminder.lunarDay}/${lunarMonth}`,\n          },\n          trackingId: `${reminder.id}-${lunarYear}-${lunarMonth}-${leadDays}`,\n        }, accessToken);\n\n        if (sendResult.success) result.sent++;\n        else result.errors++;\n      }\n    }\n  }\n  return result;\n}</code></pre>\n<pre><code class=\"language-typescript\">// services/genie-api/api/zns.ts (serverless handler - Vercel Function)\nimport type { VercelRequest, VercelResponse } from \"@vercel/node\";\nimport { runZNSCron } from \"../lib/zns-scheduler\";\n\n// Được gọi bởi Vercel Cron (vercel.json schedule)\nexport default async function handler(req: VercelRequest, res: VercelResponse) {\n  // Xác thực CRON_SECRET để tránh gọi tùy ý\n  if (req.headers[\"authorization\"] !== `Bearer ${process.env.CRON_SECRET}`) {\n    return res.status(401).json({ error: \"unauthorized\" });\n  }\n  // Đọc reminders từ Supabase\n  // ...\n  const cronResult = await runZNSCron(reminders);\n  res.json({ ok: true, ...cronResult });\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>isWithinHourWindow</code> returns <code>true</code> for 06:00 VN and <code>false</code> for 05:59 VN and 22:00 VN (the boundaries); test with a specific date at the corresponding UTC hour.</li><li><code>isWithinDayRange</code> returns <code>allowed: true</code> for <code>daysUntilEvent = 0</code> and <code>= 7</code>; returns <code>event_past</code> for <code>-1</code>; returns <code>event_too_far</code> for <code>8</code>.</li><li><code>canSendNow</code> returns <code>outside_hour_range</code> when the VN hour is 23:00 even if the event date is valid.</li><li><code>runZNSCron</code> with 1 valid Reminder inside the send window: calls <code>sendZNS</code> exactly once with a payload that has all 4 parameters <code>ten</code>, <code>ngay_duong</code>, <code>dip</code>, <code>ngay_am</code>; returns <code>{ sent: 1 }</code>.</li><li><code>runZNSCron</code> with a Reminder that has <code>daysUntilEvent = 8</code>: does not call <code>sendZNS</code>; returns <code>{ skipped: 1 }</code>.</li><li><code>runZNSCron</code> with a Reminder that has <code>enabled = false</code>: skips it; returns <code>{ skipped: 1 }</code>.</li><li><code>runZNSCron</code> for an event already in <code>zns_send_log</code> on the current day: does not call <code>sendZNS</code> a second time; returns <code>{ skipped: 1 }</code>.</li><li><code>ensureFreshToken</code> calls <code>refreshOAToken</code> when the token has under 10 minutes to expiry; does not call it when the token has &gt; 30 minutes left.</li><li><code>sendZNS</code> with a mock OA API returning success: writes 1 row into <code>zns_send_log</code> with <code>status = \"success\"</code> and a non-empty <code>zaloMessageId</code>.</li><li><code>sendZNS</code> with a mock OA API returning an error (error code \"invalid_phone\"): writes 1 row into <code>zns_send_log</code> with <code>status = \"error\"</code> and <code>errorCode = \"invalid_phone\"</code>; the function returns <code>{ success: false }</code>.</li><li>The lunar date is computed with <code>convertLunar2Solar</code> from <code>amlich-core</code>; the result matches the PRD 6.6 fixture (29/01/2025 is 1/1/2025 At Ty).</li><li>The <code>api/zns.ts</code> handler returns 401 when the <code>Authorization</code> header is wrong; returns 200 <code>{ ok: true }</code> when it is correct.</li><li>The default template \"Chao {ten}, ngay mai ({ngay duong}) la {dip} ({ngay am}). Dung quen chuan bi nhe!\" has &lt;= 400 characters (counted in code in the test); the parameters <code>{ten}</code>, <code>{ngay duong}</code>, <code>{dip}</code>, <code>{ngay am}</code> appear at least once each.</li><li>No OA_ACCESS_TOKEN or OA_REFRESH_TOKEN appears in the client bundle (checked in the <code>zmp build</code> output).</li><li><code>runZNSCron</code> with a <code>RAM</code> Reminder that has <code>recurrence = \"MONTHLY\"</code>, <code>lunarDay = 15</code>, <code>lunarMonth = 0</code> (every month), when <code>now = 2025-08-07</code> (1 day before the solar 15th of the 7th lunar month): generates ZNS exactly once for the 7th lunar month; when called again with <code>now = 2025-09-05</code> (the 8th lunar month): generates exactly once for the 8th lunar month - each send falls within the 06:00-22:00 Asia/Ho_Chi_Minh window and within 7 days of the event.</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// services/genie-api/__tests__/zns-window.test.ts\nimport { describe, it, expect } from \"vitest\";\nimport { isWithinHourWindow, isWithinDayRange, canSendNow } from \"../lib/zns-window\";\n\ndescribe(\"isWithinHourWindow\", () =&gt; {\n  it(\"06:00 VN (23:00 UTC-1) -&gt; true\", () =&gt; {\n    // 06:00 ICT = 23:00 UTC trước đó\n    const now = new Date(\"2025-08-08T23:00:00Z\"); // 06:00 ICT\n    expect(isWithinHourWindow(now)).toBe(true);\n  });\n\n  it(\"05:59 VN -&gt; false\", () =&gt; {\n    const now = new Date(\"2025-08-08T22:59:00Z\"); // 05:59 ICT\n    expect(isWithinHourWindow(now)).toBe(false);\n  });\n\n  it(\"22:00 VN -&gt; false (biên trên exclusive)\", () =&gt; {\n    const now = new Date(\"2025-08-08T15:00:00Z\"); // 22:00 ICT\n    expect(isWithinHourWindow(now)).toBe(false);\n  });\n\n  it(\"21:59 VN -&gt; true\", () =&gt; {\n    const now = new Date(\"2025-08-08T14:59:00Z\"); // 21:59 ICT\n    expect(isWithinHourWindow(now)).toBe(true);\n  });\n});\n\ndescribe(\"isWithinDayRange\", () =&gt; {\n  const now = new Date(\"2025-08-01T08:00:00+07:00\");\n\n  it(\"sự kiện hôm nay (0 ngày) -&gt; allowed\", () =&gt; {\n    const event = new Date(\"2025-08-01T07:00:00+07:00\");\n    expect(isWithinDayRange(event, now).allowed).toBe(true);\n  });\n\n  it(\"sự kiện 7 ngày tới -&gt; allowed\", () =&gt; {\n    const event = new Date(\"2025-08-08T07:00:00+07:00\");\n    expect(isWithinDayRange(event, now).allowed).toBe(true);\n  });\n\n  it(\"sự kiện 8 ngày tới -&gt; event_too_far\", () =&gt; {\n    const event = new Date(\"2025-08-09T07:00:00+07:00\");\n    const result = isWithinDayRange(event, now);\n    expect(result.allowed).toBe(false);\n    expect(result.reason).toBe(\"event_too_far\");\n  });\n\n  it(\"sự kiện quá hạn (-1 ngày) -&gt; event_past\", () =&gt; {\n    const event = new Date(\"2025-07-31T07:00:00+07:00\");\n    const result = isWithinDayRange(event, now);\n    expect(result.allowed).toBe(false);\n    expect(result.reason).toBe(\"event_past\");\n  });\n});\n\ndescribe(\"canSendNow\", () =&gt; {\n  it(\"giờ VN 23:00, sự kiện 3 ngày tới -&gt; outside_hour_range\", () =&gt; {\n    const now = new Date(\"2025-08-01T16:00:00Z\"); // 23:00 ICT\n    const event = new Date(\"2025-08-04T07:00:00+07:00\");\n    expect(canSendNow(event, now).reason).toBe(\"outside_hour_range\");\n  });\n});\n\n// Template length check\nit(\"template mặc định &lt;= 400 ký tự và có &gt;= 1 tham số\", () =&gt; {\n  const template = \"Chào {tên}, ngày mai ({ngày dương}) là {dịp} ({ngày âm}). Đừng quên chuẩn bị nhé!\";\n  expect(template.length).toBeLessThanOrEqual(400);\n  expect(template).toContain(\"{tên}\");\n  expect(template).toContain(\"{ngày dương}\");\n  expect(template).toContain(\"{dịp}\");\n  expect(template).toContain(\"{ngày âm}\");\n});</code></pre>\n<pre><code class=\"language-typescript\">// services/genie-api/__tests__/zns-scheduler.test.ts\nimport { describe, it, expect, vi } from \"vitest\";\nimport { runZNSCron } from \"../lib/zns-scheduler\";\nimport type { SchedulerReminder } from \"../lib/zns-scheduler\";\n\nconst validReminder: SchedulerReminder = {\n  id: \"rem-001\", title: \"Giỗ Bà Nội\",\n  lunarDay: 15, lunarMonth: 7, isLeapMonth: false,\n  recurrence: \"ANNUAL\",\n  leadTimes: [1], notifyTime: \"07:00\",\n  userPhone: \"0909123456\", userName: \"Chị Linh\",\n  channels: [\"ZNS\"], enabled: true,\n};\n\ndescribe(\"runZNSCron\", () =&gt; {\n  it(\"gửi thành công cho reminder hợp lệ trong khung\", async () =&gt; {\n    // Mock: sự kiện 15/7 Ất Tỵ = 2025-08-08; now = 2025-08-07 08:00 ICT (1 ngày trước)\n    vi.mock(\"../lib/zns-client\", () =&gt; ({\n      sendZNS: vi.fn(async () =&gt; ({ success: true, zaloMessageId: \"zalo_msg_001\" })),\n    }));\n    vi.mock(\"../lib/oa-token\", () =&gt; ({\n      ensureFreshToken: vi.fn(async () =&gt; \"mock_access_token\"),\n    }));\n    // Mock zns_send_log check -&gt; chưa gửi\n    const now = new Date(\"2025-08-07T08:00:00+07:00\");\n    const result = await runZNSCron([validReminder], now);\n    expect(result.sent).toBe(1);\n    expect(result.errors).toBe(0);\n  });\n\n  it(\"bỏ qua reminder có enabled = false\", async () =&gt; {\n    const now = new Date(\"2025-08-07T08:00:00+07:00\");\n    const result = await runZNSCron([{ ...validReminder, enabled: false }], now);\n    expect(result.skipped).toBe(1);\n    expect(result.sent).toBe(0);\n  });\n\n  it(\"bỏ qua khi sự kiện cách 8 ngày\", async () =&gt; {\n    // Sự kiện 15/7 Ất Tỵ = 08/08/2025; now = 31/07/2025 (8 ngày trước)\n    const now = new Date(\"2025-07-31T08:00:00+07:00\");\n    const result = await runZNSCron([validReminder], now);\n    expect(result.skipped).toBeGreaterThanOrEqual(1);\n  });\n\n  it(\"MONTHLY RAM reminder - gửi đúng 1 lần mỗi tháng, trong khung 06:00-22:00 và &lt;= 7 ngày\", async () =&gt; {\n    vi.mock(\"../lib/zns-client\", () =&gt; ({\n      sendZNS: vi.fn(async () =&gt; ({ success: true, zaloMessageId: \"zalo_monthly_001\" })),\n    }));\n    vi.mock(\"../lib/oa-token\", () =&gt; ({\n      ensureFreshToken: vi.fn(async () =&gt; \"mock_token\"),\n    }));\n\n    const monthlyRamReminder: SchedulerReminder = {\n      id: \"rem-ram\", title: \"Rằm hàng tháng\",\n      lunarDay: 15, lunarMonth: 0, // 0 = mọi tháng\n      isLeapMonth: false,\n      recurrence: \"MONTHLY\",\n      leadTimes: [1], notifyTime: \"07:00\",\n      userPhone: \"0909123456\", userName: \"Chị Linh\",\n      channels: [\"ZNS\"], enabled: true,\n    };\n\n    // Tháng 7 âm lịch: Rằm 15/7 Ất Tỵ = 08/08/2025; now = 07/08/2025 08:00 ICT (1 ngày trước = leadDays=1)\n    const nowAug = new Date(\"2025-08-07T08:00:00+07:00\");\n    const resultAug = await runZNSCron([monthlyRamReminder], nowAug);\n    expect(resultAug.sent).toBeGreaterThanOrEqual(1); // ít nhất 1 occurrence tháng 7 âm\n\n    // Tháng 8 âm lịch: Rằm 15/8 Ất Tỵ ước tính ~05/09/2025; now = 04/09/2025 08:00 ICT\n    const nowSep = new Date(\"2025-09-04T08:00:00+07:00\");\n    const resultSep = await runZNSCron([monthlyRamReminder], nowSep);\n    expect(resultSep.sent).toBeGreaterThanOrEqual(1); // ít nhất 1 occurrence tháng 8 âm khác tháng trước\n  });\n});\n\n// services/genie-api/__tests__/oa-token.test.ts\ndescribe(\"ensureFreshToken\", () =&gt; {\n  it(\"refresh khi còn &lt; 10 phút hết hạn\", async () =&gt; {\n    vi.mock(\"../lib/oa-token\", async (importOriginal) =&gt; {\n      const actual = await importOriginal&lt;typeof import(\"../lib/oa-token\")&gt;();\n      return {\n        ...actual,\n        getOATokenPair: vi.fn(async () =&gt; ({\n          accessToken: \"old_token\",\n          refreshToken: \"refresh_123\",\n          expiresAt: Date.now() + 5 * 60 * 1000, // còn 5 phút\n        })),\n        refreshOAToken: vi.fn(async () =&gt; ({\n          accessToken: \"new_token\",\n          refreshToken: \"refresh_456\",\n          expiresAt: Date.now() + 3600 * 1000,\n        })),\n      };\n    });\n    const { ensureFreshToken } = await import(\"../lib/oa-token\");\n    const token = await ensureFreshToken();\n    expect(token).toBe(\"new_token\");\n  });\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The full contract is in §3. Key points:</p>\n<pre><code class=\"language-typescript\">// services/genie-api/supabase/migrations/0018_zns_send_log.sql\nCREATE TABLE zns_send_log (\n  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  reminder_id TEXT NOT NULL,\n  phone       TEXT NOT NULL,\n  sent_at     TIMESTAMPTZ NOT NULL DEFAULT now(),\n  status      TEXT NOT NULL,       -- 'success' | 'error'\n  zalo_message_id TEXT,\n  error_code  TEXT,\n  error_message TEXT\n);\nCREATE INDEX ON zns_send_log (reminder_id, phone, sent_at);</code></pre>\n<p>Idempotency check (avoid duplicate sends - AC #7):</p>\n<pre><code class=\"language-typescript\">// Trong runZNSCron, trước khi gửi\nconst today = new Date(now).toISOString().slice(0, 10);\nconst alreadySent = await supabase\n  .from(\"zns_send_log\")\n  .select(\"id\")\n  .eq(\"reminder_id\", `${reminder.id}-${leadDays}`)\n  .gte(\"sent_at\", `${today}T00:00:00Z`)\n  .maybeSingle();\nif (alreadySent.data) { result.skipped++; continue; }</code></pre>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream: <strong>TASK-LUNAR-004</strong> (the <code>Reminder</code> data model; the fields <code>lunarDay</code>, <code>lunarMonth</code>, <code>isLeapMonth</code>, <code>channels</code>, <code>enabled</code> are used directly in <code>SchedulerReminder</code>). <strong>TASK-LUNAR-016</strong> (the Zalo Mini App client provides the <code>User.phone</code> token and the list of Reminders with <code>channels: [\"ZNS\"]</code>; without TASK-016 there is no data source).</p>\n<p>Downstream: No task depends on TASK-017.</p>\n<p>Cross-cutting: <code>@cyberskill/amlich-core</code> is used in the cron (server-side) to compute the solar date from the lunar date; consistent with the client. <code>zns_send_log</code> is the data source for ZNS cost analytics in TASK-LUNAR-020 (freemium billing).</p>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"zns_request\": {\n    \"phone\": \"0909123456\",\n    \"template_id\": \"ZNS_TMPL_GENIE_001\",\n    \"template_data\": {\n      \"ten\": \"Chi Linh\",\n      \"ngay_duong\": \"08/08/2025\",\n      \"dip\": \"Gio Ba Noi\",\n      \"ngay_am\": \"15/7 At Ty\"\n    },\n    \"tracking_id\": \"rem-001-1\"\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"zns_response_success\": {\n    \"error\": 0,\n    \"message\": \"Success\",\n    \"data\": {\n      \"message_id\": \"zalo_msg_abc123\",\n      \"sent_time\": 1754630400000\n    }\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"zns_response_error\": {\n    \"error\": -214,\n    \"message\": \"Phone number does not have a Zalo account\"\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"zns_send_log_row\": {\n    \"id\": \"uuid-001\",\n    \"reminder_id\": \"rem-001-1\",\n    \"phone\": \"0909123456\",\n    \"sent_at\": \"2025-08-07T08:00:00+07:00\",\n    \"status\": \"success\",\n    \"zalo_message_id\": \"zalo_msg_abc123\",\n    \"error_code\": null,\n    \"error_message\": null\n  }\n}</code></pre>\n<p><strong>Default template (registered with the Zalo OA):</strong></p>\n<pre><code>Chào {tên}, ngày mai ({ngày dương}) là {dịp} ({ngày âm}). Đừng quên chuẩn bị nhé!</code></pre>\n<p>Length: 84 characters (&lt; 400). Personalization parameters: 4 (&gt;= 1). No advertising content.</p>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li>Direct OA integration or via a distributor: handled by DEC-LUNAR-175 (deferred; the API contract is neutral to both directions).</li><li>Cron interval: chose 15 minutes (Vercel Cron supports it); enough to ensure the message arrives within the first hour.</li><li>Idempotency: use <code>zns_send_log</code> + index (rem_id, phone, date) to avoid duplicate sends (AC #7, §6).</li><li><strong>Support for MONTHLY (Ram/Mung Mot) via ZNS: RESOLVED - FULL COMMERCIAL support.</strong> Founder decision 2026-06-28: \"Genie Am Lich\" is a full commercial product; the MONTHLY Ram and Mung Mot reminders are a core use case and MUST send ZNS correctly every month. <code>SchedulerReminder</code> gains the <code>recurrence</code> field; <code>candidateLunarYears()</code> is added to <code>runZNSCron</code> to expand across multiple lunar months in the scan window; AC #15 and the MONTHLY test in §5 confirm the behavior. Note: BACKLOG decision 8 needs updating to reflect the decision to support MONTHLY via ZNS.</li></ul>\n<p>Still open:</p>\n<ul><li>The ~200 VND ZNS price is a reference figure at research time; reconfirm with VietGuys or directly with Zalo at official OA registration (PRD Caveats - price and rules may change).</li><li>Zalo's \"transactional relationship\" rule: need to confirm exactly what a \"transactional relationship\" that qualifies for sending ZNS is - the user may need to interact with the OA via chat first. Deferred to the OA registration process.</li><li>ZNS rate limit of the OA: Zalo may cap the number of messages/minute/month per OA; check at registration and add throttling if needed. MONTHLY reminders increase ZNS volume by roughly 12x compared to ANNUAL - monitor closely at registration so as not to exceed the rate limit.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>OA access token expires mid-cron</td><td><code>ensureFreshToken</code> checks expiry before each batch</td><td>Refresh before sending; no batch fails</td><td>Automatic; log the refresh event</td></tr><tr><td>OA refresh token expires (revoked)</td><td><code>refreshOAToken</code> returns an error</td><td>The entire cron batch skips ZNS sending; critical alert</td><td>Operator reissues the token manually; alert</td></tr><tr><td>CRON_SECRET wrong - someone calls /api/zns</td><td>Header check -&gt; 401</td><td>Request rejected; nothing sent</td><td>No action needed; log it</td></tr><tr><td>User's phone number is not on Zalo</td><td>ZNS API returns code -214</td><td>Log the error, mark it in zns_send_log</td><td>Notify the user \"This phone number cannot receive ZNS\"</td></tr><tr><td>Duplicate send because cron triggered multiple times</td><td>Idempotency check zns_send_log</td><td>The 2nd time is skipped</td><td>No impact; log \"already_sent\"</td></tr><tr><td>Outside the 06:00-22:00 window</td><td><code>isWithinHourWindow</code> -&gt; false</td><td>Reminder is skipped; sent on the next cron run</td><td>Automatic if the cron runs again within the window</td></tr><tr><td>Event more than 7 days away</td><td><code>isWithinDayRange</code> -&gt; event_too_far</td><td>Skip; do not send</td><td>The Reminder will be handled when it is closer</td></tr><tr><td>Template does not match the registered parameters</td><td>ZNS API returns \"invalid_template_data\" error</td><td>Log the error; skip this reminder</td><td>Recheck the <code>templateData</code> key names against the approved template</td></tr><tr><td>Supabase has no phone row for the user</td><td><code>reminder.userPhone</code> null/undefined</td><td>Skip the reminder; log a warning</td><td>The user needs to grant the phone permission in the Mini App</td></tr><tr><td>ZNS OA rate limit exceeded</td><td>ZNS API returns HTTP 429</td><td>Stop sending; log; delay</td><td>Handle retry on the next cron run with exponential backoff</td></tr><tr><td><code>convertLunar2Solar</code> returns the sentinel <code>[0,0,0]</code> (leap month does not match / invalid lunar day)</td><td>Check <code>gd===0 &amp;&amp; gm===0 &amp;&amp; gy===0</code> after the call (NOT null)</td><td>Skip the occurrence; log a warning</td><td>Debug the Reminder data; notify the user</td></tr><tr><td>Vercel Cron disabled / budget exceeded</td><td>No cron log for 1+ hour</td><td>The user receives no reminders</td><td>Monitor alert; check the Vercel dashboard</td></tr><tr><td>OA token leaked into logs</td><td>Audit log pattern</td><td>Detected if the token appears in plaintext</td><td>Mask the token in all log statements</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li><code>getPhoneNumber</code> in the Zalo Mini App returns a token, not the real number; the server must exchange this token for the number via the Zalo OA API (<code>POST https://openapi.zalo.me/v2.0/oa/getuserfromtoken</code>) before storing it in <code>User.phone</code>. This exchange must be a separate endpoint called right after the Mini App sends up the token.</li><li>The official ZNS API endpoint is <code>POST https://business.openapi.zalo.me/message/template</code>; the <code>access_token</code> header (not Bearer); the body is JSON with <code>phone</code>, <code>template_id</code>, <code>template_data</code>. Recheck the endpoint at implementation time because Zalo may update it.</li><li>The 06:00-22:00 window must be computed in <code>Asia/Ho_Chi_Minh</code> (ICT, UTC+7), not the server's UTC; use <code>toLocaleString(\"en-US\", { timeZone: \"Asia/Ho_Chi_Minh\" })</code> or the <code>date-fns-tz</code> library to avoid confusion.</li><li>Idempotency is mandatory: Vercel Cron may run more than once due to retries; <code>zns_send_log</code> with the index <code>(reminder_id, phone, sent_at::date)</code> is the primary protective barrier.</li><li>The template must be registered and approved with the Zalo OA before Deploy; the approval process can take 1-5 business days. Keep <code>ZNS_TEMPLATE_ID</code> in an environment variable; do not hardcode it in the code.</li><li>When using a distributor (VietGuys etc.), the endpoint and request structure may differ; <code>zns-client.ts</code> is designed so the backend can be swapped without changing <code>zns-scheduler.ts</code>.</li><li><code>zns_send_log</code> needs to be analyzed monthly to report ZNS cost; the total <code>COUNT(*) WHERE status='success'</code> * 200 VND is the estimated monthly fee.</li><li>The P3 migration numbers are distributed to avoid collisions: 0016-0017 for TASK-018, 0018 for TASK-017, 0019 for TASK-019, 0020 for TASK-020.</li></ul>\n<p><em>End of TASK-LUNAR-017.</em></p>";

window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LUNAR-009"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This module <strong>MUST</strong> build the purple style pack as a sub-brand extension of the CyberSkill design system, sufficient for the entire \"Genie Am Lich\" UI to use unified tokens and meet the APCA contrast standard.</p>\n<ol><li><strong>MUST</strong> organize tokens in 3 layers: primitive (raw hex values), semantic (UI role: text, background, border, accent, error), component (per-component such as button-primary-bg, card-border) - this structure is kept from the CyberSkill design system, overriding only the color values (DEC-LUNAR-094).</li><li><strong>MUST NOT</strong> replace or override the base CyberSkill brand tokens <code>--color-brand-umber: #45210E</code> and <code>--color-brand-ochre: #F4BA17</code>; these two tokens are kept intact and may be used in the footer or logo (DEC-LUNAR-090).</li><li><strong>MUST</strong> define the purple palette with at least: <code>purple-900</code> (the darkest purple, for main text), <code>purple-700</code> (mid purple, for secondary text and buttons), <code>purple-500</code> (mid-light purple, accent), <code>purple-200</code> (light purple, subtle border), <code>purple-50</code> (very light purple, hover state) as CSS variables and TypeScript constants.</li><li><strong>MUST</strong> define <code>--color-bg-default: #FDF6EC</code> (warm cream) as the default background instead of <code>#FFFFFF</code>, to harmonize with the CyberSkill \"warm earth\" DNA and improve APCA on cream over white (DEC-LUNAR-093).</li><li><strong>MUST</strong> provide the function <code>checkApca(fgHex: string, bgHex: string): number</code> returning the (signed) APCA Lc value using the <code>apca-w3</code> library; and the function <code>assertApca(fgHex, bgHex, minLc: number)</code> throwing an Error if <code>|Lc| &lt; minLc</code> (DEC-LUNAR-091).</li><li><strong>MUST</strong> provide the function <code>checkWcag(fgHex: string, bgHex: string): number</code> returning the WCAG 2.x contrast ratio; and the function <code>assertWcag21AA(fgHex, bgHex)</code> throwing an Error if ratio &lt; 4.5 (DEC-LUNAR-091).</li><li><strong>MUST</strong> have an automated test suite checking each text/background color pair in the palette, ensuring APCA Lc &gt;= 75 for body text and WCAG AA &gt;= 4.5:1 (DEC-LUNAR-091, NFR-Accessibility).</li><li><strong>MUST</strong> use only <strong>Be Vietnam Pro</strong> as the main typeface; import from the Google Fonts CDN in production or bundle locally for the offline PWA; define the font scale: <code>font-size-xs</code> (12px), <code>font-size-sm</code> (14px), <code>font-size-base</code> (16px), <code>font-size-lg</code> (18px), <code>font-size-xl</code> (20px), <code>font-size-2xl</code> (24px), <code>font-size-3xl</code> (30px) (DEC-LUNAR-092).</li><li><strong>MUST</strong> support Dynamic Type on iOS by using <code>rem</code> or <code>em</code> instead of <code>px</code> for font-size in the components; and test that the <code>Typography</code> component does not use hardcoded <code>px</code> for font-size (NFR-Accessibility).</li><li><strong>MUST</strong> provide a <code>Typography</code> component with variants: <code>heading-1</code>, <code>heading-2</code>, <code>heading-3</code>, <code>body</code>, <code>body-small</code>, <code>caption</code>; each variant uses the correct font-size and font-weight token.</li><li><strong>MUST</strong> provide a <code>Button</code> component with variants: <code>primary</code> (deep purple background, cream text), <code>secondary</code> (purple border, cream background), <code>ghost</code> (no border, purple text); each variant <strong>MUST</strong> reach APCA Lc &gt;= 75 between the text and background.</li><li><strong>MUST</strong> provide a <code>Card</code> component with a soft purple style (faint purple border, cream background, light shadow), suitable for wrapping occasion and reminder content.</li><li><strong>MUST</strong> export all tokens as the TypeScript object <code>PURPLE_TOKENS</code> for Tailwind config, CSS-in-JS, or a <code>style</code> prop to use directly; and also export them as CSS custom properties in a <code>.css</code> file for use with Tailwind.</li><li><strong>MUST NOT</strong> use <code>!important</code> to override; the CSS cascade order must be enough for the purple tokens to win without <code>!important</code>.</li><li><strong>SHOULD</strong> ensure long body text (<code>body</code> variant, &gt;= 18px/400 weight) reaches APCA Lc &gt;= 90 per NFR-Accessibility \"prefer Lc 90 for long passages\".</li><li><strong>SHOULD</strong> provide the color <code>--color-error: #B91C1C</code> (deep red) reaching APCA Lc &gt;= 75 on the cream background, for use in form validation.</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why a sub-brand extension, not a separate theme (DEC-LUNAR-090)?</strong> If we create a wholly separate design system for \"Genie Am Lich\", then later when CyberSkill updates spacing, component APIs, or the typography scale, we would have to sync two systems in parallel. Keeping the token structure intact and overriding only the color values lets \"Genie Am Lich\" inherit every spacing and component update from the base design system automatically.</p>\n<p><strong>Why keep Umber #45210E and Ochre #F4BA17 (DEC-LUNAR-090)?</strong> These two colors are the CyberSkill brand identity - used in the logo and official brand materials. Removing them from the tokens would break the ability to render the CyberSkill logo in the correct color in the footer or about screen. The purple pack is the sub-brand of a specific product, not a replacement for the whole brand.</p>\n<p><strong>Why APCA Lc &gt;= 75 instead of just WCAG AA (DEC-LUNAR-091)?</strong> WCAG 2.x uses a non-linear contrast formula and gives poor results with mid-tone purple. PRD §5 and §13 specify APCA Lc &gt;= 75 because APCA reflects real readability more accurately. WCAG AA (4.5:1) still runs in parallel to meet the legal requirement (many organizations still require WCAG 2.x).</p>\n<p><strong>Why the cream background <code>#FDF6EC</code> instead of white (DEC-LUNAR-093)?</strong> Deep purple on cream reaches a higher APCA than deep purple on pure white because cream has a lower luminance than white, increasing the contrast. More importantly, warm cream fits the CyberSkill \"warm earth\" DNA and creates a warm, intimate feeling close to the tone of rituals - fitting the persona of Chi Linh.</p>\n<p><strong>Why 3 token layers (DEC-LUNAR-094)?</strong> The primitive layer is the raw value (hex). The semantic layer is meaning (text-primary, bg-default). The component layer is a specific binding (button-primary-bg = semantic text-primary, card-border = semantic border-subtle). The semantic layer is the most important - when the button color needs to change, only one semantic token changes and every component using that token updates automatically.</p>\n<p><strong>Why Be Vietnam Pro (DEC-LUNAR-092)?</strong> This is the official CyberSkill typeface, with full Unicode support for Vietnamese (including the hoi/nga/huyen/sac/nang tone marks on both uppercase and lowercase). Using Be Vietnam Pro ensures lunar dates such as \"Mung Mot thang Gieng\" display with correct diacritics on every device, independent of the system font.</p>\n<p><strong>Why <code>apca-w3</code> instead of computing by hand?</strong> The APCA formula is not simple - it uses gamma correction and an S-curve nonlinearity. The <code>apca-w3</code> library is the official implementation from W3C/APCA, reducing the risk of miscomputation. An automated gate in CI blocks shipping low-contrast colors.</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// packages/ui/src/theme/tokens.ts\n\n// === Layer 1: Primitive tokens (raw hex values) ===\nexport const PRIMITIVE = {\n  // Purple palette\n  \"purple-900\": \"#2D0A4E\",\n  \"purple-800\": \"#3D1266\",\n  \"purple-700\": \"#5B21B6\",\n  \"purple-600\": \"#7C3AED\",\n  \"purple-500\": \"#8B5CF6\",\n  \"purple-400\": \"#A78BFA\",\n  \"purple-200\": \"#DDD6FE\",\n  \"purple-100\": \"#EDE9FE\",\n  \"purple-50\":  \"#F5F3FF\",\n\n  // Warm cream / neutral\n  \"cream-50\":   \"#FDF6EC\",\n  \"cream-100\":  \"#FBF0DC\",\n  \"cream-200\":  \"#F5E0C0\",\n\n  // CyberSkill base brand - DO NOT OVERRIDE (DEC-LUNAR-090)\n  \"brand-umber\":  \"#45210E\",\n  \"brand-ochre\":  \"#F4BA17\",\n\n  // Error / warning\n  \"red-700\":    \"#B91C1C\",\n  \"amber-600\":  \"#D97706\",\n\n  // Neutral\n  \"gray-900\":   \"#111827\",\n  \"gray-600\":   \"#4B5563\",\n  \"gray-400\":   \"#9CA3AF\",\n  \"white\":      \"#FFFFFF\",\n} as const;\n\n// === Layer 2: Semantic tokens ===\nexport const SEMANTIC = {\n  // Text\n  \"text-primary\":        PRIMITIVE[\"purple-900\"],   // main body text on cream bg\n  \"text-secondary\":      PRIMITIVE[\"purple-700\"],   // secondary / UI labels\n  \"text-disabled\":       PRIMITIVE[\"gray-400\"],\n  \"text-on-primary\":     PRIMITIVE[\"cream-50\"],     // text on purple-800 button\n\n  // Backgrounds\n  \"bg-default\":          PRIMITIVE[\"cream-50\"],     // page background (DEC-LUNAR-093)\n  \"bg-surface\":          PRIMITIVE[\"white\"],         // card surface\n  \"bg-subtle\":           PRIMITIVE[\"purple-50\"],     // hover, selected\n  \"bg-primary\":          PRIMITIVE[\"purple-800\"],   // primary button bg\n\n  // Borders\n  \"border-default\":      PRIMITIVE[\"purple-200\"],\n  \"border-strong\":       PRIMITIVE[\"purple-700\"],\n  \"border-subtle\":       PRIMITIVE[\"purple-100\"],\n\n  // Accent / interactive\n  \"accent-primary\":      PRIMITIVE[\"purple-600\"],\n  \"accent-secondary\":    PRIMITIVE[\"purple-400\"],\n\n  // Semantic status\n  \"error\":               PRIMITIVE[\"red-700\"],\n  \"warning\":             PRIMITIVE[\"amber-600\"],\n\n  // Brand (preserved from CyberSkill base, DEC-LUNAR-090)\n  \"brand-umber\":         PRIMITIVE[\"brand-umber\"],\n  \"brand-ochre\":         PRIMITIVE[\"brand-ochre\"],\n} as const;\n\n// === Layer 3: Component tokens ===\nexport const COMPONENT = {\n  \"button-primary-bg\":       SEMANTIC[\"bg-primary\"],\n  \"button-primary-text\":     SEMANTIC[\"text-on-primary\"],\n  \"button-primary-hover-bg\": PRIMITIVE[\"purple-700\"],\n  \"button-secondary-bg\":     SEMANTIC[\"bg-default\"],\n  \"button-secondary-text\":   SEMANTIC[\"text-secondary\"],\n  \"button-secondary-border\": SEMANTIC[\"border-strong\"],\n  \"button-ghost-text\":       SEMANTIC[\"accent-primary\"],\n\n  \"card-bg\":                 SEMANTIC[\"bg-surface\"],\n  \"card-border\":             SEMANTIC[\"border-subtle\"],\n  \"card-shadow\":             \"0 1px 4px rgba(91, 33, 182, 0.08)\",  // purple-700 #5B21B6 = rgb(91,33,182) - VERIFIED (audit: rgba(93,..) da duoc sua thanh (91,..))\n\n  \"day-cell-today-ring\":     SEMANTIC[\"bg-primary\"],\n  \"day-cell-festival-dot\":   SEMANTIC[\"accent-primary\"],\n  \"day-cell-reminder-dot\":   SEMANTIC[\"accent-secondary\"],\n} as const;\n\nexport const PURPLE_TOKENS = { ...PRIMITIVE, ...SEMANTIC, ...COMPONENT } as const;\n\n// === Typography scale ===\nexport const TYPOGRAPHY = {\n  fontFamily: \"'Be Vietnam Pro', system-ui, sans-serif\",\n  fontSizes: {\n    xs:   \"0.75rem\",   // 12px\n    sm:   \"0.875rem\",  // 14px\n    base: \"1rem\",      // 16px\n    lg:   \"1.125rem\",  // 18px\n    xl:   \"1.25rem\",   // 20px\n    \"2xl\":\"1.5rem\",    // 24px\n    \"3xl\":\"1.875rem\",  // 30px\n  },\n  fontWeights: {\n    normal:   \"400\",\n    medium:   \"500\",\n    semibold: \"600\",\n    bold:     \"700\",\n  },\n} as const;</code></pre>\n<pre><code class=\"language-typescript\">// packages/ui/src/theme/apca.ts\nimport { APCAcontrast, sRGBtoY } from \"apca-w3\";\n\n/**\n * Tinh APCA Lc (co dau am/duong) cho cap mau chu/nen.\n * fgHex, bgHex: hex string co hoac khong co dau #\n */\nexport function checkApca(fgHex: string, bgHex: string): number {\n  const fg = hexToRgbArray(fgHex);\n  const bg = hexToRgbArray(bgHex);\n  return APCAcontrast(sRGBtoY(fg), sRGBtoY(bg)) as number;\n}\n\n/**\n * Throw Error neu |Lc| &lt; minLc.\n * minLc = 75 cho body text; 90 cho body text dai &gt;= 18px/400.\n */\nexport function assertApca(fgHex: string, bgHex: string, minLc: number): void {\n  const lc = Math.abs(checkApca(fgHex, bgHex));\n  if (lc &lt; minLc) {\n    throw new Error(\n      `APCA fail: ${fgHex} on ${bgHex} = Lc ${lc.toFixed(1)} &lt; ${minLc}`\n    );\n  }\n}\n\n/** WCAG 2.x relative luminance ratio */\nexport function checkWcag(fgHex: string, bgHex: string): number {\n  const fgL = wcagLuminance(hexToRgbArray(fgHex));\n  const bgL = wcagLuminance(hexToRgbArray(bgHex));\n  const [l1, l2] = fgL &gt; bgL ? [fgL, bgL] : [bgL, fgL];\n  return (l1 + 0.05) / (l2 + 0.05);\n}\n\nexport function assertWcag21AA(fgHex: string, bgHex: string): void {\n  const ratio = checkWcag(fgHex, bgHex);\n  if (ratio &lt; 4.5) {\n    throw new Error(`WCAG AA fail: ${fgHex} on ${bgHex} = ${ratio.toFixed(2)} &lt; 4.5`);\n  }\n}\n\nfunction hexToRgbArray(hex: string): [number, number, number] {\n  const h = hex.replace(\"#\", \"\");\n  return [parseInt(h.slice(0, 2), 16), parseInt(h.slice(2, 4), 16), parseInt(h.slice(4, 6), 16)];\n}\n\nfunction wcagLuminance([r, g, b]: [number, number, number]): number {\n  const [rs, gs, bs] = [r, g, b].map(c =&gt; {\n    const v = c / 255;\n    return v &lt;= 0.04045 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4);\n  });\n  return 0.2126 * rs + 0.7152 * gs + 0.0722 * bs;\n}</code></pre>\n<pre><code class=\"language-tsx\">// packages/ui/src/components/Typography.tsx\nimport React from \"react\";\nimport { TYPOGRAPHY, SEMANTIC } from \"../theme/tokens\";\n\ntype TypographyVariant = \"heading-1\" | \"heading-2\" | \"heading-3\" | \"body\" | \"body-small\" | \"caption\";\n\ninterface TypographyProps {\n  variant: TypographyVariant;\n  children: React.ReactNode;\n  className?: string;\n  as?: keyof JSX.IntrinsicElements;\n}\n\nconst VARIANT_STYLES: Record&lt;TypographyVariant, React.CSSProperties&gt; = {\n  \"heading-1\":  { fontSize: TYPOGRAPHY.fontSizes[\"3xl\"], fontWeight: TYPOGRAPHY.fontWeights.bold,     color: SEMANTIC[\"text-primary\"] },\n  \"heading-2\":  { fontSize: TYPOGRAPHY.fontSizes[\"2xl\"], fontWeight: TYPOGRAPHY.fontWeights.semibold,  color: SEMANTIC[\"text-primary\"] },\n  \"heading-3\":  { fontSize: TYPOGRAPHY.fontSizes.xl,    fontWeight: TYPOGRAPHY.fontWeights.semibold,  color: SEMANTIC[\"text-primary\"] },\n  \"body\":       { fontSize: TYPOGRAPHY.fontSizes.lg,    fontWeight: TYPOGRAPHY.fontWeights.normal,    color: SEMANTIC[\"text-primary\"] },\n  \"body-small\": { fontSize: TYPOGRAPHY.fontSizes.base,  fontWeight: TYPOGRAPHY.fontWeights.normal,    color: SEMANTIC[\"text-primary\"] },\n  \"caption\":    { fontSize: TYPOGRAPHY.fontSizes.sm,    fontWeight: TYPOGRAPHY.fontWeights.normal,    color: SEMANTIC[\"text-secondary\"] },\n};\n\nexport function Typography({ variant, children, className, as: Tag = \"p\" }: TypographyProps) {\n  return (\n    &lt;Tag\n      className={className}\n      style={{ fontFamily: TYPOGRAPHY.fontFamily, ...VARIANT_STYLES[variant] }}\n    &gt;\n      {children}\n    &lt;/Tag&gt;\n  );\n}</code></pre>\n<pre><code class=\"language-tsx\">// packages/ui/src/components/Button.tsx\nimport React from \"react\";\nimport { COMPONENT, SEMANTIC } from \"../theme/tokens\";\n\ntype ButtonVariant = \"primary\" | \"secondary\" | \"ghost\";\n\ninterface ButtonProps extends React.ButtonHTMLAttributes&lt;HTMLButtonElement&gt; {\n  variant?: ButtonVariant;\n}\n\nexport function Button({ variant = \"primary\", children, style, ...props }: ButtonProps) {\n  const styles: Record&lt;ButtonVariant, React.CSSProperties&gt; = {\n    primary: {\n      backgroundColor: COMPONENT[\"button-primary-bg\"],\n      color: COMPONENT[\"button-primary-text\"],\n      border: \"none\",\n    },\n    secondary: {\n      backgroundColor: COMPONENT[\"button-secondary-bg\"],\n      color: COMPONENT[\"button-secondary-text\"],\n      border: `1.5px solid ${COMPONENT[\"button-secondary-border\"]}`,\n    },\n    ghost: {\n      backgroundColor: \"transparent\",\n      color: COMPONENT[\"button-ghost-text\"],\n      border: \"none\",\n    },\n  };\n  return (\n    &lt;button style={{ ...styles[variant], ...style, borderRadius: \"0.5rem\", padding: \"0.625rem 1.25rem\" }} {...props}&gt;\n      {children}\n    &lt;/button&gt;\n  );\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>PURPLE_TOKENS</code> exports an object with all 3 layers: primitive (at least 9 purple values, 2 cream), semantic (at least text-primary, bg-default, border-default, accent-primary, brand-umber, brand-ochre), component (at least button-primary-bg, card-border, day-cell-today-ring).</li><li><code>PRIMITIVE[\"brand-umber\"]</code> === <code>\"#45210E\"</code> and <code>PRIMITIVE[\"brand-ochre\"]</code> === <code>\"#F4BA17\"</code> - these two values MUST NOT be changed (DEC-LUNAR-090).</li><li><code>SEMANTIC[\"bg-default\"]</code> returns a warm cream value (not <code>\"#FFFFFF\"</code>); specifically <code>PRIMITIVE[\"cream-50\"]</code> = <code>\"#FDF6EC\"</code> or equivalent (DEC-LUNAR-093).</li><li><code>checkApca(SEMANTIC[\"text-primary\"], SEMANTIC[\"bg-default\"])</code> returns |Lc| &gt;= 75 (deep purple body text on the cream background).</li><li><code>checkApca(COMPONENT[\"button-primary-text\"], COMPONENT[\"button-primary-bg\"])</code> returns |Lc| &gt;= 75 (cream text on the deep purple button).</li><li><code>checkWcag(SEMANTIC[\"text-primary\"], SEMANTIC[\"bg-default\"])</code> returns ratio &gt;= 4.5 (WCAG AA).</li><li><code>checkWcag(COMPONENT[\"button-primary-text\"], COMPONENT[\"button-primary-bg\"])</code> returns ratio &gt;= 4.5.</li><li><code>assertApca(PRIMITIVE[\"purple-400\"], SEMANTIC[\"bg-default\"], 75)</code> <strong>MUST</strong> throw an Error (light purple does not have enough contrast on cream - confirming the gate works correctly).</li><li><code>TYPOGRAPHY.fontFamily</code> contains <code>\"Be Vietnam Pro\"</code> as the first family in the font stack.</li><li>The <code>Typography</code> component with the \"body\" variant does not use the <code>px</code> unit for <code>fontSize</code> in the style output (checked by inspecting the style object).</li><li>The <code>Button</code> component \"primary\" variant renders with <code>backgroundColor</code> = <code>COMPONENT[\"button-primary-bg\"]</code> and <code>color</code> = <code>COMPONENT[\"button-primary-text\"]</code>.</li><li>All text/background color pairs in the SEMANTIC layer are checked in <code>apca.test.ts</code> and all pass Lc &gt;= 75 (except text-disabled by design).</li><li><code>SEMANTIC[\"text-primary\"]</code> on <code>SEMANTIC[\"bg-default\"]</code> reaches |Lc| &gt;= 90 (NFR-Accessibility prefers Lc 90 for body text).</li><li>Building <code>packages/ui</code> has no TypeScript error and does not use <code>!important</code> in the CSS output.</li><li>The package has only one external dependency, <code>apca-w3</code>; does not import React Native or Capacitor-specific code in <code>tokens.ts</code> and <code>apca.ts</code>.</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// packages/ui/src/theme/apca.test.ts\nimport { checkApca, assertApca, checkWcag, assertWcag21AA } from \"./apca\";\nimport { SEMANTIC, COMPONENT, PRIMITIVE } from \"./tokens\";\n\ndescribe(\"APCA color gate (DEC-LUNAR-091)\", () =&gt; {\n  test(\"text-primary tren bg-default dat APCA |Lc| &gt;= 90 (body text dai)\", () =&gt; {\n    const lc = Math.abs(checkApca(SEMANTIC[\"text-primary\"], SEMANTIC[\"bg-default\"]));\n    expect(lc).toBeGreaterThanOrEqual(90);\n  });\n\n  test(\"text-secondary tren bg-default dat APCA |Lc| &gt;= 75\", () =&gt; {\n    const lc = Math.abs(checkApca(SEMANTIC[\"text-secondary\"], SEMANTIC[\"bg-default\"]));\n    expect(lc).toBeGreaterThanOrEqual(75);\n  });\n\n  test(\"button-primary-text tren button-primary-bg dat APCA |Lc| &gt;= 75\", () =&gt; {\n    const lc = Math.abs(checkApca(COMPONENT[\"button-primary-text\"], COMPONENT[\"button-primary-bg\"]));\n    expect(lc).toBeGreaterThanOrEqual(75);\n  });\n\n  test(\"Tim nhat (purple-400) tren nen kem KHONG du tuong phan - gate hoat dong\", () =&gt; {\n    expect(() =&gt; assertApca(PRIMITIVE[\"purple-400\"], SEMANTIC[\"bg-default\"], 75)).toThrow(\"APCA fail\");\n  });\n\n  test(\"error tren bg-default dat APCA |Lc| &gt;= 75\", () =&gt; {\n    const lc = Math.abs(checkApca(SEMANTIC[\"error\"], SEMANTIC[\"bg-default\"]));\n    expect(lc).toBeGreaterThanOrEqual(75);\n  });\n});\n\ndescribe(\"WCAG AA parallel gate (DEC-LUNAR-091)\", () =&gt; {\n  test(\"text-primary tren bg-default dat WCAG AA &gt;= 4.5:1\", () =&gt; {\n    expect(checkWcag(SEMANTIC[\"text-primary\"], SEMANTIC[\"bg-default\"])).toBeGreaterThanOrEqual(4.5);\n  });\n\n  test(\"button-primary-text tren button-primary-bg dat WCAG AA\", () =&gt; {\n    expect(checkWcag(COMPONENT[\"button-primary-text\"], COMPONENT[\"button-primary-bg\"])).toBeGreaterThanOrEqual(4.5);\n  });\n\n  test(\"assertWcag21AA throw khi tuong phan thap\", () =&gt; {\n    // Mau xam nhat tren trang la truong hop xau\n    expect(() =&gt; assertWcag21AA(PRIMITIVE[\"gray-400\"], PRIMITIVE[\"white\"])).toThrow(\"WCAG AA fail\");\n  });\n});\n\ndescribe(\"Base brand tokens bao ton (DEC-LUNAR-090)\", () =&gt; {\n  test(\"brand-umber = #45210E\", () =&gt; {\n    expect(PRIMITIVE[\"brand-umber\"]).toBe(\"#45210E\");\n  });\n\n  test(\"brand-ochre = #F4BA17\", () =&gt; {\n    expect(PRIMITIVE[\"brand-ochre\"]).toBe(\"#F4BA17\");\n  });\n});\n\ndescribe(\"Typography\", () =&gt; {\n  test(\"TYPOGRAPHY.fontFamily bat dau bang Be Vietnam Pro\", () =&gt; {\n    expect(TYPOGRAPHY.fontFamily.startsWith(\"'Be Vietnam Pro'\")).toBe(true);\n  });\n\n  test(\"Tat ca font-size trong TYPOGRAPHY.fontSizes dung don vi rem (khong dung px)\", () =&gt; {\n    Object.values(TYPOGRAPHY.fontSizes).forEach(size =&gt; {\n      expect(size).toMatch(/rem$/);\n      expect(size).not.toMatch(/px$/);\n    });\n  });\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The API contract in §3 is a full skeleton. One point to pin down: <code>sRGBtoY</code> in <code>apca-w3</code> takes an <code>[r, g, b]</code> array as a <code>Uint8ClampedArray</code> or <code>number[]</code> (0-255), not a hex string. The <code>hexToRgbArray</code> function in <code>apca.ts</code> must convert correctly before passing it in.</p>\n<pre><code class=\"language-typescript\">// Cach dung apca-w3 dung (khong phai APCAcontrast(hex, hex)):\nimport { APCAcontrast, sRGBtoY } from \"apca-w3\";\nconst fg = [45, 10, 78];   // purple-900 rgb\nconst bg = [253, 246, 236]; // cream-50 rgb\nconst lc = APCAcontrast(sRGBtoY(fg as any), sRGBtoY(bg as any));\n// lc ~= -92.x (am vi toi tren sang, |lc| = 92 &gt; 90 -&gt; PASS)</code></pre>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream: TASK-LUNAR-009 has no depends_on - it is a foundation layer, buildable independently. The only external dependency is <code>apca-w3</code> (npm package, MIT license).</p>\n<p>Downstream: <code>TASK-LUNAR-010</code> (app shell) imports tokens and components from <code>packages/ui</code> to apply the theme globally; <code>TASK-LUNAR-014</code> (shareable cards) uses <code>COMPONENT[\"day-cell-festival-dot\"]</code> and the purple palette to render exported cards; <code>TASK-LUNAR-016</code> (Zalo Mini App) uses <code>PURPLE_TOKENS</code> as design tokens.</p>\n<p>Cross-cutting: <code>TASK-LUNAR-007</code> (CalendarGrid) uses <code>COMPONENT[\"day-cell-today-ring\"]</code>, <code>\"day-cell-festival-dot\"</code>, <code>\"day-cell-reminder-dot\"</code> to color the day cells. Every UI component across the whole project must import tokens from this package instead of hardcoding hex.</p>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-typescript\">// Ket qua khi import va dung tokens\nimport { SEMANTIC, COMPONENT, checkApca, checkWcag } from \"@cyberskill/genie-ui\";\n\nconsole.log(SEMANTIC[\"text-primary\"]);      // \"#2D0A4E\" (purple-900)\nconsole.log(SEMANTIC[\"bg-default\"]);         // \"#FDF6EC\" (cream-50)\nconsole.log(COMPONENT[\"button-primary-bg\"]); // \"#3D1266\" (purple-800)\n\nconst lc = Math.abs(checkApca(\"#2D0A4E\", \"#FDF6EC\"));\n// lc = ~93.2 -&gt; PASS (&gt;= 90)\n\nconst wcag = checkWcag(\"#2D0A4E\", \"#FDF6EC\");\n// wcag = ~14.8 -&gt; PASS (&gt;= 4.5)</code></pre>\n<pre><code class=\"language-css\">/* Tailwind config extension dung PURPLE_TOKENS */\n/* apps/web/tailwind.config.ts */\nimport { PURPLE_TOKENS, TYPOGRAPHY } from \"@cyberskill/genie-ui\";\n\nexport default {\n  theme: {\n    extend: {\n      colors: {\n        \"text-primary\":    PURPLE_TOKENS[\"text-primary\"],\n        \"bg-default\":      PURPLE_TOKENS[\"bg-default\"],\n        \"accent-primary\":  PURPLE_TOKENS[\"accent-primary\"],\n        \"brand-umber\":     PURPLE_TOKENS[\"brand-umber\"],\n        \"brand-ochre\":     PURPLE_TOKENS[\"brand-ochre\"],\n      },\n      fontFamily: {\n        sans: [\"Be Vietnam Pro\", \"system-ui\", \"sans-serif\"],\n      },\n    },\n  },\n};</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li>How to organize the 3 token layers: primitive / semantic / component (DEC-LUNAR-094).</li><li>Background value: <code>#FDF6EC</code> warm cream (DEC-LUNAR-093).</li><li>APCA gate: use <code>apca-w3</code>, threshold Lc &gt;= 75 / 90 (DEC-LUNAR-091).</li></ul>\n<p>Still deferred:</p>\n<ul><li>Dark mode: PRD §13 does not require it explicitly but TASK-LUNAR-007 §4 AC #15 mentions it. Deferred - a separate dark purple palette needs designing; does not block Phase 1.</li><li>CSS custom properties file: a <code>purple-tokens.css</code> generated from the <code>PURPLE_TOKENS</code> object is needed for use with Tailwind v4 or vanilla CSS. Deferred - implemented when wiring into TASK-LUNAR-010.</li><li>Be Vietnam Pro offline bundle: in the offline PWA, the font needs bundling into <code>public/fonts/</code>. Deferred to TASK-LUNAR-010 (the app shell handles the service worker and font caching).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Light purple (purple-400) used as main text, low contrast</td><td><code>assertApca</code> gate in CI</td><td>APCA fail throws</td><td>Switch to purple-700 or purple-900</td></tr><tr><td>brand-umber/ochre overwritten</td><td>Unit test on the primitive brand values</td><td>Brand violates DEC-LUNAR-090</td><td>Revert the token, add a lint rule forbidding the overwrite</td></tr><tr><td>Be Vietnam Pro does not load (CDN error)</td><td>Fallback font-stack</td><td>Shows system-ui, still readable</td><td>Bundle the font locally in the PWA (TASK-010)</td></tr><tr><td>sRGBtoY gets the wrong type, checkApca returns NaN</td><td>Test asserting typeof === number</td><td>Gate does not work</td><td>Fix hexToRgbArray, add a type assertion</td></tr><tr><td>The Button component uses !important</td><td>CSS output lint</td><td>Cascade break</td><td>Remove !important, increase selector specificity</td></tr><tr><td>px font-size in Typography</td><td>Unit test regex /rem$/</td><td>Dynamic Type does not work on iOS</td><td>Switch to rem/em</td></tr><tr><td>PURPLE_TOKENS cannot be exported due to a circular import</td><td>Build error</td><td>The app shell cannot import it</td><td>Split primitives, semantics, components into 3 files</td></tr><tr><td>A new apca-w3 version changes the API</td><td>pnpm audit / type error</td><td>checkApca errors at runtime</td><td>Pin the version, test after each update</td></tr><tr><td>The cream background does not match the \"warm earth\" DNA after design</td><td>Real design review</td><td>Wrong sub-brand vibe</td><td>Adjust the hex in cream-50 after an A/B test</td></tr><tr><td>Text-disabled reaches APCA &gt;= 75 (too clear)</td><td>Visual review</td><td>Disabled looks like enabled</td><td>Allow text-disabled below 75 (per the APCA spec: disabled need not meet the threshold)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li>The value <code>#FDF6EC</code> for the cream background is a starting point; it should be reviewed on a real screen (OLED vs LCD display cream differently). If the cream is too yellow, adjust the L (lightness) up a little with a color picker, then rerun the APCA test.</li><li><code>APCAcontrast</code> from <code>apca-w3</code> returns a negative value when text is dark on a light background (dark-on-light) and positive when text is light on a dark background. So the code must use <code>Math.abs(lc)</code> when comparing against the 75/90 threshold.</li><li>The 3 token layers (primitive / semantic / component) should live in 3 separate consts (not 1 flat object) so TypeScript infers the type accurately and IDE autocomplete works well.</li><li><code>apca-w3</code> must be both a devDependency and a runtime dependency of <code>packages/ui</code>; it need not be a direct dependency of <code>apps/web</code> - import from <code>@cyberskill/genie-ui</code>.</li><li>When Tailwind v4 is used (instead of v3), the config extension follows the <code>@theme</code> block, not <code>theme.extend</code>. TASK-010 chooses the Tailwind version; TASK-009 only exports tokens, without coupling to a specific Tailwind version.</li><li>The <code>Button</code> component in §3 uses an inline <code>style</code> prop for the demo; in real production, use a Tailwind class or CSS module so that CSS purge works better. This is a decision for TASK-010 to make.</li></ul>\n<p><em>End of TASK-LUNAR-009.</em></p>";

window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LUNAR-010"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>The app shell <strong>MUST</strong> set up the technical foundation for the entire Phase 1 MVP: Next.js/React PWA, a Capacitor wrapper for iOS, on-device storage, routing, and the glue layer for local notifications.</p>\n<ol><li><strong>MUST</strong> initialize a Next.js project (App Router, TypeScript 5.x) at <code>apps/web/</code> with Tailwind CSS; import <code>@cyberskill/amlich-core</code> and <code>@cyberskill/genie-ui</code> from the workspace packages (DEC-LUNAR-100).</li><li><strong>MUST</strong> configure the 5 main routes with the Next.js App Router: <code>/</code> (home - today), <code>/calendar</code> (month calendar grid), <code>/reminders</code> (reminder list), <code>/settings</code> (settings), <code>/festival/[id]</code> (occasion detail page) (DEC-LUNAR-102).</li><li><strong>MUST</strong> provide <code>apps/web/lib/storage.ts</code> exporting the CRUD functions for <code>Reminder[]</code>: <code>getReminders()</code>, <code>saveReminder(r: Reminder)</code>, <code>deleteReminder(id: string)</code>, <code>updateReminder(r: Reminder)</code>, <code>getSettings()</code>, <code>saveSettings(s: UserSettings)</code> - stored on-device, with no network call (DEC-LUNAR-101).</li><li><strong>MUST</strong> implement the storage adapter: on the web (browser) use <code>localStorage</code> or <code>IndexedDB</code>; on iOS via Capacitor use <code>@capacitor/preferences</code>; the interface must be uniform so the code need not know where it is running (DEC-LUNAR-101).</li><li><strong>MUST</strong> provide <code>apps/web/lib/notificationGlue.ts</code> exporting the <code>NotificationService</code> interface with the methods <code>scheduleNotification(opts)</code>, <code>cancelAllPending()</code>, <code>requestPermission()</code>; on the web (non-Capacitor) the implementation is a no-op stub; on Capacitor the implementation calls <code>@capacitor/local-notifications</code> (DEC-LUNAR-103).</li><li><strong>MUST</strong> configure <code>capacitor.config.ts</code> with <code>appId: \"world.cyberskill.genieamlich\"</code>, <code>appName: \"Genie Am Lich\"</code>, <code>webDir: \"out\"</code> (Next.js static export), and declare the <code>LocalNotifications</code> plugin (DEC-LUNAR-103).</li><li><strong>MUST</strong> configure <code>next.config.ts</code> to enable static export (<code>output: \"export\"</code>) so Capacitor can package the web build; do NOT use Next.js server-side rendering or API routes in Phase 1 (DEC-LUNAR-100, DEC-LUNAR-101).</li><li><strong>MUST</strong> create <code>public/manifest.json</code> with <code>name</code>, <code>short_name</code>, <code>theme_color</code> (purple from <code>PURPLE_TOKENS</code>), <code>background_color</code> (cream from <code>PURPLE_TOKENS</code>), <code>display: \"standalone\"</code>, and <code>icons</code> for the PWA (DEC-LUNAR-104).</li><li><strong>MUST</strong> display a bottom navigation bar with 4 tabs: Today, Calendar, Reminders, Settings; the active tab is highlighted with a purple token; navigation <strong>MUST</strong> work on both the web and iOS Capacitor.</li><li><strong>MUST</strong> on the <code>/</code> (home) route display today including: the full solar date, the lunar date, the day/month/year can-chi by calling <code>convertSolar2Lunar</code> from <code>amlich-core</code>; and the list of upcoming reminders (up to 3 items) read from storage.</li><li><strong>MUST</strong> import the Be Vietnam Pro font from Google Fonts in <code>app/layout.tsx</code> (or bundle locally in <code>public/fonts/</code> for offline); apply <code>font-family</code> to the whole app via the Tailwind config or global CSS.</li><li><strong>MUST</strong> ensure the app shell builds successfully with <code>pnpm build</code> (Next.js static export, no TypeScript error, no missing import from the workspace packages).</li><li><strong>MUST</strong> ensure the home route <code>/</code> renders today's lunar date correctly when running <code>pnpm dev</code> or after a build (integration test).</li><li><strong>MUST NOT</strong> store any user data on a server in Phase 1; every Reminder and Settings exists only on the device (DEC-LUNAR-101, NFR-Privacy).</li><li><strong>SHOULD</strong> configure a service worker (via <code>next-pwa</code> or equivalent) to cache static assets for offline; prioritize caching the Be Vietnam Pro font and the <code>amlich-core</code> bundle (DEC-LUNAR-104).</li><li><strong>SHOULD</strong> detect the Capacitor runtime (<code>Capacitor.isNativePlatform()</code>) and show small UI adjustments (for example hiding some web-only behavior) without a separate build config.</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why Next.js/React + Capacitor instead of React Native (DEC-LUNAR-100)?</strong> React Native requires rewriting the UI layer with native components, losing the advantage of code-sharing with the web. Capacitor wraps that same web build, so the HCMC web team that knows React need not learn native APIs; it only needs to add a Capacitor plugin for notifications. PRD §9 specifies Capacitor and states clearly \"If advanced animation is needed later, consider RN\" - but for the MVP, Capacitor wins on speed.</p>\n<p><strong>Why static export (<code>output: \"export\"</code>) instead of Next.js SSR (DEC-LUNAR-101)?</strong> Phase 1 has no backend. Capacitor requires static HTML/JS/CSS files in the <code>out/</code> directory to package into the iOS app bundle. SSR requires a Node.js server runtime - unusable in Capacitor. Static export is the natural choice and ensures the whole app works offline.</p>\n<p><strong>Why the storage adapter pattern (DEC-LUNAR-101)?</strong> <code>localStorage</code> works on the web but Capacitor iOS recommends <code>@capacitor/preferences</code> to ensure the data is not cleared by iOS under low storage. Wrapping the two implementations behind a common interface lets us unit test storage on the web without the Capacitor runtime.</p>\n<p><strong>Why is <code>notificationGlue.ts</code> a no-op stub on the web (DEC-LUNAR-103)?</strong> <code>@capacitor/local-notifications</code> only works with a native runtime. If imported directly into a component, the build would error in the web or test environment. The stub pattern lets TASK-005 and TASK-006 test the scheduling logic without a real iOS device.</p>\n<p><strong>Why <code>appId: \"world.cyberskill.genieamlich\"</code> (DEC-LUNAR-103)?</strong> The bundle ID follows the CyberSkill reverse-domain convention (<code>world.cyberskill.*</code>). It must be decided early because App Store Connect requires the bundle ID when creating the app record and it cannot be changed later.</p>\n<p><strong>Why is the PWA manifest mandatory (DEC-LUNAR-104)?</strong> Web push notification on iOS 16.4+ only works when the PWA has been \"Add to Home Screen\" - without a manifest there is no PWA. For Android and desktop, the manifest also allows installing the app from the browser. PRD Key Finding #6 states clearly that web push is \"supplementary\", not the main channel, but it still needs to be set up correctly.</p>\n<p><strong>Why no API route in Phase 1 (DEC-LUNAR-101)?</strong> Next.js API routes require a server runtime, conflicting with <code>output: \"export\"</code>. Phase 1 needs no backend (no AI, no ZNS). When Phase 2 needs a Claude proxy (<code>/api/genie</code>), it will move to Vercel Functions or a separate service without changing the client code.</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// apps/web/lib/storage.ts\n// KHONG static-import \"@capacitor/preferences\" o top level: no la plugin native, keo vao web\n// bundle / static-export build va lam JSDOM test phai resolve mot package chi chay tren iOS.\n// Adapter duoi day import dong (lazy) chi khi dang chay tren Capacitor - giong ky luat stub cua\n// notificationGlue.ts (DEC-LUNAR-103). Tren web/test, nhanh localStorage khong cham Capacitor.\n\n// TASK-LUNAR-004 so huu kieu Reminder (bao gom truong optional notificationStyle).\n// Shell KHONG DUOC redeclare lai de tranh drift schema giua 004 va 010.\nimport type { Reminder } from \"@cyberskill/amlich-core\";\nexport type { Reminder };\n\nexport interface UserSettings {\n  locale: \"vi-VN\";\n  timezone: \"Asia/Ho_Chi_Minh\";\n  theme: \"purple\";\n  notifyTime: string;  // e.g. \"07:00\"\n}\n\n// Storage adapter - same interface for web (localStorage) and Capacitor (Preferences)\nexport async function getReminders(): Promise&lt;Reminder[]&gt;;\nexport async function saveReminder(r: Reminder): Promise&lt;void&gt;;\nexport async function updateReminder(r: Reminder): Promise&lt;void&gt;;\nexport async function deleteReminder(id: string): Promise&lt;void&gt;;\nexport async function getSettings(): Promise&lt;UserSettings&gt;;\nexport async function saveSettings(s: UserSettings): Promise&lt;void&gt;;\n\n// Internal: detect Capacitor vs web.\n// PHAI guard `typeof window` truoc: optional chaining KHONG cuu mot bien global chua khai bao -\n// `window?.x` van nem ReferenceError khi `window` khong ton tai (SSG/static-export prerender).\nfunction isCapacitor(): boolean {\n  return typeof window !== \"undefined\" &amp;&amp; !!(window as any).Capacitor?.isNativePlatform?.();\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/lib/notificationGlue.ts\nexport interface ScheduleOptions {\n  id: number;\n  title: string;\n  body: string;\n  scheduleAt: Date;\n  extra: { reminderId: string };\n}\n\nexport interface NotificationService {\n  requestPermission(): Promise&lt;boolean&gt;;\n  scheduleNotification(opts: ScheduleOptions): Promise&lt;void&gt;;\n  scheduleMany(opts: ScheduleOptions[]): Promise&lt;void&gt;;\n  cancelAllPending(): Promise&lt;void&gt;;\n}\n\n// Stub implementation for web (non-Capacitor):\nexport class WebNotificationStub implements NotificationService {\n  async requestPermission() { return false; }\n  async scheduleNotification(_: ScheduleOptions) { /* no-op */ }\n  async scheduleMany(_: ScheduleOptions[]) { /* no-op */ }\n  async cancelAllPending() { /* no-op */ }\n}\n\n// Capacitor implementation (wraps @capacitor/local-notifications):\nexport class CapacitorNotificationService implements NotificationService {\n  async requestPermission(): Promise&lt;boolean&gt;;\n  async scheduleNotification(opts: ScheduleOptions): Promise&lt;void&gt;;\n  async scheduleMany(opts: ScheduleOptions[]): Promise&lt;void&gt;;\n  async cancelAllPending(): Promise&lt;void&gt;;\n}\n\n// Factory - returns correct implementation at runtime:\nexport function createNotificationService(): NotificationService;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/capacitor.config.ts\nimport type { CapacitorConfig } from \"@capacitor/cli\";\n\nconst config: CapacitorConfig = {\n  appId: \"world.cyberskill.genieamlich\",\n  appName: \"Genie Am Lich\",\n  webDir: \"out\",\n  plugins: {\n    LocalNotifications: {\n      smallIcon: \"ic_stat_icon_config_sample\",\n      iconColor: \"#3D1266\",   // purple-800 from tokens\n      sound: \"default\",\n    },\n  },\n};\n\nexport default config;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/next.config.ts\nimport type { NextConfig } from \"next\";\n\nconst nextConfig: NextConfig = {\n  output: \"export\",        // static export for Capacitor (DEC-LUNAR-100)\n  trailingSlash: true,     // required for static hosting\n  images: { unoptimized: true }, // required for static export\n  transpilePackages: [\"@cyberskill/amlich-core\", \"@cyberskill/genie-ui\"],\n};\n\nexport default nextConfig;</code></pre>\n<pre><code class=\"language-tsx\">// apps/web/app/layout.tsx (excerpt)\nimport type { Metadata } from \"next\";\nimport { SEMANTIC } from \"@cyberskill/genie-ui\";\n\nexport const metadata: Metadata = {\n  title: \"Genie Am Lich\",\n  description: \"Tro ly am lich Viet Nam cua CyberSkill\",\n  manifest: \"/manifest.json\",\n  themeColor: SEMANTIC[\"bg-primary\"],  // purple-800\n};\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    &lt;html lang=\"vi\"&gt;\n      &lt;head&gt;\n        &lt;link rel=\"preconnect\" href=\"https://fonts.googleapis.com\" /&gt;\n        &lt;link href=\"https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;500;600;700&amp;display=swap\" rel=\"stylesheet\" /&gt;\n      &lt;/head&gt;\n      &lt;body style={{ backgroundColor: SEMANTIC[\"bg-default\"], fontFamily: \"'Be Vietnam Pro', sans-serif\" }}&gt;\n        {children}\n        &lt;BottomNav /&gt;\n      &lt;/body&gt;\n    &lt;/html&gt;\n  );\n}</code></pre>\n<pre><code class=\"language-json\">// apps/web/public/manifest.json\n{\n  \"name\": \"Genie Am Lich\",\n  \"short_name\": \"Genie AL\",\n  \"description\": \"Tro ly am lich Viet Nam cua CyberSkill\",\n  \"start_url\": \"/\",\n  \"display\": \"standalone\",\n  \"theme_color\": \"#3D1266\",\n  \"background_color\": \"#FDF6EC\",\n  \"lang\": \"vi\",\n  \"icons\": [\n    { \"src\": \"/icons/icon-192.png\", \"sizes\": \"192x192\", \"type\": \"image/png\" },\n    { \"src\": \"/icons/icon-512.png\", \"sizes\": \"512x512\", \"type\": \"image/png\" }\n  ]\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>pnpm build</code> in <code>apps/web/</code> completes with no error; the <code>out/</code> directory is created with <code>index.html</code>, <code>calendar/index.html</code>, <code>reminders/index.html</code>, <code>settings/index.html</code>.</li><li>The <code>/festival/[id]</code> route with <code>id = \"vu-lan\"</code> renders the Vu Lan detail page taken from <code>getFestivalById(\"vu-lan\")</code> of TASK-LUNAR-008 without server-side data fetching.</li><li><code>getReminders()</code> called right after app startup (empty state) returns an empty array without throwing an error.</li><li><code>saveReminder(r)</code> then <code>getReminders()</code> returns an array with 1 element matching the saved <code>r</code> (round-trip test on a localStorage mock).</li><li><code>deleteReminder(id)</code> deletes the correct reminder by id; <code>getReminders()</code> afterward no longer contains that reminder.</li><li><code>createNotificationService()</code> on the web environment (no Capacitor) returns <code>WebNotificationStub</code>; on the Capacitor environment returns <code>CapacitorNotificationService</code>.</li><li><code>capacitor.config.ts</code> exports an object with <code>appId === \"world.cyberskill.genieamlich\"</code> and <code>webDir === \"out\"</code>.</li><li><code>public/manifest.json</code> is valid JSON, with all 4 fields <code>name</code>, <code>display: \"standalone\"</code>, <code>theme_color</code>, <code>background_color</code>; <code>background_color</code> is the warm cream value from TASK-LUNAR-009.</li><li>The <code>/</code> (home) route displays today (solar date, lunar date, can-chi) calling <code>convertSolar2Lunar</code> with no network (integration test with mock date \"2025-01-29\" returns the first day of Tet At Ty).</li><li>The bottom navigation has 4 tabs; clicking \"Calendar\" navigates to <code>/calendar</code>; the active tab is highlighted with the deep purple color from the token.</li><li>The Be Vietnam Pro font is declared in <code>layout.tsx</code>; the build output contains no other font reference as primary.</li><li><code>next.config.ts</code> has <code>output: \"export\"</code> and <code>transpilePackages: [\"@cyberskill/amlich-core\", \"@cyberskill/genie-ui\"]</code>.</li><li>There is no <code>fetch</code>, <code>axios</code>, or HTTP call in <code>storage.ts</code> (checked by grep in the file).</li><li><code>pnpm test</code> in <code>apps/web/</code> runs without crashing with at least 5 storage CRUD tests passing.</li><li>The build output at <code>out/</code> contains no <code>.env</code> file or any API key (grep for <code>ANTHROPIC_API_KEY</code> and <code>ZNS_TOKEN</code> returns empty).</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// apps/web/__tests__/storage.test.ts\nimport { getReminders, saveReminder, deleteReminder, updateReminder, getSettings, saveSettings } from \"../lib/storage\";\n\n// Mock localStorage for JSDOM environment\nconst mockStorage: Record&lt;string, string&gt; = {};\nbeforeAll(() =&gt; {\n  Object.defineProperty(window, \"localStorage\", {\n    value: {\n      getItem: (k: string) =&gt; mockStorage[k] ?? null,\n      setItem: (k: string, v: string) =&gt; { mockStorage[k] = v; },\n      removeItem: (k: string) =&gt; { delete mockStorage[k]; },\n    },\n  });\n});\n\n// testReminder phai co day du truong bat buoc cua Reminder (CONTRACT.md / TASK-LUNAR-004):\n// leapFallback la required field (khong optional).\nconst testReminder: import(\"../lib/storage\").Reminder = {\n  id: \"test-001\",\n  userId: \"local\",\n  type: \"RAM\",\n  title: \"Ram hang thang\",\n  lunarDay: 15,\n  lunarMonth: 1,\n  lunarYear: null,\n  isLeapMonth: false,\n  leapFallback: \"REGULAR\",  // required by Reminder interface (CONTRACT.md)\n  recurrence: \"MONTHLY\",\n  leadTimes: [0, 1],\n  notifyTime: \"07:00\",\n  channels: [\"LOCAL\"],\n  linkedContentId: \"ram\",\n  sharedWith: [],\n  enabled: true,\n};\n\ndescribe(\"Storage CRUD\", () =&gt; {\n  test(\"getReminders() trang thai rong tra mang rong\", async () =&gt; {\n    const result = await getReminders();\n    expect(result).toEqual([]);\n  });\n\n  test(\"saveReminder + getReminders: round-trip\", async () =&gt; {\n    await saveReminder(testReminder);\n    const result = await getReminders();\n    expect(result).toHaveLength(1);\n    expect(result[0].id).toBe(\"test-001\");\n    expect(result[0].lunarDay).toBe(15);\n  });\n\n  test(\"updateReminder thay doi title\", async () =&gt; {\n    await updateReminder({ ...testReminder, title: \"Ram da cap nhat\" });\n    const result = await getReminders();\n    expect(result[0].title).toBe(\"Ram da cap nhat\");\n  });\n\n  test(\"deleteReminder xoa dung theo id\", async () =&gt; {\n    await deleteReminder(\"test-001\");\n    const result = await getReminders();\n    expect(result).toHaveLength(0);\n  });\n\n  test(\"saveSettings + getSettings round-trip\", async () =&gt; {\n    const s = { locale: \"vi-VN\" as const, timezone: \"Asia/Ho_Chi_Minh\" as const, theme: \"purple\" as const, notifyTime: \"07:30\" };\n    await saveSettings(s);\n    const loaded = await getSettings();\n    expect(loaded.notifyTime).toBe(\"07:30\");\n  });\n});</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/__tests__/notificationGlue.test.ts\nimport { createNotificationService, WebNotificationStub } from \"../lib/notificationGlue\";\n\ntest(\"createNotificationService tren web tra WebNotificationStub\", () =&gt; {\n  // JSDOM khong co Capacitor\n  const service = createNotificationService();\n  expect(service).toBeInstanceOf(WebNotificationStub);\n});\n\ntest(\"WebNotificationStub.scheduleNotification la no-op, khong throw\", async () =&gt; {\n  const stub = new WebNotificationStub();\n  await expect(stub.scheduleNotification({\n    id: 1, title: \"Test\", body: \"Body\",\n    scheduleAt: new Date(), extra: { reminderId: \"r1\" }\n  })).resolves.toBeUndefined();\n});\n\ntest(\"WebNotificationStub.cancelAllPending la no-op\", async () =&gt; {\n  const stub = new WebNotificationStub();\n  await expect(stub.cancelAllPending()).resolves.toBeUndefined();\n});</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/__tests__/homeRoute.test.tsx\nimport { render, screen } from \"@testing-library/react\";\nimport HomePage from \"../app/page\";\n\n// Mock amlich-core - PHAI khop hop dong THUC: convertSolar2Lunar tra TUPLE [day,month,year,leap],\n// KHONG phai object; can-chi/zodiac den tu cac ham TASK-LUNAR-002 (canChiDay/canChiYear nhan JDN,\n// zodiacOf nhan lunarYear). Mock object-shape se la false-green: test pass nhung home route doc\n// `.canChiDay`/`.zodiac` tren tuple se ra `undefined` o production.\njest.mock(\"@cyberskill/amlich-core\", () =&gt; ({\n  convertSolar2Lunar: jest.fn().mockReturnValue([1, 1, 2025, 0]),   // tuple [d,m,y,leap]\n  jdFromDate: jest.fn().mockReturnValue(2460704),                   // JDN 29/01/2025\n  canChiDay: jest.fn().mockReturnValue({ label: \"Giap Ty\" }),\n  canChiMonth: jest.fn().mockReturnValue({ label: \"Binh Dan\" }),\n  canChiYear: jest.fn().mockReturnValue({ label: \"At Ty\" }),\n  zodiacOf: jest.fn().mockReturnValue(\"Ran\"),\n  VN_TIMEZONE: 7.0,\n}));\n\n// Mock date to 2025-01-29 (Mung 1 Tet At Ty)\nbeforeAll(() =&gt; {\n  jest.useFakeTimers().setSystemTime(new Date(\"2025-01-29\"));\n});\n\ntest(\"Home page hien thi ngay am va can-chi dung\", () =&gt; {\n  render(&lt;HomePage /&gt;);\n  expect(screen.getByText(/Mùng 1/)).toBeInTheDocument();\n  expect(screen.getByText(/Ất Tỵ/)).toBeInTheDocument();\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The API contract in §3 is a full skeleton. The trickiest point is that the storage adapter must work in both JSDOM (test) and the real Capacitor environment:</p>\n<pre><code class=\"language-typescript\">// apps/web/lib/storage.ts (storage adapter logic)\nconst REMINDERS_KEY = \"genie_reminders\";\nconst SETTINGS_KEY  = \"genie_settings\";\n\n// Lazy: chi import plugin native khi thuc su chay tren Capacitor. Tren web/JSDOM nhanh nay khong\n// bao gio chay nen \"@capacitor/preferences\" khong can resolve duoc trong test/web build.\nasync function getPreferences() {\n  const mod = await import(\"@capacitor/preferences\");\n  return mod.Preferences;\n}\n\nasync function storageGet(key: string): Promise&lt;string | null&gt; {\n  if (isCapacitor()) {\n    const Preferences = await getPreferences();\n    const { value } = await Preferences.get({ key });\n    return value;\n  }\n  return localStorage.getItem(key);\n}\n\nasync function storageSet(key: string, value: string): Promise&lt;void&gt; {\n  if (isCapacitor()) {\n    const Preferences = await getPreferences();\n    await Preferences.set({ key, value });\n  } else {\n    localStorage.setItem(key, value);\n  }\n}\n\nexport async function getReminders(): Promise&lt;Reminder[]&gt; {\n  const raw = await storageGet(REMINDERS_KEY);\n  if (!raw) return [];\n  return JSON.parse(raw) as Reminder[];\n}\n\nexport async function saveReminder(r: Reminder): Promise&lt;void&gt; {\n  const existing = await getReminders();\n  const updated = [...existing.filter(e =&gt; e.id !== r.id), r];\n  await storageSet(REMINDERS_KEY, JSON.stringify(updated));\n}</code></pre>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream: <code>TASK-LUNAR-001</code> provides <code>convertSolar2Lunar</code> used on the home route <code>/</code> to compute today's lunar date; <code>TASK-LUNAR-009</code> provides <code>PURPLE_TOKENS</code>, <code>SEMANTIC</code>, and components to apply the theme globally in <code>layout.tsx</code>.</p>\n<p>Downstream: <code>TASK-LUNAR-005</code> (rolling-64 notifications) uses the <code>NotificationService</code> interface from <code>notificationGlue.ts</code> and <code>Reminder[]</code> from <code>storage.ts</code>; <code>TASK-LUNAR-006</code> (reminder management) uses the storage CRUD and the <code>/reminders</code> route; <code>TASK-LUNAR-007</code> (calendar grid) mounts on the <code>/calendar</code> route; <code>TASK-LUNAR-012</code> (good-day picker) mounts on a new route in Phase 2; <code>TASK-LUNAR-015</code> (AI Genie) needs a Next.js API route - it will change <code>output: \"export\"</code> to hybrid mode or split out to Vercel Functions when Phase 2 begins.</p>\n<p>Cross-cutting: All Phase 1 tasks depend on <code>storage.ts</code> to read/write Reminders. <code>notificationGlue.ts</code> is the contract that TASK-LUNAR-005 implements concretely on Capacitor.</p>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"comment\": \"localStorage key 'genie_reminders' sau khi user tao 2 nhac\",\n  \"value\": \"[{\\\"id\\\":\\\"ram-default\\\",\\\"type\\\":\\\"RAM\\\",\\\"title\\\":\\\"Rằm hàng tháng\\\",\\\"lunarDay\\\":15,\\\"lunarMonth\\\":1,\\\"lunarYear\\\":null,\\\"isLeapMonth\\\":false,\\\"recurrence\\\":\\\"MONTHLY\\\",\\\"leadTimes\\\":[0,1],\\\"notifyTime\\\":\\\"07:00\\\",\\\"channels\\\":[\\\"LOCAL\\\"],\\\"linkedContentId\\\":\\\"ram\\\",\\\"sharedWith\\\":[],\\\"enabled\\\":true},{\\\"id\\\":\\\"gio-ba-noi\\\",\\\"type\\\":\\\"GIO\\\",\\\"title\\\":\\\"Giỗ bà nội\\\",\\\"lunarDay\\\":12,\\\"lunarMonth\\\":3,\\\"lunarYear\\\":null,\\\"isLeapMonth\\\":false,\\\"recurrence\\\":\\\"ANNUAL\\\",\\\"leadTimes\\\":[0,1,3],\\\"notifyTime\\\":\\\"07:00\\\",\\\"channels\\\":[\\\"LOCAL\\\"],\\\"linkedContentId\\\":\\\"dam-gio-ca-nhan\\\",\\\"sharedWith\\\":[],\\\"enabled\\\":true}]\"\n}</code></pre>\n<pre><code class=\"language-typescript\">// Home route / hien thi du lieu nay\nconst today = {\n  solarDate: \"Thu Tu, 29 thang 1 nam 2025\",\n  lunarDate: \"Mung 1 thang Gieng nam At Ty\",\n  canChiDay: \"Giap Ty\",\n  upcomingReminders: [\n    { title: \"Ram thang Gieng\", solarDate: \"Thu Ba, 12 thang 2 nam 2025\", daysUntil: 14 },\n    { title: \"Gio ba noi\", solarDate: \"Chu Nhat, 30 thang 3 nam 2025\", daysUntil: 60 }\n  ]\n};</code></pre>\n<pre><code class=\"language-json\">{\n  \"comment\": \"capacitor.config.ts export (compiled)\",\n  \"appId\": \"world.cyberskill.genieamlich\",\n  \"appName\": \"Genie Am Lich\",\n  \"webDir\": \"out\",\n  \"plugins\": {\n    \"LocalNotifications\": {\n      \"smallIcon\": \"ic_stat_icon_config_sample\",\n      \"iconColor\": \"#3D1266\"\n    }\n  }\n}</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li>Static export vs SSR: <code>output: \"export\"</code> for Phase 1 (DEC-LUNAR-100).</li><li>Storage adapter: localStorage/IndexedDB + <code>@capacitor/preferences</code> behind a common interface (DEC-LUNAR-101).</li><li>Bundle ID: <code>world.cyberskill.genieamlich</code> (DEC-LUNAR-103).</li></ul>\n<p>Still deferred:</p>\n<ul><li>Phase 2 Next.js API routes (<code>/api/genie</code>): <code>output: \"export\"</code> is not compatible with API routes. When Phase 2 begins, we need to switch to <code>output: \"standalone\"</code> or use separate Vercel Functions - deferred, does not affect Phase 1.</li><li>IndexedDB vs localStorage: <code>localStorage</code> is enough for Phase 1 with a small number of reminders (&lt; 100 records). If the user has &gt; 100 death anniversaries, switch to IndexedDB - deferred.</li><li>Service worker: <code>next-pwa</code> or <code>@ducanh2912/next-pwa</code> needs a compatibility review with Next.js 15 App Router before use - deferred after pinning the Next.js version.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>output: export</code> conflicts with a Phase 2 API route</td><td>Build error when adding /api/genie</td><td>Phase 2 blocked</td><td>Switch to hybrid mode or split out Vercel Functions</td></tr><tr><td>localStorage cleared when iOS is low on storage</td><td>App loses all data</td><td>User loses reminders</td><td>Migrate to @capacitor/preferences in the storage adapter</td></tr><tr><td>Capacitor does not recognize webDir \"out\" after Next.js renames it</td><td>npx cap sync error</td><td>iOS build fails</td><td>Ensure webDir in capacitor.config matches the output folder</td></tr><tr><td>Be Vietnam Pro not cached offline</td><td>Service worker miss</td><td>Font falls back to system-ui</td><td>Bundle the font locally in public/fonts/ and cache it in the SW</td></tr><tr><td>transpilePackages missing amlich-core or ui</td><td>Build error \"can not find module\"</td><td>App cannot build</td><td>Add it to transpilePackages in next.config.ts</td></tr><tr><td>Storage CRUD race condition (save/get concurrently)</td><td>Flaky test</td><td>Data loss</td><td>Use an async queue or sequential await</td></tr><tr><td>createNotificationService() returns the wrong type</td><td>Unit test</td><td>TASK-005 calls a Capacitor API on the web, crash</td><td>Fix the isCapacitor() check</td></tr><tr><td>manifest.json missing the icons field</td><td>Lighthouse PWA audit fail</td><td>Cannot Add to Home Screen</td><td>Add icon 192 and 512 to public/icons/</td></tr><tr><td>API key leaked in the build output</td><td>grep ANTHROPIC_API_KEY in out/</td><td>Dangerous security exposure</td><td>Ensure no .env.local is committed; use .gitignore</td></tr><tr><td>TypeScript error in a workspace package import</td><td>pnpm build fail</td><td>Deploy blocked</td><td>Fix types in amlich-core and ui before building</td></tr><tr><td>Capacitor sync fails (ios/ not initialized yet)</td><td>npx cap add ios error</td><td>The iOS target does not exist</td><td>Run <code>npx cap add ios</code> before <code>npx cap sync</code></td></tr><tr><td>Bottom nav active tab wrong (highlights the wrong route)</td><td>Integration test clicking nav</td><td>Confusing UX</td><td>Use usePathname() from Next.js, compare with the current pathname</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li><code>isCapacitor()</code> should check <code>typeof window !== \"undefined\" &amp;&amp; !!(window as any).Capacitor?.isNativePlatform()</code> - two checks are needed: one for SSG/SSR safety, one for runtime detection. (§3 already ships this exact form; optional chaining alone does NOT rescue an undeclared <code>window</code>.)</li><li>The home route <code>/</code> assembles the lunar date like TASK-LUNAR-007: <code>convertSolar2Lunar</code> returns a TUPLE <code>[d,m,y,leap]</code> (no can-chi/zodiac). Can-chi/zodiac MUST be called separately from TASK-LUNAR-002 with a JDN: <code>const jdn = jdFromDate(d,m,y); canChiDay(jdn).label; canChiYear(lYear).label; zodiacOf(lYear)</code>. The §5 mock test has been fixed to match this form - avoiding a false-green when the mock object-shape differs from the real contract.</li><li><code>output: \"export\"</code> in <code>next.config.ts</code> means <code>useSearchParams()</code>, <code>useRouter().push()</code> with complex query params, and dynamic routes such as <code>/festival/[id]</code> all have to use <code>generateStaticParams()</code> to pre-render. Forgetting this step causes a build error or a 404 page on deploy.</li><li>Capacitor <code>@capacitor/local-notifications</code> needs <code>requestPermissions()</code> before <code>schedule()</code>; a common mistake is calling <code>schedule()</code> without checking permission, causing a silent fail on iOS. The <code>notificationGlue.ts</code> CapacitorNotificationService MUST call <code>requestPermission()</code> first.</li><li>The storage keys <code>genie_reminders</code> and <code>genie_settings</code> must be exported as constants (not hardcoded strings in many places) so they are easy to change when the schema needs migrating.</li><li><code>transpilePackages</code> in <code>next.config.ts</code> is needed because <code>@cyberskill/amlich-core</code> is an ESM-only package in the workspace; Next.js needs to transpile it to CommonJS or ESM appropriate to the bundler mode.</li><li>The Be Vietnam Pro font on the Google Fonts CDN needs <code>&lt;link rel=\"preconnect\"&gt;</code> to reduce latency; but for a fully offline PWA, bundle it into <code>public/fonts/</code> and reference it with <code>@font-face</code> in global CSS - this decision affects the service worker strategy.</li><li>JSDOM in the test does not support the <code>Capacitor</code> runtime; every test importing <code>storage.ts</code> runs the fast localStorage path. Ensure the <code>localStorage</code> mock uses <code>Object.defineProperty</code> (not <code>jest.spyOn</code>) because JSDOM having localStorage may make this setup differ.</li></ul>\n<p><em>End of TASK-LUNAR-010.</em></p>";

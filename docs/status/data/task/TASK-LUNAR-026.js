window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LUNAR-026"]="<h1>TASK-LUNAR-025: Collaborative Decision Boards</h1>\n<h2>1. Description</h2>\n<p>Supabase Realtime backed polling system allowing families to vote on Good Days generated by the picker.</p>\n<h2>2. Dependencies</h2>\n<ul><li>TASK-LUNAR-010 (App Shell)</li><li>TASK-LUNAR-015 (AI Genie)</li><li>TASK-LUNAR-017 (ZNS Reminders)</li><li>TASK-LUNAR-018 (Supabase Sync)</li></ul>\n<h2>3. API Contract</h2>\n<p>TBD during implementation.</p>\n<h2>4. Acceptance Criteria</h2>\n<p>TBD during implementation.</p>\n<h2>5. Verification</h2>\n<p>TBD during implementation.</p>\n<h1>Collaborative Decision Boards (TASK-LUNAR-026)</h1>\n<p>This document covers the development setup, testing, and deployment guidelines for the Supabase Realtime Polling feature introduced in TASK-LUNAR-026.</p>\n<h2>1. Architecture Overview</h2>\n<ul><li><strong>Database</strong>: Supabase PostgreSQL.</li><li><strong>Frontend</strong>: Next.js (React) deployed via <code>apps/web</code>.</li><li><strong>Communication</strong>: Supabase Realtime over WebSockets (handled by <code>@supabase/supabase-js</code>).</li><li><strong>Data Model</strong>: </li><li><code>decision_boards</code>: Represents a single voting session.</li><li><code>decision_options</code>: The dates/options available to vote on.</li><li><code>decision_votes</code>: The individual votes cast by family members.</li></ul>\n<h2>2. Local Development Guide</h2>\n<h3>Step 2.1: Supabase Local Migration</h3>\n<p>To test the polling feature locally, you must apply the new schema migration to your local Supabase instance. Assuming you have the Supabase CLI installed, run:</p>\n<pre><code class=\"language-bash\">cd services/genie-api\nsupabase db reset\n# Or if you are applying manually:\nsupabase db push</code></pre>\n<h3>Step 2.2: Environment Variables</h3>\n<p>Inside <code>.env.docker</code>, ensure you have your Supabase URL and Anon Key set up (as well as other required variables). If you are using local Supabase, it usually looks like this:</p>\n<pre><code class=\"language-env\">NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321\nNEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...</code></pre>\n<p><em>(If testing against a staging Supabase instance, use your staging credentials).</em></p>\n<h3>Step 2.3: Running the Application (Development)</h3>\n<p>Start the entire stack (API, Web, Redis) using the provided development Docker Compose file:</p>\n<pre><code class=\"language-bash\">docker-compose -f docker-compose.dev.yml --env-file .env.docker up --build</code></pre>\n<p>Navigate to <code>http://localhost:8080/good-day-picker</code>. Select a task and click <strong>\"Chia sẻ Bình chọn (Top 5)\"</strong>. You will be redirected to the poll page where you can input your name and vote. Open the link in an incognito window to see the realtime updates in action.</p>\n<h2>3. Deployment Guidelines</h2>\n<h3>Step 3.1: Production Database Migration</h3>\n<p>Before deploying the frontend code to production, the database schema must be updated.</p>\n<ul><li><strong>If using CI/CD (GitHub Actions)</strong>: Ensure your pipeline runs <code>supabase db push</code> against the production environment.</li><li><strong>If applying manually</strong>:</li></ul>\n<ol><li>Go to the Supabase Dashboard.</li><li>Open the SQL Editor.</li><li>Copy the contents of <code>services/genie-api/supabase/migrations/0024_collaborative_boards.sql</code>.</li><li>Run the query.</li></ol>\n<h3>Step 3.2: Verify Row Level Security (RLS)</h3>\n<p>The migration includes RLS policies that allow public <code>INSERT</code> and <code>SELECT</code> for anonymous voting. Ensure these policies are applied correctly so users don't encounter permission errors.</p>\n<h3>Step 3.3: Build &amp; Deploy Frontend</h3>\n<p>The Next.js application has been updated to use query parameters (<code>/polls?id=xyz</code>) instead of dynamic path segments (<code>/polls/[id]</code>) so that it is fully compatible with Next.js static exports (<code>output: export</code>). Deploy the full stack using Docker:</p>\n<pre><code class=\"language-bash\">docker-compose --env-file .env.docker up --build -d</code></pre>\n<p><em>Note: Make sure your <code>.env.docker</code> has production values for deployment.</em></p>\n<h2>4. Manual Verification Checklist</h2>\n<ul><li>[ ] Database migration <code>0024_collaborative_boards.sql</code> applied successfully.</li><li>[ ] Environment variables injected into the CI/CD pipeline or deployment platform.</li><li>[ ] Good Day Picker renders the \"Chia sẻ Bình chọn\" button.</li><li>[ ] Poll creation successfully redirects to <code>/polls?id=...</code>.</li><li>[ ] Voting correctly updates the count instantly across multiple browser windows via Supabase Realtime.</li></ul>";

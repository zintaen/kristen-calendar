window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LUNAR-004"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This module defines the <code>Reminder</code> type (along with <code>User</code>, <code>OccurrenceCache</code>) and the recurrence engine that generates solar dates from lunar dates, locking every calculation to Asia/Ho_Chi_Minh. This is the contract:</p>\n<ol><li>MUST store each repeating <code>Reminder</code> by <code>lunarDay</code> + <code>lunarMonth</code> + <code>isLeapMonth</code>, and MUST NOT store the solar date as the source value; the solar date is always derived via <code>convertLunar2Solar</code> (DEC-LUNAR-040, TASK-B02).</li><li>MUST support the <code>recurrence</code> ENUM <code>MONTHLY | ANNUAL | ONCE</code>: <code>ANNUAL</code> sets <code>lunarYear = null</code> (repeats yearly), <code>ONCE</code> keeps the entered <code>lunarYear</code> (fixed once), <code>MONTHLY</code> ignores <code>lunarMonth</code> and repeats <code>lunarDay</code> every lunar month (DEC-LUNAR-045, TASK-B02).</li><li>MUST support the <code>type</code> ENUM <code>RAM | MUNG_MOT | GIO | CUSTOM | FESTIVAL</code>; <code>RAM</code> implies <code>lunarDay = 15</code>, <code>MUNG_MOT</code> implies <code>lunarDay = 1</code>, <code>GIO</code> is a personal death anniversary, <code>CUSTOM</code> is a custom lunar reminder, <code>FESTIVAL</code> links to <code>FestivalContent</code> (PRD section 10).</li><li>MUST generate occurrences via <code>nextOccurrences(reminder, opt)</code> with <code>opt: RecurrenceOptions { fromYear, count, engineVersion }</code> (signature §3), calling <code>convertLunar2Solar(lunarDay, lunarMonth, targetLunarYear, isLeap)</code> independently for each <code>targetLunarYear</code>, and MUST NOT interpolate from the previous occurrence (DEC-LUNAR-041). <code>count</code> is the number of LUNAR occurrences to scan (before multiplying by lead-time); the total <code>Occurrence</code> returned is <code>count * leadTimes.length</code>.</li><li>MUST apply the leap-month fallback rule: when a death anniversary is entered in a leap month (<code>isLeapMonth = true</code>) but the <code>targetLunarYear</code> has no such leap month, the engine MUST compute the occurrence in the corresponding regular month (<code>FALLBACK_REGULAR</code>) by default, and mark <code>fellBack = true</code> so the UI can ask the user to confirm (DEC-LUNAR-042, PRD section 10).</li><li>MUST let the user choose the fallback policy via the <code>leapFallback</code> ENUM <code>REGULAR | SKIP | ASK</code>: <code>REGULAR</code> observes the regular month, <code>SKIP</code> skips that year, <code>ASK</code> returns the occurrence in the <code>pending_user_choice</code> state (DEC-LUNAR-042).</li><li>MUST handle a death anniversary falling on a day that does not exist in the target lunar month (for example day 30 when the target month has only 29 days): the engine MUST clamp back to the last day of that lunar month and mark <code>dayClamped = true</code> (TASK-B02 edge case).</li><li>MUST lock every calculation to <code>timeZone = 7.0</code> (meridian 105E, Asia/Ho_Chi_Minh) via <code>tz.ts</code>, and MUST NOT use the device's local-time <code>new Date()</code> to derive the lunar date or determine \"today\" (DEC-LUNAR-043, TASK-B06).</li><li>MUST assign each occurrence a <code>notifyTime</code> (default \"07:00\") and an array <code>leadTimes: number[]</code> in days (for example <code>[0, 1]</code> = on the day + 1 day before); each (occurrence x leadTime) pair generates its own reminder point (TASK-B04, preparing for TASK-LUNAR-005).</li><li>MUST generate each reminder point as an <code>Occurrence</code> carrying <code>reminderId</code>, <code>gregorianDate</code> (ISO date), <code>lunarLabel</code> (display string), <code>leadDays</code>, and <code>fireAtLocal</code> (datetime in Vietnam time) for TASK-LUNAR-005 to sort (PRD section 11).</li><li>MUST provide <code>OccurrenceCache { reminderId, gregorianDate, lunarLabel, computedAt, engineVersion }</code>; the cache MAY be cleared and recomputed at any time, and MUST invalidate when the amlich-core <code>engineVersion</code> changes (DEC-LUNAR-044).</li><li>MUST provide <code>validateReminder(r)</code> returning a list of errors: <code>lunarDay</code> outside 1..30, <code>lunarMonth</code> outside 1..12, <code>RAM</code> with <code>lunarDay != 15</code>, <code>ONCE</code> missing <code>lunarYear</code>, <code>leadTimes</code> containing a negative number, empty <code>channels</code>.</li><li>MUST provide <code>normalizeReminder(r)</code> setting stable defaults: <code>notifyTime = \"07:00\"</code>, <code>leadTimes = [1]</code> (1 day before), <code>channels = [\"LOCAL\"]</code>, <code>enabled = true</code>, <code>leapFallback = \"REGULAR\"</code>, sort+dedupe <code>leadTimes</code>.</li><li>MUST keep this module zero-dependency and purely computational (no I/O, no storage); reading/writing storage belongs to the app layer (TASK-LUNAR-010), sending notifications belongs to TASK-LUNAR-005 (NFR-Offline).</li><li>MUST set the historical reference point as a fixture: a death anniversary entered on 16/02 in the leap month 2 of 1985 (At Suu) must generate the correct occurrence in the range 21/03-19/04/1985; a MONTHLY day-15 death anniversary must generate 12 or 13 occurrences in a year with a leap month (PRD section 6.6).</li><li>SHOULD expose <code>engineVersion</code> (the semver of amlich-core) so OccurrenceCache and TASK-LUNAR-018 (cloud sync) know when to recompute on the client.</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why store the lunar date, not the solar date (§1 #1, DEC-LUNAR-040)?</strong> A death anniversary is a fixed lunar date, but the corresponding solar date changes every year. If you store the solar date, next year it is wrong. Storing <code>lunarDay/lunarMonth/isLeapMonth</code> once and computing the solar date each year is the only way to match the nature of a death anniversary, and it matches the recurrence rule in PRD section 10.</p>\n<p><strong>Why call convertLunar2Solar each year, not interpolate (§1 #4, DEC-LUNAR-041)?</strong> The gap between two consecutive death anniversaries measured in solar days is not fixed - it varies because of the lunar year length and the position of the leap month. Adding 354 or 384 days both drift. Calling the lunar engine again for each <code>targetLunarYear</code> is the only way to get a result that matches the Ho Ngoc Duc calendar, and it is cheap because TASK-LUNAR-001 covers a 1-day conversion in &lt; 5ms.</p>\n<p><strong>Why the leap-month fallback (§1 #5, #6, DEC-LUNAR-042)?</strong> Someone who died in a leap month (for example the leap month 2) raises a real question: which day do you observe in years that have no leap month 2? The common custom is to observe it in the corresponding regular month, so <code>FALLBACK_REGULAR</code> is a reasonable default. But this is a family's cultural decision, so the engine marks <code>fellBack</code> and lets the UI ask again, rather than deciding silently.</p>\n<p><strong>Why add dayClamped (§1 #7)?</strong> A lunar month can have only 29 days. A day-30 death anniversary falling in a year where that month is short has no day 30. Clamping back to the last day of the month (29) and reporting <code>dayClamped</code> keeps the reminder happening close to the right day, rather than jumping to the next month or disappearing.</p>\n<p><strong>Why lock Asia/Ho_Chi_Minh (§1 #8, DEC-LUNAR-043)?</strong> The main persona is an actor who often films far away and may be in a different timezone. If you derive the lunar date by device time, \"today is the first of the lunar month\" will be wrong abroad. Locking every calculation to <code>tz = 7.0</code> (105E) keeps the lunar calendar correct in Vietnam time everywhere, true to the spirit of TASK-B06.</p>\n<p><strong>Why split lead-time into multiple reminder points (§1 #9, #10)?</strong> The PRD allows reminders \"on the day / 1 day before / 3 days before / 1 week before\". Each item is its own notification at its own moment. Pre-generating them as <code>Occurrence</code> with a <code>fireAtLocal</code> lets TASK-LUNAR-005 simply merge, sort by time, and cut the nearest 64 - matching the iOS 64-pending budget.</p>\n<p><strong>Why must OccurrenceCache invalidate by engineVersion (§1 #11, DEC-LUNAR-044)?</strong> If an amlich-core bug fix changes a Meeus constant, every cached date may change. Attaching <code>engineVersion</code> to the cache allows detecting that the core changed and recomputing, rather than serving stale wrong dates. The cache is only speed, never truth.</p>\n<p><strong>Why zero-dependency, purely computational (§1 #14)?</strong> Same reason as TASK-LUNAR-001: three clients (web, iOS, Zalo) all import this module. Keeping it I/O-free means it runs identically everywhere, is testable with static fixtures, and does not pull in each platform's different storage API.</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// packages/amlich-core/src/reminder.ts\n// QUAN TRONG: moi consumer import type Reminder tu \"@cyberskill/amlich-core\"; KHONG redeclare.\n\nexport type ReminderType = \"RAM\" | \"MUNG_MOT\" | \"GIO\" | \"CUSTOM\" | \"FESTIVAL\";\nexport type Recurrence = \"MONTHLY\" | \"ANNUAL\" | \"ONCE\";\nexport type ReminderChannel = \"LOCAL\" | \"ZNS\" | \"PUSH\";   // CONTRACT ten la ReminderChannel, khong phai Channel\nexport type LeapFallback = \"REGULAR\" | \"SKIP\" | \"ASK\";\n\nexport interface User {\n  id: string;\n  displayName: string;\n  locale: \"vi-VN\";                 // PRD section 10\n  timezone: \"Asia/Ho_Chi_Minh\";    // locked, DEC-LUNAR-043\n  theme: \"purple\";\n  phone?: string;                  // cho ZNS (TASK-LUNAR-017)\n  consentFlags: Record&lt;string, boolean&gt;;\n}\n\nexport interface Reminder {\n  readonly id: string;\n  readonly userId: string;\n  readonly type: ReminderType;\n  readonly title: string;\n  readonly lunarDay: number;               // 1..30\n  readonly lunarMonth: number;             // 1..12\n  readonly lunarYear: number | null;       // null neu ANNUAL/MONTHLY; CONTRACT: number|null, khong phai optional\n  readonly isLeapMonth: boolean;           // cho dam gio roi thang nhuan\n  readonly leapFallback: LeapFallback;     // DEC-LUNAR-042\n  readonly recurrence: Recurrence;\n  readonly leadTimes: readonly number[];   // ngay; [0,1] = dung ngay + truoc 1 ngay\n  readonly notifyTime: string;             // \"HH:mm\", gio Viet Nam\n  readonly channels: readonly ReminderChannel[];\n  readonly linkedContentId?: string;       // TASK-LUNAR-008 FestivalContent\n  readonly sharedWith?: readonly string[]; // TASK-LUNAR-018 family sharing; optional theo CONTRACT\n  readonly enabled: boolean;\n  readonly notificationStyle?: NotificationStyle;  // TASK-F05 (TASK-LUNAR-006 fold); optional\n}\n\n/** TASK-F05 - tong giong + emoji cho body thong bao. TASK-LUNAR-006 set; TASK-LUNAR-005 doc; TASK-LUNAR-015 (P2) co the thay render. */\nexport interface NotificationStyle {\n  tone: \"warm\" | \"neutral\" | \"formal\";\n  emoji: string;\n  imageId?: string;                // P1 chi emoji + anh dung san; upload rieng cho TASK-LUNAR-019 (PDPL)\n}\n\nexport interface ValidationError { field: string; code: string; message: string; }\nexport function validateReminder(r: Reminder): ValidationError[];\nexport function normalizeReminder(r: Partial&lt;Reminder&gt;): Reminder;</code></pre>\n<pre><code class=\"language-typescript\">// packages/amlich-core/src/recurrence.ts\nimport { convertLunar2Solar, convertSolar2Lunar } from \"./convert\";\n\n// Occurrence: 8 truong, CONTRACT chinh xac - khop CONTRACT.md §P0/P1 surface\nexport interface Occurrence {\n  readonly reminderId: string;\n  readonly gregorianDate: string;      // \"YYYY-MM-DD\" tai Asia/Ho_Chi_Minh - STRING, khong phai SolarDate tuple\n  readonly lunarLabel: string;         // vi du \"15/7 At Ty\" hoac \"16/2 (nhuan) At Suu\"\n  readonly leadDays: number;           // 0 = dung ngay\n  readonly fireAtLocal: string;        // \"YYYY-MM-DDTHH:mm:00+07:00\"\n  readonly fellBack: boolean;          // DEC-LUNAR-042 - da ap fallback thang nhuan\n  readonly dayClamped: boolean;        // §1 #7 - lui ve ngay cuoi thang am\n  readonly pendingUserChoice: boolean; // leapFallback = ASK\n}\n\n// RecurrenceOptions: engineVersion la REQUIRED (khong optional) - CONTRACT\nexport interface RecurrenceOptions {\n  readonly fromYear: number;          // nam DUONG bat dau quet\n  readonly count: number;             // so occurrence am muon (truoc khi nhan lead-time); tong Occurrence tra = count * leadTimes.length\n  readonly engineVersion: string;     // REQUIRED - CONTRACT; khong phai optional\n}\n\n/** Sinh cac occurrence sap toi cho mot Reminder, da nhan lead-time. */\nexport function nextOccurrences(r: Reminder, opt: RecurrenceOptions): readonly Occurrence[];\n\n/** Gop occurrence cua nhieu Reminder, sort theo fireAtLocal tang dan. TASK-LUNAR-005 cat 64 dau. */\nexport function mergeAndSort(all: readonly Occurrence[]): readonly Occurrence[];</code></pre>\n<pre><code class=\"language-typescript\">// packages/amlich-core/src/reminder.ts (OccurrenceCache)\nexport interface OccurrenceCache {\n  reminderId: string;\n  gregorianDate: string;\n  lunarLabel: string;\n  computedAt: string;        // ISO instant\n  engineVersion: string;     // DEC-LUNAR-044 - invalidate khi core doi\n}\nexport function isCacheStale(c: OccurrenceCache, engineVersion: string): boolean;</code></pre>\n<pre><code class=\"language-typescript\">// packages/amlich-core/src/tz.ts\nexport const VN_TZ = 7.0;                    // 105E, DEC-LUNAR-043\nexport const VN_TZ_ID = \"Asia/Ho_Chi_Minh\";\n/** \"Hom nay\" theo gio Viet Nam, khong phu thuoc TZ thiet bi.\n *  Tra SolarDate TUPLE [day, month, year] - CONTRACT chinh xac.\n *  Tham so la Date tuy chon (khong phai nowUtcMs: number) - CONTRACT chinh xac.\n *  import type { SolarDate } from \"./types\"; */\nexport function todayInHCM(now?: Date): SolarDate;   // SolarDate = readonly [day, month, year]</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Store lunar, derive solar</strong> - a GIO <code>Reminder</code> <code>{lunarDay:10, lunarMonth:8, lunarYear:null}</code> contains no solar-date field; <code>nextOccurrences</code> returns different solar dates for 2025 and 2026.</li><li><strong>ANNUAL repeats yearly</strong> - with <code>recurrence:\"ANNUAL\"</code>, <code>nextOccurrences(count:3)</code> returns 3 occurrences in 3 consecutive lunar years, each calling <code>convertLunar2Solar</code> with its own <code>targetLunarYear</code>.</li><li><strong>MONTHLY generates 12-13</strong> - <code>recurrence:\"MONTHLY\", lunarDay:15</code> in a normal lunar year generates 12 occurrences; in a year with a leap month it generates 13 (repeating the leap month too).</li><li><strong>ONCE fixes the year</strong> - <code>recurrence:\"ONCE\", lunarYear:2025</code> returns exactly 1 occurrence in lunar year 2025, with no recurrence.</li><li><strong>Leap-month fallback REGULAR</strong> - a death anniversary <code>{lunarDay:16, lunarMonth:2, isLeapMonth:true, leapFallback:\"REGULAR\"}</code> in a year without a leap month 2 returns an occurrence in the regular month 2 with <code>fellBack === true</code>.</li><li><strong>Fallback SKIP</strong> - the same death anniversary with <code>leapFallback:\"SKIP\"</code> skips the year without that leap month (no occurrence that year).</li><li><strong>Fallback ASK</strong> - with <code>leapFallback:\"ASK\"</code>, the occurrence in a year missing the leap month has <code>pendingUserChoice === true</code> and does not observe it automatically.</li><li><strong>Matches leap year 1985</strong> - a death anniversary entered in the leap month 2 of 1985 generates a solar-date occurrence falling in the range 21/03..19/04/1985 (cross-check TASK-LUNAR-001 fixture).</li><li><strong>Day clamp</strong> - a death anniversary <code>{lunarDay:30}</code> in a target lunar month with only 29 days clamps back to day 29 with <code>dayClamped === true</code>, not jumping to the next month.</li><li><strong>Lead-time fan-out</strong> - <code>leadTimes:[0,1,7]</code> on 1 occurrence generates exactly 3 <code>Occurrence</code> with <code>leadDays</code> 0/1/7 and <code>fireAtLocal</code> stepped back exactly 0/1/7 days.</li><li><strong>notifyTime applied</strong> - <code>notifyTime:\"06:30\"</code> gives a <code>fireAtLocal</code> ending <code>T06:30:00+07:00</code>.</li><li><strong>Timezone lock</strong> - <code>todayInHCM(new Date(...))</code> returns the tuple <code>[day, month, year]</code> in Vietnam time; running with <code>process.env.TZ=\"America/New_York\"</code> still returns the correct value (not off by 1 day). CONTRACT signature: <code>todayInHCM(now?: Date): SolarDate</code>.</li><li><strong>Cache stale by engineVersion</strong> - <code>isCacheStale(cache, \"1.1.0\")</code> returns <code>true</code> when cache.engineVersion is \"1.0.0\".</li><li><strong>validateReminder catches errors</strong> - <code>RAM</code> with <code>lunarDay:14</code> returns error <code>code:\"RAM_DAY_MISMATCH\"</code>; <code>ONCE</code> missing <code>lunarYear</code> returns <code>code:\"ONCE_NEEDS_YEAR\"</code>.</li><li><strong>normalizeReminder defaults</strong> - empty input returns <code>notifyTime:\"07:00\"</code>, <code>leadTimes:[1]</code>, <code>channels:[\"LOCAL\"]</code>, <code>leapFallback:\"REGULAR\"</code>, <code>enabled:true</code>.</li><li><strong>mergeAndSort ascending</strong> - merging occurrences of 3 Reminders, the result is sorted ascending by <code>fireAtLocal</code>, with the nearest occurrence first.</li><li><strong>Round-trip consistency</strong> - for each generated occurrence, <code>convertSolar2Lunar</code> of <code>gregorianDate</code> returns exactly the entered <code>(lunarDay, lunarMonth)</code> (except for the <code>fellBack</code>/<code>dayClamped</code> cases already marked).</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// packages/amlich-core/test/recurrence.test.ts\nimport { describe, it, expect } from \"vitest\";\nimport { normalizeReminder, validateReminder, isCacheStale, type Reminder, type SolarDate } from \"../src/reminder\";\nimport { nextOccurrences, mergeAndSort } from \"../src/recurrence\";\nimport { todayInHCM } from \"../src/tz\";\n// CONTRACT: todayInHCM(now?: Date): SolarDate (tuple [day, month, year]); import SolarDate de type-check\n\nconst ENGINE = \"1.0.0\";\nconst gio = (over: Partial&lt;Reminder&gt;): Reminder =&gt; normalizeReminder({\n  id: \"r1\", userId: \"u1\", type: \"GIO\", title: \"Gio ba\", lunarDay: 10, lunarMonth: 8,\n  recurrence: \"ANNUAL\", isLeapMonth: false, ...over,\n});\n\ndescribe(\"recurrence engine\", () =&gt; {\n  it(\"ANNUAL derives different solar dates per year (DEC-LUNAR-041)\", () =&gt; {\n    const occ = nextOccurrences(gio({ leadTimes: [0] }), { fromYear: 2025, count: 2, engineVersion: ENGINE });\n    expect(occ).toHaveLength(2);\n    expect(occ[0].gregorianDate).not.toEqual(occ[1].gregorianDate);\n  });\n\n  it(\"MONTHLY produces 13 occurrences in a leap year (AC #3)\", () =&gt; {\n    const r = gio({ recurrence: \"MONTHLY\", lunarDay: 15, leadTimes: [0] });\n    // 2025 am co thang 6 nhuan -&gt; 13 lan Ram\n    const occ = nextOccurrences(r, { fromYear: 2025, count: 13, engineVersion: ENGINE });\n    expect(occ.filter(o =&gt; o.lunarLabel.startsWith(\"15/\")).length).toBe(13);\n  });\n\n  it(\"leap-month giỗ falls back to regular month (DEC-LUNAR-042, AC #5)\", () =&gt; {\n    const r = gio({ lunarDay: 16, lunarMonth: 2, isLeapMonth: true, leapFallback: \"REGULAR\", leadTimes: [0] });\n    const occ = nextOccurrences(r, { fromYear: 2030, count: 1, engineVersion: ENGINE });\n    expect(occ[0].fellBack).toBe(true);\n    expect(occ[0].lunarLabel).toContain(\"16/2\");\n  });\n\n  it(\"ASK fallback yields pendingUserChoice, no auto-cung (AC #7)\", () =&gt; {\n    const r = gio({ lunarDay: 16, lunarMonth: 2, isLeapMonth: true, leapFallback: \"ASK\", leadTimes: [0] });\n    const occ = nextOccurrences(r, { fromYear: 2030, count: 1, engineVersion: ENGINE });\n    expect(occ[0].pendingUserChoice).toBe(true);\n  });\n\n  it(\"1985 leap month 2 lands in the Mar-Apr window (AC #8, cross-check TASK-001)\", () =&gt; {\n    const r = gio({ lunarDay: 16, lunarMonth: 2, lunarYear: 1985, isLeapMonth: true, recurrence: \"ONCE\", leadTimes: [0] });\n    const occ = nextOccurrences(r, { fromYear: 1985, count: 1, engineVersion: ENGINE });\n    const d = occ[0].gregorianDate;            // \"1985-MM-DD\"\n    expect(d &gt;= \"1985-03-21\" &amp;&amp; d &lt;= \"1985-04-19\").toBe(true);\n  });\n\n  it(\"day 30 clamps to last day of a 29-day month (AC #9)\", () =&gt; {\n    const r = gio({ lunarDay: 30, lunarMonth: 9, leadTimes: [0] });\n    const occ = nextOccurrences(r, { fromYear: 2027, count: 1, engineVersion: ENGINE });\n    // chi assert chung: khi clamp xay ra thi co co dayClamped, ngay am &lt;= 30\n    if (occ[0].dayClamped) expect(occ[0].lunarLabel).toMatch(/^(29|30)\\//);\n  });\n\n  it(\"lead-times fan out to multiple Occurrence (AC #10)\", () =&gt; {\n    const occ = nextOccurrences(gio({ leadTimes: [0, 1, 7] }), { fromYear: 2025, count: 1, engineVersion: ENGINE });\n    expect(occ.map(o =&gt; o.leadDays).sort((a, b) =&gt; a - b)).toEqual([0, 1, 7]);\n  });\n\n  it(\"timezone is locked to HCM regardless of process TZ (DEC-LUNAR-043, AC #12)\", () =&gt; {\n    // todayInHCM(now?: Date): SolarDate - nhan Date, tra TUPLE [day, month, year] (CONTRACT)\n    const prev = process.env.TZ; process.env.TZ = \"America/New_York\";\n    const fixedNow = new Date(Date.UTC(2026, 1, 17, 5, 0, 0)); // 17/02/2026 12:00 +07\n    const result = todayInHCM(fixedNow);   // SolarDate = readonly [day, month, year]\n    expect(result[0]).toBe(17);            // day\n    expect(result[1]).toBe(2);             // month\n    expect(result[2]).toBe(2026);          // year\n    process.env.TZ = prev;\n  });\n\n  it(\"validateReminder catches RAM day mismatch and ONCE without year (AC #14)\", () =&gt; {\n    const e1 = validateReminder(gio({ type: \"RAM\", lunarDay: 14 }));\n    expect(e1.some(e =&gt; e.code === \"RAM_DAY_MISMATCH\")).toBe(true);\n    const e2 = validateReminder(gio({ recurrence: \"ONCE\", lunarYear: undefined }));\n    expect(e2.some(e =&gt; e.code === \"ONCE_NEEDS_YEAR\")).toBe(true);\n  });\n\n  it(\"normalizeReminder applies stable defaults (AC #15)\", () =&gt; {\n    const r = normalizeReminder({ id: \"x\", userId: \"u\", type: \"GIO\", title: \"t\", lunarDay: 1, lunarMonth: 1, recurrence: \"ANNUAL\" });\n    expect(r.notifyTime).toBe(\"07:00\");\n    expect(r.leadTimes).toEqual([1]);\n    expect(r.channels).toEqual([\"LOCAL\"]);\n    expect(r.leapFallback).toBe(\"REGULAR\");\n    expect(r.enabled).toBe(true);\n  });\n\n  it(\"cache invalidates when engineVersion changes (DEC-LUNAR-044, AC #13)\", () =&gt; {\n    expect(isCacheStale({ reminderId: \"r\", gregorianDate: \"2025-01-01\", lunarLabel: \"1/1\", computedAt: \"2025-01-01T00:00:00Z\", engineVersion: \"1.0.0\" }, \"1.1.0\")).toBe(true);\n  });\n\n  it(\"mergeAndSort orders ascending by fireAtLocal (AC #16)\", () =&gt; {\n    const a = nextOccurrences(gio({ id: \"a\", lunarMonth: 1, leadTimes: [0] }), { fromYear: 2026, count: 1, engineVersion: ENGINE });\n    const b = nextOccurrences(gio({ id: \"b\", lunarMonth: 7, leadTimes: [0] }), { fromYear: 2026, count: 1, engineVersion: ENGINE });\n    const merged = mergeAndSort([...b, ...a]);\n    for (let i = 1; i &lt; merged.length; i++) expect(merged[i - 1].fireAtLocal &lt;= merged[i].fireAtLocal).toBe(true);\n  });\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The API contract in §3 is the skeleton. The hardest detail being pinned down is the occurrence-generation loop for ANNUAL, because it is where the fallback and clamp rules meet:</p>\n<pre><code class=\"language-typescript\">// packages/amlich-core/src/recurrence.ts (lõi - skeleton noi bo; barrel export tra readonly Occurrence[])\nexport function nextOccurrences(r: Reminder, opt: RecurrenceOptions): readonly Occurrence[] {\n  const out: Occurrence[] = [];   // mutable noi bo, cast khi return\n  let lunarYear = solarYearToStartLunarYear(opt.fromYear);   // moc nam am bat dau quet\n  let lunarOccCount = 0;                                      // dem occurrence AM (truoc lead-time)\n  let scanned = 0;                                            // guard chong vong lap khi SKIP nhieu nam lien tiep\n  // out.length = lunarOccCount * leadTimes.length; quet du opt.count occurrence am (§1 #4)\n  while (lunarOccCount &lt; opt.count &amp;&amp; scanned &lt; opt.count + MAX_SKIP_SCAN) {\n    scanned++;\n    const targetYear = r.recurrence === \"ONCE\" ? r.lunarYear! : lunarYear;\n    // 1) thang nhuan: nam dich co dung thang nhuan da nhap khong?\n    const leapOffset = getLeapMonthOffset(getLunarMonth11(targetYear, VN_TZ), VN_TZ);\n    const resolved = resolveLeap(r, targetYear, leapOffset);  // -&gt; {month, isLeap, fellBack, skip, pending}\n    if (resolved.skip) {                                       // leapFallback = SKIP - bo nam nay, KHONG sinh occurrence\n      if (r.recurrence === \"ONCE\") break;                     // ONCE + skip: nam co dinh -&gt; dung, tranh vong vo han\n      lunarYear++; continue;\n    }\n    // 2) clamp ngay neu thang am thieu ngay\n    const { day, dayClamped } = clampLunarDay(r.lunarDay, targetYear, resolved.month, resolved.isLeap);\n    // 3) goi engine TASK-LUNAR-001 - tinh moi nam, khong noi suy (DEC-LUNAR-041)\n    //    resolved.isLeap CHI = true khi nam dich that su co thang nhuan do (REGULAR fallback -&gt; isLeap=false),\n    //    nen convertLunar2Solar khong tra sentinel [0,0,0]; truong hop pending tra occurrence pendingUserChoice.\n    const [gd, gm, gy] = convertLunar2Solar(day, resolved.month, targetYear, resolved.isLeap ? 1 : 0, VN_TZ);\n    // 4) nhan lead-time + notifyTime -&gt; nhieu Occurrence (count nay la 1 occurrence am)\n    for (const lead of r.leadTimes) out.push(makeOccurrence(r, gd, gm, gy, lead, resolved, dayClamped));\n    lunarOccCount++;\n    if (r.recurrence === \"ONCE\") break;\n    lunarYear++;\n  }\n  return out;\n}\n// MAX_SKIP_SCAN bao tran so nam quet them khi SKIP lien tiep (vd giỗ thang nhuan hiem); &gt;= ~30 du an toan.</code></pre>\n<p><code>occurrenceBudget</code>/<code>opt.count</code> counts by LUNAR occurrence; each lunar occurrence generates <code>leadTimes.length</code> <code>Occurrence</code>. The loop increases <code>lunarOccCount</code> only when it actually generates an occurrence (not when it SKIPs), and has a <code>MAX_SKIP_SCAN</code> guard so a rare leap-month death anniversary with <code>leapFallback = SKIP</code> does not scan forever - the same spirit as the <code>i &lt; 14</code> of <code>getLeapMonthOffset</code> in TASK-LUNAR-001.</p>\n<p><code>makeOccurrence</code> steps <code>gregorianDate</code> back by <code>lead</code> days and attaches <code>notifyTime</code> to build <code>fireAtLocal</code> with a hard <code>+07:00</code> offset. Every date comparison uses the ISO string <code>YYYY-MM-DD</code>, so sorting is string comparison, not local <code>Date</code>.</p>\n<hr>\n<h2>§7 - Dependencies</h2>\n<ul><li>Upstream: TASK-LUNAR-001 (<code>convertLunar2Solar</code>, <code>convertSolar2Lunar</code>, <code>getLunarMonth11</code>, <code>getLeapMonthOffset</code>). This module does no astronomy of its own; it only calls the core engine and wraps the results in a data model.</li><li>Downstream: TASK-LUNAR-005 (the rolling-64 scheduler reads the <code>Occurrence[]</code> after <code>mergeAndSort</code> and cuts the nearest 64), TASK-LUNAR-006 (reminder management CRUD on the <code>Reminder</code> type, showing the upcoming list), TASK-LUNAR-016 (Zalo Mini App reuses Reminder + computes dates on the fly), TASK-LUNAR-017 (ZNS scans Reminders with the <code>ZNS</code> channel), TASK-LUNAR-018 (cloud sync syncs Reminder + <code>sharedWith</code>).</li><li>Cross-cutting: <code>engineVersion</code> relates to TASK-LUNAR-001 (the core semver) and TASK-LUNAR-018 (invalidate the client cache when the core changes). <code>linkedContentId</code> points to TASK-LUNAR-008 FestivalContent.</li></ul>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"reminder\": {\n    \"id\": \"rem_gio_ba_noi\",\n    \"userId\": \"u_linh\",\n    \"type\": \"GIO\",\n    \"title\": \"Giỗ bà nội\",\n    \"lunarDay\": 16,\n    \"lunarMonth\": 2,\n    \"lunarYear\": null,\n    \"isLeapMonth\": false,\n    \"recurrence\": \"ANNUAL\",\n    \"leapFallback\": \"REGULAR\",\n    \"leadTimes\": [0, 1],\n    \"notifyTime\": \"06:30\",\n    \"channels\": [\"LOCAL\"],\n    \"linkedContentId\": \"festival_dam_gio\",\n    \"sharedWith\": [],\n    \"enabled\": true\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"occurrences\": [\n    { \"reminderId\": \"rem_gio_ba_noi\", \"gregorianDate\": \"2026-03-25\", \"lunarLabel\": \"16/2 Bính Ngọ\",\n      \"leadDays\": 1, \"fireAtLocal\": \"2026-03-25T06:30:00+07:00\", \"fellBack\": false, \"dayClamped\": false, \"pendingUserChoice\": false },\n    { \"reminderId\": \"rem_gio_ba_noi\", \"gregorianDate\": \"2026-03-26\", \"lunarLabel\": \"16/2 Bính Ngọ\",\n      \"leadDays\": 0, \"fireAtLocal\": \"2026-03-26T06:30:00+07:00\", \"fellBack\": false, \"dayClamped\": false, \"pendingUserChoice\": false }\n  ]\n}</code></pre>\n<pre><code class=\"language-json\">{ \"occurrenceCache\": { \"reminderId\": \"rem_gio_ba_noi\", \"gregorianDate\": \"2026-03-26\",\n  \"lunarLabel\": \"16/2 Bính Ngọ\", \"computedAt\": \"2026-06-27T01:00:00Z\", \"engineVersion\": \"1.0.0\" } }</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved in this version:</p>\n<ul><li>The leap-month fallback default -&gt; <code>REGULAR</code> (observe the regular month), with <code>fellBack</code> for the UI to confirm (DEC-LUNAR-042).</li><li>Lock every calculation to Asia/Ho_Chi_Minh, not device time (DEC-LUNAR-043).</li><li>A day that does not exist in the lunar month -&gt; clamp to the last day of the month (§1 #7).</li></ul>\n<p>Deferred (close to the Caveats / later tasks):</p>\n<ul><li>A dual lunar-solar calendar for one reminder (for example reminding by a fixed solar date and by a lunar date at once) - not needed for the MVP; this version supports only lunar recurrence.</li><li>A region-specific fallback rule (some households observe the leap month rather than the regular month) - left for TASK-LUNAR-008 to connect the customs content; this version only offers the 3 policies REGULAR/SKIP/ASK.</li><li>Accuracy in very distant years (Caveats: a Soc point near midnight) - the responsibility of TASK-LUNAR-003 (the golden harness); this module only calls the engine and trusts its result.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Storing the solar date as the source by mistake</td><td>the Reminder type has no solar field</td><td>wrong next year, does not happen</td><td>none (by design; AC #1)</td></tr><tr><td>Interpolating 365 days instead of recomputing</td><td>the recurrence engine calls convertLunar2Solar each year</td><td>no drift</td><td>none (DEC-LUNAR-041; AC #2)</td></tr><tr><td>Leap-month death anniversary, target year has none</td><td>resolveLeap checks leapOffset</td><td>fallback REGULAR + fellBack</td><td>UI asks the user (DEC-LUNAR-042; AC #5)</td></tr><tr><td>Leap-month death anniversary, user chose SKIP</td><td>leapFallback = SKIP</td><td>skips that year</td><td>remind next year with a leap month (AC #6)</td></tr><tr><td>Leap-month death anniversary, user chose ASK</td><td>leapFallback = ASK</td><td>pendingUserChoice</td><td>UI shows the choice (AC #7)</td></tr><tr><td>Day 30 in a short month</td><td>clampLunarDay</td><td>clamps back to day 29 + dayClamped</td><td>none (AC #9)</td></tr><tr><td>Deriving by device time abroad</td><td>todayInHCM locks tz=7.0</td><td>correct VN date</td><td>none (DEC-LUNAR-043; AC #12)</td></tr><tr><td>Core changes a constant, stale cache is wrong</td><td>engineVersion mismatch</td><td>isCacheStale = true</td><td>recompute (DEC-LUNAR-044; AC #13)</td></tr><tr><td>RAM entered with lunarDay != 15 by mistake</td><td>validateReminder</td><td>RAM_DAY_MISMATCH error</td><td>UI blocks the save (AC #14)</td></tr><tr><td>ONCE missing lunarYear</td><td>validateReminder</td><td>ONCE_NEEDS_YEAR error</td><td>UI requires the year (AC #14)</td></tr><tr><td>leadTimes containing a negative number</td><td>validateReminder</td><td>LEAD_NEGATIVE error</td><td>normalize filters it out</td></tr><tr><td>Empty channels</td><td>validateReminder</td><td>NO_CHANNEL error</td><td>normalize sets [\"LOCAL\"]</td></tr><tr><td>MONTHLY forgets to repeat the leap month</td><td>the expander walks the leap month too</td><td>13 times in a leap year</td><td>none (AC #3)</td></tr><tr><td>Module pulled into storage I/O</td><td>zero-dep code review</td><td>reject the merge</td><td>keep it purely computational (§1 #14)</td></tr><tr><td>Occurrence not sorted</td><td>mergeAndSort</td><td>sorted ascending by fireAtLocal</td><td>none (AC #16)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li>The golden rule: for each target year, call <code>convertLunar2Solar</code> independently. Never add a fixed number of days from the previous occurrence - that is the difference between matching the calendar and gradually drifting.</li><li><code>resolveLeap</code> is where the fallback policy lives. Before computing the date, ask <code>getLeapMonthOffset</code> whether the target year has the entered leap month; if not, branch on <code>leapFallback</code> (REGULAR / SKIP / ASK) and mark <code>fellBack</code>/<code>pendingUserChoice</code>.</li><li>The day clamp must use the true lunar month length (29 or 30 days), not assume 30. Compute the length as the distance between two consecutive Soc points via the core engine.</li><li><code>fireAtLocal</code> always carries a hard <code>+07:00</code> offset and every date comparison uses ISO strings. Avoid <code>new Date(str)</code> and then reading <code>getDate()</code> because it reads by the runtime TZ - exactly the reason TASK-B06 requires locking Asia/Ho_Chi_Minh.</li><li><code>engineVersion</code> should come from the amlich-core <code>package.json</code> and be passed into <code>RecurrenceOptions</code>, so OccurrenceCache (DEC-LUNAR-044) and TASK-LUNAR-018 share a single source of truth for the core version.</li><li>MONTHLY can repeat day 30 in a month with only 29 days - reuse the shared clamp; and in a leap year, the expander must generate an occurrence for the leap month too (13 times), matching AC #3.</li><li>Keep <code>Occurrence</code> the only structure that TASK-LUNAR-005 reads. Do not let the scheduler derive dates itself - all the lunar logic lives here, the scheduler only merges, sorts, and cuts 64.</li></ul>\n<hr>\n<p><em>End of TASK-LUNAR-004.</em></p>";

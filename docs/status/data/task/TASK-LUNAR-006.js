window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LUNAR-006"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This module is the reminder management layer: monthly Ram/Mung Mot toggles, a form for entering gio/custom reminders by lunar date, choosing lead-time and reminder time, an upcoming list with solar dates, and choosing the notification tone (TASK-F05). This is the contract:</p>\n<ol><li><strong>MUST</strong> provide two independent monthly toggles \"Remind Ram (15 lunar)\" and \"Remind Mung Mot (1 lunar)\"; turning each toggle on creates a <code>Reminder</code> with <code>type</code> <code>RAM</code>/<code>MUNG_MOT</code> and <code>recurrence</code> <code>MONTHLY</code>, turning it off deletes it and reschedules (DEC-LUNAR-060, TASK-B01).</li><li><strong>MUST</strong> allow entering a death anniversary (<code>type</code> <code>GIO</code>) by LUNAR date: <code>lunarDay</code> + <code>lunarMonth</code> + the <code>isLeapMonth</code> flag; entered once, the app recurs it every year automatically (TASK-B02 was implemented in TASK-LUNAR-004; here it is the entry UI). When <code>isLeapMonth = true</code>, the form <strong>MUST</strong> show and allow choosing <code>leapFallback</code> (<code>REGULAR</code> / <code>SKIP</code> / <code>ASK</code>, default <code>REGULAR</code>) - this is the choice the TASK-LUNAR-004 §1 #5/#6 model exposes; if the UI does not surface it, the model exposes a decision the user never sees (DEC-LUNAR-042).</li><li><strong>MUST</strong> allow creating a custom lunar reminder (<code>type</code> <code>CUSTOM</code>), for example \"the God of Wealth day, 10th of the first lunar month\" (<code>lunarDay:10, lunarMonth:1</code>) (TASK-B03).</li><li><strong>MUST</strong> take the LUNAR date as the input source; the UI <strong>MUST</strong> show the computed solar date alongside for reference, and <strong>MUST NOT</strong> let the user enter a solar date directly as the source of a gio (DEC-LUNAR-061, TASK-B02).</li><li><strong>MUST</strong> let each reminder choose a lead-time that is a subset of <code>{0 = on the day, 1 = 1 day before, 3 = 3 days before, 7 = 1 week before}</code> (multiple selected at once) and one <code>notifyTime</code> (default \"07:00\") (DEC-LUNAR-062, TASK-B04).</li><li><strong>MUST</strong> show the list of upcoming reminders sorted by the solar date computed from TASK-LUNAR-004; each row <strong>MUST</strong> show the solar date, the lunar label (for example \"16/2 lunar\"), and the lead-time (DEC-LUNAR-063, TASK-B07). Any row that maps to an occurrence with <code>fellBack = true</code> <strong>MUST</strong> show a \"moved to the regular month\" label, and an occurrence with <code>pendingUserChoice = true</code> (leapFallback = ASK) <strong>MUST</strong> show a \"needs a month to observe\" prompt - this is where the UI surfaces the flags the TASK-LUNAR-004 model emits and TASK-LUNAR-005 carries in userInfo (DEC-LUNAR-042).</li><li><strong>MUST</strong> call <code>reschedule()</code> from TASK-LUNAR-005 in the same flow after each CRUD or toggle action, so the pending set on iOS always stays in sync with the data (DEC-LUNAR-064).</li><li><strong>MUST</strong> validate the form via <code>validateReminder</code> from TASK-LUNAR-004 before saving and show specific errors (RAM_DAY_MISMATCH, ONCE_NEEDS_YEAR...); <strong>MUST NOT</strong> save an invalid Reminder.</li><li><strong>MUST</strong> fold TASK-F05: write <code>notificationStyle { tone, emoji, imageId? }</code> into the optional field <code>Reminder.notificationStyle</code> (type <code>NotificationStyle</code> owned by TASK-LUNAR-004 in amlich-core, do NOT redeclare it here); the user preselects the tone and emoji for that reminder's notification (TASK-F05, DEC-LUNAR-065).</li><li><strong>MUST</strong> render the notification body from a static template based on <code>tone</code> (<code>warm</code> / <code>neutral</code> / <code>formal</code>) via <code>tone.ts</code>, and <strong>MUST NOT</strong> call AI in Phase 1; AI-generated reminders belong to TASK-LUNAR-015 in Phase 2 (DEC-LUNAR-065).</li><li><strong>MUST</strong> allow editing and deleting each reminder; deleting calls <code>reschedule()</code> immediately; editing runs <code>validateReminder</code> again before saving.</li><li><strong>MUST</strong> display the solar date using the value computed on-device: read from <code>OccurrenceCache</code> if still valid for the <code>engineVersion</code>, otherwise recompute with TASK-LUNAR-004 (DEC-LUNAR-066, NFR-Offline).</li><li><strong>MUST</strong> allow enabling/disabling each reminder (<code>enabled</code>) without deleting it; disabling -&gt; excludes it from reschedule but keeps the data.</li><li><strong>MUST</strong> link the reminder to the occasion content page via <code>linkedContentId</code> when available (for example gio -&gt; the death-anniversary page, Ram -&gt; the Ram page); the UI shows a \"view ritual\" link (TASK-D02, preparing for TASK-LUNAR-008).</li><li><strong>MUST</strong> work offline: entering, editing, and viewing the upcoming list require no network; only external channels (ZNS in the commercial edition) need the network (NFR-Offline).</li><li><strong>SHOULD</strong> warn when the total number of reminders generates more reminder points than the budget of 64 (using <code>getPlanDiagnostics</code> from TASK-LUNAR-005): show \"you have many reminders, some will be scheduled as they get closer\" rather than silently dropping.</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why are Ram and Mung Mot two separate toggles (§1 #1, DEC-LUNAR-060)?</strong> Many people observe both Ram and Mung Mot, but some observe only one of the two. Two independent switches give exactly the level of control the PRD TASK-B01 describes, and each toggle maps straight to a <code>MONTHLY</code> <code>Reminder</code> - no separate entry screen is needed for the two most common occasions.</p>\n<p><strong>Why is the lunar date the source and the solar date shown only alongside (§1 #4, DEC-LUNAR-061)?</strong> The user remembers \"grandma's gio on the 16th of the second lunar month\", not the solar date - because the solar date changes every year. Forcing the user to enter a solar date would drift wrong next year and break TASK-LUNAR-004's principle of storing the lunar date. Showing the computed solar date alongside reassures them they entered it correctly without turning it into the data source.</p>\n<p><strong>Why call reschedule after each change (§1 #7, DEC-LUNAR-064)?</strong> The set of pending notifications on iOS must reflect the current data exactly. If the user turns on the Ram reminder without a reschedule, the device will not ring on the right occasion. Calling <code>reschedule()</code> in the same CRUD/toggle flow ensures pending always matches - this is the most important join point between the management layer and the scheduler.</p>\n<p><strong>Why is lead-time a fixed multi-select (§1 #5, DEC-LUNAR-062)?</strong> The PRD lists exactly four levels: on the day / 1 / 3 days before / 1 week before. Allowing several at once (for example a week ahead to buy offerings and on the day to observe) fits real habits. A fixed set keeps the UI compact and makes the budget of 64 easy to calculate.</p>\n<p><strong>Why sort the upcoming list by solar date (§1 #6, DEC-LUNAR-063)?</strong> Users live by the solar calendar - they want to know \"what is coming up\" in real chronological order. Sorting by the solar date computed from TASK-LUNAR-004, with the lunar label and lead-time, gives a view that matches what the PRD section 13 describes.</p>\n<p><strong>Why fold TASK-F05 but not use AI in Phase 1 (§1 #9, #10, DEC-LUNAR-065)?</strong> TASK-F05 (choosing tone, emoji, image) is part of the personalized experience and reasonably sits next to reminder management. But generating reminders with AI belongs to TASK-LUNAR-015 (Phase 2, with a Claude proxy). In Phase 1, the notification body renders from a static template based on <code>tone</code> - still warm, still personal, without a backend or key.</p>\n<p><strong>Why read OccurrenceCache when still valid (§1 #12, DEC-LUNAR-066)?</strong> Recomputing the solar date for every row each time the list opens is wasteful when the data has not changed. Read the cache if the <code>engineVersion</code> still matches, recompute when the core has changed - both fast and correct, and fully offline per NFR-Offline.</p>\n<p><strong>Why warn rather than silently drop when &gt; 64 (§1 #16)?</strong> If a family enters many gio, the total reminder points can exceed 64 and the scheduler will drop the distant ones. Rather than surprising the user by not being reminded, showing a warning (\"some will be scheduled as they get closer\") keeps things transparent, in the spirit of the PRD Recommendations about the &gt; 50 reminders threshold.</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// apps/web/lib/reminders/tone.ts  (TASK-F05, template tinh - KHONG AI)\n// NotificationStyle la field tren Reminder, type do TASK-LUNAR-004 so huu (amlich-core).\n// Module nay KHONG redeclare - import de tranh drift schema giua 004 va 006.\nimport type { NotificationStyle } from \"@cyberskill/amlich-core\";\nexport type { NotificationStyle };\nexport type Tone = NotificationStyle[\"tone\"];   // \"warm\" | \"neutral\" | \"formal\"\n\nexport interface ToneContext {\n  title: string;            // \"Giỗ bà nội\"\n  solarLabel: string;       // \"25/03\"\n  lunarLabel: string;       // \"16/2 ÂL\"\n  leadDays: number;         // 0 = dung ngay\n}\n/** Render body thông báo từ template theo tone. Không gọi mạng, không AI (DEC-LUNAR-065). */\nexport function renderBody(style: NotificationStyle, ctx: ToneContext): string;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/lib/reminders/store.ts\nimport type { Reminder } from \"@cyberskill/amlich-core\";\nimport { reschedule } from \"../notifications/scheduler\";\n\nexport interface UpcomingItem {\n  reminderId: string;\n  title: string;\n  solarDate: string;        // \"YYYY-MM-DD\" tinh tu TASK-LUNAR-004\n  lunarLabel: string;\n  leadDays: number;\n  linkedContentId?: string;\n  fellBack?: boolean;          // §1 #6 - occurrence da chuyen sang thang thuong (DEC-LUNAR-042)\n  pendingUserChoice?: boolean; // §1 #6 - leapFallback=ASK, can user chon thang cung\n}\n\nexport interface ReminderStore {\n  list(): Reminder[];\n  upsert(r: Reminder): Promise&lt;{ errors: { field: string; code: string }[] }&gt;;  // validate truoc, reschedule sau\n  remove(id: string): Promise&lt;void&gt;;                                            // reschedule sau\n  setEnabled(id: string, enabled: boolean): Promise&lt;void&gt;;                      // reschedule sau\n  toggleMonthly(kind: \"RAM\" | \"MUNG_MOT\", on: boolean): Promise&lt;void&gt;;          // DEC-LUNAR-060\n  upcoming(nowUtcMs: number, limit?: number): UpcomingItem[];                   // sort theo solarDate\n  diagnostics(nowUtcMs: number): { scheduled: number; slotsDropped: number; remindersCovered: number };\n}\n\nexport function createReminderStore(engineVersion: string): ReminderStore;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/components/reminders/ReminderForm.tsx (props)\nimport type { Reminder, LeapFallback } from \"@cyberskill/amlich-core\";\nexport interface ReminderFormProps {\n  initial?: Partial&lt;Reminder&gt;;\n  onSubmit: (r: Reminder) =&gt; void;     // goi store.upsert\n  // UI: nhap lunarDay/lunarMonth + isLeapMonth; hien solarPreview tinh on-the-fly (DEC-LUNAR-061)\n  solarPreview: (lunarDay: number, lunarMonth: number, isLeap: boolean) =&gt; string;\n  // Khi isLeapMonth=true, PHAI surface lua chon leapFallback (REGULAR/SKIP/ASK) - model TASK-LUNAR-004 §1 #5/#6 expose (DEC-LUNAR-042)\n  onLeapFallbackChange?: (policy: LeapFallback) =&gt; void;\n}\n\n// apps/web/components/reminders/MonthlyToggles.tsx (props)\nexport interface MonthlyTogglesProps {\n  ramOn: boolean; mungMotOn: boolean;\n  onToggle: (kind: \"RAM\" | \"MUNG_MOT\", on: boolean) =&gt; void;   // goi store.toggleMonthly\n}\n\n// apps/web/components/reminders/NotificationStylePicker.tsx (props, TASK-F05)\nimport type { NotificationStyle } from \"@cyberskill/amlich-core\"; // owner: TASK-LUNAR-004; KHONG redeclare\nexport interface NotificationStylePickerProps {\n  value: NotificationStyle;\n  onChange: (s: NotificationStyle) =&gt; void;   // tone/emoji/imageId\n  preview: (s: NotificationStyle) =&gt; string;  // dung renderBody\n}</code></pre>\n<pre><code class=\"language-typescript\">// lead-time constants (DEC-LUNAR-062, TASK-B04)\nexport const LEAD_TIME_OPTIONS = [\n  { value: 0, label: \"Đúng ngày\" },\n  { value: 1, label: \"Trước 1 ngày\" },\n  { value: 3, label: \"Trước 3 ngày\" },\n  { value: 7, label: \"Trước 1 tuần\" },\n] as const;\nexport const DEFAULT_NOTIFY_TIME = \"07:00\";</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Independent Ram toggle</strong> - turning on \"Remind Ram\" creates 1 <code>Reminder</code> with <code>type:\"RAM\"</code>, <code>recurrence:\"MONTHLY\"</code>, <code>lunarDay:15</code>; turning it off deletes it.</li><li><strong>Independent Mung Mot toggle</strong> - turning on \"Remind Mung Mot\" creates <code>type:\"MUNG_MOT\"</code>, <code>lunarDay:1</code>; independent of Ram (turning this on does not close the other).</li><li><strong>Toggle calls reschedule</strong> - each <code>toggleMonthly</code> calls <code>reschedule()</code> exactly once (asserted via mock).</li><li><strong>Enter gio by lunar date</strong> - entering <code>lunarDay:16, lunarMonth:2</code> in the form creates a <code>Reminder</code> of <code>type:\"GIO\"</code> with no solar-date field.</li><li><strong>Solar preview shown alongside</strong> - the form shows the solar date string computed from <code>(lunarDay, lunarMonth, isLeap)</code> for the coming year; the user has no solar-date input field.</li><li><strong>Custom reminder</strong> - entering \"God of Wealth day\" <code>lunarDay:10, lunarMonth:1</code> creates <code>type:\"CUSTOM\"</code>.</li><li><strong>Lead-time multi-select</strong> - selecting <code>[0,7]</code> saves <code>leadTimes:[0,7]</code>; setting <code>notifyTime:\"06:30\"</code> saves it correctly.</li><li><strong>Upcoming sorted by solar date</strong> - <code>upcoming()</code> returns a list sorted ascending by <code>solarDate</code>; each item has <code>solarDate</code>, <code>lunarLabel</code>, <code>leadDays</code>.</li><li><strong>CRUD calls reschedule</strong> - <code>upsert</code>, <code>remove</code>, <code>setEnabled</code> each call <code>reschedule()</code> after changing the data (asserted via mock).</li><li><strong>Validation blocks bad saves</strong> - <code>upsert</code> of a <code>RAM</code> with <code>lunarDay:14</code> returns <code>errors</code> containing <code>RAM_DAY_MISMATCH</code> and does NOT save.</li><li><strong>notificationStyle saved</strong> - choosing <code>tone:\"warm\", emoji:\"🌼\"</code> saves into <code>Reminder.notificationStyle</code>.</li><li><strong>renderBody makes no network call</strong> - <code>renderBody</code> is a pure function, returns different strings for <code>warm</code> vs <code>formal</code>, with no fetch.</li><li><strong>disabling keeps the data</strong> - <code>setEnabled(id,false)</code> keeps the Reminder in <code>list()</code> but excludes it from reschedule.</li><li><strong>Offline</strong> - <code>upcoming()</code> and <code>upsert()</code> run without a network (no network call in the CRUD/display path).</li><li><strong>Diagnostics warning</strong> - with many reminders, <code>diagnostics()</code> returns <code>slotsDropped &gt; 0</code> so the UI can warn.</li><li><strong>Content link</strong> - a reminder with <code>linkedContentId</code> shows the upcoming item with <code>linkedContentId</code> so the UI can render a \"view ritual\" link.</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// apps/web/test/reminders.store.test.ts\nimport { describe, it, expect, vi } from \"vitest\";\n\nvi.mock(\"../lib/notifications/scheduler\", () =&gt; ({ reschedule: vi.fn(async () =&gt; ({ permission: \"granted\", plan: { notifications: [], diagnostics: { scheduled: 0, slotsDropped: 0, remindersCovered: 0, horizonMonths: 12 } } })) }));\nimport { reschedule } from \"../lib/notifications/scheduler\";\nimport { createReminderStore } from \"../lib/reminders/store\";\nimport { renderBody } from \"../lib/reminders/tone\";\nimport type { Reminder } from \"@cyberskill/amlich-core\";\n\nconst ENGINE = \"1.0.0\";\nconst NOW = Date.UTC(2026, 0, 1);\n\ndescribe(\"reminder store\", () =&gt; {\n  it(\"RAM toggle creates a MONTHLY day-15 reminder and reschedules (DEC-LUNAR-060, AC #1/#3)\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    await s.toggleMonthly(\"RAM\", true);\n    const r = s.list().find(x =&gt; x.type === \"RAM\")!;\n    expect(r.recurrence).toBe(\"MONTHLY\");\n    expect(r.lunarDay).toBe(15);\n    expect(reschedule).toHaveBeenCalledTimes(1);\n  });\n\n  it(\"RAM and MUNG_MOT toggles are independent (AC #2)\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    await s.toggleMonthly(\"RAM\", true);\n    expect(s.list().some(x =&gt; x.type === \"MUNG_MOT\")).toBe(false);\n    await s.toggleMonthly(\"MUNG_MOT\", true);\n    expect(s.list().filter(x =&gt; x.type === \"RAM\" || x.type === \"MUNG_MOT\").length).toBe(2);\n  });\n\n  it(\"giỗ is entered by lunar date, never solar (DEC-LUNAR-061, AC #4)\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    const { errors } = await s.upsert({ id: \"g1\", userId: \"u\", type: \"GIO\", title: \"Gio ba\",\n      lunarDay: 16, lunarMonth: 2, isLeapMonth: false, recurrence: \"ANNUAL\" } as any);\n    expect(errors).toHaveLength(0);\n    const r = s.list().find(x =&gt; x.id === \"g1\")! as Record&lt;string, unknown&gt;;\n    expect(\"gregorianDate\" in r).toBe(false);          // khong luu ngay duong\n    expect(\"solarDate\" in r).toBe(false);\n  });\n\n  it(\"upcoming is sorted ascending by solarDate (DEC-LUNAR-063, AC #8)\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    await s.upsert({ id: \"a\", userId: \"u\", type: \"CUSTOM\", title: \"A\", lunarDay: 10, lunarMonth: 7, isLeapMonth: false, recurrence: \"ANNUAL\", leadTimes: [0] } as any);\n    await s.upsert({ id: \"b\", userId: \"u\", type: \"CUSTOM\", title: \"B\", lunarDay: 10, lunarMonth: 1, isLeapMonth: false, recurrence: \"ANNUAL\", leadTimes: [0] } as any);\n    const up = s.upcoming(NOW, 10);\n    for (let i = 1; i &lt; up.length; i++) expect(up[i - 1].solarDate &lt;= up[i].solarDate).toBe(true);\n    expect(up[0]).toHaveProperty(\"lunarLabel\");\n    expect(up[0]).toHaveProperty(\"leadDays\");\n  });\n\n  it(\"CRUD calls reschedule after every mutation (DEC-LUNAR-064, AC #9)\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    (reschedule as any).mockClear();\n    await s.upsert({ id: \"x\", userId: \"u\", type: \"CUSTOM\", title: \"X\", lunarDay: 5, lunarMonth: 5, isLeapMonth: false, recurrence: \"ANNUAL\" } as any);\n    await s.setEnabled(\"x\", false);\n    await s.remove(\"x\");\n    expect((reschedule as any).mock.calls.length).toBe(3);\n  });\n\n  it(\"validateReminder blocks an invalid RAM (AC #10)\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    const { errors } = await s.upsert({ id: \"bad\", userId: \"u\", type: \"RAM\", title: \"Ram sai\", lunarDay: 14, lunarMonth: 1, isLeapMonth: false, recurrence: \"MONTHLY\" } as any);\n    expect(errors.some(e =&gt; e.code === \"RAM_DAY_MISMATCH\")).toBe(true);\n    expect(s.list().some(x =&gt; x.id === \"bad\")).toBe(false);\n  });\n\n  it(\"setEnabled(false) keeps the reminder but drops it from scheduling (AC #13)\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    await s.upsert({ id: \"k\", userId: \"u\", type: \"CUSTOM\", title: \"K\", lunarDay: 3, lunarMonth: 3, isLeapMonth: false, recurrence: \"ANNUAL\" } as any);\n    await s.setEnabled(\"k\", false);\n    expect(s.list().find(x =&gt; x.id === \"k\")!.enabled).toBe(false);\n  });\n\n  it(\"renderBody is pure and tone-sensitive, no AI (DEC-LUNAR-065, AC #12)\", () =&gt; {\n    const ctx = { title: \"Giỗ bà nội\", solarLabel: \"25/03\", lunarLabel: \"16/2 ÂL\", leadDays: 1 };\n    const warm = renderBody({ tone: \"warm\", emoji: \"🌼\" }, ctx);\n    const formal = renderBody({ tone: \"formal\", emoji: \"\" }, ctx);\n    expect(warm).not.toEqual(formal);\n    expect(warm).toContain(\"Giỗ bà nội\");\n  });\n\n  it(\"diagnostics surfaces dropped slots for a heavy load (AC #15)\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    for (let i = 0; i &lt; 80; i++) {\n      await s.upsert({ id: `r${i}`, userId: \"u\", type: \"GIO\", title: `G${i}`, lunarDay: (i % 28) + 1, lunarMonth: (i % 12) + 1, isLeapMonth: false, recurrence: \"ANNUAL\", leadTimes: [0, 1] } as any);\n    }\n    expect(s.diagnostics(NOW).slotsDropped).toBeGreaterThan(0);\n  });\n\n  // AC #5 - solarPreview la prop cua ReminderForm, khong phai field Reminder\n  it(\"solarPreview computes solar string from lunar coords; upsert never stores solarDate (DEC-LUNAR-061, AC #5)\", async () =&gt; {\n    // store.ts PHAI export solarPreviewFromLunar(d, m, leap): string de ReminderForm dung (DEC-LUNAR-061)\n    const { solarPreviewFromLunar } = await import(\"../lib/reminders/store\") as unknown as\n      { solarPreviewFromLunar?: (d: number, m: number, leap: boolean) =&gt; string };\n    if (typeof solarPreviewFromLunar === \"function\") {\n      const result = solarPreviewFromLunar(10, 1, false);\n      expect(typeof result).toBe(\"string\");\n      expect(result.length).toBeGreaterThan(0); // phai tra chuoi ngay duong khong rong\n    }\n    // Cho du solarPreviewFromLunar ton tai hay khong, upsert khong bao gio luu solarDate\n    const s = createReminderStore(ENGINE);\n    await s.upsert({ id: \"sp1\", userId: \"u\", type: \"CUSTOM\", title: \"Solar preview test\",\n      lunarDay: 10, lunarMonth: 3, isLeapMonth: false, recurrence: \"ANNUAL\", leadTimes: [0] } as any);\n    const saved = s.list().find(x =&gt; x.id === \"sp1\") as Record&lt;string, unknown&gt; | undefined;\n    expect(saved).toBeDefined();\n    expect(\"solarDate\" in (saved ?? {})).toBe(false); // khong luu truong solar (DEC-LUNAR-061)\n    expect(\"gregorianDate\" in (saved ?? {})).toBe(false);\n  });\n\n  // AC #6\n  it(\"custom 'via Than Tai' reminder creates type CUSTOM\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    const { errors } = await s.upsert({\n      id: \"tt1\", userId: \"u\", type: \"CUSTOM\", title: \"via Than Tai\",\n      lunarDay: 10, lunarMonth: 1, isLeapMonth: false, recurrence: \"ANNUAL\", leadTimes: [0],\n    } as any);\n    expect(errors).toHaveLength(0);\n    const r = s.list().find(x =&gt; x.id === \"tt1\")!;\n    expect(r.type).toBe(\"CUSTOM\");\n    expect(r.lunarDay).toBe(10);\n    expect(r.lunarMonth).toBe(1);\n  });\n\n  // AC #7\n  it(\"lead-time multi-select saves leadTimes:[0,7] and notifyTime correctly\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    const { errors } = await s.upsert({\n      id: \"lt1\", userId: \"u\", type: \"CUSTOM\", title: \"Lead multi\",\n      lunarDay: 5, lunarMonth: 5, isLeapMonth: false, recurrence: \"ANNUAL\",\n      leadTimes: [0, 7], notifyTime: \"06:30\",\n    } as any);\n    expect(errors).toHaveLength(0);\n    const r = s.list().find(x =&gt; x.id === \"lt1\")! as Reminder &amp; { leadTimes: number[]; notifyTime: string };\n    expect(r.leadTimes).toEqual(expect.arrayContaining([0, 7]));\n    expect(r.leadTimes).toHaveLength(2);\n    expect(r.notifyTime).toBe(\"06:30\");\n  });\n\n  // AC #14\n  it(\"upcoming and upsert run without network (no fetch calls in CRUD path)\", async () =&gt; {\n    const fetchSpy = vi.spyOn(globalThis, \"fetch\");\n    const s = createReminderStore(ENGINE);\n    await s.upsert({ id: \"off1\", userId: \"u\", type: \"CUSTOM\", title: \"Offline test\", lunarDay: 8, lunarMonth: 4, isLeapMonth: false, recurrence: \"ANNUAL\", leadTimes: [0] } as any);\n    s.upcoming(NOW, 5);\n    expect(fetchSpy).not.toHaveBeenCalled();\n    fetchSpy.mockRestore();\n  });\n\n  // AC #16\n  it(\"reminder with linkedContentId surfaces linkedContentId in upcoming item\", async () =&gt; {\n    const s = createReminderStore(ENGINE);\n    await s.upsert({\n      id: \"lc1\", userId: \"u\", type: \"CUSTOM\", title: \"Nghi le test\",\n      lunarDay: 3, lunarMonth: 3, isLeapMonth: false, recurrence: \"ANNUAL\",\n      leadTimes: [0], linkedContentId: \"content-nghi-le-123\",\n    } as any);\n    const items = s.upcoming(NOW, 20);\n    const found = items.find(x =&gt; x.reminderId === \"lc1\");\n    expect(found).toBeDefined();\n    expect(found!.linkedContentId).toBe(\"content-nghi-le-123\");\n  });\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The API in §3 is the skeleton. The detail worth pinning is <code>upsert</code>: it is where validate, persist, and reschedule chain together in exactly the right order:</p>\n<pre><code class=\"language-typescript\">// apps/web/lib/reminders/store.ts (lõi upsert)\nasync upsert(input: Reminder) {\n  const r = normalizeReminder(input);                 // TASK-LUNAR-004\n  const errors = validateReminder(r);                 // TASK-LUNAR-004\n  if (errors.length) return { errors };               // §1 #8 - khong luu neu sai\n  await storage.putReminder(r);                       // TASK-LUNAR-010 storage.ts\n  await reschedule(adapter, this.list(), Date.now(), this.engineVersion);  // §1 #7 / DEC-LUNAR-064\n  return { errors: [] };\n}</code></pre>\n<p><code>upcoming</code> reads <code>OccurrenceCache</code> if it is still valid, otherwise calls <code>nextOccurrences</code> then caches the result, sorted by <code>solarDate</code>. <code>toggleMonthly(\"RAM\", true)</code> creates a <code>Reminder</code> <code>{type:\"RAM\", lunarDay:15, recurrence:\"MONTHLY\"}</code> through <code>upsert</code> itself; <code>false</code> calls <code>remove</code> on the existing RAM reminder.</p>\n<hr>\n<h2>§7 - Dependencies</h2>\n<ul><li>Upstream: TASK-LUNAR-004 (type <code>Reminder</code>, <code>normalizeReminder</code>, <code>validateReminder</code>, <code>nextOccurrences</code> to compute <code>solarDate</code>, <code>OccurrenceCache</code>); TASK-LUNAR-005 (<code>reschedule</code>, <code>getPlanDiagnostics</code> called after each change); TASK-LUNAR-010 (the app shell provides on-device <code>storage.ts</code> and routing, and hosts these components).</li><li>Downstream: does not block any task (it is a leaf of the MVP). Related to TASK-LUNAR-008 (occasion content link via <code>linkedContentId</code>).</li><li>Cross-cutting: <code>notificationStyle</code> (TASK-F05) is rendered by <code>tone.ts</code> in Phase 1; in Phase 2, TASK-LUNAR-015 <strong>MAY</strong> replace the template body with a Claude-generated reminder, but does not change the <code>notificationStyle</code> schema. The deep-link route <code>/reminder/:id</code> from TASK-LUNAR-005 opens the right detail screen here.</li></ul>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"monthlyToggles\": { \"ramOn\": true, \"mungMotOn\": false },\n  \"createdRam\": {\n    \"id\": \"rem_ram_monthly\", \"userId\": \"u_linh\", \"type\": \"RAM\", \"title\": \"Nhắc Rằm\",\n    \"lunarDay\": 15, \"lunarMonth\": 1, \"isLeapMonth\": false, \"recurrence\": \"MONTHLY\",\n    \"leadTimes\": [1], \"notifyTime\": \"07:00\", \"channels\": [\"LOCAL\"], \"enabled\": true,\n    \"notificationStyle\": { \"tone\": \"warm\", \"emoji\": \"🌕\" }\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"upcoming\": [\n    { \"reminderId\": \"rem_ram_monthly\", \"title\": \"Nhắc Rằm\", \"solarDate\": \"2026-03-03\", \"lunarLabel\": \"15/1 ÂL\", \"leadDays\": 1, \"linkedContentId\": \"festival_ram\" },\n    { \"reminderId\": \"rem_gio_ba_noi\", \"title\": \"Giỗ bà nội\", \"solarDate\": \"2026-03-25\", \"lunarLabel\": \"16/2 ÂL\", \"leadDays\": 0, \"linkedContentId\": \"festival_dam_gio\" }\n  ]\n}</code></pre>\n<pre><code class=\"language-json\">{ \"notificationBody\": { \"tone\": \"warm\", \"rendered\": \"🌼 Mai (25/03) là giỗ bà nội (16/2 ÂL) rồi. Nhớ mua hoa cúc vàng nhé!\" } }</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li>Ram/Mung Mot are two independent toggles mapping to MONTHLY Reminders (DEC-LUNAR-060).</li><li>The lunar date is the source, the solar date is shown only for reference (DEC-LUNAR-061).</li><li>TASK-F05 is folded in here, but the body renders from a static template, with AI in Phase 2 (DEC-LUNAR-065).</li></ul>\n<p>Deferred (near later tasks / Caveats):</p>\n<ul><li>Generating personalized reminders with Claude (a dynamic warm tone, by context) - belongs to TASK-LUNAR-015 Phase 2; here there is only a static template by tone.</li><li>Snooze / marking an occurrence as \"observed\" - improves UX but is not part of the MVP core; deferred.</li><li>Entering a custom image for <code>notificationStyle.imageId</code> (for example a photo of the deceased) - touches culturally sensitive data; only opened when there is a PDPL/consent layer (TASK-LUNAR-019); Phase 1 allows only emoji + built-in images.</li><li>A detailed warning when &gt; 64 that suggests which reminders to remove - depends on the fairness of TASK-LUNAR-005; initially just show a general notice.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Ram reminder turned on but the device does not ring</td><td>toggle calls reschedule</td><td>pending in sync</td><td>none (DEC-LUNAR-064; AC #3)</td></tr><tr><td>User enters a solar date as the source</td><td>UI has only a lunar-date input</td><td>does not happen</td><td>none (DEC-LUNAR-061; AC #4/#5)</td></tr><tr><td>Edit/delete a reminder but pending does not change</td><td>CRUD calls reschedule</td><td>resynced</td><td>none (DEC-LUNAR-064; AC #9)</td></tr><tr><td>Save a RAM with the wrong day (!=15)</td><td>validateReminder</td><td>blocks save + reports error</td><td>user fixes it (AC #10)</td></tr><tr><td>ONCE missing the year</td><td>validateReminder</td><td>blocks save</td><td>user enters the year</td></tr><tr><td>Calling AI to generate the body in Phase 1</td><td>tone.ts is pure, no fetch</td><td>static template</td><td>AI in TASK-LUNAR-015 (DEC-LUNAR-065; AC #12)</td></tr><tr><td>Upcoming list in the wrong order</td><td>sort by solarDate</td><td>ascending</td><td>none (AC #8)</td></tr><tr><td>Recomputing the solar date on every open (slow)</td><td>read OccurrenceCache while valid</td><td>fast, offline</td><td>recompute when engineVersion changes (DEC-LUNAR-066)</td></tr><tr><td>Disabling a reminder loses the data</td><td>setEnabled keeps the Reminder</td><td>kept, only excluded from schedule</td><td>re-enable (AC #13)</td></tr><tr><td>Too many reminders, silently dropped</td><td>diagnostics slotsDropped</td><td>UI warning</td><td>user disables some (AC #15)</td></tr><tr><td>Network needed to view the list</td><td>CRUD/display path is offline</td><td>runs offline</td><td>none (NFR-Offline; AC #14)</td></tr><tr><td>Reminder not linked to occasion content</td><td>linkedContentId optional</td><td>item still shows, no link</td><td>attach content later (TASK-D02; AC #16)</td></tr><tr><td>Sensitive emoji/image leaks data</td><td>Phase 1 allows only emoji + built-in images</td><td>no custom image entry</td><td>opened when PDPL is available (TASK-LUNAR-019)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li>The most important join point is that <code>upsert</code>/<code>remove</code>/<code>toggleMonthly</code> always end with <code>reschedule()</code>. This is where the management layer and TASK-LUNAR-005 hook into each other; forgetting it produces the hard-to-trace \"reminder on but no ring\" bug.</li><li>The form never has a solar-date input field. It takes only the lunar date and shows the computed solar date alongside as a preview. This is how the store-the-lunar-date principle of TASK-LUNAR-004 is kept at the UI layer.</li><li>Ram/Mung Mot toggles map straight to a <code>MONTHLY</code> Reminder with <code>lunarDay</code> 15/1 through <code>upsert</code> itself, so they go through the proper validate + reschedule path, not a shortcut.</li><li><code>tone.ts</code> is a static template: a table of phrases by <code>tone</code> (warm/neutral/formal) combined with the context (title, solar date, lunar label, lead). No fetch, no AI - so Phase 1 runs offline and TASK-LUNAR-015 can replace it later without changing the <code>notificationStyle</code> schema.</li><li><code>upcoming</code> reads OccurrenceCache if the <code>engineVersion</code> still matches for speed; it recomputes only when the core changes. The sort uses the ISO string <code>solarDate</code> (string comparison), avoiding local <code>Date</code>.</li><li>The &gt; 64 warning comes from <code>getPlanDiagnostics</code> in TASK-LUNAR-005 (<code>slotsDropped</code>), showing a transparent notice rather than surprising the user - in the spirit of the &gt; 50 reminders threshold in the PRD Recommendations.</li><li><code>notificationStyle.imageId</code> in Phase 1 accepts only emoji and built-in images, not custom image uploads (for example a photo of the deceased) yet, because this is culturally sensitive data - only opened when there is the consent/PDPL layer of TASK-LUNAR-019.</li></ul>\n<hr>\n<p><em>End of TASK-LUNAR-006.</em></p>";

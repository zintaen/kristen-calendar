window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LUNAR-022"]="<h1>Task</h1>\n<blockquote><p>Turn Your Will Into Real.</p></blockquote>\n<h2>Summary</h2>\n<p>Integrate O2O (Online-to-Offline) commerce into Genie by building a system that suggests offering supplies and spiritual services through affiliate links. When users view the details of an occasion (e.g., the full moon of the 7th month) or receive a ZNS, they see shopping suggestions (flowers, offering trays, cleaning services) linking to affiliate partners (Tiki, Shopee, GrabMart, or local partners), helping monetize the app.</p>\n<h2>Problem</h2>\n<p>Reminding users of lunar occasions only solves 50% of their needs. The other 50% is \"what do I prepare for this occasion and where do I buy it?\". The app currently has no clear monetization flow beyond freemium (TASK-LUNAR-020). Providing affiliate links solves both the user's pain and the revenue problem.</p>\n<h2>Customer Quotes</h2>\n<p>&lt;untrusted_content source=\"user-interview\"&gt; \"The full moon of the 7th month is coming up and I am too busy to make it to the market to buy offering supplies. If the app reminded me and also had a place to order an offering tray delivered to my home, I would order right away.\" &lt;/untrusted_content&gt;</p>\n<h2>§1 - Description (Normative Clauses)</h2>\n<ol><li><strong>MUST</strong> implement a generic <code>AffiliateResolver</code> in <code>amlich-core</code> that accepts a <code>LunarEventId</code> and returns a list of matching <code>AffiliateOffer</code> objects (e.g., matching \"Ram\" (full moon) with \"Hoa tuoi\" (fresh flowers), \"Trai cay\" (fruit)).</li><li><strong>MUST</strong> define <code>AffiliateOffer</code> schema including <code>id</code>, <code>provider_name</code>, <code>category</code>, <code>image_url</code>, <code>click_url</code>, and <code>priority</code>.</li><li><strong>MUST</strong> display an <code>AffiliateWidget</code> on the Event Detail page in the Zalo Mini App if the event has active matching offers.</li><li><strong>MUST</strong> inject a dynamic <code>action_link</code> (an affiliate link or a deep link to the Zalo Mini App event page) into the Proactive ZNS template (TASK-LUNAR-021).</li><li><strong>MUST</strong> append a <code>utm_source=genie_lunar</code> and <code>utm_campaign={event_id}</code> to all out-bound affiliate URLs for tracking.</li><li><strong>MUST</strong> log every outgoing click to <code>genie.action_log</code> with <code>event_kind=commerce.affiliate_click</code> to calculate CTR and reconciliation.</li><li><strong>SHOULD</strong> allow A/B testing of different affiliate providers by rotating offers based on server-side probability.</li><li><strong>MUST NOT</strong> process any payments directly within the app; all commerce must hand off to the affiliate partner's site/app.</li></ol>\n<h2>§2 - Why this design</h2>\n<p><strong>Why affiliate links only (§1 #8)?</strong>  Building a full e-commerce checkout, handling payments, refunds, and logistics is extremely complex and outside the core competency of a calendar app. Affiliate out-links provide 80% of the monetization value with 5% of the effort.</p>\n<p><strong>Why build AffiliateResolver in <code>amlich-core</code> (§1 #1)?</strong> The mapping between a lunar event (e.g., \"Tet Han Thuc\", the Cold Food Festival) and the required items (e.g., \"banh troi, banh chay\", glutinous rice balls) is domain logic. It belongs in the core engine so any client (Zalo, iOS, Web) can resolve it identically.</p>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">export interface AffiliateOffer {\n  id: string;\n  provider_name: string; // e.g. \"GrabMart\", \"ShopeeFood\"\n  category: \"flowers\" | \"offerings\" | \"services\";\n  title: string;\n  image_url: string;\n  click_url: string;\n  priority: number;\n}\n\nexport interface AffiliateResolver {\n  // Returns sorted offers for a specific event\n  getOffersForEvent(eventId: string, region?: string): AffiliateOffer[];\n  \n  // Track click to audit log\n  logClick(offerId: string, userId: string, traceId: string): Promise&lt;void&gt;;\n}</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Tier 1 hits first</strong> - When an event is \"Mung 1\" (the first of the lunar month), the event detail page successfully displays at least one \"Hoa tuoi\" (fresh flowers) affiliate offer.</li><li><strong>URL Construction</strong> - When a user clicks an offer, the resulting URL strictly contains <code>utm_source=genie_lunar</code> and the correct campaign tracking ID.</li><li><strong>Telemetry</strong> - When a click occurs, exactly one <code>commerce.affiliate_click</code> row is written to <code>genie.action_log</code>.</li><li><strong>Proactive ZNS Injection</strong> - When generating a ZNS payload (via TASK-021), the <code>action_link</code> is populated with a valid redirect link provided by the AffiliateResolver.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// test/commerce/affiliate-resolver.test.ts\nimport { AffiliateResolverImpl } from '../../src/commerce/affiliate-resolver';\n\ndescribe('AffiliateResolver', () =&gt; {\n  it('MUST append UTM params to click_url', () =&gt; {\n    const resolver = new AffiliateResolverImpl();\n    const offers = resolver.getOffersForEvent('mung_1');\n    expect(offers.length).toBeGreaterThan(0);\n    expect(offers[0].click_url).toContain('utm_source=genie_lunar');\n    expect(offers[0].click_url).toContain('utm_campaign=mung_1');\n  });\n  \n  it('MUST return strictly localized offers if region provided', () =&gt; {\n     // test regional targeting (e.g. only GrabMart in VN)\n  });\n});</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">export class AffiliateResolverImpl implements AffiliateResolver {\n  getOffersForEvent(eventId: string, region?: string): AffiliateOffer[] {\n    const offers = DB.getOffersByTag(eventId); // e.g. mock DB or config\n    \n    return offers.map(offer =&gt; {\n      const url = new URL(offer.base_url);\n      url.searchParams.append('utm_source', 'genie_lunar');\n      url.searchParams.append('utm_campaign', eventId);\n      \n      return {\n        ...offer,\n        click_url: url.toString()\n      };\n    }).sort((a, b) =&gt; b.priority - a.priority);\n  }\n\n  async logClick(offerId: string, userId: string, traceId: string) {\n    await auditLogger.log({\n       module: 'commerce',\n       event_kind: 'affiliate_click',\n       user_id: userId,\n       payload: { offer_id: offerId },\n       trace_id: traceId\n    });\n  }\n}</code></pre>\n<h2>§7 - Dependencies</h2>\n<ul><li><strong>Upstream:</strong> TASK-LUNAR-010 (Event detail pages to host the widgets).</li><li><strong>Upstream:</strong> TASK-LUNAR-016 (Zalo Mini App runtime environment).</li><li><strong>Upstream:</strong> TASK-LUNAR-021 (Proactive AI - to inject links into ZNS).</li></ul>\n<h2>§8 - Example payloads</h2>\n<p><strong>Audit Log Row (Redacted PII):</strong></p>\n<pre><code class=\"language-json\">{\n  \"event\": \"commerce.affiliate_click\",\n  \"user_id\": \"00000000-0000-0000-0000-000000000001\",\n  \"timestamp\": \"2026-08-14T01:00:05Z\",\n  \"payload\": {\n    \"offer_id\": \"grabmart_flowers_01\",\n    \"event_id\": \"ram_thang_7\",\n    \"region\": \"VN\"\n  },\n  \"trace_id\": \"5b23d9161a03f8373b...\"\n}</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li><code>Deferred: P4 slice 2</code> - Affiliate partner onboarding strategy (who are the initial partners?). For now, mock URLs will be used for system testing.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Affiliate URL is malformed</td><td>Unit test URL parser fails</td><td>URL construction panics</td><td>Wrap in try-catch, drop offer from list</td></tr><tr><td>Affiliate network goes down</td><td>User clicks link and gets 404/500 on partner site</td><td>Bad UX</td><td>Out of our control; monitor CTR to detect anomalous drops</td></tr><tr><td>Missing <code>utm_source</code></td><td>E2E integration test assertion</td><td>Attribution lost</td><td>CI blocks PR</td></tr><tr><td>Click audit logging fails (DB down)</td><td><code>logClick</code> throws exception</td><td>Audit log lost</td><td>Swallow error so user is still redirected, but log locally to stderr</td></tr><tr><td>Widget rendering fails on ZMA</td><td>React Error Boundary catches</td><td>Widget omitted</td><td>Core page still functions</td></tr><tr><td>No offers found for event</td><td><code>getOffersForEvent</code> returns []</td><td>Widget hidden</td><td>Expected behavior</td></tr><tr><td>ZNS template max chars exceeded</td><td>Zalo API returns 400</td><td>ZNS send fails</td><td>Truncate URL or use shortlink service (e.g. bit.ly wrapper)</td></tr><tr><td>Invalid UUID format for user_id</td><td><code>Uuid::parse</code> fails</td><td>Click logging fails</td><td>Fallback to Uuid::nil()</td></tr><tr><td>Regional targeting misconfigured</td><td>User sees irrelevant offer</td><td>Low CTR</td><td>Fix in admin DB</td></tr><tr><td>Ad blocker strips UTMs</td><td>Analytics mismatch</td><td>Under-reporting</td><td>Acceptable baseline loss</td></tr></tbody></table>\n<h2>§11 - Implementation notes</h2>\n<ul><li><strong>Shortlinks:</strong> If the affiliate URLs are too long for Zalo ZNS templates, we must route them through a URL shortener or our own <code>genie-api/r/:id</code> endpoint that expands and redirects.</li><li><strong>Click Logging:</strong> The <code>logClick</code> function should execute synchronously <em>before</em> the client-side redirect happens (or fire-and-forget simultaneously) to ensure the click is recorded before the browser unloads the page.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> LLM agent acting as task-author</li><li><strong>Scope:</strong> The entire task content.</li><li><strong>Human review:</strong> Reviewed by the operator after generation.</li></ul>\n<p><em>End of TASK-LUNAR-022.</em></p>";

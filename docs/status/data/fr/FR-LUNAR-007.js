window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LUNAR-007"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This component <strong>MUST</strong> display the dual-system month calendar grid (solar + lunar) on the main screen, meeting the render performance standard of under 100ms per NFR-Performance.</p>\n<ol><li><strong>MUST</strong> display a 7-column grid (Sunday to Saturday), each row a solar week, each cell corresponding to one solar day of the month (FR-A05).</li><li><strong>MUST</strong> display in each cell: the solar date (large, prominent number), the corresponding lunar date (small number, bottom-left corner), and the day's can-chi (small text below the solar date) (FR-A05, DEC-LUNAR-070).</li><li><strong>MUST</strong> mark the day's tiet khi with a small label or icon on the cell when that day is the start day of one of the 24 tiet khi (FR-A05, FR-A04 from FR-LUNAR-002).</li><li><strong>MUST</strong> show a small color dot on the day cell when that day has at least one active reminder or is a festival/occasion day in the festival content list (DEC-LUNAR-073).</li><li><strong>MUST</strong> distinguish 3 dot levels: none (blank cell), an ordinary reminder/ritual (primary color dot), a major festival in the main occasions list (accent color dot) - do not use more than 3 levels (DEC-LUNAR-073).</li><li><strong>MUST</strong> compute the entire <code>DayInfo[]</code> for the month in a single call to <code>buildMonthGrid(year, month)</code> before rendering, and must not call <code>convertSolar2Lunar</code> per cell individually inside the render loop (DEC-LUNAR-070, NFR-Performance).</li><li><strong>MUST</strong> memoize the <code>buildMonthGrid</code> result by <code>[year, month]</code> using <code>useMemo</code> or an equivalent mechanism, avoiding recomputation when the component re-renders without changing the month (DEC-LUNAR-071).</li><li><strong>MUST</strong> display a month header containing the solar month name (for example \"January 2025\") and the corresponding lunar month of the grid's first day (DEC-LUNAR-074).</li><li><strong>MUST</strong> have buttons to navigate to the previous and next month; changing the month <strong>MUST</strong> update the header and grid without reloading the page.</li><li><strong>MUST</strong> correctly handle months that do not start on Sunday by leaving the first cells of the row empty, and months with 28/29/30/31 days.</li><li><strong>MUST</strong> highlight today (the current solar date) with a distinct style (for example a deep purple ring).</li><li><strong>MUST</strong> meet NFR-Performance: the time from when the user taps \"next month\" to when the new grid is fully displayed <strong>MUST</strong> be under 100ms measured on a mid-range device (DEC-LUNAR-071).</li><li><strong>MUST</strong> handle tap/click events on a day cell by opening the <code>DayDetailPanel</code> showing that day's full information from the precomputed <code>DayInfo</code> (DEC-LUNAR-072).</li><li>The <code>DayDetailPanel</code> <strong>MUST</strong> display at least: the full solar date, the full lunar date (leap month if any), the day/month/year can-chi, the tiet khi if any, and the list of reminders on that day; the Hoang dao/Truc/28 mansions fields <strong>SHOULD</strong> be placeholders for FR-LUNAR-011 when ready (DEC-LUNAR-072).</li><li><strong>MUST NOT</strong> call the network to get calendar data; all computation must be offline from <code>amlich-core</code> (NFR-Offline).</li><li><strong>SHOULD</strong> support horizontal swipe (swipe left/right) to change months on touch devices.</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why compute the whole month in a single pass (DEC-LUNAR-070)?</strong> Calling <code>convertSolar2Lunar</code> for each cell inside the render loop means React has to wait for the computation to finish per cell before it can render - with 31 days and each conversion under 5ms, the total synchronous time can reach 150ms, violating NFR-Performance. The <code>buildMonthGrid</code> function computes a flat array first, and the render only reads from that array.</p>\n<p><strong>Why use <code>useMemo</code> instead of computing in <code>useEffect</code> (DEC-LUNAR-071)?</strong> <code>useEffect</code> runs after render, meaning the user sees a blank grid and then it fills in - creating a \"flicker\" effect. <code>useMemo</code> runs synchronously during render, and the value is ready when the DOM is painted. With 31 days and an algorithm under 5ms/day, a total under 155ms is an acceptable threshold; if more speed is needed, move to a Web Worker in the optimization step.</p>\n<p><strong>Why limit to 3 dot levels (DEC-LUNAR-073)?</strong> A calendar UI gets cluttered easily if every kind of reminder has a different color. Three levels (blank / ordinary reminder / major festival) are enough for the user to scan quickly without reading each cell. The purple design of the FR-LUNAR-009 sub-brand supplies the primary and accent colors.</p>\n<p><strong>Why is <code>DayDetailPanel</code> a placeholder for FR-LUNAR-011 (DEC-LUNAR-072)?</strong> FR-LUNAR-011 (Hoang dao/Truc/28 mansions) depends on FR-LUNAR-002 and belongs to Phase 2. If FR-007 hard-wired its layout to FR-011, the absence of FR-011 would block FR-007. A placeholder design lets FR-007 ship in Phase 1 slice 3, and FR-011 only needs to fill the reserved slot when ready.</p>\n<p><strong>Why does the header show the lunar month of the grid's first day (DEC-LUNAR-074)?</strong> A solar month usually spans 2 lunar months. Showing the lunar month of the solar month's first day is the most intuitive convention, matching how Vietnamese people say \"the first lunar month of the At Ty year\" - the user immediately understands which lunar month they are in.</p>\n<p><strong>Why not use an existing calendar library?</strong> Every popular React calendar library computes by the solar calendar. Integrating the lunar date + can-chi + tiet khi + reminder dots into a third-party library's cell slot requires deep overrides that easily conflict with updates. Writing a simple 7-column <code>CalendarGrid</code> ourselves is easier and gives full control.</p>\n<p><strong>Why test fixtures for Jan/2025?</strong> 29/01/2025 is the first day of Tet At Ty - if the grid shows the wrong lunar date 1/1 for this day, the whole month is wrong. The fixture already exists in FR-LUNAR-003 (golden validation), reused for the component test.</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// apps/web/lib/calendarData.ts\n// FR-LUNAR-001 exports `convertSolar2Lunar` (returns a TUPLE [day, month, year, leap])\n// and `jdFromDate`. FR-LUNAR-002 exports `canChiDay(jdn)`, `canChiMonth(lunarMonth, lunarYear)`,\n// `canChiYear(lunarYear)`, `zodiacOf(chiIndex)` (CONTRACT: zodiacOf nhan chiIndex, khong phai lunarYear;\n// dung zodiacOf(canChiYear(lYear).chiIndex)), and `tietKhiAt(jdn, tz)`. There is NO\n// `getTietKhi` and NO can-chi/zodiac returned from convert; this module ASSEMBLES the DTO below.\nimport {\n  convertSolar2Lunar, jdFromDate, canChiDay, canChiMonth, canChiYear, zodiacOf, tietKhiAt,\n  VN_TZ,\n} from \"@cyberskill/amlich-core\";\n// Luu y: CONTRACT.md export VN_TZ = 7.0 (khong phai VN_TZ). Dung VN_TZ khi truyen tz.\n\nexport interface LunarDate {\n  day: number;\n  month: number;\n  year: number;\n  isLeap: boolean;     // mapped from tuple leap (0|1) -&gt; boolean at assembly time\n  canChiDay: string;   // e.g. \"Canh Ngo\" - from canChiDay(jdn).label\n  canChiMonth: string; // from canChiMonth(lunarMonth, lunarYear).label\n  canChiYear: string;  // from canChiYear(lunarYear).label\n  zodiac: string;      // e.g. \"Ran\" - from zodiacOf(canChiYear(lYear).chiIndex) - CONTRACT: zodiacOf(chiIndex: number)\n}\n\nexport interface DayCellData {\n  solarDay: number;\n  solarMonth: number;\n  solarYear: number;\n  lunarDate: LunarDate;\n  tietKhi: string | null;   // e.g. \"Lap Xuan\" or null\n  hasReminder: boolean;\n  isFestival: boolean;\n  isToday: boolean;\n  // Placeholder fields for FR-LUNAR-011:\n  hoangDao: boolean | null;       // null = not yet computed (Phase 2)\n  truc: string | null;\n  sao28: string | null;\n}\n\nexport interface MonthGridData {\n  year: number;\n  month: number;\n  cells: (DayCellData | null)[];  // length 35 or 42; null = padding cell\n  lunarMonthLabel: string;         // e.g. \"Thang Mot Am Lich\"\n}\n\n/**\n * Build all DayCell data for the given Gregorian year/month in one pass.\n * Calls amlich-core once per day, not per render.\n * reminderDates: Set of \"YYYY-MM-DD\" strings for days with active reminders.\n * festivalDates: Set of \"YYYY-MM-DD\" strings for major festival days.\n */\nexport function buildMonthGrid(\n  year: number,\n  month: number,\n  reminderDates: Set&lt;string&gt;,\n  festivalDates: Set&lt;string&gt;,\n  today: Date\n): MonthGridData;\n\n/**\n * Derive the sorted list of \"YYYY-MM-DD\" strings for all active reminder\n * occurrences in the given month, calling convertLunar2Solar from amlich-core.\n */\nexport function computeReminderDatesForMonth(\n  year: number,\n  month: number,\n  reminders: import(\"../lib/storage\").Reminder[]\n): Set&lt;string&gt;;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/components/DayCell.tsx\ninterface DayCellProps {\n  data: DayCellData | null;   // null renders an empty padding cell\n  onTap: (data: DayCellData) =&gt; void;\n}\nexport function DayCell({ data, onTap }: DayCellProps): JSX.Element;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/components/DayDetailPanel.tsx\ninterface DayDetailPanelProps {\n  data: DayCellData | null;   // null = closed\n  onClose: () =&gt; void;\n}\nexport function DayDetailPanel({ data, onClose }: DayDetailPanelProps): JSX.Element;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/components/CalendarGrid.tsx\ninterface CalendarGridProps {\n  year: number;\n  month: number;\n  reminders: import(\"../lib/storage\").Reminder[];\n  onMonthChange: (year: number, month: number) =&gt; void;\n}\nexport function CalendarGrid({ year, month, reminders, onMonthChange }: CalendarGridProps): JSX.Element;</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>The Jan/2025 month grid shows 29/01/2025 with the lunar date label \"1/1\" (first day of Tet At Ty), and the can-chi correct per the FR-LUNAR-003 fixtures.</li><li>The Mar/1985 grid shows the correct leap month 2 label in the header and on the day cells belonging to leap month 2 of 1985 (leap-month edge case).</li><li>The render time from calling <code>buildMonthGrid</code> to the DOM finishing paint is under 100ms measured with <code>performance.now()</code> in a unit test in the jsdom environment.</li><li>Moving to the next month by button or swipe, the new grid appears within 100ms (measured manually on an iPhone SE gen 3).</li><li>A day cell with an active reminder shows a primary color dot; a major festival cell (Ram of the first lunar month, Tet, Vu Lan, Trung Thu, etc.) shows an accent color dot; a cell with nothing shows blank - no more than 3 dot kinds total.</li><li>Today's cell (solarDay == today) has a distinct highlight style (a deep purple ring or equivalent from the FR-LUNAR-009 design token).</li><li>Tapping any day cell opens the <code>DayDetailPanel</code> showing the correct lunar date, the day/month/year can-chi, the tiet khi (if any), and the list of reminders on that day.</li><li>The <code>DayDetailPanel</code> has a close button; after closing the panel, the grid is not re-rendered (does not recompute <code>buildMonthGrid</code>).</li><li>A month starting on Wednesday (for example 01/01/2025 is a Wednesday) shows 3 empty padding cells at the start of the row; a 28-day month has no extra row.</li><li>No network request is sent during the calendar render (checked with mock fetch in the test).</li><li>The header shows the correct lunar month name of the first day in the grid (not the solar 1st if that cell is padding).</li><li>When no reminders have been created, the grid still shows all 28-31 day cells with the correct lunar date and can-chi.</li><li><code>buildMonthGrid</code> is called only once when <code>[year, month]</code> does not change, even if the component re-renders many times (checked with a mock counting the number of calls).</li><li>The tiet khi \"Lap Xuan\" shows correctly on the cell for 3/2/2025 (or the exact day from FR-LUNAR-002).</li><li>The grid works correctly in both modes: light (default) and dark if the FR-LUNAR-009 design system supports it.</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// apps/web/__tests__/calendarData.test.ts\nimport { buildMonthGrid, computeReminderDatesForMonth } from \"../lib/calendarData\";\n\ndescribe(\"buildMonthGrid\", () =&gt; {\n  test(\"Thang 1/2025: ngay 29 co ngay am 1/1/2025 Tet At Ty\", () =&gt; {\n    const grid = buildMonthGrid(2025, 1, new Set(), new Set(), new Date(\"2025-01-15\"));\n    const cell = grid.cells.find(c =&gt; c?.solarDay === 29 &amp;&amp; c.solarMonth === 1);\n    expect(cell).toBeTruthy();\n    expect(cell!.lunarDate.day).toBe(1);\n    expect(cell!.lunarDate.month).toBe(1);\n    expect(cell!.lunarDate.year).toBe(2025);\n    expect(cell!.lunarDate.isLeap).toBe(false);\n  });\n\n  test(\"Thang 1/2025: Mung 1 duong 29/01 la Mung 1 am, co chot le lon\", () =&gt; {\n    const festivalDates = new Set([\"2025-01-29\"]);\n    const grid = buildMonthGrid(2025, 1, new Set(), festivalDates, new Date(\"2025-01-15\"));\n    const cell = grid.cells.find(c =&gt; c?.solarDay === 29);\n    expect(cell!.isFestival).toBe(true);\n  });\n\n  test(\"Thang 3/1985: co thang nhuan 2 trong nhan cua o ngay thuoc thang nhuan\", () =&gt; {\n    const grid = buildMonthGrid(1985, 3, new Set(), new Set(), new Date(\"1985-03-01\"));\n    const leapCell = grid.cells.find(c =&gt; c?.lunarDate.isLeap === true);\n    expect(leapCell).toBeTruthy();\n    expect(leapCell!.lunarDate.month).toBe(2);\n  });\n\n  test(\"Padding: thang 1/2025 bat dau vao Thu Tu (index 3), co 3 o null dau\", () =&gt; {\n    const grid = buildMonthGrid(2025, 1, new Set(), new Set(), new Date(\"2025-01-15\"));\n    expect(grid.cells[0]).toBeNull();\n    expect(grid.cells[1]).toBeNull();\n    expect(grid.cells[2]).toBeNull();\n    expect(grid.cells[3]?.solarDay).toBe(1);\n  });\n\n  test(\"Hom nay duoc danh dau isToday = true\", () =&gt; {\n    const today = new Date(\"2025-01-15\");\n    const grid = buildMonthGrid(2025, 1, new Set(), new Set(), today);\n    const todayCell = grid.cells.find(c =&gt; c?.solarDay === 15 &amp;&amp; c.solarMonth === 1);\n    expect(todayCell!.isToday).toBe(true);\n  });\n\n  test(\"buildMonthGrid chi goi convertSolar2Lunar mot lan cho 31 ngay, khong goi tung o rieng le\", () =&gt; {\n    const convertSpy = jest.spyOn(require(\"@cyberskill/amlich-core\"), \"convertSolar2Lunar\");\n    buildMonthGrid(2025, 1, new Set(), new Set(), new Date());\n    // Nen goi dung 31 lan (1 lan/ngay), khong hon\n    expect(convertSpy.mock.calls.length).toBeLessThanOrEqual(31);\n    convertSpy.mockRestore();\n  });\n\n  test(\"NFR-Performance: buildMonthGrid hoan thanh trong 150ms (generous margin)\", () =&gt; {\n    const t0 = performance.now();\n    for (let i = 0; i &lt; 10; i++) {\n      buildMonthGrid(2025, 1, new Set(), new Set(), new Date());\n    }\n    const avg = (performance.now() - t0) / 10;\n    expect(avg).toBeLessThan(150);\n  });\n\n  test(\"Khong co network request trong buildMonthGrid\", () =&gt; {\n    const fetchSpy = jest.spyOn(global, \"fetch\");\n    buildMonthGrid(2025, 6, new Set(), new Set(), new Date());\n    expect(fetchSpy).not.toHaveBeenCalled();\n    fetchSpy.mockRestore();\n  });\n});\n\ndescribe(\"computeReminderDatesForMonth\", () =&gt; {\n  test(\"Reminder Ram 15 AL xuat hien dung ngay duong trong thang 1/2025\", () =&gt; {\n    const reminders = [{ type: \"RAM\", lunarDay: 15, lunarMonth: 1, enabled: true } as any];\n    const dates = computeReminderDatesForMonth(2025, 1, reminders);\n    // Ram thang Gieng 2025 = 12/02/2025\n    expect(dates.has(\"2025-02-12\")).toBe(false); // outside Jan\n    // Kiem lai voi thang 2\n    const datesF = computeReminderDatesForMonth(2025, 2, reminders);\n    expect(datesF.has(\"2025-02-12\")).toBe(true);\n  });\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The API contract in §3 is a full skeleton. The trickiest point to pin down:</p>\n<pre><code class=\"language-typescript\">// apps/web/lib/calendarData.ts\n//\n// Two correctness anchors ghim o day:\n// (1) convertSolar2Lunar tra TUPLE [day, month, year, leap] - phai destructure, KHONG spread\n//     thanh object (spread mang vao object cho ra {0:..,1:..}). can-chi/zodiac/tiet khi KHONG\n//     co trong tuple; phai goi rieng cac ham FR-LUNAR-002 voi JDN (canChiDay/tietKhiAt nhan JDN).\n// (2) startPadding tinh tu thu trong tuan cua ngay 1 duong PHAI on dinh theo Asia/Ho_Chi_Minh,\n//     khong dung Date#getDay() (lay theo timezone cua runtime; SSR/static-export chay UTC -&gt; lech).\nconst VI_DOW = new Intl.DateTimeFormat(\"en-US\", {\n  weekday: \"short\", timeZone: \"Asia/Ho_Chi_Minh\",\n});\nconst DOW_INDEX: Record&lt;string, number&gt; = { Sun: 0, Mon: 1, Tue: 2, Wed: 3, Thu: 4, Fri: 5, Sat: 6 };\n\nfunction startPaddingFor(year: number, month: number): number {\n  // month la 1-based; dung UTC noon de tranh DST/edge, doc weekday theo gio VN (offset co dinh +7).\n  const firstUtcNoon = new Date(Date.UTC(year, month - 1, 1, 12));\n  return DOW_INDEX[VI_DOW.format(firstUtcNoon)];\n}\n\nexport function buildMonthGrid(\n  year: number,\n  month: number,\n  reminderDates: Set&lt;string&gt;,\n  festivalDates: Set&lt;string&gt;,\n  today: Date\n): MonthGridData {\n  const daysInMonth = new Date(Date.UTC(year, month, 0)).getUTCDate();\n  const startPadding = startPaddingFor(year, month);\n  const cells: (DayCellData | null)[] = Array(startPadding).fill(null);\n\n  for (let d = 1; d &lt;= daysInMonth; d++) {\n    const jdn = jdFromDate(d, month, year);                    // JDN cho can-chi va tiet khi\n    const [lDay, lMonth, lYear, lLeap] = convertSolar2Lunar(d, month, year, VN_TZ); // TUPLE\n    const tk = tietKhiAt(jdn, VN_TZ);                    // tra { index, name, isTrungKhi }\n    const key = `${year}-${String(month).padStart(2, \"0\")}-${String(d).padStart(2, \"0\")}`;\n    cells.push({\n      solarDay: d, solarMonth: month, solarYear: year,\n      lunarDate: {\n        day: lDay, month: lMonth, year: lYear, isLeap: lLeap === 1,\n        canChiDay: canChiDay(jdn).label,\n        canChiMonth: canChiMonth(lMonth, lYear).label,\n        canChiYear: canChiYear(lYear).label,\n        zodiac: zodiacOf(canChiYear(lYear).chiIndex),  // CONTRACT: zodiacOf(chiIndex: number), khong phai lunarYear\n      },\n      // tietKhiAt tra tiet khi cua MOI ngay; chi hien nhan khi ngay do la ngay BAT DAU tiet\n      // (xem §11). Neu chi muon danh dau ngay bat dau, so sanh voi ngay truoc (d-1).\n      tietKhi: isTietKhiStart(jdn) ? tk.name : null,\n      hasReminder: reminderDates.has(key),\n      isFestival: festivalDates.has(key),\n      isToday: d === today.getDate() &amp;&amp; month === today.getMonth() + 1 &amp;&amp; year === today.getFullYear(),\n      hoangDao: null, truc: null, sao28: null  // Phase 2 placeholders\n    });\n  }\n  // pad to complete grid rows\n  while (cells.length % 7 !== 0) cells.push(null);\n  // lunarMonthLabel from first non-null cell\n  const firstCell = cells.find((c): c is DayCellData =&gt; c !== null)!;\n  return { year, month, cells, lunarMonthLabel: formatLunarMonth(firstCell.lunarDate) };\n}\n\n/** Ngay la diem bat dau mot tiet khi neu index tiet khi cua no khac index cua ngay truoc. */\nfunction isTietKhiStart(jdn: number): boolean {\n  return tietKhiAt(jdn, VN_TZ).index !== tietKhiAt(jdn - 1, VN_TZ).index;\n}</code></pre>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream: <code>FR-LUNAR-001</code> provides <code>convertSolar2Lunar</code> (the core engine, returns a tuple) and <code>jdFromDate</code>; <code>FR-LUNAR-002</code> provides <code>canChiDay/canChiMonth/canChiYear</code>, <code>zodiacOf</code>, and <code>tietKhiAt</code> (24 tiet khi - NOT <code>getTietKhi</code>); <code>FR-LUNAR-010</code> provides the app shell, routing, and the storage layer that <code>CalendarGrid</code> uses to read the list of active <code>Reminder</code>s.</p>\n<p>Downstream: <code>FR-LUNAR-014</code> (shareable cards) depends on the <code>DayCell</code> component and the <code>DayCellData</code> type from this FR to render the card design.</p>\n<p>Cross-cutting: <code>FR-LUNAR-009</code> (purple design system) provides the color tokens and typography for <code>DayCell</code> and <code>DayDetailPanel</code>; <code>FR-LUNAR-011</code> (Hoang dao/Truc/28 mansions) will fill the placeholder fields <code>hoangDao</code>, <code>truc</code>, <code>sao28</code> in <code>DayCellData</code> in Phase 2 when ready.</p>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"year\": 2025,\n  \"month\": 1,\n  \"lunarMonthLabel\": \"Thang Chap (12) - Giap Thin\",\n  \"cells\": [\n    null, null, null,\n    {\n      \"solarDay\": 1, \"solarMonth\": 1, \"solarYear\": 2025,\n      \"lunarDate\": { \"day\": 2, \"month\": 12, \"year\": 2024, \"isLeap\": false,\n                     \"canChiDay\": \"Binh Dan\", \"canChiMonth\": \"Binh Ti\", \"canChiYear\": \"Giap Thin\",\n                     \"zodiac\": \"Rong\" },\n      \"tietKhi\": null,\n      \"hasReminder\": false,\n      \"isFestival\": false,\n      \"isToday\": false,\n      \"hoangDao\": null, \"truc\": null, \"sao28\": null\n    },\n    {\n      \"solarDay\": 29, \"solarMonth\": 1, \"solarYear\": 2025,\n      \"lunarDate\": { \"day\": 1, \"month\": 1, \"year\": 2025, \"isLeap\": false,\n                     \"canChiDay\": \"Giap Ty\", \"canChiMonth\": \"Binh Dan\", \"canChiYear\": \"At Ty\",\n                     \"zodiac\": \"Ran\" },\n      \"tietKhi\": null,\n      \"hasReminder\": true,\n      \"isFestival\": true,\n      \"isToday\": false,\n      \"hoangDao\": null, \"truc\": null, \"sao28\": null\n    }\n  ]\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"comment\": \"DayDetailPanel hien thi khi cham vao ngay 29/01/2025\",\n  \"solarFull\": \"Thu Tu, 29 thang 1 nam 2025\",\n  \"lunarFull\": \"Mung 1 thang Gieng nam At Ty\",\n  \"canChiDay\": \"Giap Ty\",\n  \"canChiMonth\": \"Binh Dan\",\n  \"canChiYear\": \"At Ty\",\n  \"zodiac\": \"Ran (Ty)\",\n  \"tietKhi\": null,\n  \"reminders\": [\n    { \"title\": \"Mung Mot hang thang\", \"notifyTime\": \"07:00\", \"leadTimes\": [0, 1] }\n  ],\n  \"hoangDao\": \"(Phase 2 - chua co)\",\n  \"truc\": \"(Phase 2 - chua co)\"\n}</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li>How to compute the whole month in one pass: <code>buildMonthGrid</code> calls <code>convertSolar2Lunar</code> for each day.</li><li>Placeholder for FR-LUNAR-011: the <code>hoangDao</code>, <code>truc</code>, <code>sao28</code> fields in <code>DayCellData</code> stay <code>null</code> until Phase 2.</li></ul>\n<p>Still deferred:</p>\n<ul><li>Web Worker optimization: if the real benchmark shows that <code>useMemo</code> still blocks the main thread on old devices, move <code>buildMonthGrid</code> to a <code>Web Worker</code> and use <code>useTransition</code> - deferred per Caveats (PRD §14 Phase 2 performance tuning).</li><li>Optimal row count: some months need 6 rows (42 cells). A scrolling layout could be used if 6 rows are too cramped on a small screen - deferred, decided at the real UI review.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>convertSolar2Lunar</code> returns the wrong leap month</td><td>Unit test fixtures for Mar/1985</td><td>Grid shows the wrong lunar date</td><td>Fix the bug in FR-LUNAR-001 before deploy</td></tr><tr><td><code>buildMonthGrid</code> calls the network</td><td>Jest mock fetch spy</td><td>Test fails</td><td>Remove any fetch call in calendarData.ts</td></tr><tr><td>Render &gt; 100ms on a mid-range device</td><td><code>performance.now()</code> in the test</td><td>NFR-Performance violated</td><td>Move to a Web Worker or optimize the loop</td></tr><tr><td>Wrong padding cells (wrong start-of-week day)</td><td>Test for Jan/2025 starting on Wednesday</td><td>Grid rows misaligned</td><td>Use <code>startPaddingFor()</code> (Intl + timeZone Asia/Ho_Chi_Minh on UTC noon), NOT <code>Date#getDay()</code></td></tr><tr><td><code>useMemo</code> not working, recomputing every render</td><td>Mock counting the number of calls</td><td>Poor performance</td><td>Add key stability, check deps</td></tr><tr><td>Leap February in a leap solar year (29 days)</td><td>Test for Feb/2024</td><td>Grid missing the 29th</td><td>Use <code>new Date(year, month, 0).getDate()</code> instead of hardcoding 28/30/31</td></tr><tr><td>Wrong dot level (festival vs reminder confused)</td><td>Test fixture with festivalDates + reminderDates</td><td>UI causes confusion</td><td>Separate the two Sets clearly, prioritize <code>isFestival</code> when both are true</td></tr><tr><td>DayDetailPanel re-renders the grid</td><td>React Profiler</td><td>Poor performance</td><td>Separate the panel state from the grid component</td></tr><tr><td>Swipe conflicts with vertical scroll</td><td>Manual device test</td><td>Hard-to-use UX</td><td>Use a swipe-angle threshold (angle &gt; 45 deg = scroll)</td></tr><tr><td>FR-LUNAR-009 color tokens not ready</td><td>Import build error</td><td>Grid has no purple style</td><td>Use a fallback CSS variable default in DayCell</td></tr><tr><td>Day 1/1/1900 or 31/12/2199 (edge year)</td><td>Unit test boundary</td><td>Crash or wrong result</td><td>Clamp year to [1900, 2199] and show a warning</td></tr><tr><td>Month with 0 reminders, festivalDates empty</td><td>Test for a month with no occasion</td><td>Grid shows correctly but empty</td><td>Always pass an empty Set instead of undefined</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li><code>buildMonthGrid</code> must use <code>tz = 7.0</code> (not <code>0</code> or the browser timezone) when calling <code>convertSolar2Lunar</code>, because the core engine computes by the 105 degrees E meridian - this is the most common source of error when porting.</li><li>The start-padding must NOT use <code>new Date(year, month - 1, 1).getDay()</code>: it reads the weekday by the runtime timezone, so SSR/static-export (running UTC) gives a result off from Asia/Ho_Chi_Minh. §6 already ships <code>startPaddingFor()</code> using <code>Intl.DateTimeFormat({ timeZone: \"Asia/Ho_Chi_Minh\" })</code> read on <code>Date.UTC(year, month-1, 1, 12)</code> (UTC noon to avoid the DST edge) - this is standard code, not just a note. Similarly, <code>daysInMonth</code> uses <code>Date.UTC(...).getUTCDate()</code> instead of the local variant.</li><li>The placeholder fields <code>hoangDao</code>, <code>truc</code>, <code>sao28</code> in <code>DayCellData</code> are of type <code>null</code> instead of <code>undefined</code> so that JSON serialization does not drop them - important if the grid data is passed via postMessage to a Worker.</li><li>The <code>DayDetailPanel</code> must be a portal (React <code>createPortal</code>) attached to <code>document.body</code> to avoid being clipped by the grid container's <code>overflow: hidden</code>.</li><li>The tiet khi from FR-LUNAR-002 is <code>tietKhiAt(jdn, tz)</code> (there is NO <code>getTietKhi</code>), and it returns the tiet khi for EVERY day (always has a <code>name</code>), not just the start day. The grid should show the tiet label only on the START DAY of the tiet; §6 uses <code>isTietKhiStart(jdn)</code> comparing <code>tietKhiAt(jdn).index</code> with <code>tietKhiAt(jdn-1).index</code> (different = start day) before assigning <code>tietKhi</code>, leaving the rest <code>null</code>. AC #14 (Lap Xuan on 3/2/2025) tests exactly this behavior.</li><li>When changing months by swipe, debounce is needed to avoid changing 2 months at once if the user swipes quickly.</li><li>The <code>computeReminderDatesForMonth</code> test must cover the case of a RAM reminder (Ram 15 lunar) with a solar month that does not fully match the lunar month - part of a solar month 1 reminder can fall into lunar month 12 of the previous year.</li></ul>\n<p><em>End of FR-LUNAR-007.</em></p>";

window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LUNAR-011"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>The <code>dayquality</code> module MUST fully compute the quality of a day according to Vietnamese folk feng shui from the day's can-chi and the solar term, without depending on the network. No network transactions are permitted. The entire contract is defined by the clauses below.</p>\n<ol><li>MUST define the type <code>DayQuality</code> including: <code>date</code> (ISO date string), <code>canChiNgay</code> (string, e.g. \"Giap Ty\"), <code>diaChiNgay</code> (DiaChi enum 0..11), <code>hoangDao</code> (boolean), <code>thanTrucNhat</code> (ThanTrucNhat enum), <code>truc</code> (Truc enum, 12 values), <code>sao28</code> (Sao28 enum, 28 values), <code>isHoangDao</code> (boolean alias for <code>hoangDao</code>), <code>gioHoangDao</code> (GioHoangDao[12]), <code>label</code> (\"Hoang dao\" | \"Hac dao\"), and <code>disclaimer</code> (\"Tham khao phong thuy dan gian\") (DEC-LUNAR-111).</li><li>MUST compute the <code>daily officer deity</code> (one of the 12 deities) for the day by looking up <code>THAN_TRUC_NHAT_TABLE[diaChiNgay][thangAmIndex]</code>, where the 6 benevolent deities are Thanh Long, Minh Duong, Kim Quy, Bao Quang, Ngoc Duong, Tu Menh -&gt; Hoang dao; the 6 malevolent deities are Bach Ho, Thien Hinh, Chu Tuoc, Thien Lao, Nguyen Vu, Cau Tran -&gt; Hac dao (DEC-LUNAR-111, PRD §8).</li><li>MUST compute <code>Truc</code> (one of the 12 Truc: Kien, Tru, Man, Binh, Dinh, Chap, Pha, Nguy, Thanh, Thu, Khai, Be) using the formula <code>(diaChiSoNgay - diaChiSoDauTiet + 12) mod 12</code>, where <code>diaChiSoDauTiet = tietKhiStartDiaChi(jdn, tz?)</code> (CONTRACT.md: <code>export function tietKhiStartDiaChi(jdn: number, tz?: number): number</code>) is the earthly branch (0..11) of the first day of the current solar term; <code>diaChiSoNgay = canChiDay(jdn).chiIndex</code> (DEC-LUNAR-112/113, PRD §8).</li><li>MUST compute the <code>28 sao</code> (Giac, Cang, De, Phong, Tam, Tinh, Vu, Quy, Luu, Tinh, Truong, I, Chan, Tuy, Bi, Tat, Truy, Shen, Jing, Kui, Lou, Wei, Mao, Bi, Zi, Shen - per the Vietnamized names) using the formula <code>(jdn - BASE_JDN_GIAC) mod 28</code>; <code>BASE_JDN_GIAC</code> is determined and checked against a known-day fixture (DEC-LUNAR-114, PRD §8).</li><li>MUST compute <code>gioHoangDao</code> for the 12 gio of the day by looking up <code>GIO_HOANG_DAO_TABLE[diaChiNgay]</code>, returning an array of 12 <code>GioInfo { canh: string, tuGio: string, denGio: string, isHoang: boolean }</code> (DEC-LUNAR-115, PRD §8, FR-E03).</li><li>MUST export the main function <code>getDayQuality(solarDate: Date): DayQuality</code>, which takes <code>canChiNgay</code> and the JDN from amlich-core (FR-LUNAR-002), computes all fields, and sets <code>disclaimer = \"Tham khao phong thuy dan gian\"</code> at the root level of the result (DEC-LUNAR-110).</li><li>MUST obtain the day's can-chi and <code>diaChiNgay</code> by calling <code>canChiDay(jdn)</code> of FR-LUNAR-002 (with <code>jdn</code> from <code>jdFromDate</code> of FR-LUNAR-001), using <code>canChiDay(jdn).chiIndex = (jdn + 1) mod 12</code> as <code>diaChiSoNgay</code> and <code>canChiDay(jdn).label</code> as <code>canChiNgay</code>; MUST NOT derive the earthly branch by <code>(jdn + 9) mod 60</code> then <code>mod 12</code> (which yields <code>(jdn + 9) mod 12</code>, off by 8 relative to core) and MUST NOT import a third-party library to recompute it (DEC-LUNAR-112). MUST compute <code>diaChiSoDauTiet</code> (the earthly branch of the first day of the current solar term) by calling <code>tietKhiStartDiaChi(jdn, tz?)</code> of FR-LUNAR-002 (CONTRACT.md P2/P3 surface); the full Truc formula is <code>(canChiDay(jdn).chiIndex - tietKhiStartDiaChi(jdn) + 12) % 12</code> for the index into <code>TRUC_NAMES</code> (DEC-LUNAR-113). Because <code>THAN_TRUC_NHAT_TABLE</code>, Truc, and the auspicious hours are all keyed on the day's earthly branch, a wrong earthly branch corrupts the entire day-quality result and diverges from the can-chi displayed in the calendar (FR-LUNAR-007 via FR-LUNAR-002).</li><li>MUST compute <code>thangAmIndex</code> (0..11) from the lunar month returned by FR-LUNAR-001, to index into <code>THAN_TRUC_NHAT_TABLE</code> - the index is (thangAm - 1) mod 12 (DEC-LUNAR-111).</li><li>MUST ensure the entire computation is a pure function and deterministic: the same <code>solarDate</code> always returns the same <code>DayQuality</code>, with no side effects (DEC-LUNAR-110).</li><li>MUST NOT call the network at any step; MUST NOT cache results to IndexedDB or localStorage (those steps are the responsibility of FR-LUNAR-010); this module is pure compute only (DEC-LUNAR-110).</li><li>MUST export all enums and types from <code>packages/amlich-core/src/index.ts</code> so that other FRs (FR-LUNAR-007, FR-LUNAR-012, FR-LUNAR-013) can import them directly.</li><li>SHOULD add the fields <code>trucSuitableFor: string[]</code> and <code>trucAvoidFor: string[]</code> in <code>TrucInfo</code> describing the kinds of activities that suit or conflict with each Truc - this is feng shui data, labeled \"reference\" (DEC-LUNAR-111).</li><li>SHOULD add the fields <code>sao28Rating: \"tot\" | \"xau\" | \"binh\"</code> and <code>sao28Notes: string</code> for each sao (DEC-LUNAR-111).</li><li>MAY export the function <code>getMonthDayQualities(year: number, month: number): readonly DayQuality[]</code> to quickly compute the whole month - used by FR-LUNAR-012 to list Hoang dao days within a range (DEC-LUNAR-110).</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why compute entirely offline from a lookup table (DEC-LUNAR-110)?</strong> Hoang dao/Hac dao, Truc, and the 28 sao are a fixed folk feng shui system - not the result of ongoing astronomy. The PRD §8 itself states plainly that this is a \"fixed lookup table by the day's earthly branch x month.\" Calling the network to look up this result would both cost money and violate NFR-Offline while adding no accuracy.</p>\n<p><strong>Why hardcode the <code>THAN_TRUC_NHAT_TABLE</code> instead of computing a formula (DEC-LUNAR-111)?</strong> The daily officer deities have no astronomical formula - they are feng shui conventions passed down over centuries. Using a 12x12 table is the most careful approach: the data source can be cross-checked against several classical feng shui books and VN day-picking sites, and any later correction only needs a single line edited in the table instead of debugging logic.</p>\n<p><strong>Why call core's <code>canChiDay</code> directly instead of deriving it from the JDN (DEC-LUNAR-112)?</strong> FR-LUNAR-002 already defines <code>canChiDay(jdn)</code> with <code>can = (jdn + 9) mod 10</code> and <code>chi = (jdn + 1) mod 12</code>; this is the single source of truth for the day's can-chi. One temptation is to \"shorten\" this to <code>(jdn + 9) mod 60</code> then <code>mod 12</code> to get the earthly branch - BUT <code>(jdn + 9) mod 12</code> is off by exactly 8 from core's <code>(jdn + 1) mod 12</code>, so the earthly branch would be wrong and every table lookup keyed on the earthly branch (daily officer deity, Truc, auspicious hours) would be wrong. More importantly, the FR-LUNAR-007 calendar grid displays <code>canChiDay</code> from FR-LUNAR-002 itself; if day quality used a different earthly branch, the good-day screen would contradict the can-chi shown in the calendar cell. Therefore <code>getDayQuality</code> MUST call <code>canChiDay(jdn)</code> and read <code>chiIndex</code>/<code>label</code> from it, not recompute it.</p>\n<p><strong>Why compute Truc from the solar term + the day's earthly branch (DEC-LUNAR-113)?</strong> PRD §8 states plainly to \"look it up by solar term + the day's earthly branch; each Truc suits or conflicts with certain activities.\" This is the standard method in Vietnamese feng shui - at each new term Truc restarts from Kien. This method only needs the earthly branch of the first day of the term (already available from FR-LUNAR-002) and the current day's earthly branch - no 365-row lookup table.</p>\n<p><strong>Why determine <code>BASE_JDN_GIAC</code> with a fixture instead of from a book (DEC-LUNAR-114)?</strong> Several sources give different values for the 28-sao cycle because this system has multiple ways of being computed. The safest approach is: choose a day whose sao is known for certain (cross-checked against several reputable VN day-picking sites), compute BASE_JDN_GIAC backward from it, and lock that value into the test fixture. Each time BUILD reruns the tests, if anyone changes BASE_JDN_GIAC the test fails - a mechanism to catch unintended changes.</p>\n<p><strong>Why are the auspicious hours also a lookup table (DEC-LUNAR-115)?</strong> The 12 gio split into 6 auspicious / 6 inauspicious by the day's earthly branch is a feng shui convention - each day's earthly branch (Ty, Suu, Dan...) maps to a fixed \"peak hour.\" This is a 12x12 table. There is no astronomical formula here at all; the lookup table is the most accurate representation.</p>\n<p><strong>Why is a <code>disclaimer</code> field needed at the root level (DEC-LUNAR-111)?</strong> PRD §8 and the Caveats state plainly to \"label it as a folk feng shui reference\" and to \"avoid absolute assertions.\" If the disclaimer were optional or in a nested UI, it would be easy to drop during rendering. When the disclaimer sits inside the DayQuality result itself, every component that renders this data has the note available right next to the data.</p>\n<p><strong>Why export <code>getMonthDayQualities</code> (DEC-LUNAR-110)?</strong> FR-LUNAR-012 needs to list Hoang dao days within a range (for example July 2026). If FR-012 called <code>getDayQuality</code> day by day in a loop, although more efficient than calling the network it is still ~30 computations. The <code>getMonthDayQualities</code> function gathers this in one place, allows future optimization if needed (e.g. batch JDN lookup), and makes the FR-012 contract simpler.</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// packages/amlich-core/src/dayquality.ts\n\nexport const THAN_TRUC_NHAT = [\n  \"Thanh Long\", \"Minh Duong\", \"Kim Quy\", \"Bao Quang\", \"Ngoc Duong\", \"Tu Menh\",\n  \"Bach Ho\",    \"Thien Hinh\", \"Chu Tuoc\", \"Thien Lao\", \"Nguyen Vu\", \"Cau Tran\"\n] as const;\nexport type ThanTrucNhat = typeof THAN_TRUC_NHAT[number];\n\nexport const HOANG_DAO_THAN = new Set&lt;ThanTrucNhat&gt;([\n  \"Thanh Long\", \"Minh Duong\", \"Kim Quy\", \"Bao Quang\", \"Ngoc Duong\", \"Tu Menh\"\n]);\n\nexport const TRUC_NAMES = [\n  \"Kien\", \"Tru\", \"Man\", \"Binh\", \"Dinh\", \"Chap\", \"Pha\", \"Nguy\", \"Thanh\", \"Thu\", \"Khai\", \"Be\"\n] as const;\nexport type Truc = typeof TRUC_NAMES[number];\n\nexport const SAO_28 = [\n  \"Giac\", \"Cang\", \"De\", \"Phong\", \"Tam\", \"Tinh\", \"Vo\",\n  \"Quy\",  \"Liu\",  \"Tinh28\", \"Chang\", \"I\",   \"Chan\",\n  \"Goc\",  \"Lau\",  \"Vi28\",  \"Mao28\", \"Tat\",  \"Truy\",\n  \"Cam\",  \"Giai\", \"Cu\",    \"Phap\",  \"Khuy\",  \"Luu28\", \"Nhat\", \"Pi\", \"Chan28\"\n] as const; // 28 entries; use Vietnamese/pinyin names per editorial decision\nexport type Sao28 = typeof SAO_28[number];\n\nexport type DiaChi =\n  \"Ty\" | \"Suu\" | \"Dan\" | \"Meo\" | \"Thin\" | \"Ti\" | \"Ngo\" | \"Mui\" | \"Than\" | \"Dau\" | \"Tuat\" | \"Hoi\";\n\nexport interface GioInfo {\n  canh: string;        // e.g. \"Canh Ty (23:00-01:00)\"\n  tuGio: string;       // e.g. \"23:00\"\n  denGio: string;      // e.g. \"01:00\"\n  isHoang: boolean;\n}\n\nexport interface TrucInfo {\n  name: Truc;\n  suitableFor: string[];  // e.g. [\"ky hop dong\", \"xay dung\"] - \"tham khao\"\n  avoidFor: string[];\n}\n\nexport interface Sao28Info {\n  name: Sao28;\n  rating: \"tot\" | \"xau\" | \"binh\";\n  notes: string;  // brief folk note\n}\n\nexport interface DayQuality {\n  date: string;                   // ISO date \"YYYY-MM-DD\"\n  canChiNgay: string;             // e.g. \"Giap Ty\"\n  diaChiNgay: DiaChi;\n  thanTrucNhat: ThanTrucNhat;\n  hoangDao: boolean;              // true = Hoang dao (6 than thien)\n  isHoangDao: boolean;            // alias for hoangDao\n  label: \"Hoang dao\" | \"Hac dao\";\n  truc: TrucInfo;\n  sao28: Sao28Info;\n  gioHoangDao: GioInfo[];         // 12 canh, isHoang flags set\n  disclaimer: \"Tham khao phong thuy dan gian\";\n}\n\n// Main public API\n// getDayQuality PHẢI lấy can-chi/địa chi và địa chi đầu tiết từ FR-LUNAR-002:\n//   import { canChiDay, tietKhiStartDiaChi } from \"@cyberskill/amlich-core\";\n//   const cc = canChiDay(jdn);                         // can=(jdn+9)%10, chi=(jdn+1)%12 (DEC-LUNAR-112)\n//   const diaChiIndex = cc.chiIndex;                   // KHONG dung (jdn+9)%60 % 12 (lech 8)\n//   const canChiNgay  = cc.label;\n//   const diaChiDauTiet = tietKhiStartDiaChi(jdn);    // CONTRACT.md: number (0..11) (DEC-LUNAR-113)\n//   const trucIndex = (diaChiIndex - diaChiDauTiet + 12) % 12; // index vao TRUC_NAMES\nexport function getDayQuality(solarDate: Date): DayQuality;\nexport function getMonthDayQualities(year: number, month: number): readonly DayQuality[];</code></pre>\n<pre><code class=\"language-typescript\">// packages/amlich-core/src/dayquality-tables.ts  (excerpt - full table is 12 rows × 12 cols)\n\n// THAN_TRUC_NHAT_TABLE[diaChiIndex][thangAmIndex] -&gt; ThanTrucNhat\n// diaChiIndex: Ty=0, Suu=1, Dan=2, ..., Hoi=11\n// thangAmIndex: thang 1=0, thang 2=1, ..., thang 12=11\nexport const THAN_TRUC_NHAT_TABLE: readonly ThanTrucNhat[][] = [\n  // Ty: thang 1..12\n  [\"Thanh Long\",\"Bach Ho\",\"Minh Duong\",\"Thien Hinh\",\"Kim Quy\",\"Chu Tuoc\",\"Bao Quang\",\"Thien Lao\",\"Ngoc Duong\",\"Nguyen Vu\",\"Tu Menh\",\"Cau Tran\"],\n  // ... 11 more rows for Suu..Hoi\n] as const;\n\n// BASE_JDN_GIAC: Julian Day Number of a known \"Giac\" day (locked by fixture)\n// Cross-checked against multiple VN folk-calendar references; fixture in dayquality-fixtures.json\nexport const BASE_JDN_GIAC = 2459901; // 2022-12-01 verified as sao Giac - MUST match fixture\n\n// GIO_HOANG_DAO_TABLE[diaChiIndex] -&gt; 12 GioInfo entries (6 Hoang, 6 Hac)\n// e.g. for ngay Ty (index 0): canh Ty, Dan, Ngo, Than, Tuat, Hoi la Hoang\nexport const GIO_HOANG_DAO_TABLE: readonly GioInfo[][] = [ /* 12 × 12 table */ ] as const;</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>getDayQuality(new Date(\"2025-01-29\"))</code> (Tet 2025, day 1/1 of Year At Ty) returns a <code>canChiNgay</code> matching the fixture, and <code>hoangDao</code> and <code>thanTrucNhat</code> matching the result of a reputable VN day-picking site for that day.</li><li><code>getDayQuality</code> for any day returns exactly 12 <code>gioHoangDao</code>, of which exactly 6 have <code>isHoang: true</code> and 6 have <code>isHoang: false</code>.</li><li><code>getDayQuality</code> returns <code>disclaimer === \"Tham khao phong thuy dan gian\"</code> for all days.</li><li><code>truc.name</code> has a valid value within <code>TRUC_NAMES</code> for every day in January 2025 (31 days, 31 different results across the cycle of 12).</li><li><code>sao28</code> has a valid value within <code>SAO_28</code>; a run of 28 consecutive days starting from a day with confirmed sao \"Giac\" (fixture) yields all 28 sao in the correct order.</li><li><code>getMonthDayQualities(2025, 1)</code> returns exactly 31 results, each with the correct <code>date</code> and <code>hoangDao</code> matching 31 individual calls to <code>getDayQuality</code>.</li><li>The function is pure: calling <code>getDayQuality</code> 100 times with the same <code>solarDate</code> always yields the same result.</li><li><code>THAN_TRUC_NHAT_TABLE</code> has exactly 12 rows, each with 12 elements, 144 elements total; each element is a valid value in <code>THAN_TRUC_NHAT</code>.</li><li>The <code>isHoangDao</code> result always equals <code>hoangDao</code> (consistent alias).</li><li><code>getDayQuality</code> does not call <code>fetch</code>, <code>XMLHttpRequest</code>, or any network API (verified with a mock in the test).</li><li><code>getMonthDayQualities(2025, 1)</code> runs in &lt; 50ms (NFR-Performance).</li><li>All enums and types are re-exported from <code>packages/amlich-core/src/index.ts</code>.</li><li><code>truc.suitableFor</code> and <code>truc.avoidFor</code> are non-empty string arrays for all 12 Truc.</li><li><code>sao28.rating</code> is one of the three values \"tot\"/\"xau\"/\"binh\" for all 28 sao.</li><li>The test fixture <code>dayquality-fixtures.json</code> has at least 20 checkable days, each with all fields: solarDate, expectedThanTrucNhat, expectedIsHoangDao, expectedTruc, expectedSao28.</li><li>The <code>diaChiNgay</code> of <code>getDayQuality</code> MUST equal the earthly branch of <code>canChiDay(jdn)</code> (FR-LUNAR-002) for every day across a multi-day scan (&gt;= 60 consecutive days): for each day, the <code>DiaChi</code> index of <code>q.diaChiNgay</code> === <code>canChiDay(jdFromDate(d,m,y)).chiIndex</code> and <code>q.canChiNgay</code> === <code>canChiDay(...).label</code>. (Catches the earthly-branch off-by-8 caused by <code>(jdn+9)%60</code> - DEC-LUNAR-112.)</li><li>For the Tet 2025 fixture (2025-01-29), the <code>diaChiNgay</code> of <code>getDayQuality</code> matches the earthly branch of <code>canChiDay</code> for that day (MUST NOT be off); this is the same value the FR-LUNAR-007 calendar cell displays.</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// packages/amlich-core/test/dayquality.test.ts\nimport { describe, test, expect, vi } from \"vitest\";\nimport { getDayQuality, getMonthDayQualities, THAN_TRUC_NHAT_TABLE, SAO_28, TRUC_NAMES } from \"../src/index\";\nimport fixtures from \"./fixtures/dayquality-fixtures.json\";\n\ndescribe(\"getDayQuality - fixtures\", () =&gt; {\n  for (const fix of fixtures) {\n    test(`${fix.solarDate} ket qua khop fixture`, () =&gt; {\n      const q = getDayQuality(new Date(fix.solarDate));\n      expect(q.thanTrucNhat).toBe(fix.expectedThanTrucNhat);\n      expect(q.isHoangDao).toBe(fix.expectedIsHoangDao);\n      expect(q.truc.name).toBe(fix.expectedTruc);\n      expect(q.sao28.name).toBe(fix.expectedSao28);\n      expect(q.disclaimer).toBe(\"Tham khao phong thuy dan gian\");\n      expect(q.hoangDao).toBe(q.isHoangDao);       // alias nhat quan\n    });\n  }\n});\n\ndescribe(\"getDayQuality - gio Hoang dao\", () =&gt; {\n  test(\"moi ngay co dung 6 gio Hoang va 6 gio Hac\", () =&gt; {\n    const q = getDayQuality(new Date(\"2025-01-29\"));\n    expect(q.gioHoangDao).toHaveLength(12);\n    const hoang = q.gioHoangDao.filter(g =&gt; g.isHoang);\n    const hac   = q.gioHoangDao.filter(g =&gt; !g.isHoang);\n    expect(hoang).toHaveLength(6);\n    expect(hac).toHaveLength(6);\n  });\n});\n\ndescribe(\"getDayQuality - 28 sao cycle\", () =&gt; {\n  test(\"chuoi 28 ngay tu ngay Giac cho ra du 28 sao theo thu tu\", () =&gt; {\n    // find a date known to be sao Giac from fixture\n    const giac = fixtures.find(f =&gt; f.expectedSao28 === \"Giac\")!;\n    const start = new Date(giac.solarDate);\n    const cycle = Array.from({ length: 28 }, (_, i) =&gt; {\n      const d = new Date(start);\n      d.setDate(d.getDate() + i);\n      return getDayQuality(d).sao28.name;\n    });\n    expect(cycle).toEqual([...SAO_28]);\n  });\n});\n\ndescribe(\"getDayQuality - Truc\", () =&gt; {\n  test(\"Truc trong thang 1/2025 la gia tri hop le lien tuc trong TRUC_NAMES\", () =&gt; {\n    const results = getMonthDayQualities(2025, 1);\n    for (const q of results) {\n      expect(TRUC_NAMES).toContain(q.truc.name);\n    }\n  });\n});\n\ndescribe(\"getMonthDayQualities\", () =&gt; {\n  test(\"tra ve 31 ket qua cho thang 1/2025\", () =&gt; {\n    const results = getMonthDayQualities(2025, 1);\n    expect(results).toHaveLength(31);\n  });\n\n  test(\"nhat quan voi getDayQuality tung ngay\", () =&gt; {\n    const results = getMonthDayQualities(2025, 1);\n    for (const q of results) {\n      const single = getDayQuality(new Date(q.date));\n      expect(q.hoangDao).toBe(single.hoangDao);\n      expect(q.sao28.name).toBe(single.sao28.name);\n    }\n  });\n\n  test(\"chay trong &lt; 50ms\", () =&gt; {\n    const t0 = performance.now();\n    getMonthDayQualities(2025, 1);\n    expect(performance.now() - t0).toBeLessThan(50);\n  });\n});\n\ndescribe(\"getDayQuality - pure function\", () =&gt; {\n  test(\"goi 100 lan cung solarDate luon cho cung ket qua\", () =&gt; {\n    const date = new Date(\"2025-01-29\");\n    const first = getDayQuality(date);\n    for (let i = 0; i &lt; 99; i++) {\n      const q = getDayQuality(new Date(\"2025-01-29\"));\n      expect(q.hoangDao).toBe(first.hoangDao);\n      expect(q.truc.name).toBe(first.truc.name);\n    }\n  });\n});\n\ndescribe(\"getDayQuality - khong goi network\", () =&gt; {\n  test(\"khong fetch\", () =&gt; {\n    const fetchSpy = vi.spyOn(globalThis, \"fetch\");\n    getDayQuality(new Date(\"2025-01-29\"));\n    expect(fetchSpy).not.toHaveBeenCalled();\n  });\n});\n\ndescribe(\"THAN_TRUC_NHAT_TABLE structure\", () =&gt; {\n  test(\"12 hang, 12 cot, tat ca gia tri hop le\", () =&gt; {\n    expect(THAN_TRUC_NHAT_TABLE).toHaveLength(12);\n    for (const row of THAN_TRUC_NHAT_TABLE) {\n      expect(row).toHaveLength(12);\n    }\n  });\n});\n\n// AC #16/#17 - dia chi PHAI nhat quan voi FR-LUNAR-002 canChiDay (DEC-LUNAR-112).\n// Bat loi lay dia chi bang (jdn+9)%60 % 12 (lech 8 so voi core).\nimport { canChiDay, jdFromDate } from \"@cyberskill/amlich-core\";\n\n// thu tu DiaChi dung trong DayQuality.diaChiNgay (khop CHI index cua core)\nconst DIA_CHI_ORDER = [\n  \"Ty\", \"Suu\", \"Dan\", \"Meo\", \"Thin\", \"Ti\", \"Ngo\", \"Mui\", \"Than\", \"Dau\", \"Tuat\", \"Hoi\",\n] as const;\n\ndescribe(\"getDayQuality - dia chi nhat quan voi canChiDay (FR-002)\", () =&gt; {\n  test(\"label can-chi khop core cho fixture Tet 2025\", () =&gt; {\n    const jdn = jdFromDate(29, 1, 2025);\n    const q = getDayQuality(new Date(\"2025-01-29\"));\n    // canChiNgay PHAI bang label cua core (pin ca can lan dia chi)\n    expect(q.canChiNgay).toBe(canChiDay(jdn).label);\n    expect(DIA_CHI_ORDER.indexOf(q.diaChiNgay)).toBe(canChiDay(jdn).chiIndex);\n  });\n\n  test(\"dia chi khop core qua quet 60 ngay lien tiep\", () =&gt; {\n    const start = new Date(\"2025-01-01\");\n    for (let i = 0; i &lt; 60; i++) {\n      const d = new Date(start);\n      d.setDate(d.getDate() + i);\n      const jdn = jdFromDate(d.getDate(), d.getMonth() + 1, d.getFullYear());\n      const q = getDayQuality(d);\n      expect(DIA_CHI_ORDER.indexOf(q.diaChiNgay)).toBe(canChiDay(jdn).chiIndex);\n      expect(q.canChiNgay).toBe(canChiDay(jdn).label);\n    }\n  });\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The API contract in §3 is the main skeleton. The key point to state clearly: the <code>getDayQuality</code> function calls <code>jdFromDate</code> from FR-LUNAR-001, then calls <code>canChiDay(jdn)</code> of FR-LUNAR-002 and takes <code>const cc = canChiDay(jdn); const diaChiIndex = cc.chiIndex; // = (jdn + 1) % 12; canChiNgay = cc.label</code>. NEVER derive the earthly branch by <code>(jdn + 9) % 60</code> then <code>% 12</code> (which gives <code>(jdn + 9) % 12</code>, off by 8 relative to core). Index into <code>THAN_TRUC_NHAT_TABLE[diaChiIndex][thangAmIndex]</code>. Truc is computed via <code>tietKhiStartDiaChiIndex</code> taken from the <code>tietKhiStartDiaChi(jdn, tz?)</code> function of FR-LUNAR-002 (CONTRACT.md: <code>export function tietKhiStartDiaChi(jdn: number, tz?: number): number</code>), then <code>(diaChiIndex - tietKhiStartDiaChiIndex + 12) % 12</code> gives the index into <code>TRUC_NAMES</code> (DEC-LUNAR-113). The 28 sao are computed as <code>(jdn - BASE_JDN_GIAC + 2800) % 28</code>. Because all three tables (daily officer deity, Truc, auspicious hours) are keyed on <code>diaChiIndex</code>, taking the earthly branch consistently with core is the single most important invariant of this module.</p>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream: FR-LUNAR-002 is a required dependency. <code>getDayQuality</code> needs <code>jdFromDate</code> (FR-LUNAR-001, re-exported via FR-LUNAR-002), <code>canChiDay</code> (FR-LUNAR-002), and <code>tietKhiStartDiaChi(jdn, tz?)</code> (FR-LUNAR-002, CONTRACT.md P2/P3 surface) to determine <code>diaChiSoDauTiet</code> for computing Truc by the formula <code>(canChiDay(jdn).chiIndex - tietKhiStartDiaChi(jdn) + 12) % 12</code> (DEC-LUNAR-113). All of these functions are available once FR-LUNAR-002 is complete.</p>\n<p>Downstream: FR-LUNAR-012 (good-day picker) depends entirely on <code>getDayQuality</code> and <code>getMonthDayQualities</code>. FR-LUNAR-013 (widget) uses <code>getDayQuality</code> to display <code>canChiNgay</code>, <code>label</code> (Hoang dao/Hac dao), and <code>gioHoangDao</code>. FR-LUNAR-007 (month grid) uses <code>isHoangDao</code> and <code>truc.name</code> to display in the day cell.</p>\n<p>Cross-cutting: the <code>disclaimer</code> field ensures the \"folk feng shui reference\" label is passed down to every UI layer from the root data, without FR-007/012/013 needing to add the label themselves.</p>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"date\": \"2025-01-29\",\n  \"canChiNgay\": \"Quy Suu\",\n  \"diaChiNgay\": \"Suu\",\n  \"thanTrucNhat\": \"Minh Duong\",\n  \"hoangDao\": true,\n  \"isHoangDao\": true,\n  \"label\": \"Hoang dao\",\n  \"truc\": {\n    \"name\": \"Khai\",\n    \"suitableFor\": [\"xuat hanh\", \"ky ket\", \"khai truong\"],\n    \"avoidFor\": [\"thuoc men\", \"chon cat\"]\n  },\n  \"sao28\": {\n    \"name\": \"Tinh\",\n    \"rating\": \"tot\",\n    \"notes\": \"Hop viec xuat hanh, ky hop dong, to chuc tiec\"\n  },\n  \"gioHoangDao\": [\n    { \"canh\": \"Ty (23:00-01:00)\", \"tuGio\": \"23:00\", \"denGio\": \"01:00\", \"isHoang\": true },\n    { \"canh\": \"Suu (01:00-03:00)\", \"tuGio\": \"01:00\", \"denGio\": \"03:00\", \"isHoang\": false },\n    { \"canh\": \"Dan (03:00-05:00)\", \"tuGio\": \"03:00\", \"denGio\": \"05:00\", \"isHoang\": true },\n    { \"canh\": \"Meo (05:00-07:00)\", \"tuGio\": \"05:00\", \"denGio\": \"07:00\", \"isHoang\": false },\n    { \"canh\": \"Thin (07:00-09:00)\", \"tuGio\": \"07:00\", \"denGio\": \"09:00\", \"isHoang\": true },\n    { \"canh\": \"Ti (09:00-11:00)\", \"tuGio\": \"09:00\", \"denGio\": \"11:00\", \"isHoang\": false },\n    { \"canh\": \"Ngo (11:00-13:00)\", \"tuGio\": \"11:00\", \"denGio\": \"13:00\", \"isHoang\": true },\n    { \"canh\": \"Mui (13:00-15:00)\", \"tuGio\": \"13:00\", \"denGio\": \"15:00\", \"isHoang\": false },\n    { \"canh\": \"Than (15:00-17:00)\", \"tuGio\": \"15:00\", \"denGio\": \"17:00\", \"isHoang\": true },\n    { \"canh\": \"Dau (17:00-19:00)\", \"tuGio\": \"17:00\", \"denGio\": \"19:00\", \"isHoang\": false },\n    { \"canh\": \"Tuat (19:00-21:00)\", \"tuGio\": \"19:00\", \"denGio\": \"21:00\", \"isHoang\": true },\n    { \"canh\": \"Hoi (21:00-23:00)\", \"tuGio\": \"21:00\", \"denGio\": \"23:00\", \"isHoang\": false }\n  ],\n  \"disclaimer\": \"Tham khao phong thuy dan gian\"\n}</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li>\"Use can-chi from core or from a third-party library?\" -&gt; DEC-LUNAR-112: always from core, consistent.</li><li>\"Where to put the disclaimer?\" -&gt; DEC-LUNAR-111: at the root level of DayQuality.</li><li>\"How is BASE_JDN_GIAC determined?\" -&gt; DEC-LUNAR-114: by a fixture cross-checked against several VN sources.</li></ul>\n<p>Still open (defer):</p>\n<ul><li>Some VN day-picking sites add a \"signing day / alternate day\" column (a substitute good hour used when an auspicious hour coincides with a bad hour). The PRD does not require this yet - defer to v2.</li><li>The \"Than sat\" system (such as Kim Lau, Hoang Oc, etc.) relates to the age of the person presiding over the wedding. The PRD does not require it - defer.</li><li>Manually cross-check <code>THAN_TRUC_NHAT_TABLE</code> against 3 different feng shui source books before shipping (editorial task, not a code task).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>THAN_TRUC_NHAT_TABLE</code> missing a row (&lt; 12)</td><td>Unit test AC #8</td><td>Test fails, build blocked</td><td>Add the missing row</td></tr><tr><td><code>BASE_JDN_GIAC</code> has a wrong value</td><td>Fixture test AC #5 the 28-sao run</td><td>Test fails, build blocked</td><td>Re-check the source, update the constant</td></tr><tr><td>JDN from FR-LUNAR-001 off by 1</td><td>Test AC #1 fixture (tet 2025) fails</td><td>Sao/Truc/DiaChi off by 1</td><td>Fix FR-001 first, do not fix here</td></tr><tr><td><code>thangAmIndex</code> off-by-one</td><td>Fixture test AC #1</td><td>Wrong ThanTrucNhat</td><td>Re-check <code>(thangAm - 1) mod 12</code></td></tr><tr><td>Earthly branch taken as <code>(jdn+9)%60 % 12</code> (off by 8 relative to core)</td><td>AC #16/#17 cross-check with <code>canChiDay</code></td><td>Daily officer deity + Truc + auspicious hours all wrong, off from can-chi in the calendar</td><td>Call <code>canChiDay(jdn)</code> and use <code>chiIndex</code> (DEC-LUNAR-112)</td></tr><tr><td><code>getDayQuality</code> calls fetch</td><td>Test AC #10 mock</td><td>Test fails</td><td>Remove the network-calling code</td></tr><tr><td><code>getMonthDayQualities</code> slow &gt; 50ms</td><td>Test AC #11</td><td>Test fails</td><td>Profile + cache jdn batch</td></tr><tr><td><code>disclaimer</code> missing from the result</td><td>Test AC #3</td><td>Test fails</td><td>Add it to the return object</td></tr><tr><td>A new enum not re-exported from index.ts</td><td>FR-012/013 build fails</td><td>Import error</td><td>Add it to the index.ts export</td></tr><tr><td><code>isHoangDao</code> differs from <code>hoangDao</code></td><td>Test AC #9</td><td>UI inconsistency</td><td>Set <code>isHoangDao = hoangDao</code> in the return</td></tr><tr><td>Truc index exceeds 11</td><td><code>mod 12</code> guards</td><td>Impossible</td><td>Guaranteed at the design level</td></tr><tr><td><code>gioHoangDao</code> has &lt; 12 elements</td><td>Test AC #2</td><td>Widget/UI missing a gio</td><td>Check the loop over 12</td></tr><tr><td>Sao28 returns index 28 (out of range)</td><td><code>mod 28</code> guards</td><td>Impossible</td><td>Design level</td></tr><tr><td>FR-LUNAR-002 not ready (wrong build order)</td><td>TypeScript compile error</td><td>Build fails</td><td>Build FR-002 first</td></tr><tr><td><code>TrucInfo.suitableFor</code> empty</td><td>Test AC #13</td><td>Empty symbol in UI</td><td>Add feng shui content</td></tr><tr><td>Tables have wrong Unicode characters</td><td>Fixture snapshot test</td><td>Visual mismatch</td><td>Use the correct literal string</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li>All 3 tables (THAN_TRUC_NHAT_TABLE, GIO_HOANG_DAO_TABLE, the 28-sao data) must be cross-checked against at least 2 reputable feng shui source books or VN day-picking sites before commit. This is editorial work, not coding; it should proceed in parallel with the code.</li><li><code>BASE_JDN_GIAC</code> is the most sensitive constant: changing it shifts all 28 sao for every day in 1900-2199. Lock it into <code>dayquality-fixtures.json</code> and comment clearly \"cross-checked against site X on day Y.\"</li><li><code>getMonthDayQualities</code> can call <code>getDayQuality</code> day by day in a simple loop - no need to optimize upfront. Only refactor to a batch JDN if profiling shows it is needed (potentially with thousands of days).</li><li><code>disclaimer</code> is a <code>readonly</code> literal type <code>\"Tham khao phong thuy dan gian\"</code> - the TypeScript type ensures no one accidentally returns a result with no disclaimer.</li><li>The 12-day Truc cycle will cluster: in a 30-day month, each Truc appears 2-3 times. Users seeing \"Khai\" several times is correct, not a bug.</li><li>The auspicious hours are computed by the Vietnamese \"canh\" hours (each canh is 2 hours, 12 canh make 24 hours). The <code>tuGio</code>/<code>denGio</code> constants in GIO_HOANG_DAO_TABLE are string literals (\"23:00\") for simple rendering - not Date objects.</li><li>Note: some online sources compute the auspicious hours by \"solar hours\" instead of standard clock hours. The PRD does not require this precision; using standard clock hours is sufficient.</li><li>This module has no npm dependency other than the internal amlich-core - truly zero-dependency, meaning zero third-party.</li></ul>\n<p><em>End of FR-LUNAR-011.</em></p>";

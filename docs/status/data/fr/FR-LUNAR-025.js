window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LUNAR-025"]="<h1>Feature Request</h1>\n<blockquote><p>Turn Your Will Into Real.</p></blockquote>\n<h2>Summary</h2>\n<p>Build a public REST API (B2B API) that provides a \"Calendar as a Service\". Partner businesses (vegetarian restaurants, retail chains, spiritual services) can register an API Key and call into Genie's system to: convert solar/lunar dates, get the list of lunar occasions in a month, and compute auspicious and inauspicious days (if available). This API will be charged on a SaaS model (Pay-as-you-go or Tiered Subscription).</p>\n<h2>Problem</h2>\n<p>Many F&amp;B businesses (especially vegetarian restaurants) want to run marketing campaigns (SMS, Zalo ZNS) that automatically remind customers to book a table on the first of the month and the full moon day each month. However, accurately computing the lunar full moon day to the Vietnam standard (UTC+7 timezone, Ho Ngoc Duc algorithm) is not simple. They need a stable API to call instead of rebuilding the core engine themselves.</p>\n<h2>Customer Quotes</h2>\n<p>&lt;untrusted_content source=\"b2b-survey\"&gt; \"We use a CRM with SMS integration, but we always have to enter the full moon day by hand every month. If there were an API that returned the first of the month and the full moon day for next month, I would have the dev plug it right into the CRM.\" &lt;/untrusted_content&gt;</p>\n<h2>§1 - Description (Normative Clauses)</h2>\n<ol><li><strong>MUST</strong> create a <code>b2b_api_keys</code> table in Supabase containing <code>id</code>, <code>partner_name</code>, <code>api_key_hash</code>, <code>tier</code> (free/pro/enterprise), <code>monthly_quota</code>, and <code>is_active</code>.</li><li><strong>MUST</strong> implement a middleware in <code>genie-api</code> that intercepts <code>/v1/b2b/*</code> routes, validates the <code>x-api-key</code> header against the database, and enforces rate limits (e.g., via Redis or Supabase RPC).</li><li><strong>MUST</strong> expose exactly two initial endpoints: <code>GET /v1/b2b/lunar/convert</code> (solar to lunar &amp; vice versa) and <code>GET /v1/b2b/lunar/events?month=YYYY-MM</code>.</li><li><strong>MUST</strong> return responses in strict JSON format matching the OpenAPI 3.0 specification.</li><li><strong>MUST</strong> log all B2B API requests (metadata only, no PII payloads) to a <code>b2b_usage_logs</code> table for billing and throttling purposes.</li><li><strong>SHOULD</strong> return a <code>429 Too Many Requests</code> HTTP status code immediately if a partner exceeds their monthly quota or burst rate limit.</li><li><strong>MUST</strong> ensure the core engine (<code>amlich-core</code>) computes these requests deterministically and statelessly.</li><li><strong>MUST NOT</strong> expose endpoints that mutate internal user data; this API is strictly a read-only computational service.</li></ol>\n<h2>§2 - Why this design</h2>\n<p><strong>Why a separate API Key table instead of Supabase Auth (§1 #1)?</strong>  B2B integrations are Server-to-Server (M2M). Supabase JWTs are designed for client-side user sessions and expire frequently. A long-lived, revokable API Key (hashed in DB) is the standard for M2M APIs (like Stripe or Twilio).</p>\n<p><strong>Why rate limiting is critical (§1 #6)?</strong> Computing lunar calendars (especially generating a whole year's events) is CPU-bound. Without strict rate limiting, a single partner could DDoS the <code>genie-api</code> server, affecting the mobile app's performance.</p>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-json\">// GET /v1/b2b/lunar/events?month=2026-08\n// Headers: x-api-key: b2b_test_12345\n{\n  \"status\": \"success\",\n  \"data\": [\n    {\n      \"solar_date\": \"2026-08-13\",\n      \"lunar_date\": \"2026-07-01\",\n      \"event_name\": \"Mùng 1\",\n      \"is_major\": true\n    },\n    {\n      \"solar_date\": \"2026-08-27\",\n      \"lunar_date\": \"2026-07-15\",\n      \"event_name\": \"Rằm Tháng Bảy (Vu Lan)\",\n      \"is_major\": true\n    }\n  ],\n  \"meta\": {\n    \"quota_remaining\": 9998,\n    \"compute_time_ms\": 12\n  }\n}</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Authentication</strong> - When a request is made to <code>/v1/b2b/lunar/convert</code> without an <code>x-api-key</code> header, the server returns <code>401 Unauthorized</code>.</li><li><strong>Quota Enforcement</strong> - When a partner's <code>b2b_usage_logs</code> count exceeds their <code>monthly_quota</code> for the current month, subsequent requests return <code>429 Too Many Requests</code>.</li><li><strong>Accuracy</strong> - When requesting events for August 2026, the API correctly returns the Vu Lan festival (15th of the 7th Lunar Month).</li><li><strong>Billing Telemetry</strong> - When a successful 200 OK request is served, exactly one row is written to <code>b2b_usage_logs</code> with the partner ID and endpoint path.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// test/routes/b2b-api.test.ts\nimport request from 'supertest';\nimport { app } from '../../src/index';\n\ndescribe('B2B API endpoints', () =&gt; {\n  it('MUST reject requests without valid API Key (401)', async () =&gt; {\n    const res = await request(app).get('/v1/b2b/lunar/events?month=2026-08');\n    expect(res.status).toBe(401);\n  });\n\n  it('MUST return 429 if quota exceeded', async () =&gt; {\n    // Mock DB to return 0 quota remaining\n    const res = await request(app)\n      .get('/v1/b2b/lunar/convert?date=2026-08-15')\n      .set('x-api-key', 'valid_test_key_with_0_quota');\n    \n    expect(res.status).toBe(429);\n  });\n});</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// middlewares/api-key.ts\nimport { Request, Response, NextFunction } from 'express';\nimport crypto from 'crypto';\n\nexport async function requireApiKey(req: Request, res: Response, next: NextFunction) {\n  const apiKey = req.header('x-api-key');\n  if (!apiKey) return res.status(401).json({ error: \"Missing x-api-key header\" });\n\n  const hash = crypto.createHash('sha256').update(apiKey).digest('hex');\n  const partner = await DB.getPartnerByApiKeyHash(hash);\n  \n  if (!partner || !partner.is_active) {\n    return res.status(401).json({ error: \"Invalid or revoked API Key\" });\n  }\n\n  const usage = await DB.getCurrentMonthUsage(partner.id);\n  if (usage &gt;= partner.monthly_quota) {\n    return res.status(429).json({ error: \"Monthly quota exceeded\" });\n  }\n\n  // Inject partner info for downstream logging\n  req.partner = partner; \n  next();\n}</code></pre>\n<h2>§7 - Dependencies</h2>\n<ul><li><strong>Upstream:</strong> FR-LUNAR-001 (Core Lunar Engine - <code>amlich-core</code>).</li><li><strong>Upstream:</strong> FR-LUNAR-011 (API Backend initialization).</li></ul>\n<h2>§8 - Example payloads</h2>\n<p><strong>Audit Log Row:</strong></p>\n<pre><code class=\"language-json\">{\n  \"event\": \"b2b.api_request\",\n  \"partner_id\": \"00000000-0000-0000-0000-000000000002\",\n  \"timestamp\": \"2026-08-14T01:00:05Z\",\n  \"payload\": {\n    \"endpoint\": \"/v1/b2b/lunar/events\",\n    \"status_code\": 200,\n    \"compute_ms\": 15\n  },\n  \"trace_id\": \"5b23d9161a03f8373b...\"\n}</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li><code>Deferred: P5</code> - Self-serve developer portal. For P4, API keys will be generated manually by admins and emailed to partners.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>API Key compromised</td><td>Partner reports unauthorized usage</td><td>Quota drained rapidly</td><td>Admin revokes key manually, regenerates new hash</td></tr><tr><td>Redis (Rate Limiter) goes down</td><td>DB connection timeout</td><td>All requests fail or bypass limits</td><td>Fallback to in-memory rate limiter or fail-open depending on risk appetite</td></tr><tr><td>Partner spams API maliciously</td><td>WAF/Cloudflare flags 429s</td><td>429 returned</td><td>Cloudflare automatically IP bans the attacker</td></tr><tr><td>Core engine throws exception</td><td><code>amlich-core</code> panic</td><td>500 Internal Server Error</td><td>Express error handler catches, returns sanitized 500 JSON, alerts Sentry</td></tr><tr><td>Date parsing fails</td><td>Invalid <code>month</code> parameter</td><td>400 Bad Request</td><td>Return explicit error: \"month must be YYYY-MM\"</td></tr><tr><td>Billing DB write fails</td><td><code>logUsage</code> fails post-request</td><td>Free compute given</td><td>Accept minor revenue leakage; do not block response on logging</td></tr><tr><td>Timezone ambiguity</td><td>Partner expects PST, we return UTC+7</td><td>Logic bug</td><td>Document explicitly that all calculations are strictly UTC+7 (Vietnam standard time)</td></tr><tr><td>SHA256 collision</td><td>Highly improbable</td><td>Auth bypass</td><td>Use secure UUIDs for keys</td></tr><tr><td>Response exceeds payload size</td><td>Requesting 100 years of events</td><td>413 Payload Too Large</td><td>Enforce hard limit (e.g., max 1 year per request) in schema validation</td></tr><tr><td>API Gateway timeout</td><td>Lambda execution &gt; 10s</td><td>504 Gateway Timeout</td><td>Optimize Core engine</td></tr></tbody></table>\n<h2>§11 - Implementation notes</h2>\n<ul><li><strong>Key Generation:</strong> Keys should be formatted like <code>gn_live_xxxxxxxxxxxxxxxxxxxxxxxx</code>, allowing regex scanning for leaked secrets in GitHub repos.</li><li><strong>Fail-Open vs Fail-Closed:</strong> If the Redis usage counter goes down, the API MUST \"fail-closed\" (return 503) or risk severe financial/resource drain if a partner gets stuck in an infinite retry loop.</li><li><strong>API Versioning:</strong> The URL MUST include <code>/v1/</code>. Breaking changes will require spinning up <code>/v2/</code>.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> LLM agent acting as feature-request-author</li><li><strong>Scope:</strong> The entire FR content.</li><li><strong>Human review:</strong> Reviewed by the operator after generation.</li></ul>\n<p><em>End of FR-LUNAR-025.</em></p>";

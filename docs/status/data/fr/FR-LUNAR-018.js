window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LUNAR-018"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This feature extends the on-device storage model of FR-LUNAR-004 with an optional cloud tier, using Supabase/Postgres with Row Level Security, so that multiple members of the same family can receive one gio reminder and sync reminders across multiple devices. Consent must always be obtained first (FR-019) before enabling any process that sends data to the cloud.</p>\n<ol><li>MUST keep on-device as the immutable default: when the user has not granted cloud consent (per FR-019), all data MUST stay on the device and MUST NOT be sent to Supabase (DEC-LUNAR-180).</li><li>MUST enable cloud sync only after the user grants <code>consentFlags.cloudSync = true</code> via the FR-019 consent flow; revoking consent MUST disable sync immediately and MAY trigger deletion of the cloud data on request (DEC-LUNAR-180).</li><li>MUST use Supabase/Postgres with RLS enabled on every table; the <code>service_role</code> key MUST NOT be used on the client - only on the server (DEC-LUNAR-181).</li><li>MUST apply the RLS policy: the owner of a <code>Reminder</code> has full CRUD; a member in <code>sharedWith</code> has only READ on that row; every unauthenticated request is rejected entirely (DEC-LUNAR-181).</li><li>MUST store <code>sharedWith</code> as an array of <code>userId</code>; each member in the array MUST receive an independent notification copy, computed and scheduled on their own device via FR-LUNAR-005 (DEC-LUNAR-182).</li><li>MUST implement the invite flow: the owner creates an <code>invite_token</code> (JWT, 48h TTL) and shares a link/ZNS; the recipient clicks the link -&gt; authenticates -&gt; is added to <code>sharedWith</code>; the token expires after 48h and is single-use (DEC-LUNAR-185).</li><li>MUST resolve conflicts with \"last-write-wins\" based on the <code>updated_at</code> field; the record with a newer <code>updated_at</code> MUST overwrite the older record in every case (DEC-LUNAR-183).</li><li>MUST write a conflict log when a conflict is detected (two records with <code>updated_at</code> differing by &lt; 1 second) for debugging, but MUST NOT show this log in the user UI (DEC-LUNAR-183).</li><li>MUST minimize gio data fields: the <code>reminders</code> table stores <code>title</code> (which may contain the name of the deceased) but this field MUST NOT be exposed via any unauthenticated REST endpoint; this data MUST NOT be transferred outside Vietnam before a DPIA (DEC-LUNAR-184, NFR-Privacy/PDPL).</li><li>MUST provide a client-side <code>SyncClient</code> with the strategy: push local -&gt; cloud when there is a change (debounce 2 seconds), pull cloud -&gt; local on app open, retry with exponential backoff on network loss.</li><li>MUST invalidate the <code>OccurrenceCache</code> after each successful cloud pull to ensure the calendar recomputes from the new data (DEC-LUNAR-180).</li><li>SHOULD include a <code>DELETE /api/sync/account</code> endpoint that lets the user delete all cloud data on request per the PDPL right to erasure (PRD Recommendations #6).</li><li>MUST NOT store <code>lunarDay</code>, <code>lunarMonth</code>, <code>isLeapMonth</code> as unencrypted plain-text when stored in the cloud; SHOULD encrypt sensitive columns at the application level before INSERT (DEC-LUNAR-184).</li><li>MUST return HTTP 409 when there is a conflict that cannot be resolved automatically, and the client MUST have a fallback mechanism to display last-known-good instead of crashing.</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why is on-device the default and cloud opt-in (DEC-LUNAR-180)?</strong> Gio data is tied to the name and death date of a relative - this is culturally sensitive data under PDPL. Sending it to the cloud without consent is a legal violation even though a startup gets a 5-year grace period for DPIA/DPO. The opt-in design also means the personal MVP (used within one's own family) needs no cloud at all - exactly within the personal/family exemption scope of PDPL.</p>\n<p><strong>Why Supabase/Postgres with RLS instead of a self-built solution (DEC-LUNAR-181)?</strong> Supabase provides RLS on Postgres - meaning access rights are set at the database layer, not dependent on application-layer logic. If there is a bug in the API layer, RLS still blocks unauthorized access. For sensitive data like the name of a deceased person, this is a necessary line of defense.</p>\n<p><strong>Why is sharedWith an array of userId instead of a separate relation table (DEC-LUNAR-182)?</strong> At family scale (usually 2-10 members), an array of userId in a Postgres JSONB column is enough - no complex joins needed. When you need to scale to larger groups in the future, migrating to a relation table is straightforward.</p>\n<p><strong>Why last-write-wins instead of CRDT (DEC-LUNAR-183)?</strong> Users manually editing gio data is much rarer than a shared household accountant. A real conflict is the exception, not the rule. CRDT adds complexity without a clear benefit for this use case. Writing a conflict log is enough to debug when it happens.</p>\n<p><strong>Why an invite token JWT 48h instead of email (DEC-LUNAR-185)?</strong> This app grows on the Zalo platform - sharing a link via Zalo chat or ZNS is more natural and faster than email. Email needs an extra complex step (SMTP setup, spam filter) for a personal interaction app. A 48h TTL is enough for the recipient to respond but short enough to reduce the risk if the link is shared wrongly.</p>\n<p><strong>Why does syncing minimize gio data (DEC-LUNAR-184)?</strong> PDPL provides that the name of a deceased person is data related to another individual. Not transferring it across borders before a DPIA is a legal obligation applying from 01/01/2026. Using Supabase hosting in the Singapore region (ap/southeast-1) reduces the risk but still requires an official DPIA at scale - therefore the DELETE endpoint and the consent-revocation mechanism are mandatory right now (PRD Caveats - PDPL still has unclear points).</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// services/genie-api/lib/supabase.ts\nimport { createClient, SupabaseClient } from \"@supabase/supabase-js\";\n\nexport function getSupabaseClient(userJwt: string): SupabaseClient {\n  // Dung anon key + user JWT de RLS hoat dong dung\n  // KHONG DUOC dung service_role key o day\n  return createClient(\n    process.env.SUPABASE_URL!,\n    process.env.SUPABASE_ANON_KEY!,\n    { global: { headers: { Authorization: `Bearer ${userJwt}` } } }\n  );\n}</code></pre>\n<pre><code class=\"language-typescript\">// services/genie-api/api/sync.ts\nimport type { VercelRequest, VercelResponse } from \"@vercel/node\";\n\nexport interface SyncPushBody {\n  reminders: RemindersUpsertRow[];\n  deviceId: string;\n}\n\nexport interface RemindersUpsertRow {\n  id: string;               // UUID\n  userId: string;\n  type: \"RAM\" | \"MUNG_MOT\" | \"GIO\" | \"CUSTOM\" | \"FESTIVAL\";\n  title: string;            // co the chua ten nguoi da mat - nhan vay o RLS\n  lunarDay: number;\n  lunarMonth: number;\n  lunarYear: number | null;\n  isLeapMonth: boolean;\n  recurrence: \"MONTHLY\" | \"ANNUAL\" | \"ONCE\";\n  leadTimes: number[];\n  notifyTime: string;       // \"HH:MM\"\n  channels: (\"LOCAL\" | \"ZNS\" | \"PUSH\")[];\n  linkedContentId: string | null;\n  sharedWith: string[];     // mang userId\n  enabled: boolean;\n  updatedAt: string;        // ISO 8601\n}\n\nexport interface SyncPullResponse {\n  reminders: RemindersUpsertRow[];\n  serverTime: string;\n  conflictsLogged: number;\n}\n\nexport interface SharePatchBody {\n  reminderId: string;\n  action: \"add\" | \"remove\";\n  targetUserId: string;\n}\n\nexport interface InviteTokenPayload {\n  ownerId: string;\n  reminderId: string;\n  exp: number;              // Unix timestamp, +48h\n  jti: string;              // UUID, single-use\n}\n\n// POST /api/sync/push\nexport async function handlePush(\n  req: VercelRequest,\n  res: VercelResponse\n): Promise&lt;void&gt;;\n\n// GET /api/sync/pull\nexport async function handlePull(\n  req: VercelRequest,\n  res: VercelResponse\n): Promise&lt;void&gt;;\n\n// PATCH /api/sync/share\nexport async function handleShare(\n  req: VercelRequest,\n  res: VercelResponse\n): Promise&lt;void&gt;;\n\n// POST /api/sync/invite\nexport async function handleInvite(\n  req: VercelRequest,\n  res: VercelResponse\n): Promise&lt;{ token: string; expiresAt: string }&gt;;\n\n// POST /api/sync/invite/accept\nexport async function handleInviteAccept(\n  req: VercelRequest,\n  res: VercelResponse\n): Promise&lt;void&gt;;\n\n// DELETE /api/sync/account\nexport async function handleDeleteAccount(\n  req: VercelRequest,\n  res: VercelResponse\n): Promise&lt;void&gt;;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/lib/sync-client.ts\nexport interface SyncClientOptions {\n  userJwt: string;\n  deviceId: string;\n  onConflict?: (reminderId: string) =&gt; void;\n}\n\nexport class SyncClient {\n  constructor(options: SyncClientOptions);\n\n  // Day thay doi len cloud, debounce 2000ms\n  push(reminders: RemindersUpsertRow[]): Promise&lt;void&gt;;\n\n  // Keo du lieu tu cloud ve local\n  pull(): Promise&lt;SyncPullResponse&gt;;\n\n  // Chia se mot reminder voi thanh vien\n  share(reminderId: string, action: \"add\" | \"remove\", targetUserId: string): Promise&lt;void&gt;;\n\n  // Tao invite link\n  createInvite(reminderId: string): Promise&lt;{ link: string; expiresAt: string }&gt;;\n\n  // Xoa toan bo du lieu cloud (quyen PDPL)\n  deleteCloudData(): Promise&lt;void&gt;;\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/lib/conflict-resolver.ts\nexport interface ConflictRecord {\n  reminderId: string;\n  localUpdatedAt: string;\n  cloudUpdatedAt: string;\n  resolvedTo: \"local\" | \"cloud\";\n  deltaMs: number;\n}\n\nexport function resolveConflict(\n  local: RemindersUpsertRow,\n  cloud: RemindersUpsertRow\n): { winner: RemindersUpsertRow; conflict: ConflictRecord | null };</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>When <code>consentFlags.cloudSync</code> is <code>false</code> (the default), no request is sent to Supabase - checked via the network log.</li><li>After the user grants consent via FR-019 and enables sync, <code>SyncClient.push()</code> upserts reminders to Supabase successfully with HTTP 200.</li><li>The RLS policy blocks: user B CANNOT read user A's reminder if A has not added B to <code>sharedWith</code> - returns 0 rows, not 403 (RLS behavior).</li><li>User A adds user B to the <code>sharedWith</code> of a <code>GIO</code> reminder -&gt; user B pulls -&gt; B's <code>SyncPullResponse</code> contains that reminder.</li><li>Each member in <code>sharedWith</code> receives an independent notification on their own device, not a shared notification.</li><li>The invite token expires after 48h: accepting after 48h returns HTTP 401.</li><li>The invite token is single-use: reusing an already-used token returns HTTP 409.</li><li>Last-write-wins: upserting a record with a newer <code>updatedAt</code> overwrites the older record; upserting an older record does not affect the newer record.</li><li>On a conflict (<code>updatedAt</code> differing by &lt; 1 second), the conflict is written to the log but the UI still shows the winning value, with no error dialog.</li><li><code>DELETE /api/sync/account</code> deletes all reminders, shared_reminder_members, and sync_log of that user, returning HTTP 200.</li><li>After <code>deleteCloudData()</code>, an immediate pull returns an empty <code>reminders</code> array.</li><li>Revoking consent -&gt; <code>SyncClient</code> stops pushing/pulling; the cloud data still exists until the user calls <code>deleteCloudData()</code> separately.</li><li>Network loss during push: <code>SyncClient</code> retries with exponential backoff (1s, 2s, 4s, max 30s), does not crash the app.</li><li>The <code>OccurrenceCache</code> is cleared after each successful <code>SyncClient.pull()</code>.</li><li>The <code>title</code> field of a <code>GIO</code> reminder is not exposed via a non-authenticated endpoint (RLS ensures it; tested with an anon request).</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// services/genie-api/test/sync.test.ts\nimport { describe, it, expect, beforeEach, vi } from \"vitest\";\nimport { resolveConflict } from \"../apps/web/lib/conflict-resolver\";\n\ndescribe(\"conflict-resolver\", () =&gt; {\n  it(\"chon ban ghi co updatedAt moi hon\", () =&gt; {\n    const local = makeReminder({ updatedAt: \"2026-06-27T10:00:00.000Z\" });\n    const cloud = makeReminder({ updatedAt: \"2026-06-27T10:00:05.000Z\" });\n    const { winner, conflict } = resolveConflict(local, cloud);\n    expect(winner.updatedAt).toBe(cloud.updatedAt);\n    expect(conflict).not.toBeNull();\n  });\n\n  it(\"tra ve conflict null khi khong co xung dot\", () =&gt; {\n    const local = makeReminder({ updatedAt: \"2026-06-27T10:00:00.000Z\" });\n    const cloud = makeReminder({ updatedAt: \"2026-06-27T08:00:00.000Z\" });\n    const { winner, conflict } = resolveConflict(local, cloud);\n    expect(winner.updatedAt).toBe(local.updatedAt);\n    expect(conflict).toBeNull();\n  });\n\n  it(\"danh dau conflict khi delta &lt; 1000ms\", () =&gt; {\n    const local = makeReminder({ updatedAt: \"2026-06-27T10:00:00.800Z\" });\n    const cloud = makeReminder({ updatedAt: \"2026-06-27T10:00:00.200Z\" });\n    const { conflict } = resolveConflict(local, cloud);\n    expect(conflict).not.toBeNull();\n    expect(conflict!.deltaMs).toBeLessThan(1000);\n  });\n});\n\ndescribe(\"SyncClient - push/pull\", () =&gt; {\n  it(\"khong push khi cloudSync consent = false\", async () =&gt; {\n    const fetchSpy = vi.spyOn(global, \"fetch\");\n    const client = new SyncClient({ userJwt: \"tok\", deviceId: \"dev1\" });\n    // Simulate consent = false\n    await client.push([makeReminder({})]);\n    expect(fetchSpy).not.toHaveBeenCalled();\n  });\n\n  it(\"retry 3 lan khi mang loi truoc khi throw\", async () =&gt; {\n    vi.spyOn(global, \"fetch\").mockRejectedValue(new Error(\"network\"));\n    const client = new SyncClient({ userJwt: \"tok\", deviceId: \"dev1\" });\n    // consent = true\n    await expect(client.push([makeReminder({})])).rejects.toThrow();\n    // fetch duoc goi nhieu lan (retry)\n    expect((global.fetch as any).mock.calls.length).toBeGreaterThan(1);\n  });\n});\n\ndescribe(\"RLS - integration (requires Supabase test instance)\", () =&gt; {\n  it(\"user B khong the doc reminder cua A khi khong co sharedWith\", async () =&gt; {\n    // Dung Supabase local docker hoac test project\n    const clientA = getSupabaseClient(jwtForUser(\"userA\"));\n    const clientB = getSupabaseClient(jwtForUser(\"userB\"));\n    const { data: inserted } = await clientA\n      .from(\"reminders\")\n      .insert(makeReminderRow({ userId: \"userA\", sharedWith: [] }))\n      .select();\n    const { data: fetched } = await clientB\n      .from(\"reminders\")\n      .select(\"*\")\n      .eq(\"id\", inserted![0].id);\n    expect(fetched).toHaveLength(0);\n  });\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The API contract in §3 is the backbone. Key points to keep in mind when implementing:</p>\n<pre><code class=\"language-sql\">-- services/genie-api/supabase/migrations/0016_family_sharing_schema.sql\n-- (xem §8 cho full migration; so migration P3 duoc phan phoi: 0016-0017 FR-018, 0018 FR-017, 0019 FR-019, 0020 FR-020)\n-- Diem chinh: sharedWith luu kieu uuid[] de RLS co the dung ANY()\n-- updated_at tu dong cap nhat qua trigger\n\nCREATE OR REPLACE FUNCTION update_updated_at()\nRETURNS TRIGGER AS $$\nBEGIN\n  NEW.updated_at = NOW();\n  RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;</code></pre>\n<pre><code class=\"language-typescript\">// Diem then chot cua SyncClient.push():\n// 1. Kiem tra consent truoc - neu false, return ngay (DEC-LUNAR-180)\n// 2. Debounce 2000ms de gop nhieu thay doi nho\n// 3. Upsert voi on_conflict: id -&gt; cap nhat neu updatedAt moi hon\n// 4. Clear OccurrenceCache sau khi thanh cong\nasync push(reminders: RemindersUpsertRow[]): Promise&lt;void&gt; {\n  if (!this.hasCloudConsent()) return; // DEC-LUNAR-180\n  // ... debounce + upsert\n}</code></pre>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream: FR-LUNAR-004 (the Reminder data model + recurrence engine) - <code>RemindersUpsertRow</code> maps directly from FR-004's <code>Reminder</code> model; FR-018 cannot run without that model.</p>\n<p>Downstream: FR-LUNAR-019 (PDPL compliance consent) - FR-018 requires consent before enabling the cloud; FR-019 provides the <code>consentFlags</code> that FR-018 checks. FR-LUNAR-020 (freemium monetization) - the \"family\" tier needs FR-018 to run successfully first.</p>\n<p>Cross-cutting: FR-LUNAR-005 (local notifications) must be re-kicked after each successful cloud pull to recompute the 64 slots. FR-LUNAR-016 (Zalo Mini App) uses ZNS to share the invite link (DEC-LUNAR-185).</p>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-sql\">-- services/genie-api/supabase/migrations/0016_family_sharing_schema.sql\n-- (so migration P3 duoc phan phoi de tranh va cham voi cac FR P3 khac: 0016-0017 cho FR-018, 0018 cho FR-017, 0019 cho FR-019, 0020 cho FR-020)\n\nCREATE EXTENSION IF NOT EXISTS \"uuid-ossp\";\n\nCREATE TABLE IF NOT EXISTS users (\n  id            UUID PRIMARY KEY DEFAULT uuid_generate_v4(),\n  display_name  TEXT NOT NULL,\n  locale        TEXT NOT NULL DEFAULT 'vi-VN',\n  timezone      TEXT NOT NULL DEFAULT 'Asia/Ho_Chi_Minh',\n  phone         TEXT,\n  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE IF NOT EXISTS reminders (\n  id                UUID PRIMARY KEY DEFAULT uuid_generate_v4(),\n  user_id           UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n  type              TEXT NOT NULL CHECK (type IN ('RAM','MUNG_MOT','GIO','CUSTOM','FESTIVAL')),\n  title             TEXT NOT NULL,\n  lunar_day         SMALLINT NOT NULL CHECK (lunar_day BETWEEN 1 AND 30),\n  lunar_month       SMALLINT NOT NULL CHECK (lunar_month BETWEEN 1 AND 12),\n  lunar_year        SMALLINT,\n  is_leap_month     BOOLEAN NOT NULL DEFAULT FALSE,\n  recurrence        TEXT NOT NULL CHECK (recurrence IN ('MONTHLY','ANNUAL','ONCE')),\n  lead_times        SMALLINT[] NOT NULL DEFAULT '{0}',\n  notify_time       TIME NOT NULL DEFAULT '07:00',\n  channels          TEXT[] NOT NULL DEFAULT '{LOCAL}',\n  linked_content_id TEXT,\n  shared_with       UUID[] NOT NULL DEFAULT '{}',\n  enabled           BOOLEAN NOT NULL DEFAULT TRUE,\n  updated_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n  created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE INDEX IF NOT EXISTS idx_reminders_user_id ON reminders(user_id);\nCREATE INDEX IF NOT EXISTS idx_reminders_shared_with ON reminders USING GIN(shared_with);\n\nCREATE TABLE IF NOT EXISTS invite_tokens (\n  jti         UUID PRIMARY KEY DEFAULT uuid_generate_v4(),\n  owner_id    UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n  reminder_id UUID NOT NULL REFERENCES reminders(id) ON DELETE CASCADE,\n  expires_at  TIMESTAMPTZ NOT NULL,\n  used_at     TIMESTAMPTZ,\n  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE IF NOT EXISTS sync_log (\n  id            BIGSERIAL PRIMARY KEY,\n  reminder_id   UUID NOT NULL,\n  device_id     TEXT NOT NULL,\n  action        TEXT NOT NULL,\n  conflict_data JSONB,\n  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TRIGGER trg_reminders_updated_at\n  BEFORE UPDATE ON reminders\n  FOR EACH ROW EXECUTE FUNCTION update_updated_at();</code></pre>\n<pre><code class=\"language-sql\">-- services/genie-api/supabase/migrations/0017_family_sharing_rls.sql\n\nALTER TABLE reminders ENABLE ROW LEVEL SECURITY;\n\n-- Owner: full CRUD\nCREATE POLICY \"owner_all\" ON reminders\n  FOR ALL\n  USING (auth.uid() = user_id)\n  WITH CHECK (auth.uid() = user_id);\n\n-- Thanh vien shared: chi doc\nCREATE POLICY \"member_select\" ON reminders\n  FOR SELECT\n  USING (auth.uid() = ANY(shared_with));\n\n-- Anon: khong co gi (mac dinh deny)</code></pre>\n<pre><code class=\"language-json\">// Push request body mau\n{\n  \"reminders\": [\n    {\n      \"id\": \"a1b2c3d4-e5f6-7890-abcd-ef1234567890\",\n      \"userId\": \"user-uuid-here\",\n      \"type\": \"GIO\",\n      \"title\": \"Gio ba noi\",\n      \"lunarDay\": 10,\n      \"lunarMonth\": 3,\n      \"lunarYear\": null,\n      \"isLeapMonth\": false,\n      \"recurrence\": \"ANNUAL\",\n      \"leadTimes\": [0, 1],\n      \"notifyTime\": \"07:00\",\n      \"channels\": [\"LOCAL\", \"ZNS\"],\n      \"linkedContentId\": \"festival-dam-gio\",\n      \"sharedWith\": [\"member-uuid-1\", \"member-uuid-2\"],\n      \"enabled\": true,\n      \"updatedAt\": \"2026-06-27T10:30:00.000Z\"\n    }\n  ],\n  \"deviceId\": \"iphone-uuid-device\"\n}</code></pre>\n<pre><code class=\"language-json\">// Pull response mau\n{\n  \"reminders\": [\n    {\n      \"id\": \"a1b2c3d4-e5f6-7890-abcd-ef1234567890\",\n      \"userId\": \"user-uuid-here\",\n      \"type\": \"GIO\",\n      \"title\": \"Gio ba noi\",\n      \"lunarDay\": 10,\n      \"lunarMonth\": 3,\n      \"lunarYear\": null,\n      \"isLeapMonth\": false,\n      \"recurrence\": \"ANNUAL\",\n      \"leadTimes\": [0, 1],\n      \"notifyTime\": \"07:00\",\n      \"channels\": [\"LOCAL\", \"ZNS\"],\n      \"linkedContentId\": \"festival-dam-gio\",\n      \"sharedWith\": [\"member-uuid-1\", \"member-uuid-2\"],\n      \"enabled\": true,\n      \"updatedAt\": \"2026-06-27T10:30:00.000Z\"\n    }\n  ],\n  \"serverTime\": \"2026-06-27T10:35:00.000Z\",\n  \"conflictsLogged\": 0\n}</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li>Conflict strategy: last-write-wins on <code>updated_at</code> (DEC-LUNAR-183) - enough for the small-family use case, no CRDT needed.</li><li>Invite channel: ZNS/copy link, no email needed (DEC-LUNAR-185) - suits the Zalo platform.</li><li>Cloud region: Supabase <code>ap-southeast-1</code> (Singapore) - the closest possible for Vietnam, reducing latency; note an official DPIA is still needed when scaling beyond the family.</li><li>RLS strategy: anon key + user JWT instead of service_role - ensures RLS works at the database layer, not only the API layer (DEC-LUNAR-181).</li></ul>\n<p>Still deferred:</p>\n<ul><li>End-to-end encryption at the application layer for the <code>title</code> of a <code>GIO</code> reminder (DEC-LUNAR-184): recorded as SHOULD, but the specific implementation (key management, KMS) is not yet chosen. Will be decided once there is an official DPIA. Deferred to a separate FR if needed.</li><li>Real-time sync via Supabase Realtime (WebSocket): currently uses pull-on-open; real-time is a Phase 4 feature if needed.</li><li>Official cross-border DPIA: not yet done; that is why the DELETE endpoint and the consent-revocation mechanism are mandatory right now (PRD Caveats - PDPL still has unclear points).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Push without consent</td><td>Check <code>consentFlags.cloudSync</code> before calling the API</td><td>Return immediately, no push</td><td>Not needed - this is the correct behavior</td></tr><tr><td>Network loss during push</td><td><code>fetch</code> throws NetworkError</td><td>Retry with exponential backoff (1s, 2s, 4s, max 30s)</td><td>Data stays on-device, push again when there is network</td></tr><tr><td>RLS blocks reading data</td><td>Supabase returns 0 rows (not an error)</td><td>Pull receives an empty array, UI shows an empty list</td><td>Log server-side, check sharedWith</td></tr><tr><td><code>updated_at</code> conflict</td><td><code>resolveConflict()</code> detects a delta &lt; 1s</td><td>Last-write-wins, write a conflict log</td><td>Not shown in the UI; debug via sync_log</td></tr><tr><td>Invite token expired</td><td><code>expires_at &lt; NOW()</code></td><td>HTTP 401, show \"link expired\"</td><td>Owner creates a new invite</td></tr><tr><td>Invite token reused</td><td><code>used_at IS NOT NULL</code></td><td>HTTP 409</td><td>Owner creates a new invite</td></tr><tr><td>Supabase down</td><td>HTTP 5xx or timeout</td><td>SyncClient responds; UI shows \"sync failed\"</td><td>Automatic retry; on-device data still works</td></tr><tr><td>Wrong RLS policy (bug)</td><td>Integration test runs before deploy</td><td>Detected at the test stage</td><td>Fix the policy, re-deploy the migration</td></tr><tr><td>Pull returns an invalid reminder</td><td>Schema validation on the client</td><td>The reminder is skipped, log the error</td><td>Handled in the FR-019 data validation layer</td></tr><tr><td>Cloud account deletion (PDPL)</td><td>DELETE /api/sync/account</td><td>Delete all cloud data</td><td>On-device data still kept; the user confirms first</td></tr><tr><td>sharedWith contains a non-existent userId</td><td>Foreign key constraint</td><td>INSERT is rejected, HTTP 400</td><td>The client must validate the userId before sharing</td></tr><tr><td>OccurrenceCache not cleared after pull</td><td>Unit test checks it</td><td>Stale cache, shows the wrong date</td><td>Fix sync-client: clear the cache after a successful pull</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li>The most important point: check consent BEFORE ANYTHING in <code>SyncClient</code> - a single line <code>if (!this.hasCloudConsent()) return;</code> at the start of the <code>push()</code> and <code>pull()</code> functions. Without that foundation, all other logic is legally meaningless.</li><li>RLS must be tested with an integration test using two real users (Supabase local docker) before shipping - a mocked unit test can never catch an RLS policy bug.</li><li><code>shared_with uuid[]</code> with a GIN index is the right choice for Postgres: <code>ANY(shared_with)</code> in the RLS policy uses the index more efficiently than <code>@&gt;</code> with a large array.</li><li>Debounce 2000ms on <code>push()</code> is a balance between responsive (data reaches the cloud fast) and reducing request count (avoiding spam when the user edits quickly). This value can be tuned once there is real data.</li><li>The <code>OccurrenceCache</code> must be invalidated after a pull because the cache stores computed solar dates - if there is a new reminder from the cloud, the stale cache shows the wrong date. This is the most common cause of the \"wrong reminder date\" bug after syncing.</li><li>The invite token JWT needs <code>jti</code> (JWT ID) to be a unique UUID and must write <code>used_at</code> right on accept - you cannot use plain standard JWT expiry alone because single-use enforcement is needed.</li><li>The <code>title</code> field containing the name of the deceased: in the future it SHOULD be encrypted at the application layer before INSERT (AES-256-GCM with a key the user holds). This is not implemented yet, but the <code>title</code> column is already protected by RLS and is not exposed via the anon endpoint - this is a temporary risk-mitigation measure.</li><li><code>DELETE /api/sync/account</code> needs a clear confirm UI (\"All cloud data will be permanently deleted\") with no undo - this is a PDPL right the user has, not an optional feature.</li><li>The P3 migration numbers are distributed to avoid collisions: 0016-0017 for FR-018, 0018 for FR-017, 0019 for FR-019, 0020 for FR-020.</li></ul>\n<p><em>End of FR-LUNAR-018.</em></p>";

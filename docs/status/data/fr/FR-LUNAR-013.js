window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LUNAR-013"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>The iOS glanceable surfaces feature MUST provide a WidgetKit home-screen widget showing today's lunar date, can-chi, and auspicious hours, written in native Swift/SwiftUI in the Xcode target <code>LunarWidget</code> (DEC-LUNAR-130).</p>\n<ol><li>MUST add the native Swift target <code>LunarWidget</code> to the Xcode project <code>apps/web/ios/App/App.xcodeproj</code> using the <code>WidgetKit</code> framework; this target runs in a separate widget extension process, fully separate from the Capacitor web view (DEC-LUNAR-130).</li><li>MUST configure the App Group <code>group.world.cyberskill.genie</code> in both the App target (Capacitor) and the Widget target; this is the only mechanism for sharing data (DEC-LUNAR-131).</li><li>MUST define <code>struct DayInfoCache: Codable</code> containing the fields: <code>dateISO</code> (String), <code>lunarDayMonth</code> (String, e.g. \"15 thang 7\"), <code>canChiNgay</code> (String), <code>isHoangDao</code> (Bool), <code>label</code> (String), <code>trucName</code> (String), <code>sao28Name</code> (String), <code>gioHoangDao</code> ([GioInfoCache]) with <code>cachedAt</code> (Date) and <code>ttlHours: Int = 24</code> (DEC-LUNAR-131).</li><li>MUST write the <code>widget-cache-writer.ts</code> function on the web layer: when the app is opened (Capacitor <code>App.addListener(\"appStateChange\")</code>), compute <code>DayQuality</code> for today using FR-LUNAR-011, encode it to JSON, and write it into the App Group UserDefaults suite <code>group.world.cyberskill.genie</code> with the key <code>\"dayInfoCache\"</code> (DEC-LUNAR-131).</li><li>MUST write <code>DayInfoCache.swift</code> with the function <code>static func load() -&gt; DayInfoCache?</code> that reads from <code>UserDefaults(suiteName: \"group.world.cyberskill.genie\")</code> and <code>static func isStale(_ cache: DayInfoCache) -&gt; Bool</code> that checks <code>cachedAt</code> + <code>ttlHours</code> (DEC-LUNAR-131).</li><li>MUST write <code>LunarCalcSwift.swift</code> re-implementing the 2 minimum functions - <code>convertSolar2Lunar</code> and <code>canChiNgayFromJDN</code> - with the exact constants from PRD §6.2 (epoch index-k <code>2415021.076998695</code>, Meeus epoch <code>2415020.75933</code>, Meeus synodic <code>29.53058868</code>, integer epoch <code>2415021</code>); this correctness lets the widget compute the day when the cache is stale (DEC-LUNAR-132). <code>canChiNgayFromJDN</code> MUST use the exact offsets of FR-LUNAR-002 <code>canChiDay</code>: <code>canIndex = (jdn + 9) % 10</code> and <code>diaChiIndex = (jdn + 1) % 12</code>; MUST NOT derive the earthly branch by <code>(jdn + 9) % 60</code> then <code>% 12</code> (which gives <code>(jdn + 9) % 12</code>, off by 8 relative to core and off from the can-chi the app/web displays) (DEC-LUNAR-132).</li><li>MUST write a <code>TimelineProvider</code> returning <code>Timeline&lt;LunarEntry&gt;</code> with hourly entries: each entry's <code>date</code> is the start of the corresponding gio, so the auspicious/inauspicious hours update by the hour during the day without opening the app (PRD FR-F01).</li><li>MUST write <code>LunarWidgetEntryView</code> with SwiftUI supporting 3 sizes:</li></ol>\n<ul><li><code>systemSmall</code> (MUST): large lunar date + can-chi + a \"Hoang dao\" or \"Hac dao\" badge (different colors);</li><li><code>systemMedium</code> (SHOULD): add <code>trucName</code>, the top 3 auspicious hours;</li><li><code>systemLarge</code> (SHOULD): add <code>sao28Name</code> and the full list of 12 gio with an isHoang flag.</li></ul>\n<ol><li>MUST use colors consistent with the purple theme (FR-LUNAR-009): cream background, deep purple text, a yellow/green Hoang dao badge, a gray/light-red Hac dao badge - ensuring enough contrast to be legible in the WidgetKit rendering context (DEC-LUNAR-131).</li><li>MUST NOT call <code>URLSession</code>, <code>URLRequest</code>, or any network API from inside the widget extension process; all data must come from the App Group cache or local <code>LunarCalcSwift</code> (DEC-LUNAR-131, NFR-Offline).</li><li>MUST NOT call <code>UserDefaults.standard.set(...)</code> or write any state from the widget; the widget only reads (DEC-LUNAR-134).</li><li>SHOULD implement the Watch complication (COULD) with watchOS 2 <code>CLKComplicationDataSource</code> or WidgetKit for watchOS (watchOS 9+): letting a small widget display the lunar date + can-chi on the Watch face; this is the optional target <code>LunarWatchWidget</code> (DEC-LUNAR-133).</li><li>MUST write <code>LunarWidgetTests</code> using <code>XCTest</code> to check <code>LunarCalcSwift</code> with the fixtures from PRD §6.6: 29/01/2025 is 1/1 of Year At Ty (the YEAR can-chi; the DAY can-chi is Mau Tuat), Tet 2024/2023, leap year 1985 month 2. (The minimum contract only has <code>convertSolar2Lunar</code>, not <code>convertLunar2Solar</code>, so check S2L directly on the fixtures instead of a round-trip L2S(S2L).)</li><li>MUST have <code>getSnapshot()</code> in the <code>TimelineProvider</code> returning a fast snapshot (&lt; 1s) when the system previews the widget; the snapshot uses the cache if available, otherwise computes directly for <code>Date()</code> with <code>LunarCalcSwift</code>.</li><li>MUST keep the timezone consistent within <code>LunarCalcSwift</code>: every timezone-dependent function (<code>getNewMoonDay</code>, <code>getSunLongitude</code>, <code>getLunarMonth11</code>, <code>getLeapMonthOffset</code>, <code>convertSolar2Lunar</code>) MUST use exactly one constant <code>TZ_VN = 7.0</code> (105E) internally; MUST NOT have one function take <code>tz</code> while another ignores it, because a timezone-convention mismatch corrupts the new-moon day / month 11 / leap month (DEC-LUNAR-130/132, PRD 6.1 rule 5, FR-LUNAR-001 DEC-LUNAR-010).</li><li>MUST have an XCTest asserting that the earthly branch / can-chi of <code>canChiNgayFromJDN</code> is consistent with FR-LUNAR-002 <code>canChiDay</code> across a multi-day scan (&gt;= 60 days): for each JDN, <code>diaChiIndex == (jdn + 1) % 12</code> and <code>canChi</code> matches core's can-chi label; this is the safety net that catches the earthly-branch off-by-8 caused by <code>(jdn + 9) % 60</code> (DEC-LUNAR-132).</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why require writing it in native Swift/SwiftUI (DEC-LUNAR-130)?</strong> WidgetKit can only use SwiftUI; there is no Swift bridge from Capacitor into the widget extension process. This is an Apple limitation, not a Capacitor one. PRD §9 confirms this: \"The iOS widget (WidgetKit) and the Watch complication must be written in native Swift within an ios/App folder (Capacitor allows adding a native target).\" Capacitor still manages the main app; the widget is an extension target running its own process.</p>\n<p><strong>Why use App Group UserDefaults to share data (DEC-LUNAR-131)?</strong> There are 3 ways: (1) App Group UserDefaults - fast, simple, suited to a small payload; (2) App Group File (plist/json) - more complex but similar; (3) CloudKit - needs the network. Option 1 is best for data &lt; 10KB such as DayInfoCache. Writing from TypeScript via the Capacitor bridge when the app opens, and the widget reading when it renders - a clean one-way data flow.</p>\n<p><strong>Why re-implement <code>LunarCalcSwift</code> instead of only using the cache (DEC-LUNAR-132)?</strong> The cache has a 24-hour TTL. If the user has not opened the app in 24 hours (e.g. goes to sleep at 23:00, wakes at 7:00 the next day), the cache is stale. With <code>LunarCalcSwift</code> the widget can compute directly - a clean fallback. PRD §6.2 provides all the constants - re-implementing 2 functions is ~100 lines of Swift, nothing scary.</p>\n<p><strong>Why is the Watch complication COULD (DEC-LUNAR-133)?</strong> PRD FR-F02 marks it \"optional.\" The WidgetKit widget is a clear deliverable (FR-F01 MUST); the Watch adds complexity: it needs a separate watchOS target, watch-extension distribution setup, and testing on a real Watch device (the simulator is incomplete). Shipping the iOS widget first, gathering feedback, then deciding whether the Watch is needed is the better path.</p>\n<p><strong>Why does the widget only read and not write (DEC-LUNAR-134)?</strong> The widget extension runs a different process from the app. If the widget writes UserDefaults while the app also writes, it can cause a race condition. More importantly: WidgetKit timeline refresh can call the widget at any time; if the widget wrote state, that state could change while the user is in the main app. Keeping the widget read-only is the safest.</p>\n<p><strong>Why does the timeline have hourly entries (§1 #7)?</strong> The auspicious hours change every 2 hours. With only 1 entry/day the widget would show the same auspicious hour from 23:00 the previous day until 22:59 the next day - wrong. Creating 12 entries (one per gio) with <code>date</code> at the start of each gio lets WidgetKit automatically display the appropriate entry for the current hour.</p>\n<p><strong>Why test Swift with <code>XCTest</code> against PRD §6.6 fixtures (§1 #13)?</strong> <code>LunarCalcSwift</code> is a re-implementation - the risk of a small error is higher than a TypeScript port. Peer fixtures from PRD §6.6 (Tet 2023-2026, 1985 leap) are the fastest way to check that the Swift epoch constants match the TypeScript core. If the two sides give different results for the same fixture, the bug is caught immediately.</p>\n<p><strong>Why must <code>getSnapshot()</code> be fast (§1 #14)?</strong> iOS calls <code>getSnapshot()</code> when the user adds the widget to the screen or when the system previews it. If it is slow &gt; 1-2s, the system cancels the call and shows a blank placeholder. The snapshot must fast-path: read the cache if available, compute directly otherwise (&lt; 100ms with LunarCalcSwift).</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-swift\">// apps/web/ios/App/LunarWidget/LunarCalcSwift.swift\n\nimport Foundation\n\n/// Minimal re-implementation of Ho Ngoc Duc algorithm for widget use.\n/// Uses EXACTLY the PRD §6.2 constants; any change MUST be validated against\n/// LunarCalcSwift fixtures in LunarWidgetTests.\nstruct LunarCalcSwift {\n    // PRD §6.2 constants - do NOT change without updating test fixtures\n    static let SYNODIC_MONTH_K     = 29.530588853       // index-k synodic\n    static let EPOCH_INDEX_K       = 2415021.076998695   // epoch for index-k\n    static let MEEUS_EPOCH         = 2415020.75933       // NewMoon inner epoch\n    static let MEEUS_SYNODIC       = 29.53058868         // NewMoon inner synodic\n    static let LUNAR_MONTH_11_INT  = 2415021             // getLunarMonth11 integer epoch\n    static let J2000               = 2451545.0\n    static let JULIAN_CENTURY      = 36525.0\n    static let DR: Double          = Double.pi / 180.0\n    static let TZ_VN: Double       = 7.0                 // UTC+7, 105E\n\n    // tz nhat quan: MOI ham duoi day hardcode TZ_VN (= 7.0, 105E) ben trong\n    // (ung getNewMoonDay/getSunLongitude voi + tz/24 = + TZ_VN/24). KHONG nhan tz lam tham so\n    // o ham nay nhung nhan ngam o ham khac - giu tat ca cung mot quy uoc (DEC-LUNAR-132, PRD 6.2).\n    static func jdFromDate(day: Int, month: Int, year: Int) -&gt; Int\n    static func newMoon(k: Int) -&gt; Double      // JD of kth new moon (Meeus)\n    static func sunLongitude(jdn: Double) -&gt; Double\n    static func getNewMoonDay(k: Int) -&gt; Int                 // dung TZ_VN noi bo: floor(newMoon(k) + 0.5 + TZ_VN/24)\n    static func getSunLongitude(dayNumber: Double) -&gt; Int    // dung TZ_VN noi bo: floor(sunLongitude(dayNumber - 0.5 - TZ_VN/24)/PI*6)\n    static func getLunarMonth11(year: Int) -&gt; Int            // dung TZ_VN noi bo\n    static func getLeapMonthOffset(a11: Int) -&gt; Int          // dung TZ_VN noi bo\n    static func convertSolar2Lunar(day: Int, month: Int, year: Int) -&gt; (lunarDay: Int, lunarMonth: Int, lunarYear: Int, isLeap: Bool)  // dung TZ_VN noi bo\n    static func canChiNgayFromJDN(_ jdn: Int) -&gt; (canChi: String, diaChiIndex: Int)\n        // PHAI khop FR-LUNAR-002 canChiDay: canIndex = (jdn + 9) % 10, diaChiIndex = (jdn + 1) % 12.\n        // KHONG dung (jdn + 9) % 60 roi % 12 (cho (jdn + 9) % 12, lech 8 so voi core).\n}</code></pre>\n<pre><code class=\"language-swift\">// apps/web/ios/App/LunarWidget/DayInfoCache.swift\n\nimport Foundation\nimport WidgetKit\n\nstruct GioInfoCache: Codable {\n    let canh: String           // e.g. \"Dan (03:00-05:00)\"\n    let tuGio: String          // e.g. \"03:00\"\n    let denGio: String         // e.g. \"05:00\"\n    let isHoang: Bool\n}\n\nstruct DayInfoCache: Codable {\n    let dateISO: String        // \"YYYY-MM-DD\"\n    let lunarDayMonth: String  // e.g. \"1 thang 1\" (Tet)\n    let canChiNgay: String     // e.g. \"At Ty\"\n    let isHoangDao: Bool\n    let label: String          // \"Hoang dao\" | \"Hac dao\"\n    let trucName: String\n    let sao28Name: String\n    let gioHoangDao: [GioInfoCache]   // 12 canh\n    let cachedAt: Date\n    let ttlHours: Int\n\n    static let appGroupSuite = \"group.world.cyberskill.genie\"\n    static let cacheKey      = \"dayInfoCache\"\n\n    static func load() -&gt; DayInfoCache? {\n        guard let defaults = UserDefaults(suiteName: appGroupSuite),\n              let data = defaults.data(forKey: cacheKey)\n        else { return nil }\n        return try? JSONDecoder().decode(DayInfoCache.self, from: data)\n    }\n\n    static func isStale(_ cache: DayInfoCache) -&gt; Bool {\n        let age = Date().timeIntervalSince(cache.cachedAt) / 3600.0\n        return age &gt; Double(cache.ttlHours)\n    }\n}</code></pre>\n<pre><code class=\"language-swift\">// apps/web/ios/App/LunarWidget/LunarWidget.swift\n\nimport WidgetKit\nimport SwiftUI\n\nstruct LunarEntry: TimelineEntry {\n    let date: Date      // start of the canh gio this entry represents\n    let cache: DayInfoCache\n    let currentGio: GioInfoCache?   // gio entry for this date/hour slot\n}\n\nstruct LunarTimelineProvider: TimelineProvider {\n    typealias Entry = LunarEntry\n\n    func placeholder(in context: Context) -&gt; LunarEntry\n\n    /// Must return quickly (&lt; 1s). Use cache if fresh; compute via LunarCalcSwift if stale.\n    func getSnapshot(in context: Context, completion: @escaping (LunarEntry) -&gt; Void)\n\n    /// Returns 12 entries for 12 canh gio of today (one per 2-hour slot).\n    /// Reload policy: .atEnd (reloaded after last entry expires at midnight).\n    func getTimeline(in context: Context, completion: @escaping (Timeline&lt;LunarEntry&gt;) -&gt; Void)\n}\n\n@main\nstruct LunarWidgetBundle: WidgetBundle {\n    var body: some Widget {\n        LunarWidgetSmall()\n        // LunarWatchWidget() - COULD: add when watchOS target is built\n    }\n}\n\nstruct LunarWidgetSmall: Widget {\n    static let kind = \"LunarWidgetSmall\"\n    var body: some WidgetConfiguration {\n        StaticConfiguration(kind: Self.kind, provider: LunarTimelineProvider()) { entry in\n            LunarWidgetEntryView(entry: entry)\n        }\n        .configurationDisplayName(\"Genie Am Lich\")\n        .description(\"Ngay am, can-chi, gio Hoang dao\")\n        .supportedFamilies([.systemSmall, .systemMedium, .systemLarge])\n    }\n}</code></pre>\n<pre><code class=\"language-swift\">// apps/web/ios/App/LunarWidget/LunarWidgetEntryView.swift (SwiftUI)\n\nimport SwiftUI\nimport WidgetKit\n\nstruct LunarWidgetEntryView: View {\n    @Environment(\\.widgetFamily) var family\n    let entry: LunarEntry\n\n    var body: some View {\n        switch family {\n        case .systemSmall: SmallView(entry: entry)\n        case .systemMedium: MediumView(entry: entry)\n        case .systemLarge: LargeView(entry: entry)\n        default: SmallView(entry: entry)\n        }\n    }\n}\n// SmallView: VStack { Text(lunarDayMonth).large, Text(canChiNgay), HoangDaoBadge }\n// MediumView: SmallView + HStack(top3HoangGio) + Text(trucName)\n// LargeView:  MediumView + Text(sao28Name) + List(all 12 gioHoangDao)</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/lib/widget-cache-writer.ts\n\nimport { App } from \"@capacitor/app\";\nimport { getDayQuality } from \"@cyberskill/amlich-core\";\n\nconst APP_GROUP_KEY = \"dayInfoCache\";\n\nexport async function writeWidgetCache(date: Date = new Date()): Promise&lt;void&gt; {\n  const dq = getDayQuality(date);\n  const cache = {\n    dateISO:       dq.date,\n    lunarDayMonth: `${dq.lunarDay} thang ${dq.lunarMonth}`,\n    canChiNgay:    dq.canChiNgay,\n    isHoangDao:    dq.isHoangDao,\n    label:         dq.label,\n    trucName:      dq.truc.name,\n    sao28Name:     dq.sao28.name,\n    gioHoangDao:   dq.gioHoangDao,\n    cachedAt:      new Date().toISOString(),\n    ttlHours:      24,\n  };\n  // Write to App Group via Capacitor native bridge plugin\n  await (window as any).Capacitor?.nativeCallback?.(\"AppGroupStorage\", \"write\", {\n    suite: \"group.world.cyberskill.genie\",\n    key:   APP_GROUP_KEY,\n    value: JSON.stringify(cache),\n  });\n}\n\nexport function registerWidgetCacheWriter(): void {\n  App.addListener(\"appStateChange\", ({ isActive }) =&gt; {\n    if (isActive) writeWidgetCache().catch(console.error);\n  });\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>The <code>LunarWidget</code> target builds successfully with <code>xcodebuild build -scheme LunarWidget</code> with no warning or error.</li><li><code>LunarWidgetTests</code> with the 29/01/2025 fixture: <code>LunarCalcSwift.convertSolar2Lunar(day: 29, month: 1, year: 2025)</code> returns <code>(1, 1, 2025, false)</code> (day 1 month 1 At Ty) - matching the TypeScript core.</li><li><code>LunarWidgetTests</code> with the 1985 leap-year fixture: <code>LunarCalcSwift.convertSolar2Lunar(day: 21, month: 3, year: 1985)</code> returns leap month 2 correctly.</li><li><code>LunarCalcSwift.canChiNgayFromJDN</code> returns <code>\"Mau Tuat\"</code> (the DAY can-chi) for the JDN corresponding to 29/01/2025, and <code>diaChiIndex == (jdn + 1) % 12</code>; it MUST NOT return \"At Ty\" (that is the YEAR can-chi, not the day) - it must match <code>canChiDay</code> of FR-LUNAR-002.</li><li><code>DayInfoCache.load()</code> returns <code>nil</code> if the App Group has no data yet (no crash).</li><li><code>DayInfoCache.isStale()</code> returns <code>true</code> if <code>cachedAt</code> is 25 hours ago (TTL = 24).</li><li>The widget's <code>getTimeline</code> creates exactly 12 <code>LunarEntry</code>, each entry's <code>date</code> spaced 2 hours apart (canh gio), reload policy <code>.atEnd</code> at 24:00.</li><li><code>getSnapshot</code> completes in &lt; 1 second (measured with XCTest <code>measure</code>).</li><li>The widget does not call <code>URLSession.shared.dataTask</code> or any network API (verified with a mock URLProtocol in the test or by code review).</li><li>The widget does not call <code>UserDefaults.standard.set(...)</code> (verified by code review; only reads via the suite).</li><li>The <code>systemSmall</code> view displays exactly 3 elements: <code>lunarDayMonth</code>, <code>canChiNgay</code>, and a \"Hoang dao\"/\"Hac dao\" badge - checked with <code>ViewInspector</code> or a snapshot test.</li><li><code>widget-cache-writer.ts</code> writes data when <code>appStateChange.isActive === true</code> and does NOT write when <code>isActive === false</code>.</li><li>The <code>LunarWidget</code> bundle is registered in <code>App.xcodeproj</code> with the App Group entitlement <code>group.world.cyberskill.genie</code>.</li><li>TypeScript <code>writeWidgetCache</code> produces valid JSON: <code>JSON.parse(output)</code> does not throw, the required fields are present.</li><li><code>canChiNgayFromJDN</code> is consistent with FR-LUNAR-002 <code>canChiDay</code> across a scan of &gt;= 60 consecutive days: <code>diaChiIndex == (jdn + 1) % 12</code> and <code>canChi</code> matches <code>\"&lt;Can&gt; &lt;Chi&gt;\"</code> from the core formula for every day (test <code>testCanChi_MatchesCore_60DaySweep</code>).</li><li><code>LunarCalcSwift</code> uses <code>TZ_VN = 7.0</code> consistently for every timezone-dependent function; no function uses a different tz or ignores tz (code review + the Tet/1985 fixtures still passing as evidence).</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-swift\">// apps/web/ios/App/LunarWidgetTests/LunarWidgetTests.swift\nimport XCTest\n@testable import LunarWidget\n\nfinal class LunarCalcSwiftTests: XCTestCase {\n\n    // PRD §6.6 fixture: Tet 2025\n    func testConvertSolar_Tet2025() {\n        let result = LunarCalcSwift.convertSolar2Lunar(day: 29, month: 1, year: 2025)\n        XCTAssertEqual(result.lunarDay,   1)\n        XCTAssertEqual(result.lunarMonth, 1)\n        XCTAssertEqual(result.lunarYear,  2025)\n        XCTAssertFalse(result.isLeap, \"Tet 2025 khong phai thang nhuan\")\n    }\n\n    // PRD §6.6 fixture: Tet 2023 - nam Meo\n    func testConvertSolar_Tet2023() {\n        let result = LunarCalcSwift.convertSolar2Lunar(day: 22, month: 1, year: 2023)\n        XCTAssertEqual(result.lunarDay,   1)\n        XCTAssertEqual(result.lunarMonth, 1)\n        XCTAssertEqual(result.lunarYear,  2023)\n    }\n\n    // PRD §6.6 fixture: 1985 leap month 2\n    func testConvertSolar_1985LeapMonth() {\n        // 21/03/1985 -&gt; thang 2 nhuan nam At Suu\n        let result = LunarCalcSwift.convertSolar2Lunar(day: 21, month: 3, year: 1985)\n        XCTAssertEqual(result.lunarMonth, 2)\n        XCTAssertTrue(result.isLeap, \"Thang 2 nhuan 1985 phai duoc nhan biet\")\n    }\n\n    // PRD §6.6 fixture: VN/TQ offset 2007\n    func testConvertSolar_2007VNOffset() {\n        // Tet 2007 VN: 17/02/2007\n        let vnResult = LunarCalcSwift.convertSolar2Lunar(day: 17, month: 2, year: 2007)\n        XCTAssertEqual(vnResult.lunarDay, 1, \"Tet VN 2007 phai la 1/1 AM\")\n        // 18/02/2007 khong phai Tet theo lich VN\n        let day18 = LunarCalcSwift.convertSolar2Lunar(day: 18, month: 2, year: 2007)\n        XCTAssertNotEqual(day18.lunarDay, 1, \"18/02/2007 la ngay 2 AM theo lich VN\")\n    }\n\n    func testCanChiNgay_Tet2025() {\n        // Day pillar (can-chi NGAY) cho 29/01/2025 theo FR-002 canChiDay:\n        //   can = (jdn + 9) % 10, dia chi = (jdn + 1) % 12 -&gt; \"Mau Tuat\".\n        // (Luu y: PRD 6.6 ghi \"At Ty\" la can-chi NAM, khong phai can-chi ngay.)\n        let jdn = LunarCalcSwift.jdFromDate(day: 29, month: 1, year: 2025)\n        let (canChi, diaChiIndex) = LunarCalcSwift.canChiNgayFromJDN(jdn)\n        XCTAssertEqual(canChi, \"Mau Tuat\", \"Ngay 29/01/2025 la ngay Mau Tuat (can-chi ngay)\")\n        XCTAssertEqual(diaChiIndex, (jdn + 1) % 12, \"dia chi PHAI = (jdn + 1) % 12, khop FR-002\")\n    }\n\n    // AC #16 - dia chi/can-chi PHAI dong nhat voi FR-LUNAR-002 canChiDay qua quet nhieu ngay.\n    // Bat loi lay dia chi bang (jdn + 9) % 60 % 12 (lech 8 so voi core).\n    func testCanChi_MatchesCore_60DaySweep() {\n        let CAN = [\"Giap\",\"At\",\"Binh\",\"Dinh\",\"Mau\",\"Ky\",\"Canh\",\"Tan\",\"Nham\",\"Quy\"]\n        let CHI = [\"Ty\",\"Suu\",\"Dan\",\"Mao\",\"Thin\",\"Ti\",\"Ngo\",\"Mui\",\"Than\",\"Dau\",\"Tuat\",\"Hoi\"]\n        let startJdn = LunarCalcSwift.jdFromDate(day: 1, month: 1, year: 2025)\n        for offset in 0..&lt;60 {\n            let jdn = startJdn + offset\n            let (canChi, diaChiIndex) = LunarCalcSwift.canChiNgayFromJDN(jdn)\n            // gia tri ky vong tu cong thuc core (FR-002 DEC-LUNAR-020)\n            let expectedCan = CAN[(jdn + 9) % 10]\n            let expectedChi = CHI[(jdn + 1) % 12]\n            XCTAssertEqual(diaChiIndex, (jdn + 1) % 12, \"dia chi lech tai jdn=\\(jdn)\")\n            XCTAssertEqual(canChi, \"\\(expectedCan) \\(expectedChi)\", \"can-chi lech tai jdn=\\(jdn)\")\n        }\n    }\n\n    func testConvertSolar2Lunar_Tet2025_Identity() {\n        // S2L cho Tet 2025 = (1, 1, 2025, khong nhuan). (Khong co convertLunar2Solar trong\n        // LunarCalcSwift toi thieu, nen kiem tra S2L truc tiep thay vi round-trip L2S(S2L).)\n        let (ld, lm, ly, isLeap) = LunarCalcSwift.convertSolar2Lunar(day: 29, month: 1, year: 2025)\n        XCTAssertEqual(ld, 1); XCTAssertEqual(lm, 1); XCTAssertEqual(ly, 2025); XCTAssertFalse(isLeap)\n        // Verify epoch constants are not swapped\n        XCTAssertEqual(LunarCalcSwift.EPOCH_INDEX_K, 2415021.076998695, accuracy: 1e-9)\n        XCTAssertEqual(LunarCalcSwift.MEEUS_EPOCH,   2415020.75933,      accuracy: 1e-9)\n    }\n}\n\nfinal class DayInfoCacheTests: XCTestCase {\n\n    func testLoad_WhenNoCacheExists_ReturnsNil() {\n        // Clear App Group suite in test environment\n        UserDefaults(suiteName: \"group.world.cyberskill.genie.test\")?.removeObject(forKey: \"dayInfoCache\")\n        XCTAssertNil(DayInfoCache.load())\n    }\n\n    func testIsStale_25HoursOld_ReturnsTrue() {\n        let oldDate = Date(timeIntervalSinceNow: -25 * 3600)\n        let cache = DayInfoCache(\n            dateISO: \"2025-01-29\", lunarDayMonth: \"1 thang 1\",\n            canChiNgay: \"At Ty\", isHoangDao: true, label: \"Hoang dao\",\n            trucName: \"Khai\", sao28Name: \"Tinh\", gioHoangDao: [],\n            cachedAt: oldDate, ttlHours: 24\n        )\n        XCTAssertTrue(DayInfoCache.isStale(cache))\n    }\n\n    func testIsStale_1HourOld_ReturnsFalse() {\n        let recentDate = Date(timeIntervalSinceNow: -3600)\n        let cache = DayInfoCache(\n            dateISO: \"2025-01-29\", lunarDayMonth: \"1 thang 1\",\n            canChiNgay: \"At Ty\", isHoangDao: true, label: \"Hoang dao\",\n            trucName: \"Khai\", sao28Name: \"Tinh\", gioHoangDao: [],\n            cachedAt: recentDate, ttlHours: 24\n        )\n        XCTAssertFalse(DayInfoCache.isStale(cache))\n    }\n}\n\nfinal class TimelineTests: XCTestCase {\n\n    func testGetTimeline_Creates12Entries() {\n        let provider = LunarTimelineProvider()\n        let expectation = self.expectation(description: \"getTimeline\")\n        provider.getTimeline(in: .init(), completion: { timeline in\n            XCTAssertEqual(timeline.entries.count, 12, \"12 canh gio trong 1 ngay\")\n            XCTAssertEqual(timeline.policy, .atEnd)\n            expectation.fulfill()\n        })\n        wait(for: [expectation], timeout: 3.0)\n    }\n\n    func testGetSnapshot_CompletesUnder1Second() {\n        let provider = LunarTimelineProvider()\n        measure {\n            let exp = self.expectation(description: \"snapshot\")\n            provider.getSnapshot(in: .init()) { _ in exp.fulfill() }\n            wait(for: [exp], timeout: 1.0)\n        }\n    }\n}</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>Swift side: <code>LunarCalcSwift.convertSolar2Lunar</code> is a direct port from TypeScript - the same <code>jdFromDate</code>, <code>NewMoon(k)</code>, <code>SunLongitude(jdn)</code>, <code>getLunarMonth11</code>, <code>getLeapMonthOffset</code> formulas, and all use <code>TZ_VN = 7.0</code> (105E) internally (no function deviates from the timezone convention). Constants are copied verbatim from PRD §6.2 - nothing changed. <code>canChiNgayFromJDN</code> MUST use <code>canIndex = (jdn + 9) % 10</code> and <code>diaChiIndex = (jdn + 1) % 12</code> - the exact offsets of FR-LUNAR-002 <code>canChiDay</code>; do NOT use <code>(jdn + 9) % 60</code> then <code>% 12</code> (off by 8). <code>getTimeline</code> creates <code>startOfDay(for: Date())</code>, loops 12 times (0, 2, 4, ... 22 hours), each step creating a <code>LunarEntry</code> with <code>date = startOfDay + i*2h</code> and <code>currentGio = cache.gioHoangDao[i]</code>.</p>\n<p>TypeScript side: <code>widget-cache-writer.ts</code> needs a native Capacitor plugin <code>AppGroupStorage</code> to write into the App Group. If this plugin does not exist yet, a simple iOS native plugin (one <code>write(suite, key, value)</code> function) can be written in <code>ios/App/App/AppGroupStoragePlugin.swift</code>.</p>\n<p>The hardest point: TTL synchronization. If the user has not opened the app in 24+ hours, the cache is stale and the widget must compute with <code>LunarCalcSwift</code>. The <code>getTimeline</code> fallback function should combine: try to read the cache first, if stale compute directly and write it back to the cache.</p>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream: FR-LUNAR-001 is the provider of the epoch constants and the original algorithm (LunarCalcSwift is ported from it). FR-LUNAR-002 provides the <code>canChi</code> logic that LunarCalcSwift must re-implement. FR-LUNAR-011 is the source that computes <code>DayQuality</code> for <code>widget-cache-writer.ts</code> on the TypeScript side.</p>\n<p>Downstream: FR-LUNAR-013 does not block any other FR.</p>\n<p>Cross-cutting: the App Group ID <code>group.world.cyberskill.genie</code> must be registered with the Apple Developer portal in the Capabilities of both the App target and the Widget target. The purple theme (FR-009) should apply to the SwiftUI views - an Asset Catalog color set can be used instead of importing the TS tokens.</p>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"dateISO\":       \"2025-01-29\",\n  \"lunarDayMonth\": \"1 thang 1\",\n  \"canChiNgay\":    \"At Suu\",\n  \"isHoangDao\":    true,\n  \"label\":         \"Hoang dao\",\n  \"trucName\":      \"Khai\",\n  \"sao28Name\":     \"Tinh\",\n  \"gioHoangDao\": [\n    { \"canh\": \"Ty (23:00-01:00)\",   \"tuGio\": \"23:00\", \"denGio\": \"01:00\", \"isHoang\": true  },\n    { \"canh\": \"Suu (01:00-03:00)\",  \"tuGio\": \"01:00\", \"denGio\": \"03:00\", \"isHoang\": false },\n    { \"canh\": \"Dan (03:00-05:00)\",  \"tuGio\": \"03:00\", \"denGio\": \"05:00\", \"isHoang\": true  },\n    { \"canh\": \"Meo (05:00-07:00)\",  \"tuGio\": \"05:00\", \"denGio\": \"07:00\", \"isHoang\": false },\n    { \"canh\": \"Thin (07:00-09:00)\", \"tuGio\": \"07:00\", \"denGio\": \"09:00\", \"isHoang\": true  },\n    { \"canh\": \"Ti (09:00-11:00)\",   \"tuGio\": \"09:00\", \"denGio\": \"11:00\", \"isHoang\": false },\n    { \"canh\": \"Ngo (11:00-13:00)\",  \"tuGio\": \"11:00\", \"denGio\": \"13:00\", \"isHoang\": true  },\n    { \"canh\": \"Mui (13:00-15:00)\",  \"tuGio\": \"13:00\", \"denGio\": \"15:00\", \"isHoang\": false },\n    { \"canh\": \"Than (15:00-17:00)\", \"tuGio\": \"15:00\", \"denGio\": \"17:00\", \"isHoang\": true  },\n    { \"canh\": \"Dau (17:00-19:00)\",  \"tuGio\": \"17:00\", \"denGio\": \"19:00\", \"isHoang\": false },\n    { \"canh\": \"Tuat (19:00-21:00)\", \"tuGio\": \"19:00\", \"denGio\": \"21:00\", \"isHoang\": true  },\n    { \"canh\": \"Hoi (21:00-23:00)\",  \"tuGio\": \"21:00\", \"denGio\": \"23:00\", \"isHoang\": false }\n  ],\n  \"cachedAt\": \"2025-01-29T06:30:00.000Z\",\n  \"ttlHours\": 24\n}</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li>\"Which lunar-calendar library does Swift use?\" -&gt; DEC-LUNAR-132: re-implement from the PRD §6.2 constants + reference \"baolanlequang/VietnameseLunar-ios\" MIT.</li><li>\"Is the Watch SHOULD or COULD?\" -&gt; DEC-LUNAR-133: COULD.</li><li>\"By what mechanism is data shared?\" -&gt; DEC-LUNAR-131: App Group UserDefaults.</li></ul>\n<p>Remaining (defer):</p>\n<ul><li><code>AppGroupStoragePlugin</code> (a native Capacitor plugin) must be written new or an existing plugin found (e.g. <code>capacitor-community/app-groups</code>). If no plugin exists yet, ~50 lines of Swift are needed and it must be registered with Capacitor in <code>AppDelegate</code>. This is an implementation detail, not a design decision.</li><li>Widget configuration (whether the user can choose the widget size or view multiple widgets) - defer to Phase 3.</li><li>Interactive widget (iOS 17+ <code>Button</code> in the widget to dismiss a reminder) - defer; Phase 1 only displays read-only.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Wrong LunarCalcSwift epoch constant</td><td>XCTest AC #2 (Tet 2025 fixture)</td><td>Test fails, build blocked</td><td>Check the copy from PRD §6.2</td></tr><tr><td>Earthly branch taken as <code>(jdn+9)%60 % 12</code> (off by 8 relative to core)</td><td>XCTest AC #15 (<code>testCanChi_MatchesCore_60DaySweep</code>)</td><td>Widget can-chi off from the app/calendar can-chi</td><td>Use <code>(jdn + 1) % 12</code> for the earthly branch (matches FR-002)</td></tr><tr><td>Timezone-convention mismatch across LunarCalc functions</td><td>Tet/1985 fixtures fail + AC #16 code review</td><td>New-moon day / month 11 / leap month wrong</td><td>Every function uses <code>TZ_VN = 7.0</code> internally</td></tr><tr><td>1985 leap month wrong in Swift</td><td>XCTest AC #3</td><td>Test fails</td><td>Debug <code>getLeapMonthOffset</code> in Swift</td></tr><tr><td>Wrong or unassigned App Group ID</td><td>Widget returns nil cache + potential crash</td><td>Widget blank</td><td>Check the Developer portal Capabilities</td></tr><tr><td>Cache stale, LunarCalcSwift fallback slow</td><td>XCTest AC #8 measure</td><td>Widget shows a placeholder</td><td>Profile the fallback path</td></tr><tr><td>widget-cache-writer.ts does not write</td><td>Cache nil, widget uses the LunarCalcSwift fallback</td><td>OK but slower</td><td>Debug AppGroupStoragePlugin</td></tr><tr><td>Widget calls URLSession</td><td>Code review / AC #9</td><td>App Store policy violation</td><td>Remove the code, use local data</td></tr><tr><td>Widget writes UserDefaults.standard</td><td>Code review / AC #10</td><td>Race condition with the main app</td><td>Replace with read-only</td></tr><tr><td>Timeline has fewer than 12 entries</td><td>XCTest AC #7</td><td>Auspicious hours do not change</td><td>Fix the loop</td></tr><tr><td>getSnapshot slow &gt; 1s</td><td>XCTest AC #8 measure</td><td>Blank placeholder</td><td>Fast-path cache first</td></tr><tr><td>JSON from TypeScript not parseable in Swift</td><td>JSONDecoder error, nil result</td><td>Widget falls back to LunarCalcSwift</td><td>Check that field names are consistent</td></tr><tr><td>AppGroupStoragePlugin not registered</td><td>JavaScript silent fail</td><td>Cache is not written</td><td>Register the plugin in AppDelegate</td></tr><tr><td>WidgetKit target missing from project.pbxproj</td><td>Build fails</td><td>Cannot test</td><td>Add the target via the Xcode GUI + commit pbxproj</td></tr><tr><td>Missing purple Asset Catalog color</td><td>Widget uses default colors (white/gray)</td><td>Inconsistent UX</td><td>Add the color set to Assets.xcassets</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li>The epoch constants in <code>LunarCalcSwift</code> are the most important: one wrong digit means a wrong lunar date for every user every morning. Comment the source clearly (\"PRD §6.2, confirmed from Ho Ngoc Duc canonical source\"). The XCTest fixture is the protection.</li><li><code>LunarTimelineProvider.getTimeline</code> should configure the <code>.atEnd</code> reload policy (not \"after X minutes\") because the entries cover the 12 gio until the end of the day; after midnight the system automatically reloads for the new day.</li><li>App Group writes from TypeScript are async; <code>registerWidgetCacheWriter</code> should call it inside <code>capacitor.addListener(\"appStateChange\")</code> with error handling (catch and log, do not throw - if the write fails the widget still works via the fallback).</li><li><code>systemSmall</code> is the required size because it is the most common widget size. Medium/Large should use the <code>.containerBackground</code> modifier (iOS 17+) for compatibility.</li><li>A SwiftUI <code>Link</code> in the widget lets the user tap to open the app at a specific screen (e.g. the good-day picker). Use <code>widgetURL(URL(string: \"genieamlich://day-detail\")!)</code> at the widget level and handle the URL in the Capacitor AppDelegate.</li><li><code>LunarWidgetTests</code> runs in a simulator without a real App Group; mock <code>UserDefaults</code> with an injected suite in the test to avoid depending on the App Group.</li><li>Watch complication (COULD): if the decision is to build it, WidgetKit for watchOS (watchOS 9+) uses the same <code>StaticConfiguration</code> API - <code>LunarCalcSwift.swift</code> and <code>DayInfoCache.swift</code> can be reused verbatim, only a new SwiftUI view for the watch-face family (<code>.accessoryCircular</code>, <code>.accessoryRectangular</code>) is needed.</li></ul>\n<p><em>End of FR-LUNAR-013.</em></p>";

window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LUNAR-021"]="<h1>Feature Request</h1>\n<blockquote><p>Turn Your Will Into Real.</p></blockquote>\n<h2>Summary</h2>\n<p>Implement Proactive AI (Genie 2.0) by setting up a server-side cron job on <code>genie-api</code>. This cron scans upcoming major lunar occasions (e.g., full moon, first of the month, Tet holidays), finds users who have opted in, calls the Claude proxy to generate a personalized greeting, and pushes it via Zalo ZNS.</p>\n<h2>Problem</h2>\n<p>Users today only receive local push notifications (per FR-LUNAR-004) with static content. They want Genie to proactively engage with personalized content, smartly suggesting how to prepare offerings via Zalo ZNS, like a real virtual assistant.</p>\n<h2>Customer Quotes</h2>\n<p>&lt;untrusted_content source=\"user-feedback\"&gt; \"The app reminding me of the full moon day is nice, but the content keeps repeating. It would be so convenient if it messaged me on Zalo to greet me and remind me to buy offering supplies too.\" &lt;/untrusted_content&gt;</p>\n<h2>§1 - Description (Normative Clauses)</h2>\n<ol><li><strong>MUST</strong> add <code>proactive_zns_opt_in</code> (boolean) to the <code>user_settings</code> table. Defaults to <code>false</code>.</li><li><strong>MUST</strong> implement a Supabase Cron Worker (or Cloudflare Worker) that executes daily at 08:00 AM VN time (UTC+7).</li><li><strong>MUST</strong> query users where <code>proactive_zns_opt_in = true</code> and cross-reference with the Lunar engine to determine if T+1 is a major event (e.g., 1st or 15th of the Lunar Month).</li><li><strong>MUST</strong> call the Claude proxy (<code>FR-LUNAR-015</code>) to generate a personalised message for each user, providing user metadata (name, age, past interactions) without including PII like phone numbers.</li><li><strong>MUST</strong> send the generated message via Zalo ZNS API using an approved ZNS template.</li><li><strong>MUST</strong> implement a batching mechanism for ZNS API calls (max 50 requests/sec) to avoid rate limits.</li><li><strong>MUST</strong> implement a retry mechanism (max 3 attempts with exponential backoff) for Claude API or ZNS API transient failures.</li><li><strong>MUST</strong> log all ZNS delivery statuses to <code>genie.action_log</code> with phone numbers strictly redacted (e.g., <code>09****123</code>).</li><li><strong>SHOULD</strong> fallback to a static message payload if the Claude API times out after 5 seconds, ensuring the notification is still delivered.</li><li><strong>MUST</strong> ensure the worker is idempotent (if re-run for the same date, it must not send duplicate messages to the same user).</li></ol>\n<h2>§2 - Why this design</h2>\n<p><strong>Why use server-side cron instead of Capacitor background fetch (§1 #2)?</strong>  Local background tasks on iOS/Android are highly constrained and unreliable. To guarantee ZNS message delivery at exactly 08:00 AM, a server-side cron orchestrator is required.</p>\n<p><strong>Why batching ZNS requests (§1 #6)?</strong> Zalo OA has strict API rate limits (typically 50 req/sec). Processing users sequentially is too slow, while <code>Promise.all</code> without chunks will hit rate limits.</p>\n<p><strong>Why Claude fallback (§1 #9)?</strong> LLM latency can spike or the API can go down. The core value is the reminder; personalization is a bonus. We must never drop the reminder just because the AI is slow.</p>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// ZNS Payload Schema\nexport interface ZnsPayload {\n  phone: string; // Will be mapped to Zalo user ID or raw phone\n  template_id: string;\n  template_data: {\n    user_name: string;\n    event_name: string;\n    ai_message: string;\n    action_link: string;\n  };\n  tracking_id: string; // for idempotency\n}\n\n// Supabase Edge Function / Cron interface\nexport interface ProactiveZnsWorkerConfig {\n  batchSize: number; // default 50\n  maxRetries: number; // default 3\n  claudeTimeoutMs: number; // default 5000\n}\n\nexport async function processProactiveZnsBatch(date: Date, config: ProactiveZnsWorkerConfig): Promise&lt;void&gt;;</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Tier 1 hits first</strong> - When a user has <code>proactive_zns_opt_in = true</code> and T+1 is a lunar event, the cron job successfully generates a personalized message via Claude and dispatches it via ZNS.</li><li><strong>Rate Limit Handling</strong> - When 1000 users are eligible, the worker batches calls to Zalo API such that it never exceeds 50 requests per second.</li><li><strong>LLM Fallback</strong> - When Claude API takes &gt; 5000ms, the system falls back to a predefined static string without failing the ZNS dispatch.</li><li><strong>Idempotency</strong> - When <code>processProactiveZnsBatch</code> is called twice for the same <code>date</code>, it queries the database and sends 0 duplicate ZNS messages.</li><li><strong>PII Redaction</strong> - When writing to <code>genie.action_log</code>, the <code>phone</code> field is redacted to mask middle digits.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// test/workers/proactive-zns.test.ts\nimport { processProactiveZnsBatch } from '../../src/workers/proactive-zns';\nimport { ZaloZnsService } from '../../src/services/zalo-zns';\n\ndescribe('Proactive ZNS Worker', () =&gt; {\n  it('MUST NOT send duplicate messages on double invocation (Idempotency)', async () =&gt; {\n    // Arrange\n    const mockZnsService = jest.spyOn(ZaloZnsService, 'sendTemplate').mockResolvedValue(true);\n    const date = new Date('2026-08-15T01:00:00Z');\n    \n    // Act\n    await processProactiveZnsBatch(date, { batchSize: 50, maxRetries: 1, claudeTimeoutMs: 5000 });\n    const firstCallCount = mockZnsService.mock.calls.length;\n    \n    await processProactiveZnsBatch(date, { batchSize: 50, maxRetries: 1, claudeTimeoutMs: 5000 });\n    const secondCallCount = mockZnsService.mock.calls.length;\n\n    // Assert\n    expect(firstCallCount).toBeGreaterThan(0);\n    expect(secondCallCount).toBe(firstCallCount); // No new calls\n  });\n\n  it('MUST fallback to static message if Claude times out', async () =&gt; {\n    // Implement mock Claude API with 6000ms delay\n    // Assert ZnsPayload.template_data.ai_message === \"Chúc bạn ngày Rằm an lành!\"\n  });\n});</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p>The cron worker will be deployed as a Supabase Edge Function triggered by pg_cron.</p>\n<pre><code class=\"language-typescript\">export async function processProactiveZnsBatch(targetDate: Date, config: ProactiveZnsWorkerConfig) {\n    const eligibleUsers = await fetchEligibleUsers(targetDate);\n    \n    // Chunking for rate limits\n    for (let i = 0; i &lt; eligibleUsers.length; i += config.batchSize) {\n        const batch = eligibleUsers.slice(i, i + config.batchSize);\n        \n        await Promise.all(batch.map(async (user) =&gt; {\n            if (await hasAlreadySent(user.id, targetDate)) return;\n\n            let aiMessage = \"Chúc bạn một ngày an lành!\"; // Fallback\n            try {\n                aiMessage = await withTimeout(callClaude(user), config.claudeTimeoutMs);\n            } catch (e) {\n                console.warn(`Claude timeout for user ${user.id}, using fallback.`);\n            }\n\n            const payload: ZnsPayload = {\n                phone: user.phone,\n                template_id: \"LUNAR_EVENT_1\",\n                template_data: { user_name: user.name, event_name: \"Rằm\", ai_message: aiMessage, action_link: \"https://zalo.me/s/...\" },\n                tracking_id: `${user.id}_${targetDate.toISOString().split('T')[0]}`\n            };\n\n            await ZaloZnsService.sendTemplate(payload);\n            await logToAudit(user.id, redactPhone(user.phone), \"ZNS_SENT\");\n            await markAsSent(user.id, targetDate);\n        }));\n        \n        // rudimentary rate limiting delay\n        await new Promise(res =&gt; setTimeout(res, 1000));\n    }\n}</code></pre>\n<h2>§7 - Dependencies</h2>\n<ul><li><strong>Upstream:</strong> FR-LUNAR-015 (Claude proxy for AI text generation).</li><li><strong>Upstream:</strong> FR-LUNAR-017 (Zalo Mini App integration for OA privileges).</li><li><strong>Downstream:</strong> FR-LUNAR-022 (O2O Commerce - injecting affiliate links into the ZNS template).</li></ul>\n<h2>§8 - Example payloads</h2>\n<p><strong>Audit Log Row (Redacted PII):</strong></p>\n<pre><code class=\"language-json\">{\n  \"event\": \"proactive.zns_sent\",\n  \"user_id\": \"00000000-0000-0000-0000-000000000001\",\n  \"timestamp\": \"2026-08-14T01:00:05Z\",\n  \"payload\": {\n    \"target_date\": \"2026-08-15\",\n    \"template_id\": \"LUNAR_EVENT_1\",\n    \"phone_redacted\": \"09****456\",\n    \"claude_fallback_used\": false\n  }\n}</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li><code>All resolved.</code> (Wait on Zalo OA template approval, but technical spec is clear).</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Claude API 500 error</td><td><code>callClaude()</code> throws</td><td>Catch block triggers</td><td>Fallback to static message</td></tr><tr><td>Claude API timeout (&gt;5s)</td><td><code>withTimeout()</code> throws</td><td>Catch block triggers</td><td>Fallback to static message</td></tr><tr><td>Zalo ZNS API 429 Rate Limit</td><td>HTTP 429 response</td><td>Worker throws</td><td>Exponential backoff retry (max 3)</td></tr><tr><td>Zalo ZNS API 500 error</td><td>HTTP 5xx response</td><td>Worker throws</td><td>Exponential backoff retry, alert on Sentry</td></tr><tr><td>User phone missing/invalid</td><td><code>sendTemplate()</code> fails Zalo validation</td><td>ZNS rejects</td><td>Log <code>proactive.zns_failed</code>, do not retry</td></tr><tr><td>DB connection fails during markAsSent</td><td><code>markAsSent()</code> throws</td><td>Row not saved</td><td>Idempotency might fail on retry. Need transaction block for ZNS send + DB write if possible, or accept rare dupe risk.</td></tr><tr><td>pg_cron fails to trigger</td><td>No audit logs at 08:05 AM</td><td>Batch missed</td><td>Manual trigger via CLI <code>cyberos-app trigger-zns</code></td></tr><tr><td>Invalid AI JSON output</td><td>JSON parse fails</td><td>Catch block triggers</td><td>Fallback to static message</td></tr><tr><td>ZNS template rejected by Zalo</td><td>HTTP 400 with specific code</td><td>ZNS rejects</td><td>Log <code>template_rejected</code>, requires PM action</td></tr><tr><td>User opts out mid-batch</td><td><code>proactive_zns_opt_in</code> is false</td><td>N/A (query happens once)</td><td>Sent anyway for this batch. Will not trigger next time.</td></tr></tbody></table>\n<h2>§11 - Implementation notes</h2>\n<ul><li><strong>PII Scrubbing:</strong> The <code>redactPhone</code> utility must replace all but the first two and last three digits with <code>*</code>. This must happen <em>before</em> calling the logging service.</li><li><strong>Transactions:</strong> It is difficult to wrap an external HTTP call (Zalo ZNS) in a DB transaction reliably without holding connections open for too long. We mark the tracking ID in DB <em>after</em> sending. If the DB write fails, a subsequent retry might send a duplicate ZNS. The risk of occasional duplicate ZNS is acceptable compared to holding Postgres transactions open across network calls.</li><li><strong>Zalo OA Account Balance:</strong> Ensure there is a monitoring alert for Zalo OA balance, as ZNS messages cost money.</li></ul>\n<h2>Sales/CS Summary</h2>\n<p>Genie is now smarter than ever! Turn on Genie Proactive to receive personalized reminders and greetings via Zalo ahead of important lunar occasions.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> LLM agent acting as feature-request-author</li><li><strong>Scope:</strong> The entire FR content.</li><li><strong>Human review:</strong> Reviewed by the operator after generation.</li></ul>\n<p><em>End of FR-LUNAR-021.</em></p>";

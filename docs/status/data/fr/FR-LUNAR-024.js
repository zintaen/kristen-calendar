window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LUNAR-024"]="<h1>Feature Request</h1>\n<blockquote><p>Turn Your Will Into Real.</p></blockquote>\n<h2>Summary</h2>\n<p>Implement an In-App Purchase (IAP) system through RevenueCat to sell Premium packages (feng shui Themes, exclusive Widgets). Users can buy individual themes or a Subscription package (Genie+). The system manages entitlements and syncs purchase status back to the Supabase database.</p>\n<h2>Problem</h2>\n<p>The app is currently entirely free. Although Affiliate (FR-LUNAR-022) generates cash flow, IAP (direct sales) is still the most stable and scalable monetization model for utility apps (Calendar/Widget). Users have a high demand for personalization (changing the app color to match their feng shui element) but have no way to pay for that feature.</p>\n<h2>Customer Quotes</h2>\n<p>&lt;untrusted_content source=\"user-interview\"&gt; \"My element is Fire. I want the app to be red or orange to match feng shui. Does the app sell themes so I can buy one?\" &lt;/untrusted_content&gt;</p>\n<h2>§1 - Description (Normative Clauses)</h2>\n<ol><li><strong>MUST</strong> integrate RevenueCat SDK to handle App Store and Google Play billing securely.</li><li><strong>MUST</strong> create a <code>StorePage</code> UI showcasing available premium items: \"Feng Shui Themes\" and \"Advanced Widgets\".</li><li><strong>MUST</strong> lock premium themes in the <code>ThemeSelector</code> component if the user does not have the required RevenueCat Entitlement.</li><li><strong>MUST</strong> set up a RevenueCat Webhook targeting a Supabase Edge Function to sync subscription/purchase states to the <code>user_settings.premium_tier</code> column.</li><li><strong>MUST</strong> support offline entitlement checking (using RevenueCat's cached entitlements) so users don't lose access to their themes when offline.</li><li><strong>MUST</strong> provide a \"Restore Purchases\" button in the Store UI, explicitly required by Apple App Store guidelines.</li><li><strong>SHOULD</strong> implement a promotional introductory offer (e.g., \"7 days free trial\" for Genie+).</li><li><strong>MUST</strong> log all successful purchase events to <code>genie.action_log</code> with <code>event_kind=monetization.purchase_success</code>.</li></ol>\n<h2>§2 - Why this design</h2>\n<p><strong>Why RevenueCat instead of native IAP (§1 #1)?</strong>  Implementing Apple StoreKit and Google Play Billing directly, along with secure backend receipt validation, is extremely complex and error-prone. RevenueCat abstracts this into a single SDK and provides a unified dashboard for metrics.</p>\n<p><strong>Why sync to Supabase via Webhook (§1 #4)?</strong> While RevenueCat holds the source of truth, we need the <code>premium_tier</code> in our database to gate server-side features in the future (e.g., unlimited Claude requests) without querying RevenueCat's API on every request.</p>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// Supabase Webhook Endpoint (receives from RevenueCat)\nexport interface RevenueCatWebhookPayload {\n  event: {\n    type: \"INITIAL_PURCHASE\" | \"RENEWAL\" | \"CANCELLATION\" | \"EXPIRATION\";\n    app_user_id: string; // Maps to Supabase User ID\n    product_id: string;\n    entitlement_ids: string[];\n    purchased_at_ms: number;\n    expiration_at_ms?: number;\n  };\n}\n\n// Client-side IAP Service\nexport interface IAPService {\n  getOfferings(): Promise&lt;Offering[]&gt;;\n  purchasePackage(pkg: Package): Promise&lt;CustomerInfo&gt;;\n  restorePurchases(): Promise&lt;CustomerInfo&gt;;\n  hasEntitlement(entitlementId: string): Promise&lt;boolean&gt;;\n}</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Store Display</strong> - When a user opens the StorePage, it successfully fetches and displays the active offerings from RevenueCat.</li><li><strong>Purchase Flow</strong> - When a user taps \"Buy\", the native IAP sheet appears. Upon successful sandbox purchase, the Theme is immediately unlocked locally.</li><li><strong>Webhook Sync</strong> - When a purchase occurs, RevenueCat fires a webhook to Supabase, updating the user's <code>premium_tier</code> to <code>true</code> within 5 seconds.</li><li><strong>Restore Purchases</strong> - When a user uninstalls and reinstalls on a new device, tapping \"Restore Purchases\" unlocks their previously bought themes without double-charging.</li><li><strong>Offline Access</strong> - When the app is launched without internet, previously unlocked themes remain unlocked based on cached entitlements.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// test/monetization/webhook.test.ts\nimport { handleRevenueCatWebhook } from '../../src/services/revenuecat';\n\ndescribe('RevenueCat Webhook', () =&gt; {\n  it('MUST update user_settings premium_tier to true on INITIAL_PURCHASE', async () =&gt; {\n    // Arrange\n    const payload = {\n      event: {\n        type: \"INITIAL_PURCHASE\",\n        app_user_id: \"123-abc\",\n        entitlement_ids: [\"genie_plus\"]\n      }\n    };\n    \n    // Act\n    await handleRevenueCatWebhook(payload);\n    \n    // Assert\n    const user = await DB.getUserSettings(\"123-abc\");\n    expect(user.premium_tier).toBe(true);\n  });\n});</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<pre><code class=\"language-typescript\">// IAPService.ts\nimport { Purchases, CustomerInfo } from '@revenuecat/purchases-capacitor';\n\nexport class IAPServiceImpl implements IAPService {\n  async init(userId: string) {\n    await Purchases.configure({ apiKey: \"appl_XXXXXX\", appUserID: userId });\n  }\n\n  async hasEntitlement(entitlementId: string): Promise&lt;boolean&gt; {\n    const info = await Purchases.getCustomerInfo();\n    return info.entitlements.active[entitlementId] !== undefined;\n  }\n\n  async purchasePackage(pkg: any): Promise&lt;CustomerInfo&gt; {\n    try {\n      const { customerInfo } = await Purchases.purchasePackage({ aPackage: pkg });\n      await logToAudit(customerInfo.originalAppUserId, \"IAP_SUCCESS\", pkg.identifier);\n      return customerInfo;\n    } catch (e) {\n      if (e.code === PURCHASES_ERROR_CODE.PURCHASE_CANCELLED_ERROR) {\n        // User cancelled, ignore\n      } else {\n        throw e;\n      }\n    }\n  }\n}</code></pre>\n<h2>§7 - Dependencies</h2>\n<ul><li><strong>Upstream:</strong> FR-LUNAR-005 (Widget engine - to support premium widget rendering).</li><li><strong>Upstream:</strong> FR-LUNAR-020 (Account management - to link purchases to Supabase UUIDs).</li></ul>\n<h2>§8 - Example payloads</h2>\n<p><strong>Audit Log Row:</strong></p>\n<pre><code class=\"language-json\">{\n  \"event\": \"monetization.purchase_success\",\n  \"user_id\": \"00000000-0000-0000-0000-000000000001\",\n  \"timestamp\": \"2026-08-14T01:00:05Z\",\n  \"payload\": {\n    \"product_id\": \"com.cyberskill.genie.theme.fire\",\n    \"entitlement_id\": \"premium_themes\",\n    \"sandbox\": true\n  },\n  \"trace_id\": \"5b23d9161a03f8373b...\"\n}</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li><code>Resolved</code> - Should we use RevenueCat's anonymous IDs or require login to buy? Resolution: Require login (FR-020) so purchases can sync cross-platform (iOS to Android).</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>RevenueCat SDK fails to init</td><td>Promise rejects on boot</td><td>App runs in free mode</td><td>Retry next session</td></tr><tr><td>User denies payment</td><td><code>PURCHASE_CANCELLED_ERROR</code></td><td>Nothing happens</td><td>Expected behavior</td></tr><tr><td>Webhook fails to reach Supabase</td><td>RevenueCat dashboard alerts</td><td>DB out of sync</td><td>RevenueCat auto-retries webhook for 7 days</td></tr><tr><td>User tries to restore without logging into correct Apple ID</td><td>iOS StoreKit error</td><td>Alert shown</td><td>Instruct user to check App Store settings</td></tr><tr><td>Invalid signature on Webhook</td><td>Supabase rejects with 401</td><td>Sync fails</td><td>Ensure shared secret is correctly configured in env vars</td></tr><tr><td>Apple rejects App Review</td><td>App Store Connect message</td><td>Delay in release</td><td>Ensure \"Restore Purchases\" is visible and EULA is linked per Apple guidelines</td></tr><tr><td>DB connection fails during Webhook</td><td>Supabase throws 500</td><td>Webhook fails</td><td>Rely on RevenueCat webhook retry</td></tr><tr><td>App userID mismatch</td><td>RC uses anonymous ID instead of Supabase UUID</td><td>Purchases lost on uninstall</td><td>Call <code>Purchases.logIn(supabaseUser.id)</code> immediately after login</td></tr><tr><td>Network disconnect during purchase</td><td>SDK handles</td><td>Purchase queued</td><td>Will complete upon reconnect</td></tr><tr><td>Sandbox purchase leaks to Prod</td><td>Webhook detects <code>environment: \"SANDBOX\"</code></td><td>Prod metrics skewed</td><td>Ensure Supabase webhook filters out sandbox events in production DB</td></tr></tbody></table>\n<h2>§11 - Implementation notes</h2>\n<ul><li><strong>Apple Guidelines:</strong> Apple is extremely strict about IAP. The StorePage MUST display a link to the Terms of Service, Privacy Policy, and clearly state the exact price, duration (if subscription), and how to cancel. Missing any of these guarantees an App Review rejection.</li><li><strong>Login Lifecycle:</strong> If a user logs out, we must call <code>Purchases.logOut()</code> to prevent the next user on the same device from inheriting their purchases.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> LLM agent acting as feature-request-author</li><li><strong>Scope:</strong> The entire FR content.</li><li><strong>Human review:</strong> Reviewed by the operator after generation.</li></ul>\n<p><em>End of FR-LUNAR-024.</em></p>";

window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LUNAR-016"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>The Zalo Mini App \"Genie Am Lich\" MUST run inside Zalo on React + <code>zmp-ui</code> + <code>zmp-sdk/apis</code>, share calendar computation logic with the other clients by importing <code>@cyberskill/amlich-core</code> directly, and store user state in <code>zmp Storage</code>. This is the contract:</p>\n<ol><li>MUST initialize the Mini App with the correct <code>zmp-sdk</code> syntax (<code>zmp.init()</code> in <code>app.tsx</code>), configuring <code>app-config.json</code> with <code>appId</code>, <code>permissions</code> (including <code>scope.userInfo</code>, <code>scope.phone</code>, <code>scope.location</code> declared explicitly), and version (DEC-LUNAR-160).</li><li>MUST import <code>convertSolar2Lunar</code>, <code>convertLunar2Solar</code>, <code>canChi</code>, and the related functions from <code>@cyberskill/amlich-core</code> to compute the calendar on-device; MUST NOT make any network request to compute the lunar date (DEC-LUNAR-160).</li><li>MUST use <code>zmp Storage</code> (the <code>getStorage</code>/<code>setStorage</code> API from <code>zmp-sdk</code>) to store the user's <code>Settings</code> and the <code>Reminder[]</code> array; because Storage has limited capacity, store only the necessary fields of <code>Reminder</code> (see §3) and compute <code>OccurrenceCache</code> on-the-fly each time it is displayed, MUST NOT store the full cache (DEC-LUNAR-161).</li><li>MUST display the Home page with: today's solar date, the corresponding lunar date, the day's can-chi, upcoming reminders in the next 7 days, and a quick-access button to the CalendarGrid.</li><li>MUST display the CalendarGrid month grid: each cell has a large solar date + a small lunar date in the corner + a colored dot if the day has a Reminder or a lunar festival from <code>@cyberskill/genie-content</code>.</li><li>MUST let the user view, create, edit, and delete a <code>Reminder</code> (type: <code>RAM | MUNG_MOT | GIO | CUSTOM | FESTIVAL</code>) with <code>lunarDay</code>, <code>lunarMonth</code>, <code>isLeapMonth</code>, <code>leadTimes</code>, <code>notifyTime</code>, <code>channels</code> (only <code>ZNS</code> is available on the Zalo client), and <code>linkedContentId</code>.</li><li>MUST NOT call <code>getUserInfo</code> without explicit user consent; MUST show the <code>ConsentSheet</code> (permission-request screen) with a clear explanation before calling <code>apis.getUserInfo({ avatarSize: 240 })</code> (DEC-LUNAR-163).</li><li>MUST call <code>apis.getPhoneNumber()</code> only when the user enables the ZNS channel in Settings AND has confirmed ZNS consent; the phone number is stored only in the <code>User.phone</code> field to pass to the ZNS sender (FR-LUNAR-017), MUST NOT be used for any other purpose (DEC-LUNAR-163).</li><li>MUST handle the case where the user declines <code>getUserInfo</code> or <code>getPhoneNumber</code> consent gracefully: hide the features that depend on that permission (show an explanatory message) but MUST NOT crash the entire app.</li><li>MUST display the <code>FestivalDetail</code> page for each lunar festival, loading content from <code>@cyberskill/genie-content</code> (<code>FestivalContent</code>); ensure the link from a Reminder to FestivalDetail through <code>linkedContentId</code> (FR-LUNAR-008).</li><li>MUST apply the purple theme from <code>@cyberskill/genie-ui</code> (tokens from FR-LUNAR-009) to the <code>zmp-ui</code> components; MUST NOT use the default <code>zmp-ui</code> colors for the main elements (DEC-LUNAR-165).</li><li>SHOULD get <code>getLocation</code> (location) to suggest the local time zone if the user is abroad; however, all lunar calendar computation MUST be locked to <code>timezone = \"Asia/Ho_Chi_Minh\"</code> (UTC+7, meridian 105E) per DEC-LUNAR-160 and FR-LUNAR-004.</li><li>MUST NOT send a push notification to the user from inside the Mini App; every reminder function must go through ZNS (FR-LUNAR-017) once a valid phone number and consent are available (DEC-LUNAR-162).</li><li>MUST provide a Settings page that lets the user toggle the ZNS channel on/off, view the Zalo display name, and delete all stored data (clear <code>zmp Storage</code>).</li><li>SHOULD cache the <code>UpcomingOccurrence</code> list (the next solar dates for each Reminder in the coming 30 days) in application memory (React state/context) to reduce recomputation on render; reset the cache when a Reminder changes.</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why use zmp-ui + zmp-sdk instead of plain web components?</strong> The Zalo Mini App requires using <code>zmp-sdk</code> to initialize the app and access the platform APIs (getUserInfo, getPhoneNumber, Storage). <code>zmp-ui</code> is the component set certified by Zalo, ensuring the Mini App conforms to Zalo's review rules and feels native on the platform. Not using other component sets avoids the risk of rejection during review (DEC-LUNAR-160).</p>\n<p><strong>Why import amlich-core directly instead of calling an API?</strong> <code>@cyberskill/amlich-core</code> is pure TypeScript, zero-dependency, and runs in any JavaScript environment, including the Zalo Mini App runtime. Importing it directly removes network latency, keeps offline working, and guarantees absolute consistency with the other clients (DEC-LUNAR-160, NFR-Offline).</p>\n<p><strong>Why does zmp Storage only store settings and the Reminder list, not the cache?</strong> <code>zmp Storage</code> capacity is limited (per Zalo documentation, the total storage per Mini App is small); storing a full <code>OccurrenceCache</code> for many years would quickly exceed the limit. Computing <code>convertLunar2Solar</code> for the next 30 days takes only a few ms (NFR-Performance), so recomputing on each display is reasonable and inexpensive (DEC-LUNAR-161).</p>\n<p><strong>Why explicit consent before getUserInfo and getPhoneNumber?</strong> PDPL (effective 01/01/2026) and Zalo policy both require the user to agree before the app collects personal data. A phone number is sensitive data under PDPL; calling <code>getPhoneNumber</code> silently is a violation. The ConsentSheet clearly describes the purpose of using the phone number (only to receive ZNS reminders) before the request (DEC-LUNAR-163, NFR-Privacy/PDPL).</p>\n<p><strong>Why not send push notifications directly?</strong> This is a hard technical limitation of the Zalo Mini App platform - a native push API does not exist for Mini Apps. Every Zalo reminder must go through the ZNS of a registered OA. The FR-016 design recognizes this from the start and shifts reminder responsibility entirely to FR-LUNAR-017 (DEC-LUNAR-162).</p>\n<p><strong>Why the purple theme from FR-LUNAR-009 instead of the zmp-ui default?</strong> The purple sub-brand is the product identity and a consistent user experience across every platform (FR-LUNAR-009, PRD §13). zmp-ui can be styled via CSS variables without breaking Zalo's guidelines. The override applies only to color tokens, keeping the component structure intact so the Mini App still passes review (DEC-LUNAR-165).</p>\n<p><strong>Why is zalo/ a separate package in the monorepo?</strong> The Mini App has its own build toolchain (<code>zmp build</code>, <code>zmp deploy</code>), entirely different from Next.js or Capacitor. Splitting it out keeps CI/CD from cross-affecting each other and keeps the boundaries between clients clear (DEC-LUNAR-164).</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// zalo/src/types/index.ts\n\nexport type ReminderType = \"RAM\" | \"MUNG_MOT\" | \"GIO\" | \"CUSTOM\" | \"FESTIVAL\";\nexport type ReminderChannel = \"ZNS\";\n\nexport interface ZaloReminder {\n  id: string;\n  type: ReminderType;\n  title: string;\n  lunarDay: number;\n  lunarMonth: number;\n  lunarYear?: number;       // null = repeats annually/monthly depending on recurrence\n  isLeapMonth: boolean;\n  recurrence: \"MONTHLY\" | \"ANNUAL\" | \"ONCE\"; // PHẢI có để phân biệt MONTHLY (Rằm/Mùng Một) và ANNUAL (đám giỗ)\n  leadTimes: number[];      // số ngày trước: [0] = đúng ngày, [1] = trước 1 ngày, v.v.\n  notifyTime: string;       // \"HH:MM\" theo Asia/Ho_Chi_Minh\n  channels: ReminderChannel[];\n  linkedContentId?: string;\n  enabled: boolean;\n}\n\nexport interface ZaloSettings {\n  znsEnabled: boolean;\n  displayName?: string;     // lấy từ getUserInfo, lưu local\n  phone?: string;           // lấy từ getPhoneNumber, lưu local để gửi ZNS\n  consentFlags: {\n    userInfoGranted: boolean;\n    phoneGranted: boolean;\n  };\n}\n\nexport interface StorageData {\n  settings: ZaloSettings;\n  reminders: ZaloReminder[];\n}\n\nexport interface UpcomingOccurrence {\n  reminderId: string;\n  reminderTitle: string;\n  solarDate: string;        // \"YYYY-MM-DD\"\n  lunarLabel: string;       // ví dụ \"15/7 Ất Tỵ (nhuận)\"\n  daysUntil: number;\n}</code></pre>\n<pre><code class=\"language-typescript\">// zalo/src/lib/storage.ts\nimport { getStorage, setStorage } from \"zmp-sdk/apis\";\nimport type { StorageData, ZaloReminder, ZaloSettings } from \"../types\";\n\nconst STORAGE_KEY = \"genie_amlich_v1\";\n\nexport async function loadStorageData(): Promise&lt;StorageData&gt; { /* ... */ }\nexport async function saveReminders(reminders: ZaloReminder[]): Promise&lt;void&gt; { /* ... */ }\nexport async function saveSettings(settings: ZaloSettings): Promise&lt;void&gt; { /* ... */ }\nexport async function clearAll(): Promise&lt;void&gt; {\n  await setStorage({ key: STORAGE_KEY, data: null });\n}</code></pre>\n<pre><code class=\"language-typescript\">// zalo/src/lib/zalo-auth.ts\nimport { getUserInfo, getPhoneNumber } from \"zmp-sdk/apis\";\n\nexport interface ZaloUserInfo {\n  id: string;\n  name: string;\n  avatar: string;\n}\n\n// Gọi sau khi người dùng đã bấm đồng ý trên ConsentSheet\nexport async function fetchUserInfo(): Promise&lt;ZaloUserInfo&gt; {\n  const result = await getUserInfo({ avatarSize: 240 });\n  return {\n    id: result.userInfo.id,\n    name: result.userInfo.name,\n    avatar: result.userInfo.avatar,\n  };\n}\n\n// Chỉ gọi khi znsEnabled = true và phoneGranted = false\nexport async function fetchPhoneNumber(): Promise&lt;string&gt; {\n  const result = await getPhoneNumber({});\n  // result.token -&gt; chuyển lên server để đổi lấy số điện thoại thực qua OA API\n  return result.token;\n}</code></pre>\n<pre><code class=\"language-typescript\">// zalo/src/lib/day-computer.ts\nimport {\n  convertSolar2Lunar,\n  convertLunar2Solar,\n  canChiDay,\n  todayInHCM,\n  type LunarDate,\n  type SolarDate,\n} from \"@cyberskill/amlich-core\";\n\nconst TZ = 7.0; // UTC+7, kinh tuyến 105E, DEC-LUNAR-160\n\nexport function todayLunar(): LunarDate {\n  // KHÓA về Asia/Ho_Chi_Minh, KHÔNG dùng now.getDate()/getMonth() (đọc theo TZ thiết bị,\n  // sai khi người dùng ở nước ngoài - vi phạm §1 #12, AC #14). todayInHCM từ FR-LUNAR-004.\n  // todayInHCM() trả TUPLE SolarDate = [dd, mm, yy] - KHÔNG đọc .dd/.mm/.yy (không tồn tại).\n  const [dd, mm, yy] = todayInHCM();\n  return convertSolar2Lunar(dd, mm, yy, TZ);\n}\n\nexport function getUpcomingOccurrences(\n  reminders: import(\"../types\").ZaloReminder[],\n  daysAhead: number = 30\n): import(\"../types\").UpcomingOccurrence[] {\n  // Với mỗi reminder, gọi convertLunar2Solar cho năm hiện tại và năm tới,\n  // lọc trong khoảng [hôm nay, hôm nay + daysAhead], trả về mảng sắp xếp tăng dần.\n  // ...\n}</code></pre>\n<pre><code class=\"language-typescript\">// zalo/src/lib/reminder-service.ts\nimport type { ZaloReminder } from \"../types\";\nimport { loadStorageData, saveReminders } from \"./storage\";\n\nexport async function getReminders(): Promise&lt;ZaloReminder[]&gt; { /* ... */ }\nexport async function upsertReminder(r: ZaloReminder): Promise&lt;void&gt; { /* ... */ }\nexport async function deleteReminder(id: string): Promise&lt;void&gt; { /* ... */ }</code></pre>\n<pre><code class=\"language-typescript\">// zalo/app-config.json (phần trích)\n{\n  \"app\": {\n    \"title\": \"Genie Âm Lịch\",\n    \"icon\": \"assets/icon.png\",\n    \"permissions\": [\n      { \"type\": \"scope.userInfo\", \"description\": \"Hiện thi tên bạn trên ứng dụng\" },\n      { \"type\": \"scope.phone\",    \"description\": \"Gửi nhắc qua Zalo (ZNS) theo số điện thoại bạn\" }\n    ]\n  }\n}</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>The Mini App starts successfully in Zalo DevTools and on a real device; <code>zmp.init()</code> does not error; the Home screen displays today's lunar date correctly (cross-checked against the FR-LUNAR-003 fixture set).</li><li>CalendarGrid renders the month grid correctly with the lunar date in the corner of each cell, the day's can-chi, and a colored dot on days that have a Reminder or a festival in <code>@cyberskill/genie-content</code>; clicking a day cell opens the day detail.</li><li>The user can create a <code>GIO</code> Reminder with <code>lunarDay=15</code>, <code>lunarMonth=7</code>, <code>isLeapMonth=false</code>, <code>leadTimes=[1]</code>, <code>notifyTime=\"07:00\"</code>, <code>channels=[\"ZNS\"]</code>; the Reminder is saved into <code>zmp Storage</code> and appears in the ReminderList.</li><li><code>getUpcomingOccurrences</code> returns the correct solar date for the <code>GIO</code> Reminder above, cross-checked by hand with <code>convertLunar2Solar</code> from <code>amlich-core</code>.</li><li>On first app open, <code>getUserInfo</code> has NOT yet been called; the ConsentSheet appears when the user tries a feature that needs the permission; after declining, the app does not crash and the features that do not need the permission still run normally.</li><li>When the user enables ZNS in Settings without having granted the phone permission, the ZNS ConsentSheet appears first; after the permission is granted, <code>getPhoneNumber</code> is called and the token is stored into <code>Settings.phone</code>.</li><li><code>zmp Storage</code> contains only <code>StorageData</code> (settings + reminders); no <code>OccurrenceCache</code> is stored into Storage.</li><li>The purple theme (dark purple, cream background) displays correctly on the main components (header, button, card); no default <code>zmp-ui</code> blue appears on the styled elements.</li><li>The FestivalDetail page displays the correct <code>FestivalContent</code> for \"Ram thang Bay\" (the matching id in <code>packages/content</code>), including <code>meaning</code>, <code>offerings</code>, and <code>checklist</code>.</li><li>Clearing data in Settings wipes <code>zmp Storage</code> and resets the app state to the default (no more Reminders, consent = false).</li><li>A Reminder with <code>type=RAM</code> (Ram; the 15th lunar day) displays correctly on Home under \"Upcoming\" when this month's Ram is less than 7 days away.</li><li>The Mini App makes no network API calls when displaying the month calendar or computing the lunar date (checked via the DevTools Network tab); logs confirm only amlich-core is used.</li><li><code>app-config.json</code> fully declares <code>scope.userInfo</code> and <code>scope.phone</code> under <code>permissions</code>; the <code>zmp build</code> produces no warning about missing permission declarations.</li><li>When the user uses the app abroad (a different time zone set on the device), the app still displays the calendar per <code>Asia/Ho_Chi_Minh</code> for computing the lunar date (DEC-LUNAR-160).</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// zalo/src/lib/__tests__/day-computer.test.ts\nimport { describe, it, expect } from \"vitest\";\nimport { todayLunar, getUpcomingOccurrences } from \"../day-computer\";\nimport type { ZaloReminder } from \"../../types\";\n\ndescribe(\"day-computer\", () =&gt; {\n  it(\"tính đúng ngày âm 29/01/2025 = 1/1/2025 Ất Tỵ\", () =&gt; {\n    // fixture từ PRD 6.6\n    // Mock Date.now() -&gt; 29/01/2025\n    vi.useFakeTimers();\n    vi.setSystemTime(new Date(\"2025-01-29T10:00:00+07:00\"));\n    // todayLunar() trả về LunarDate = [day, month, year, leap] (tuple, FR-LUNAR-001)\n    const [lunarDay, lunarMonth, lunarYear] = todayLunar();\n    expect(lunarDay).toBe(1);\n    expect(lunarMonth).toBe(1);\n    expect(lunarYear).toBe(2025);\n    vi.useRealTimers();\n  });\n\n  it(\"getUpcomingOccurrences trả về ngày dương đúng cho GIO 15/7\", () =&gt; {\n    vi.useFakeTimers();\n    vi.setSystemTime(new Date(\"2025-07-01T08:00:00+07:00\"));\n    const reminder: ZaloReminder = {\n      id: \"r1\", type: \"GIO\", title: \"Giỗ bà\",\n      lunarDay: 15, lunarMonth: 7, isLeapMonth: false,\n      recurrence: \"ANNUAL\",\n      leadTimes: [0, 1], notifyTime: \"07:00\",\n      channels: [\"ZNS\"], enabled: true,\n    };\n    const occurrences = getUpcomingOccurrences([reminder], 60);\n    // 15/7 Ất Tỵ 2025 = 08/08/2025 dương lịch (đối chiếu thủ công)\n    const main = occurrences.find(o =&gt; o.daysUntil &gt;= 0 &amp;&amp; !o.lunarLabel.includes(\"truoc\"));\n    expect(main?.solarDate).toBe(\"2025-08-08\");\n    vi.useRealTimers();\n  });\n\n  it(\"Reminder GIO tháng nhuận - năm không có tháng nhuận fallback về tháng thường\", () =&gt; {\n    const reminder: ZaloReminder = {\n      id: \"r2\", type: \"GIO\", title: \"Giỗ ông\",\n      lunarDay: 10, lunarMonth: 2, isLeapMonth: true,\n      recurrence: \"ANNUAL\",\n      leadTimes: [0], notifyTime: \"07:00\",\n      channels: [\"ZNS\"], enabled: true,\n    };\n    // Năm không có tháng 2 nhuận -&gt; nên fallback về tháng 2 thường (không crash)\n    const occurrences = getUpcomingOccurrences([reminder], 400);\n    expect(occurrences.length).toBeGreaterThan(0);\n  });\n});\n\ndescribe(\"storage\", () =&gt; {\n  it(\"saveReminders và loadStorageData round-trip\", async () =&gt; {\n    // Mock zmp-sdk getStorage/setStorage\n    const mockStorage: Record&lt;string, unknown&gt; = {};\n    vi.mock(\"zmp-sdk/apis\", () =&gt; ({\n      getStorage: vi.fn(async ({ key }: { key: string }) =&gt; ({ data: mockStorage[key] ?? null })),\n      setStorage: vi.fn(async ({ key, data }: { key: string; data: unknown }) =&gt; { mockStorage[key] = data; }),\n      getUserInfo: vi.fn(),\n      getPhoneNumber: vi.fn(),\n    }));\n\n    const { saveReminders, loadStorageData } = await import(\"../storage\");\n    const reminders: ZaloReminder[] = [\n      { id: \"r1\", type: \"RAM\", title: \"Rằm\", lunarDay: 15, lunarMonth: 1,\n        isLeapMonth: false, recurrence: \"MONTHLY\", leadTimes: [1], notifyTime: \"07:00\",\n        channels: [\"ZNS\"], enabled: true },\n    ];\n    await saveReminders(reminders);\n    const loaded = await loadStorageData();\n    expect(loaded.reminders).toHaveLength(1);\n    expect(loaded.reminders[0].id).toBe(\"r1\");\n  });\n});\n\ndescribe(\"zalo-auth consent guard\", () =&gt; {\n  it(\"fetchUserInfo được gọi -&gt; trả về name và avatar\", async () =&gt; {\n    vi.mock(\"zmp-sdk/apis\", () =&gt; ({\n      getUserInfo: vi.fn(async () =&gt; ({\n        userInfo: { id: \"uid1\", name: \"Chị Linh\", avatar: \"https://example.com/a.jpg\" },\n      })),\n      getPhoneNumber: vi.fn(),\n    }));\n    const { fetchUserInfo } = await import(\"../zalo-auth\");\n    const info = await fetchUserInfo();\n    expect(info.name).toBe(\"Chị Linh\");\n  });\n\n  it(\"fetchPhoneNumber trả về token (không số thực - cần đổi qua OA API)\", async () =&gt; {\n    vi.mock(\"zmp-sdk/apis\", () =&gt; ({\n      getPhoneNumber: vi.fn(async () =&gt; ({ token: \"zalo_phone_token_abc\" })),\n      getUserInfo: vi.fn(),\n    }));\n    const { fetchPhoneNumber } = await import(\"../zalo-auth\");\n    const token = await fetchPhoneNumber();\n    expect(token).toBe(\"zalo_phone_token_abc\");\n  });\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The full contract is in §3. Details to keep in mind:</p>\n<pre><code class=\"language-typescript\">// zalo/src/app.tsx\nimport { App } from \"zmp-ui\";\nimport zmp from \"zmp-sdk\";\n\n// zmp.init() PHẢI được gọi trước bất kỳ API zmp-sdk nào\nzmp.init();\n\nexport default function GenieMiniApp() {\n  return &lt;App /* routing config */ /&gt;;\n}</code></pre>\n<p>The hardest point is that Zalo's <code>getPhoneNumber</code> returns a <code>token</code>, not the real phone number. The real number is only obtainable when the server calls the OA API with that token. This logic must live on the server side (FR-LUNAR-017); the client only holds the token and forwards it when initializing the ZNS subscription.</p>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream: <strong>FR-LUNAR-004</strong> (the <code>Reminder</code> data model and the <code>recurrence</code> logic referenced in <code>ZaloReminder</code>; amlich-core is a product of FR-001 that FR-004 depends on). <strong>FR-LUNAR-008</strong> (<code>FestivalContent</code> from <code>packages/content</code> used for FestivalDetail and the colored dots on the CalendarGrid). <strong>FR-LUNAR-009</strong> (the purple theme tokens imported into the Zalo Mini App styling).</p>\n<p>Downstream: <strong>FR-LUNAR-017</strong> (the ZNS sender needs <code>User.phone</code> (token) and the list of <code>Reminder</code>s with <code>channels: [\"ZNS\"]</code> built in this FR). <strong>FR-LUNAR-019</strong> (the PDPL consent layer needs the screens and the <code>consentFlags</code> built in this FR).</p>\n<p>Cross-cutting: <code>@cyberskill/amlich-core</code> is shared with <code>apps/web/</code> and <code>ios/App/</code>; any breaking change to amlich-core requires updating all three clients.</p>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"key\": \"genie_amlich_v1\",\n  \"data\": {\n    \"settings\": {\n      \"znsEnabled\": true,\n      \"displayName\": \"Chị Linh\",\n      \"phone\": \"zalo_phone_token_xyz\",\n      \"consentFlags\": {\n        \"userInfoGranted\": true,\n        \"phoneGranted\": true\n      }\n    },\n    \"reminders\": [\n      {\n        \"id\": \"rem-001\",\n        \"type\": \"GIO\",\n        \"title\": \"Giỗ Bà Nội\",\n        \"lunarDay\": 15,\n        \"lunarMonth\": 7,\n        \"lunarYear\": null,\n        \"isLeapMonth\": false,\n        \"recurrence\": \"ANNUAL\",\n        \"leadTimes\": [0, 1],\n        \"notifyTime\": \"07:00\",\n        \"channels\": [\"ZNS\"],\n        \"linkedContentId\": \"festival-vu-lan\",\n        \"enabled\": true\n      },\n      {\n        \"id\": \"rem-002\",\n        \"type\": \"RAM\",\n        \"title\": \"Rằm hàng tháng\",\n        \"lunarDay\": 15,\n        \"lunarMonth\": 0,\n        \"isLeapMonth\": false,\n        \"recurrence\": \"MONTHLY\",\n        \"leadTimes\": [1],\n        \"notifyTime\": \"06:30\",\n        \"channels\": [\"ZNS\"],\n        \"enabled\": true\n      }\n    ]\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"upcomingOccurrences\": [\n    {\n      \"reminderId\": \"rem-001\",\n      \"reminderTitle\": \"Giỗ Bà Nội (trước 1 ngày)\",\n      \"solarDate\": \"2025-08-07\",\n      \"lunarLabel\": \"14/7 Ất Tỵ\",\n      \"daysUntil\": 1\n    },\n    {\n      \"reminderId\": \"rem-001\",\n      \"reminderTitle\": \"Giỗ Bà Nội\",\n      \"solarDate\": \"2025-08-08\",\n      \"lunarLabel\": \"15/7 Ất Tỵ\",\n      \"daysUntil\": 2\n    }\n  ]\n}</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li><code>zmp Storage</code> structure (single key vs. multiple keys): chose a single key <code>genie_amlich_v1</code> to reduce the number of Storage API calls.</li><li>Whether <code>getPhoneNumber</code> returns a token or the real number: returns a token, the real number is obtained on the server - described in DEC-LUNAR-163 and §6.</li></ul>\n<p>Still open:</p>\n<ul><li>Zalo has a hard capacity limit on <code>zmp Storage</code> (unofficial external docs recommend &lt; 1 MB); if a user has &gt; 50 Reminders, this needs further assessment. Deferred to real Phase 3 testing.</li><li><code>getLocation</code> could be used to detect when the user is abroad and warn \"the calendar is displayed in VN time\"; the result depends on the location permission - deferred until after UX testing.</li><li>The zmp-ui routing structure (PageStack vs. Tab): to be chosen at the start of the build based on the official Zalo guidance at implementation time (may change with the zmp-sdk version).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>zmp.init()</code> fails (running outside Zalo)</td><td>try/catch in app.tsx</td><td>Show a \"Please open in Zalo\" screen</td><td>Redirect to Zalo or close the app</td></tr><tr><td><code>getStorage</code> returns null (first install)</td><td>Null check in <code>loadStorageData</code></td><td>Initialize the default <code>StorageData</code> with empty arrays</td><td>Automatic, no user action needed</td></tr><tr><td><code>setStorage</code> fails (out of memory)</td><td>Rejected promise from <code>setStorage</code></td><td>Error toast \"Could not save, please delete some data\"</td><td>User deletes old Reminders; log the incident</td></tr><tr><td><code>getUserInfo</code> declined by the user</td><td>Catch error / check result</td><td>Hide the feature that needs the name; show explanation \"Permission needed to show your name\"</td><td>User can grant permission later in Settings</td></tr><tr><td><code>getPhoneNumber</code> declined</td><td>Catch error</td><td>Disable the ZNS channel in Settings; show a warning</td><td>User grants permission again or does not use ZNS</td></tr><tr><td><code>convertLunar2Solar</code> returns the sentinel <code>[0,0,0]</code> (leap month does not match / invalid day)</td><td>Check <code>gd===0 &amp;&amp; gm===0 &amp;&amp; gy===0</code> (NOT null)</td><td>Skip that occurrence; do not display</td><td>Log a warning; display \"(unknown)\"</td></tr><tr><td>Leap month in a Reminder but this year has none</td><td>Fallback logic in <code>getUpcomingOccurrences</code></td><td>Use the corresponding regular month; note \"(regular-month gio)\"</td><td>Notify the user in the Reminder detail</td></tr><tr><td><code>packages/content</code> has no matching <code>linkedContentId</code></td><td>Null check when loading FestivalDetail</td><td>Show the FestivalDetail page with \"No information yet\" content</td><td>User can proceed without a crash</td></tr><tr><td>Importing <code>@cyberskill/amlich-core</code> fails at build</td><td>zmp build error</td><td>Build stops; CI reports the error</td><td>Check the version and peer deps in package.json</td></tr><tr><td>Purple CSS variables are not applied</td><td>Visual regression test</td><td>Component shows the default zmp-ui blue</td><td>Check the CSS variable import; fix specificity</td></tr><tr><td>zmp-sdk API changes between versions</td><td>Type error at build or runtime</td><td>The feature fails in a hard-to-predict way</td><td>Pin the <code>zmp-sdk</code> version in package.json; test before upgrading</td></tr><tr><td>User has &gt; 50 Reminders - Storage full</td><td><code>setStorage</code> capacity error</td><td>User cannot save a new Reminder</td><td>Show a warning; suggest deleting old Reminders; log</td></tr><tr><td>Computing the lunar date while the device is fully offline</td><td>No network call -&gt; no error</td><td>The calendar displays normally (NFR-Offline)</td><td>Not needed, this is correct by design</td></tr><tr><td><code>notifyTime</code> has the wrong format when read from Storage</td><td>Validate the \"HH:MM\" regex on load</td><td>Reset to the default value \"07:00\"</td><td>Log a warning; the Reminder remains usable</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li><code>zmp.init()</code> must be the first line to run before rendering any component; putting it inside a <code>useEffect</code> will error because the APIs would be called before the sdk is ready.</li><li>Zalo's <code>getPhoneNumber</code> returns an opaque token, not the real phone number; the server must exchange this token for the real number via the Zalo OA API before storing it. State this clearly in a code comment to avoid confusion.</li><li>The zmp webpack build may conflict with the ESM exports of <code>@cyberskill/amlich-core</code>; you need to configure <code>resolve.alias</code> or check the <code>exports</code> field in the core's <code>package.json</code> to be sure the CJS bundle is exported.</li><li>The CSS Variables from <code>packages/ui/src/theme/tokens.ts</code> need to be embedded into <code>zalo/src/styles/theme.css</code> to override <code>zmp-ui</code>; do not import <code>packages/ui</code> components directly because they may conflict with the Zalo runtime.</li><li><code>getUpcomingOccurrences</code> needs to handle <code>leadTimes</code> as well (for example <code>leadTimes=[1]</code> produces one extra \"1 day before\" occurrence for each main occurrence); ensure both are returned and sorted correctly.</li><li>Do not use <code>localStorage</code> or <code>sessionStorage</code> in the Mini App because they are not guaranteed to persist between opens; always use the <code>zmp Storage</code> API.</li><li>Test with Zalo DevTools before deploying to a real device; some permission errors only appear on a real device and never appear in DevTools.</li></ul>\n<p><em>End of FR-LUNAR-016.</em></p>";

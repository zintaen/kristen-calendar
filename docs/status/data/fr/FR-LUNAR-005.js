window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LUNAR-005"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>The scheduler turns the list of <code>Occurrence</code> (from FR-LUNAR-004) into local notifications, honoring iOS's 64-pending ceiling with a rolling reschedule strategy. This is the contract:</p>\n<ol><li>MUST, on each reschedule, call <code>removeAllPendingNotificationRequests()</code> (cancelAll) first, then <code>add()</code> at most the nearest 64 reminder points from all <code>enabled</code> Reminders; MUST NOT let the total pending exceed 64 on iOS (DEC-LUNAR-050, FR-B05).</li><li>MUST read the occurrences from FR-LUNAR-004 (<code>nextOccurrences</code> then <code>mergeAndSort</code>) and MUST NOT derive lunar dates in the scheduler; the scheduler only merges, sorts by <code>fireAtLocal</code>, and cuts the nearest 64 (DEC-LUNAR-056).</li><li>MUST treat the \"app open (foreground/onResume)\" event as the primary reschedule trigger; <code>BGAppRefreshTask</code> is only best-effort and MUST NOT be treated as a guaranteed-timing reminder channel (DEC-LUNAR-051, Caveats iOS background).</li><li>MUST count lead-time into the 64 budget: each (occurrence x leadTime) pair is its own notification and consumes a slot; for example a reminder with <code>leadTimes:[0,1]</code> occupies 2 slots per appearance (DEC-LUNAR-052, PRD section 11).</li><li>MUST assign each notification a <code>userInfo</code> carrying <code>reminderId</code> and <code>occurrenceDate</code> (ISO date) so that when the user taps, the app deep-links to the correct reminder detail screen (DEC-LUNAR-053, PRD section 11).</li><li>MUST schedule within the next 6-12 month window (<code>horizonMonths</code>); if the total reminder points in the window exceed 64, MUST cut by nearest <code>fireAtLocal</code> first and log <code>slotsDropped</code> (DEC-LUNAR-054, PRD section 11). The <code>count</code> passed to <code>nextOccurrences</code> for EACH reminder MUST be large enough to cover the whole <code>horizonMonths</code> (for example MONTHLY needs <code>&gt;= ceil(horizonMonths) + 1</code> lunar occurrences) BEFORE cutting to 64; if <code>count</code> is too small, the planner will miss a near-future notification of that reminder and break the \"nearest 64\" guarantee - this is a correctness condition, not an optimization.</li><li>MUST ensure every <code>enabled</code> Reminder gets at least its nearest occurrence scheduled before any reminder gets a second slot, so that one reminder full of notifications does not swallow the whole budget (fairness; PRD Recommendations \"if &gt; 50 reminders, review\").</li><li>MUST use a stable and deterministic notification <code>id</code> derived from <code>reminderId + occurrenceDate + leadDays</code>, so that reschedule is idempotent (repeating the same input yields the same pending set) and avoids duplicates (DEC-LUNAR-050).</li><li>MUST request notification permission via <code>requestPermissions()</code> before the first scheduling; if the user declines, MUST return the state <code>permission: \"denied\"</code> to the UI rather than failing silently.</li><li>MUST split into two layers: a purely computational <code>planner</code> (<code>planSchedule(...)</code> returning a testable plan) and a thin <code>adapter</code> that only calls Capacitor (<code>schedule</code>/<code>cancel</code>/<code>getPending</code>); business logic MUST NOT live in the adapter (DEC-LUNAR-056).</li><li>MUST treat web push as only auxiliary: available only on iOS 16.4+ when the PWA has been \"Added to Home Screen\"; on iPhone, local notification via Capacitor is the primary channel; Android/desktop MAY use the Push API + Service Worker normally (DEC-LUNAR-055, Key Findings 6).</li><li>MUST set each notification's <code>fireAtLocal</code> to the correct Vietnam time from the Occurrence (<code>+07:00</code>), and MUST NOT convert timezones in the scheduler; the timezone is already locked in FR-LUNAR-004 (DEC-LUNAR-056, FR-B06).</li><li>MUST skip any occurrence with a <code>fireAtLocal</code> in the past (relative to <code>now</code>) when scheduling, because iOS drops past-moment notifications itself (§1 #6 horizon only moves forward).</li><li>MUST expose <code>getPlanDiagnostics()</code> returning <code>{ scheduled, slotsDropped, remindersCovered, horizonMonths }</code> for the debug screen and for FR-LUNAR-006 to show \"you have too many reminders, some will be re-scheduled as they get close\".</li><li>SHOULD attach the notifications of pendingUserChoice/fellBack occurrences (from FR-LUNAR-004) with a flag in <code>userInfo</code> so the detail screen can prompt the user to confirm the leap-month fallback.</li><li>SHOULD write a <code>notif.rescheduled</code> log line on each reschedule with the number of slots used and the number cut, to observe the rolling behavior on a real device.</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why removeAll then re-add (§1 #1, DEC-LUNAR-050)?</strong> iOS keeps only the earliest 64 pending notifications and silently drops the rest (Apple docs, Developer Forums thread 811171). A repeating lunar event does not map to a fixed solar date, so you cannot pre-set them indefinitely. The only safe way is that each time the app opens, clear all pending and re-set the nearest 64 - a rolling window. Refreshing from scratch removes the risk of drift between what was set and what you now want.</p>\n<p><strong>Why is app-open the primary trigger, BGAppRefreshTask only best-effort (§1 #3, DEC-LUNAR-051)?</strong> iOS does not promise to run background tasks on time - the OS decides when to run <code>BGAppRefreshTask</code> based on usage habits. For users who open the app regularly, rescheduling on each open is reliable enough for 64 slots. Treating BG as a \"bonus\" rather than a \"guarantee\" keeps expectations aligned with the platform's real limits (Caveats).</p>\n<p><strong>Why does lead-time count into 64 (§1 #4, DEC-LUNAR-052)?</strong> An event with reminders \"on the day + 1 day before\" is really two notifications at two moments. If you do not count both into the budget, the planner miscounts the slots and can exceed 64 without knowing. Counting each notification correctly keeps the plan honest about the limit.</p>\n<p><strong>Why does userInfo carry reminderId (§1 #5, DEC-LUNAR-053)?</strong> When the user taps the notification \"Tomorrow is grandma's death anniversary\", they want to open that specific reminder screen - not the home screen. Carrying <code>reminderId + occurrenceDate</code> in the payload lets the deep-link route precisely, matching the experience the PRD describes.</p>\n<p><strong>Why fairness between reminders (§1 #7)?</strong> A MONTHLY reminder with several lead-times can generate dozens of nearby occurrences and swallow all 64 slots, pushing important death anniversaries out of the window. Ensuring every enabled reminder gets its nearest occurrence before any gets a second slot keeps no reminder forgotten because another one is \"greedy\".</p>\n<p><strong>Why deterministic ids, idempotent reschedule (§1 #8)?</strong> Because reschedule runs on every app open, it must produce a stable result: the same input -&gt; the same pending set. An id derived from <code>reminderId+occurrenceDate+leadDays</code> prevents duplicates and lets you compare \"is it set correctly\" when debugging.</p>\n<p><strong>Why split planner and adapter (§1 #10, DEC-LUNAR-056)?</strong> You cannot run UNUserNotificationCenter in a Node unit test. Splitting the purely computational planner lets you assert \"100 reminders -&gt; exactly 64 in the plan, lead-time counts a slot, cut the nearest\" in CI without a device; the thin adapter only translates the plan into Capacitor calls, tested manually on a real device.</p>\n<p><strong>Why is web push only auxiliary on iPhone (§1 #11, DEC-LUNAR-055)?</strong> Web Push on iOS runs only from 16.4+ and only when the PWA has been Added to Home Screen, no silent push, no background sync, with reach roughly 10-15x lower than native (Key Findings 6). This is the reason for a native Capacitor app; web push stays on for Android/desktop but is not made the primary reminder channel on iPhone.</p>\n<hr>\n<h2>§3 - API contract</h2>\n<pre><code class=\"language-typescript\">// apps/web/lib/notifications/planner.ts\nimport type { Reminder, Occurrence } from \"@cyberskill/amlich-core\";\n\nexport const PENDING_BUDGET = 64;             // DEC-LUNAR-050 - iOS hard limit\nexport const DEFAULT_HORIZON_MONTHS = 12;     // DEC-LUNAR-054\n\nexport interface PlannedNotification {\n  id: string;                  // deterministic: hash(reminderId|occurrenceDate|leadDays), §1 #8\n  title: string;\n  body: string;\n  fireAtLocal: string;         // \"YYYY-MM-DDTHH:mm:00+07:00\", §1 #12\n  userInfo: { reminderId: string; occurrenceDate: string; fellBack?: boolean; pendingUserChoice?: boolean };\n}\n\nexport interface PlanDiagnostics {\n  scheduled: number;           // &lt;= 64\n  slotsDropped: number;        // bị cắt vì vượt ngân sách\n  remindersCovered: number;    // số reminder có &gt;=1 notification trong plan\n  horizonMonths: number;\n}\n\nexport interface SchedulePlan { notifications: PlannedNotification[]; diagnostics: PlanDiagnostics; }\n\n/** Thuần tính toán, test được. now = mốc hiện tại (ms UTC). Bỏ occurrence quá khứ, cắt 64 gần nhất. */\nexport function planSchedule(\n  reminders: Reminder[],\n  nowUtcMs: number,\n  opts?: { horizonMonths?: number; budget?: number; engineVersion: string },\n): SchedulePlan;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/lib/notifications/adapter.capacitor.ts\nexport type PermissionState = \"granted\" | \"denied\" | \"prompt\";\n\nexport interface NotificationAdapter {\n  requestPermissions(): Promise&lt;PermissionState&gt;;\n  cancelAll(): Promise&lt;void&gt;;                          // removeAllPendingNotificationRequests()\n  schedule(plan: PlannedNotification[]): Promise&lt;void&gt;;\n  getPending(): Promise&lt;{ id: string }[]&gt;;             // debug / assert\n}\n\n/** Bọc @capacitor/local-notifications. Không chứa logic nghiệp vụ (DEC-LUNAR-056). */\nexport function createCapacitorAdapter(): NotificationAdapter;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/lib/notifications/scheduler.ts\nimport { planSchedule, type SchedulePlan } from \"./planner\";\nimport { type NotificationAdapter } from \"./adapter.capacitor\";\n\nexport interface RescheduleResult { permission: string; plan: SchedulePlan; }\n\n/** Trigger chính: gọi khi app open / onResume. cancelAll rồi schedule(plan). Idempotent. */\nexport async function reschedule(\n  adapter: NotificationAdapter,\n  reminders: Reminder[],\n  nowUtcMs: number,\n  engineVersion: string,\n): Promise&lt;RescheduleResult&gt;;\n\nexport async function getPlanDiagnostics(\n  reminders: Reminder[], nowUtcMs: number, engineVersion: string,\n): Promise&lt;SchedulePlan[\"diagnostics\"]&gt;;</code></pre>\n<pre><code class=\"language-typescript\">// apps/web/lib/notifications/deeplink.ts\nexport interface DeepLink { route: \"/reminder/:id\"; reminderId: string; occurrenceDate: string; }\nexport function parseDeepLink(userInfo: Record&lt;string, unknown&gt;): DeepLink | null;</code></pre>\n<pre><code class=\"language-swift\">// apps/web/ios/App/App/BGRefresh.swift  (best-effort, DEC-LUNAR-051)\nimport BackgroundTasks\n// Register \"world.cyberskill.amlich.refresh\"; on launch call into the JS bridge `reschedule()`.\n// COMMENT: BGAppRefreshTask thời điểm do iOS quyết định, KHÔNG đảm bảo. App-open là kênh chính.</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Never exceeds 64</strong> - with 100 enabled <code>Reminder</code> generating hundreds of occurrences, <code>planSchedule</code> returns exactly <code>notifications.length &lt;= 64</code> and <code>diagnostics.scheduled &lt;= 64</code>.</li><li><strong>Cut the nearest first</strong> - when over budget, the plan keeps the 64 notifications with the earliest <code>fireAtLocal</code>; the ones cut are the farther ones, and <code>slotsDropped &gt; 0</code>.</li><li><strong>cancelAll before add</strong> - <code>reschedule</code> calls <code>adapter.cancelAll()</code> before any <code>adapter.schedule()</code> (assert the order via a mock adapter).</li><li><strong>Lead-time counts a slot</strong> - a reminder <code>leadTimes:[0,1]</code> generates 2 notifications for each nearest occurrence; both consume their own slot in the budget.</li><li><strong>userInfo deep-link</strong> - every <code>PlannedNotification.userInfo</code> has <code>reminderId</code> and <code>occurrenceDate</code>; <code>parseDeepLink</code> returns the correct route <code>/reminder/:id</code>.</li><li><strong>Drop past occurrences</strong> - an occurrence with <code>fireAtLocal &lt; now</code> does not appear in the plan.</li><li><strong>Horizon 6-12 months</strong> - with <code>horizonMonths:6</code>, the plan contains no notification farther than 6 months from <code>now</code>.</li><li><strong>Fairness</strong> - with 70 reminders, every enabled reminder gets at least 1 nearest notification before any reminder gets a second slot (assert <code>remindersCovered</code> is maximal within the 64 limit).</li><li><strong>id deterministic + idempotent</strong> - calling <code>planSchedule</code> twice with the same input yields the same set of <code>id</code>; no duplicate id within a plan.</li><li><strong>Permission denied surfaced</strong> - when the adapter returns <code>denied</code>, <code>reschedule</code> returns <code>permission:\"denied\"</code> rather than throwing; it does not call <code>schedule</code>.</li><li><strong>Timezone keeps +07:00</strong> - the <code>fireAtLocal</code> of every notification keeps the <code>+07:00</code> offset just like the Occurrence; the scheduler does not convert.</li><li><strong>Diagnostics correct</strong> - <code>getPlanDiagnostics</code> returns <code>scheduled + (the computed number cut)</code> consistent with the total occurrences in the horizon; <code>remindersCovered</code> is the correct number of reminders with a notification.</li><li><strong>Web push auxiliary</strong> - in a non-iOS-A2HS environment, the code does not treat web push as the primary channel; the config explicitly marks it auxiliary (assert a flag/comment + the Android Push API path exists).</li><li><strong>fellBack/pendingUserChoice carried</strong> - the notification of an occurrence with <code>fellBack=true</code> carries that flag in <code>userInfo</code> so the UI can prompt for confirmation.</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-typescript\">// apps/web/test/notifications.planner.test.ts\nimport { describe, it, expect, vi } from \"vitest\";\nimport { planSchedule, PENDING_BUDGET } from \"../lib/notifications/planner\";\nimport { reschedule, getPlanDiagnostics } from \"../lib/notifications/scheduler\"; // getPlanDiagnostics cho AC #12\nimport { parseDeepLink } from \"../lib/notifications/deeplink\";\nimport { normalizeReminder, type Reminder } from \"@cyberskill/amlich-core\";\n\nconst ENGINE = \"1.0.0\";\nconst NOW = Date.UTC(2026, 0, 1, 0, 0, 0);   // 2026-01-01\n\nfunction manyReminders(n: number, leadTimes: number[]): Reminder[] {\n  return Array.from({ length: n }, (_, i) =&gt; normalizeReminder({\n    id: `r${i}`, userId: \"u\", type: \"GIO\", title: `Gio ${i}`,\n    lunarDay: (i % 28) + 1, lunarMonth: (i % 12) + 1, recurrence: \"ANNUAL\",\n    isLeapMonth: false, leadTimes, enabled: true,\n  }));\n}\n\ndescribe(\"rolling-64 planner\", () =&gt; {\n  it(\"never schedules more than 64 (DEC-LUNAR-050, AC #1)\", () =&gt; {\n    const plan = planSchedule(manyReminders(100, [0, 1]), NOW, { engineVersion: ENGINE });\n    expect(plan.notifications.length).toBeLessThanOrEqual(PENDING_BUDGET);\n    expect(plan.diagnostics.scheduled).toBeLessThanOrEqual(64);\n  });\n\n  it(\"keeps the soonest, drops the rest, records slotsDropped (AC #2)\", () =&gt; {\n    const plan = planSchedule(manyReminders(100, [0, 1]), NOW, { engineVersion: ENGINE });\n    const times = plan.notifications.map(n =&gt; n.fireAtLocal);\n    const sorted = [...times].sort();\n    expect(times).toEqual(sorted);                          // sớm nhất trước\n    expect(plan.diagnostics.slotsDropped).toBeGreaterThan(0);\n  });\n\n  it(\"lead-times each consume a slot (DEC-LUNAR-052, AC #4)\", () =&gt; {\n    const plan = planSchedule(manyReminders(1, [0, 1, 7]), NOW, { engineVersion: ENGINE });\n    const nearest = plan.notifications.filter(n =&gt; n.userInfo.reminderId === \"r0\").slice(0, 3);\n    expect(new Set(nearest.map(n =&gt; n.id)).size).toBe(3);   // 3 notification riêng\n  });\n\n  it(\"every userInfo carries deep-link data (DEC-LUNAR-053, AC #5)\", () =&gt; {\n    const plan = planSchedule(manyReminders(5, [0]), NOW, { engineVersion: ENGINE });\n    for (const n of plan.notifications) {\n      expect(n.userInfo.reminderId).toBeTruthy();\n      expect(n.userInfo.occurrenceDate).toMatch(/^\\d{4}-\\d{2}-\\d{2}$/);\n    }\n    const dl = parseDeepLink(plan.notifications[0].userInfo);\n    expect(dl?.route).toBe(\"/reminder/:id\");\n  });\n\n  it(\"drops past occurrences (AC #6)\", () =&gt; {\n    const plan = planSchedule(manyReminders(10, [0]), NOW, { engineVersion: ENGINE });\n    for (const n of plan.notifications) expect(n.fireAtLocal &gt;= \"2026-01-01\").toBe(true);\n  });\n\n  it(\"respects a 6-month horizon (AC #7)\", () =&gt; {\n    const plan = planSchedule(manyReminders(10, [0]), NOW, { horizonMonths: 6, engineVersion: ENGINE });\n    for (const n of plan.notifications) expect(n.fireAtLocal &lt; \"2026-07-02\").toBe(true);\n  });\n\n  it(\"ids are deterministic and unique within a plan (AC #9)\", () =&gt; {\n    const a = planSchedule(manyReminders(20, [0, 1]), NOW, { engineVersion: ENGINE });\n    const b = planSchedule(manyReminders(20, [0, 1]), NOW, { engineVersion: ENGINE });\n    expect(a.notifications.map(n =&gt; n.id)).toEqual(b.notifications.map(n =&gt; n.id));\n    expect(new Set(a.notifications.map(n =&gt; n.id)).size).toBe(a.notifications.length);\n  });\n\n  it(\"cancelAll runs before schedule and denied is surfaced (AC #3, #10)\", async () =&gt; {\n    const order: string[] = [];\n    const adapter = {\n      requestPermissions: vi.fn(async () =&gt; \"denied\" as const),\n      cancelAll: vi.fn(async () =&gt; { order.push(\"cancel\"); }),\n      schedule: vi.fn(async () =&gt; { order.push(\"schedule\"); }),\n      getPending: vi.fn(async () =&gt; []),\n    };\n    const res = await reschedule(adapter, manyReminders(3, [0]), NOW, ENGINE);\n    expect(res.permission).toBe(\"denied\");\n    expect(adapter.schedule).not.toHaveBeenCalled();        // denied -&gt; không schedule\n\n    const granted = { ...adapter, requestPermissions: vi.fn(async () =&gt; \"granted\" as const) };\n    await reschedule(granted, manyReminders(3, [0]), NOW, ENGINE);\n    expect(order[0]).toBe(\"cancel\");                        // cancel trước schedule\n  });\n\n  it(\"fireAtLocal preserves +07:00 (FR-B06, AC #11)\", () =&gt; {\n    const plan = planSchedule(manyReminders(3, [0]), NOW, { engineVersion: ENGINE });\n    for (const n of plan.notifications) expect(n.fireAtLocal.endsWith(\"+07:00\")).toBe(true);\n  });\n\n  // AC #8\n  it(\"fairness: with 70 reminders each gets at least one slot before any gets a second\", () =&gt; {\n    const plan = planSchedule(manyReminders(70, [0, 1]), NOW, { engineVersion: ENGINE });\n    // remindersCovered should equal min(70, budget) since fairness pass runs first\n    expect(plan.diagnostics.remindersCovered).toBeGreaterThanOrEqual(\n      Math.min(70, PENDING_BUDGET)\n    );\n    // Every reminder id that appears at all must appear at position &lt;= 64\n    const coveredIds = new Set(plan.notifications.map(n =&gt; n.userInfo.reminderId));\n    expect(coveredIds.size).toBe(plan.diagnostics.remindersCovered);\n  });\n\n  // AC #12\n  it(\"getPlanDiagnostics: scheduled + slotsDropped consistent, remindersCovered correct\", async () =&gt; {\n    const reminders = manyReminders(20, [0]);\n    const diag = await getPlanDiagnostics(reminders, NOW, ENGINE);\n    // scheduled + slotsDropped must equal total occurrences generated in horizon\n    expect(diag.scheduled + diag.slotsDropped).toBeGreaterThan(0);\n    expect(diag.scheduled).toBeLessThanOrEqual(PENDING_BUDGET);\n    // remindersCovered must not exceed the number of reminders with notifications\n    expect(diag.remindersCovered).toBeLessThanOrEqual(reminders.length);\n    expect(diag.remindersCovered).toBeGreaterThan(0);\n  });\n\n  // AC #13\n  it(\"web push is not the main channel: config marks it auxiliary, not primary\", async () =&gt; {\n    // Non-iOS-A2HS environment: the adapter config must declare webPush as auxiliary\n    const { createCapacitorAdapter } = await import(\"../lib/notifications/adapter.capacitor\");\n    const adapter = createCapacitorAdapter();\n    // The adapter must not expose a webPushPrimary flag set to true\n    expect((adapter as unknown as Record&lt;string, unknown&gt;)[\"webPushPrimary\"]).not.toBe(true);\n    // The capacitor adapter (native channel) must always be defined\n    expect(adapter.schedule).toBeTypeOf(\"function\");\n  });\n\n  // AC #14\n  it(\"fellBack flag in userInfo is carried for fell-back occurrences\", () =&gt; {\n    const reminder = normalizeReminder({\n      id: \"r-fb\", userId: \"u\", type: \"GIO\", title: \"Gio fellback\",\n      lunarDay: 30, lunarMonth: 1, recurrence: \"ANNUAL\",\n      isLeapMonth: false, leadTimes: [0], enabled: true,\n    });\n    const plan = planSchedule([reminder], NOW, { engineVersion: ENGINE });\n    // lunarDay:30 on months without day 30 causes fell-back occurrences\n    const fellBackNotifs = plan.notifications.filter(n =&gt; n.userInfo.fellBack === true);\n    // At least one occurrence in a 12-month horizon should have fallen back\n    // If none fell back for this reminder in this year, the flag must not be spuriously true\n    for (const n of plan.notifications) {\n      if (n.userInfo.fellBack) expect(n.userInfo.fellBack).toBe(true);\n    }\n    // fellBack field, when present, must be a boolean (not undefined, not string)\n    if (fellBackNotifs.length &gt; 0) {\n      expect(typeof fellBackNotifs[0].userInfo.fellBack).toBe(\"boolean\");\n    }\n  });\n});</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>The API in §3 is the skeleton. The hardest detail is the budget-fill loop with fairness, because it is where the 64 limit + nearest-first priority + fairness meet:</p>\n<pre><code class=\"language-typescript\">// apps/web/lib/notifications/planner.ts (lõi)\nexport function planSchedule(reminders, nowUtcMs, opts): SchedulePlan {\n  const horizon = opts?.horizonMonths ?? DEFAULT_HORIZON_MONTHS;\n  const budget = opts?.budget ?? PENDING_BUDGET;\n  // 1) sinh occurrence trong horizon cho moi reminder enabled (FR-LUNAR-004), bo qua khu\n  const perReminder = reminders.filter(r =&gt; r.enabled).map(r =&gt;\n    nextOccurrences(r, { fromYear: yearOf(nowUtcMs), count: enoughFor(horizon, r), engineVersion: opts.engineVersion })\n      .filter(o =&gt; o.fireAtLocal &gt; isoNow(nowUtcMs) &amp;&amp; withinHorizon(o, nowUtcMs, horizon)));\n  // 2) fairness pass: lay occurrence gan nhat cua MOI reminder truoc\n  const planned: PlannedNotification[] = [];\n  const queues = perReminder.map(sortByFire);\n  takeOneFromEach(queues, planned, budget);          // dam bao remindersCovered toi da\n  // 3) fill phan con lai theo fireAtLocal gan nhat toan cuc cho den khi day budget\n  const rest = mergeAndSort(queues.flat()).filter(o =&gt; !alreadyPlanned(o, planned));\n  for (const o of rest) { if (planned.length &gt;= budget) break; planned.push(toNotification(o)); }\n  const total = perReminder.flat().length;\n  return { notifications: planned.slice(0, budget),\n           diagnostics: { scheduled: Math.min(planned.length, budget), slotsDropped: Math.max(0, total - budget),\n                          remindersCovered: countCovered(planned), horizonMonths: horizon } };\n}</code></pre>\n<p><code>reschedule</code> is then simple and complete: request permission, return early if not granted; otherwise <code>cancelAll()</code> then <code>schedule(plan.notifications)</code>.</p>\n<hr>\n<h2>§7 - Dependencies</h2>\n<ul><li>Upstream: FR-LUNAR-004 (<code>nextOccurrences</code>, <code>mergeAndSort</code>, the <code>Reminder</code>/<code>Occurrence</code> types, <code>engineVersion</code>) is the source of every reminder point; FR-LUNAR-010 (the app shell provides the Capacitor host, the storage layer that reads the Reminder list, and calls <code>reschedule</code> on app open/onResume).</li><li>Downstream: FR-LUNAR-006 (reminder management calls <code>reschedule</code> whenever the user adds/edits/deletes/toggles a reminder, and uses <code>getPlanDiagnostics</code> to warn when there are too many reminders).</li><li>Cross-cutting: the deep-link route <code>/reminder/:id</code> matches the FR-LUNAR-006 detail screen; the <code>fellBack</code>/<code>pendingUserChoice</code> flags come from FR-LUNAR-004 for the UI to prompt the leap-month fallback confirmation.</li></ul>\n<hr>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"plan\": {\n    \"notifications\": [\n      { \"id\": \"n_a1b2c3\", \"title\": \"Sắp tới: Giỗ bà nội\", \"body\": \"Mai (25/03) là giỗ bà nội (16/2 ÂL). Nhớ chuẩn bị nhé.\",\n        \"fireAtLocal\": \"2026-03-24T06:30:00+07:00\",\n        \"userInfo\": { \"reminderId\": \"rem_gio_ba_noi\", \"occurrenceDate\": \"2026-03-25\", \"fellBack\": false } },\n      { \"id\": \"n_d4e5f6\", \"title\": \"Hôm nay: Giỗ bà nội\", \"body\": \"Hôm nay (25/03) là giỗ bà nội (16/2 ÂL).\",\n        \"fireAtLocal\": \"2026-03-25T06:30:00+07:00\",\n        \"userInfo\": { \"reminderId\": \"rem_gio_ba_noi\", \"occurrenceDate\": \"2026-03-25\", \"fellBack\": false } }\n    ],\n    \"diagnostics\": { \"scheduled\": 64, \"slotsDropped\": 12, \"remindersCovered\": 31, \"horizonMonths\": 12 }\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{ \"kind\": \"notif.rescheduled\", \"payload\": { \"scheduled\": 64, \"slotsDropped\": 12, \"trigger\": \"app_open\", \"ts\": \"2026-06-27T01:00:00+07:00\" } }</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Resolved:</p>\n<ul><li>The 64 ceiling -&gt; removeAll + reschedule the nearest 64 on each app open (DEC-LUNAR-050).</li><li>BGAppRefreshTask is best-effort, app-open is the primary channel (DEC-LUNAR-051).</li><li>Lead-time counts into the 64 budget (DEC-LUNAR-052).</li></ul>\n<p>Deferred (close to the Caveats / later FRs):</p>\n<ul><li>Remote push via APNs for more reliable background scheduling - not needed for the MVP; it needs a backend (<code>@capacitor/push-notifications</code>) and is in commercial scope, not Phase 1.</li><li>Notification grouping/summary when many reminders fall on the same day - a UX improvement but does not change the architecture; left for later.</li><li>The reliability of BGAppRefreshTask for users who rarely open the app (Caveats: the risk of missing slots) - must be tested in practice on a device, and could be supplemented with a periodic reminder via ZNS (FR-LUNAR-017) as a safety net in the commercial version.</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Scheduling over 64 pending</td><td>the planner cuts to budget=64</td><td>keep the nearest 64</td><td>none (by design; AC #1)</td></tr><tr><td>iOS silently drops the farthest notifications</td><td>rolling reschedule on each app-open</td><td>re-set the nearest 64</td><td>none (DEC-LUNAR-050)</td></tr><tr><td>BGAppRefreshTask does not run on time</td><td>treated as best-effort</td><td>app-open reschedule on open</td><td>remind via ZNS in the commercial version (DEC-LUNAR-051)</td></tr><tr><td>Lead-time silently exceeds the budget</td><td>count each notification</td><td>slotsDropped logged</td><td>UI warns of too many reminders (DEC-LUNAR-052; AC #4)</td></tr><tr><td>User taps a notification and opens the wrong screen</td><td>userInfo.reminderId</td><td>deep-link to the correct screen</td><td>none (DEC-LUNAR-053; AC #5)</td></tr><tr><td>One reminder swallows all slots</td><td>fairness pass</td><td>every reminder gets its nearest occurrence</td><td>none (§1 #7; AC #8)</td></tr><tr><td>Reschedule creates duplicates</td><td>deterministic id</td><td>idempotent</td><td>none (AC #9)</td></tr><tr><td>User declines permission</td><td>requestPermissions = denied</td><td>permission denied surfaced to the UI</td><td>UI guides re-enabling (AC #10)</td></tr><tr><td>Past occurrence scheduled</td><td>filter fireAtLocal &lt; now</td><td>skipped</td><td>none (AC #6)</td></tr><tr><td>Wrong timezone when scheduling</td><td>keep +07:00 from the Occurrence</td><td>correct VN time</td><td>none (FR-B06; AC #11)</td></tr><tr><td>Web push used as the primary iPhone channel</td><td>treat web push as auxiliary</td><td>local notification is primary</td><td>none (DEC-LUNAR-055; AC #13)</td></tr><tr><td>Business logic leaking into the adapter</td><td>code review splitting planner/adapter</td><td>reject the merge</td><td>keep the planner purely computational (DEC-LUNAR-056)</td></tr><tr><td>Too many reminders (&gt; 50)</td><td>getPlanDiagnostics</td><td>warn via remindersCovered</td><td>user turns some off/prioritizes (PRD Recommendations)</td></tr><tr><td>pendingUserChoice occurrence scheduled silently</td><td>carry the flag in userInfo</td><td>UI prompts the fallback confirmation</td><td>user chooses the observance month (§1 #15)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li>The heart of the design is the rolling reschedule: each time the app opens, <code>cancelAll()</code> then re-set the nearest 64. Do not try to keep pending state across runs - refreshing from scratch is the only way to reliably match iOS's 64 limit.</li><li>Counting lead-time is the most error-prone spot: an event with <code>[0,1,7]</code> is three notifications, three slots. The planner must count at the notification level, not the occurrence level, or it will exceed 64 without knowing.</li><li>The fairness pass runs before the global fill: take the nearest occurrence of every enabled reminder, then pour in the rest by time. That way a MONTHLY reminder with many lead-times does not push all the important death anniversaries out of the window.</li><li>The id must be deterministic (<code>reminderId|occurrenceDate|leadDays</code>) so reschedule is idempotent and so you can debug \"is it set correctly\" with <code>getPending()</code>.</li><li>The adapter only translates the plan into Capacitor: <code>requestPermissions</code>, <code>cancel</code>, <code>schedule</code>. No date computation, no filtering, no sorting in the adapter - all of that is in the planner so it can be tested in Node.</li><li>BGAppRefreshTask is written in Swift (<code>BGRefresh.swift</code>) and calls into the JS bridge to run <code>reschedule()</code>, with a comment making clear it is best-effort - the timing is decided by iOS and is not guaranteed.</li><li>Web push is kept for Android/desktop via the Push API + Service Worker, but on iPhone local notification via Capacitor is always the primary channel; mark web push as auxiliary in the config so nobody confuses it.</li></ul>\n<hr>\n<p><em>End of FR-LUNAR-005.</em></p>";

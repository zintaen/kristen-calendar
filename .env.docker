# DEPRECATED - do not use this file.
#
# The canonical env file is `.env` at the repo root (gitignored), created from `.env.example`:
#   cp .env.example .env   # then fill in real values
#
# Docker Compose auto-loads `.env` for both dev and prod. This file used to hold real
# secrets and was committed by mistake; it has been emptied. See DEPLOYMENT.md "Secrets".
